---
quick_id: 260719-t62
slug: projecten-showcase-from-owner-photos
created: 2026-07-19
status: in-progress
mode: quick
execution: inline (OneDrive constraint — no subagents/worktrees)
must_haves:
  truths:
    - "/projecten renders 7 real project cases with owner photos and is in nav + sitemap"
    - "No privacy-flagged photo is published (IMG_0181, IMG_0307, IMG_0454 excluded; IMG_0460 cropped)"
    - "Originals renamed descriptively with PHOTOS.md index; iCloud Photos/ gitignored"
  artifacts:
    - public/images/projecten/ (22 optimized JPEGs, descriptive Dutch names)
    - lib/projects.ts, components/ProjectCase.tsx, components/RecentWorkStrip.tsx, app/projecten/page.tsx
    - .planning/quick/260719-t62-*/THOMAS-REVIEW.md (per-photo consent sign-off note)
  key_links:
    - lib/services/registry.ts (new static PageNode → sitemap/robots via lib/seo/policy)
    - lib/constants.ts NAV_LINKS (Projecten entry consumed by Navbar + MobileMenu)
    - app/diensten/[pillar]/page.tsx (RecentWorkStrip on airconditioning pillar only)
---

# Quick Task 260719-t62: Projecten showcase from owner photos

Design approved by Oskar in-session 2026-07-19 (brainstorm → 3 decisions locked):
project-cases page **+** service-page embeds; anonymous-by-default (no addresses,
crop/skip flagged photos, Thomas per-photo sign-off note); case format per contact
sheet (artifact bc1489ac). Photo analysis: 25 photos, 9 jobs, 3 jun–10 jul 2026;
all airco + 1 heat pump; zero WTW/ventilation; .MOVs are Live-Photo clips (unused).

## Task 1 — Assets: rename originals, gitignore, generate web derivatives

**files:** `iCloud Photos/*` (untracked), `iCloud Photos/PHOTOS.md`, `.gitignore`,
`public/images/projecten/*.jpg` (22 new), `scripts/prep-project-photos.sh` (new)

**action:**
- Rename all 45 originals to `YYYY-MM-DD_<descriptive-dutch>.{JPEG,MOV}` keeping
  Live-Photo pairs aligned; write `PHOTOS.md` index (old name → new name → content
  → rating → privacy flag).
- Add `iCloud Photos/` to `.gitignore` (45 MB owner source dump, never in git).
- Script generates 22 derivatives into `public/images/projecten/`: max edge 1400px,
  JPEG q70 via sips; IMG_0460 cropped (top 430px removed → arm gone) before resize.
  Excluded: IMG_0181 (install-in-progress), IMG_0307 (family photos on pinboard),
  IMG_0454 (nursery + protective tape).

**verify:** 22 files in public/images/projecten/, each ≤ 400KB; no excluded name
present; `git status` shows no iCloud Photos/ entries.
**done:** commit `feat(projecten): add optimized project photos + source index`

## Task 2 — Feature: data, components, page, nav, registry

**files:** `lib/projects.ts` (new), `components/ProjectCase.tsx` (new),
`components/RecentWorkStrip.tsx` (new), `app/projecten/page.tsx` (new),
`lib/services/registry.ts` (+1 static node), `lib/constants.ts` (+1 NAV_LINKS entry)

**action:**
- `lib/projects.ts`: `Project` interface + `PROJECTS` (7 cases from the 9 jobs,
  photos with Dutch alt text, brands, month, pillarSlug "airconditioning").
- `ProjectCase.tsx`: server component — title, month/brand chips, blurb, 2–4 photo
  grid (plain `<img>` loading="lazy" like existing static-export imagery), tonal
  card (`bg-surface-container-low`, no 1px borders), StaggerChildren reveal.
- `app/projecten/page.tsx`: `metadata = buildMetadata(findBySlug("/projecten")!)`,
  H1 "Ons werk", intro with anonymity line ("regio Zoetermeer; adressen en
  klantgegevens delen we niet"), cases list, CTABanner. JSON-LD breadcrumb.
- Registry: static node `pathSegment: "projecten"` (draft status is fine — static
  type ⇒ indexable ⇒ auto sitemap + robots via policy.ts). Real metaTitle
  "Ons werk – airco-installaties Zoetermeer | TPS klimaattechniek".
- NAV_LINKS: `{ label: "Projecten", href: "/projecten" }` after Tarieven.

**verify:** `npx tsc --noEmit` and `npm run lint` clean.
**done:** commit `feat(projecten): ons-werk showcase page with 7 project cases`

## Task 3 — Diensten embed + owner sign-off note

**files:** `app/diensten/[pillar]/page.tsx`, THOMAS-REVIEW.md (task dir)

**action:**
- RecentWorkStrip (4 photos + "Bekijk ons werk →" link) rendered on the
  airconditioning pillar only (warmtepompen has zero publishable photos — noted
  for Thomas). Placement: after services grid, before FAQ.
- THOMAS-REVIEW.md: per-photo consent checklist (22 published), the 3 exclusions
  with reasons, and a shot-list request: WTW/ventilation jobs, finished Bosch
  monoblock, team/van, 2–3 real videos.

**verify:** tsc + lint clean; pillar pages for wtw/mv/warmtepompen unchanged visually.
**done:** commit `feat(diensten): recent-work strip on airco pillar + review note`

## Task 4 — Ship gate: push → Vercel preview → screenshot audit

**action:** push branch (Vercel preview = build gate per OneDrive constraint);
poll deployment via Vercel MCP; screenshot-audit /projecten + airco pillar
(desktop+mobile) with inline Playwright (symlink node_modules trick); fix visual
defects if any; update STATE.md Quick Tasks table + SUMMARY.md.
**done:** commit `docs(260719-t62): close quick task — projecten showcase live on preview`
