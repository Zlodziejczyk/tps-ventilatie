---
quick_id: 260628-4d3
slug: tier-2-servicehero-redesign
date: 2026-06-28
status: complete
commits:
  - b55de17  # feat: ServiceHero redesign
preview: pending  # owner runs `vercel deploy --yes` (PREVIEW) — see Next steps
follows: 260628-q2u  # TIER 1 branding fixes
source: .planning/phases/04-content-fill-editorial-gate/OWNER-FEEDBACK-2026-06-28.md  # defect #4
---

# Quick Task 260628-4d3 — SUMMARY

TIER 2 of the owner's 2026-06-28 preview review (OWNER-FEEDBACK-2026-06-28.md,
defect #4): the **ServiceHero "walls of text"** redesign. Code+design only, zero
new assets. One atomic commit on `main` (NOT pushed) — ready for a fresh
**preview** deploy for owner re-check.

## What changed (commit b55de17 — `components/ServiceHero.tsx`)

Single shared template, rendered on **every** service AND pillar page, so the
redesign stays generic (driven only by `PageBase` fields + `SITE`).

- **Two-column hero** (`grid lg:grid-cols-12`, `items-start`): scannable text
  left (`col-span-7`), engineered glass trust card right (`col-span-5`). Stacks
  on mobile. The right column was previously an empty faint glow placeholder —
  now filled.
- **Broke the wall of text** — new pure `splitLead()` helper splits the
  owner-approved `intro` into an **emphasized lead sentence** (`text-xl/2xl
  font-medium text-on-surface`) + **lighter supporting copy**
  (`text-on-surface-variant font-light`). Boundary = first `.?!` + whitespace +
  capital, with a ≥30-char min lead (won't break on "bijv."/"incl."). No
  boundary → whole string is the lead (no regression); empty intro still falls
  back to `navDescription` (keeps D-06).
  **No copy was authored or rewritten** — this is a presentation transform of
  the existing string (content stays the owner-gated `lib/services/*.ts` data).
- **Left CTA row:** primary "Offerte aanvragen" (`signature-gradient` → /contact)
  + secondary "Bel {phoneDisplay}" (tonal `bg-surface-container-high`, `tel:`).
- **`HeroFacts` trust card** (internal, non-exported): glass
  `bg-surface-container-lowest`, engineered `signature-gradient` top-accent bar,
  soft teal shadow; a live **`air-pulse`** "Direct beschikbaar voor advies" dot
  (tertiary/green); 3 universally-true fact rows from `SITE` (Werkgebied
  Zoetermeer + 60 km · Goed bereikbaar Ma–za 08:00–17:30 · Vakkundig &
  vrijblijvend); one-tap **WhatsApp** button. Deliberately **no BRL/cert claims**
  here — those belong to the TIER 3 trust band so the card stays true for every
  node (incl. reparatie/storing pages).

## Constraints honored

- Stayed on `main`; **no `git push origin main`**, **no `vercel --prod`**.
- **No local build/`tsx`** (OneDrive deadlock) — ran inline, no subagents/worktrees.
- Committed only `components/ServiceHero.tsx`; left `package*.json` +
  untracked `06-homepage/` alone.
- Still a **Server Component** (`grep '"use client"'` → none); reuses only the
  CWV-safe `AnimateOnScroll` + CSS `air-pulse` (reduced-motion aware) — protects
  the SEO-10 mobile-INP launch gate. MD3 tokens + `Icon` wrapper + `SITE` only;
  no 1px section borders, no #000.

## Verification done locally (no build)

- `grep '"use client"'` → none (Server Component preserved).
- All `SITE` fields referenced exist (`city`, `serviceRadiusKm`, `phone`,
  `phoneDisplay`, `whatsappUrl`).
- `bash scripts/check-radius-literals.sh` → **PASS** (the `{SITE.serviceRadiusKm}
  km` interpolation has no literal digit before "km", so the QA-03 gate is clean).
- Forbidden-claims gate scans only taxonomy `PAGES` content, not components — my
  card text is out of scope and adds no YMYL claims.
- `Icon` / `AnimateOnScroll` exports confirmed; props match usage.

## Next steps (owner-run — the established deploy loop)

1. `vercel deploy --yes` (**PREVIEW**) — Vercel CI doubles as the compile/type
   check + runs the prebuild gates; mints a new preview URL hash.
2. Re-check a service page (e.g. `/diensten/warmtepompen/onderhoud`) and a
   pillar page: H1 → bold lead → lighter supporting copy → CTA row, trust card
   beside on desktop / stacked on mobile, pulse animating.
3. If approved → **TIER 3** (asset-blocked on Thomas): manufacturer logos
   (Daikin/Mitsubishi) in `BrandGrid`, cert badges (BRL 100/200, erkend
   installateur), homepage trust band (Phase 6). Then 04-09 Task 3 (batch-flip
   21 review nodes → published) after whole-site sign-off.

## Known follow-ups (not this task)

- **Per-service hero imagery**: airco/warmtepompen have no photos yet — the
  trust card is the asset-free stand-in. When Thomas sends project photos, an
  optional image variant could swap in per pillar.
- **Logo asset** (carried from TIER 1): request a transparent SVG / horizontal
  lockup; optimize the 1.1 MB 1369×1149 PNG.
