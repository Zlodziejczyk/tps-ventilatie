---
quick_id: 260628-q2u
slug: tier-1-branding-fixes-04-revise-navbar-b
date: 2026-06-28
mode: quick
status: in_progress
source: .planning/phases/04-content-fill-editorial-gate/OWNER-FEEDBACK-2026-06-28.md
---

# Quick Task 260628-q2u: TIER 1 branding fixes (04-revise)

## Boundary

Owner preview review (2026-06-28) flagged design/branding defects on the live
Vercel preview. This task ships **TIER 1 only** — the high-impact, **code-only,
zero-asset** fixes — then deploys a fresh preview for owner re-check. TIER 2
(ServiceHero redesign) and TIER 3 (manufacturer/cert logo assets) are out of scope.

## Hard constraints (carried from phase 04 checkpoint)

- Do **NOT** `git push origin main` or `vercel --prod`. `main` is ~140 ahead of
  origin and maps to Vercel **production** — a push/prod-deploy IS the launch.
  Deploy **previews** only via `vercel deploy --yes`.
- Do **NOT** build locally — the OneDrive mount deadlocks `tsx`/`next build`.
  Validate on Vercel CI via the preview deploy.
- Do **NOT** touch `lib/reviews.ts` — quotes are VERBATIM customer words; the
  legacy "TPS Ventilatie" there is intentional.
- Keep the domain `tpsventilatie.nl` and email `info@tpsventilatie.nl` everywhere
  (canonical host stays the apex). Only the **brand/display name** changes.
- Run inline (no subagents / no worktrees) — OneDrive hangs spawned executors.

## Tasks

### Task 1 — Brand identity: name + logo

- **files:** `components/Navbar.tsx`, `app/contact/page.tsx`,
  `app/privacy-beleid/page.tsx`, `public/tps-logo.png`
- **action:**
  - Navbar wordmark: replace hardcoded `"TPS Ventilatie"` (line 52) with the
    logo (`public/tps-logo.png` via `next/image`, on a white rounded chip —
    PNG is opaque white-bg, RGB no-alpha) + a text wordmark `{SITE.name}`
    ("TPS klimaattechniek"). Add `SITE` + `Image` imports.
  - `app/contact/page.tsx:124`: Maps iframe `title` → source brand + address
    from `SITE` (`${SITE.name} locatie - ${SITE.address}, ${SITE.postcode} ${SITE.city}`).
  - `app/privacy-beleid/page.tsx:23,67`: brand name `TPS Ventilatie` →
    `TPS klimaattechniek` (prose only; keep `tpsventilatie.nl` + `info@` intact).
- **verify:** `grep -rn "TPS Ventilatie" app components` returns nothing
  (only `lib/reviews.ts` may retain it — protected).
- **done:** header reads "TPS klimaattechniek" with the emblem on every page;
  no stale brand name outside reviews.

### Task 2 — Mega-menu "Nieuw" badge clip

- **files:** `components/Navbar.tsx` (DienstenPanel), `components/MobileMenu.tsx`
- **action:** badge was clipped to "NIE" — `ml-auto` pushed it past the narrow
  ~174px column into the panel's `overflow-hidden`. Drop `ml-auto`; make the
  badge `shrink-0 whitespace-nowrap` inline after the title inside a wrapping
  group; widen `DienstenPanel` `w-[760px]` → `w-[840px]`. Same `shrink-0
  whitespace-nowrap` on the MobileMenu badge.
- **verify:** Warmtepompen "Nieuw" badge renders in full on the deployed preview
  (desktop mega-menu + mobile drawer).
- **done:** badge never clips.

### Deploy (post-commit)

- `vercel deploy --yes` (PREVIEW). Vercel CI build doubles as the compile/type
  check. Mints a new preview URL hash — hand it to the owner for re-check.

## Out of scope (deferred)

- TIER 2: `ServiceHero` redesign (walls of text) — code+design, load
  `sketch-findings-tps-ventilatie`.
- TIER 3 (asset-blocked on owner): manufacturer logos (Daikin/Mitsubishi),
  cert badges (BRL 100/200, erkend installateur), homepage trust band.
- Logo asset polish: transparent SVG / horizontal lockup; the current 1.1 MB
  1369×1149 PNG should be resized/optimized once a clean asset arrives.
