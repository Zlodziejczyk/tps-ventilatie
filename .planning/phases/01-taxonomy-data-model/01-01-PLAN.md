---
phase: 01-taxonomy-data-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - next.config.ts
autonomous: true
requirements: [IA-08, IA-09]
user_setup: []

must_haves:
  truths:
    - "zod is an installed dependency the taxonomy validator can import (D-07)"
    - "tsx is an installed devDependency that can run a standalone TS script (D-07)"
    - "npm run prebuild runs before npm run build and a non-zero exit from the script aborts the build (D-07)"
    - "next.config.ts declares trailingSlash: false and still declares output: export unchanged (D-03)"
  artifacts:
    - path: "package.json"
      provides: "zod in dependencies, tsx in devDependencies, prebuild script wired before build"
      contains: "\"prebuild\""
    - path: "next.config.ts"
      provides: "trailingSlash: false added; output: export untouched"
      contains: "trailingSlash"
  key_links:
    - from: "package.json prebuild script"
      to: "scripts/validate-taxonomy.ts"
      via: "npm pre<script> hook running tsx"
      pattern: "tsx scripts/validate-taxonomy.ts"
---

<objective>
Establish the build-time toolchain this phase needs: add the `zod` runtime dependency and the `tsx` dev dependency, wire an npm `prebuild` script that will run the taxonomy validator before `next build`, and add `trailingSlash: false` to `next.config.ts`. This plan creates the *gate plumbing* (script wiring + config) — the validator script itself is written in plan 01-06 against the registry built in 01-05.

Purpose: D-07 introduces `zod` (reused by Phase 5 QA-02 form validation) and the `prebuild` hook is the mechanism that makes taxonomy validation genuinely build-blocking (success criterion 3). D-03 locks the no-trailing-slash URL policy at the config layer so every downstream URL/canonical is consistent.
Output: Updated `package.json` (deps + `prebuild` script), updated `package-lock.json` (lockfileVersion 3, deps pinned), updated `next.config.ts` (`trailingSlash: false`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-taxonomy-data-model/01-CONTEXT.md
@.planning/phases/01-taxonomy-data-model/01-RESEARCH.md
@.planning/phases/01-taxonomy-data-model/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add and verify zod + tsx dependencies (supply-chain gated)</name>
  <read_first>
    - package.json (lines 5-10 — the existing `scripts` block: `dev`/`build`/`start`/`lint`; and the `dependencies`/`devDependencies` blocks) — the exact analog to extend in place
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 108-132 (Installation commands + the Package Legitimacy Audit table: zod [OK]/no-postinstall/4.4.3; tsx [ASSUMED]/verify-at-install) and lines 654-676 (Security Domain — V14 supply-chain, the only real surface this phase)
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 244-256 (`package.json` MODIFY — add `prebuild` + deps; the package-audit gate for tsx)
  </read_first>
  <action>
    Vet then install both packages. Supply-chain gate FIRST (this is the phase's only real threat surface, V14): run `slopcheck install zod`, `slopcheck install tsx`, and `npm view zod scripts.postinstall` + `npm view tsx scripts.postinstall` (both must be empty). slopcheck 0.6.1 is available in this environment and at research time `zod` was `[OK]` with no postinstall; `tsx` 4.22.4 has no postinstall but was not slopchecked at research time (RESEARCH A4) — slopcheck it now. If slopcheck returns `[OK]`/`[VERIFIED]` for both and both postinstall fields are empty, proceed with the install. If either returns `[SUS]` or `[SLOP]`, STOP and do not install (a `[SLOP]` verdict is non-negotiable per the threat model) — surface the verdict and halt the plan.
    Install with `npm install zod` (→ `dependencies`, target 4.4.3) and `npm install -D tsx` (→ `devDependencies`, target 4.x — accept the resolved 4.22.4). Do NOT hand-edit version ranges into package.json — let npm write them and commit the resulting `package-lock.json` (lockfileVersion 3). Do not add any other dependency. Do not run a global `npm update`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!p.dependencies.zod) throw new Error('zod missing from dependencies'); if(!p.devDependencies.tsx) throw new Error('tsx missing from devDependencies'); console.log('deps OK', p.dependencies.zod, p.devDependencies.tsx)"</automated>
  </verify>
  <acceptance_criteria>
    - `slopcheck install zod` AND `slopcheck install tsx` both report a non-malicious verdict (`[OK]`/`[VERIFIED]`), and `npm view zod scripts.postinstall` + `npm view tsx scripts.postinstall` are both empty (no postinstall) — recorded in the SUMMARY
    - `node -e "require('./package.json').dependencies.zod"` resolves to a `^4` / `4.4.x` range
    - `node -e "require('./package.json').devDependencies.tsx"` resolves to a `^4` / `4.22.x` range
    - `package-lock.json` is updated (git diff shows zod + tsx entries) and lockfileVersion is 3
    - No package other than `zod` and `tsx` was added to package.json
  </acceptance_criteria>
  <done>zod is in dependencies and tsx in devDependencies, both vetted clean via slopcheck + postinstall inspection, with package-lock.json updated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the prebuild gate script and add trailingSlash: false</name>
  <read_first>
    - package.json (lines 5-10 — the `scripts` block) — add the `prebuild` key here in the existing double-quoted, trailing-comma style
    - next.config.ts (lines 1-10 — the full `NextConfig` object: `output: "export"` + `images.unoptimized: true`) — the exact file to add one key to
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 269-287 (Pattern 4 — the `prebuild` hook: `pre<script>` runs before `<script>`, non-zero exit aborts the chain) and lines 354-358 (Pitfall 4 — do NOT touch `output: "export"`, only add `trailingSlash`)
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 244-275 (`package.json` + `next.config.ts` MODIFY sections — exact target strings)
  </read_first>
  <action>
    In `package.json` `scripts`, add the exact entry `"prebuild": "tsx scripts/validate-taxonomy.ts"` (npm runs `prebuild` automatically before `build`, so this gates `next build`). Keep the other scripts (`dev`, `build`, `start`, `lint`) unchanged; keep double quotes and trailing-comma style. The referenced `scripts/validate-taxonomy.ts` does NOT exist yet and is created in plan 01-06 — that is expected; this plan only wires the script name. Until 01-06 lands, `npm run prebuild` will fail with "file not found" (acceptable mid-phase); do NOT create a placeholder script here.
    In `next.config.ts`, add exactly one key `trailingSlash: false` to the `nextConfig` object (D-03). Do NOT modify or remove `output: "export"` (the static-vs-hybrid decision is Phase 5's gate) and do NOT touch `images.unoptimized`. Keep the `import type { NextConfig }` typing and `export default nextConfig`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(p.scripts.prebuild!=='tsx scripts/validate-taxonomy.ts') throw new Error('prebuild script wrong/missing: '+p.scripts.prebuild); console.log('prebuild OK')" && grep -q "trailingSlash: false" next.config.ts && grep -q 'output: "export"' next.config.ts && echo "next.config OK"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `scripts.prebuild` is exactly `tsx scripts/validate-taxonomy.ts`
    - `package.json` still contains the original `dev`, `build`, `start`, `lint` scripts unchanged
    - `grep -c "trailingSlash: false" next.config.ts` returns 1
    - `grep -c 'output: "export"' next.config.ts` returns 1 (unchanged — proves the static-export mode was NOT touched)
    - `next.config.ts` still exports `nextConfig` as default and keeps `images.unoptimized: true`
  </acceptance_criteria>
  <done>The prebuild hook points at the (future) validator script and next.config.ts adds trailingSlash: false while leaving output: export intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local node_modules | Two new third-party packages (`zod`, `tsx`) cross into the build toolchain. This is the ONLY trust boundary this phase introduces — no runtime input, no network, no secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering / Elevation | `npm install zod` / `npm install -D tsx` | mitigate | slopcheck both packages + `npm view <pkg> scripts.postinstall` (must be empty) BEFORE install; pin via lockfileVersion 3. zod verified `[OK]`/no-postinstall at research time; tsx postinstall confirmed empty, slopcheck at install. A `[SLOP]` verdict halts the plan (never auto-approved). |
| T-01-PI | Elevation of Privilege | `postinstall` lifecycle script on a new dep | mitigate | Both `npm view zod scripts.postinstall` and `npm view tsx scripts.postinstall` confirmed empty (no install-time code execution). Install runs in the normal sandbox. |
| T-01-CFG | Tampering | `next.config.ts` `output` mode | accept | Only `trailingSlash: false` is added; `output: "export"` is explicitly NOT modified (Phase 5 decision gate). The acceptance criterion greps that `output: "export"` is still present, preventing accidental change. |

**No HIGH-severity items.** The `security_block_on: high` gate is satisfied: the surface is two build-time dependencies, one (`zod`) verified clean and the other (`tsx`) gated at install with a confirmed-empty postinstall.
</threat_model>

<verification>
- `node -e "require('./package.json')"` confirms zod (dep) + tsx (devDep) present and prebuild script wired.
- `grep "trailingSlash: false" next.config.ts` and `grep 'output: "export"' next.config.ts` both match (added the one key, did not touch output mode).
- `git diff package-lock.json` shows zod + tsx resolved entries (lockfileVersion 3).
- Note: `npm run prebuild` and `npm run build` are EXPECTED to fail until plan 01-06 creates `scripts/validate-taxonomy.ts`. Do not treat that as a regression — the wiring is correct; the target is built later in the phase.
</verification>

<success_criteria>
- zod is importable from `dependencies`; tsx is runnable from `devDependencies`; both vetted clean.
- `package.json` `prebuild` = `tsx scripts/validate-taxonomy.ts`.
- `next.config.ts` has `trailingSlash: false` and retains `output: "export"`.
- `package-lock.json` updated and committed.
</success_criteria>

<output>
Create `.planning/phases/01-taxonomy-data-model/01-01-SUMMARY.md` when done. Record the slopcheck verdicts and the exact installed versions of zod and tsx.
</output>
