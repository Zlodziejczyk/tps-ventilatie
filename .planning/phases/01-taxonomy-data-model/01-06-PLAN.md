---
phase: 01-taxonomy-data-model
plan: 06
type: execute
wave: 4
depends_on: ["01-01", "01-05"]
files_modified:
  - scripts/validate-taxonomy.ts
  - scripts/check-radius-literals.sh
autonomous: true
requirements: [IA-08, IA-09, QA-03]
user_setup: []

must_haves:
  truths:
    - "npm run prebuild runs the Zod gate and exits 0 with the all-draft taxonomy (build stays green — D-08, Crit 3)"
    - "A deliberately-broken page (duplicate primaryKeyword OR a published page with a <120-word intro) makes the gate exit non-zero — proving 'blocking' is real, not assumed (Crit 2 & 3)"
    - "npm run build succeeds (prebuild gate + next build) — static export still pre-renders with the taxonomy in place"
    - "The phase-wide radius grep finds no '50km'/'100 km' literal anywhere in app/ or components/ (QA-03, Crit 4)"
  artifacts:
    - path: "scripts/validate-taxonomy.ts"
      provides: "The build-blocking prebuild gate: safeParse(PAGES), z.prettifyError, process.exit(1) on failure"
      contains: "process.exit(1)"
    - path: "scripts/check-radius-literals.sh"
      provides: "Grep assertion that the 50/100 km radius literals are gone (Crit 4)"
      contains: "straal van"
  key_links:
    - from: "scripts/validate-taxonomy.ts"
      to: "lib/services/registry.ts PAGES + lib/services/types.ts pagesSchema"
      via: "import + pagesSchema.safeParse(PAGES)"
      pattern: "safeParse"
    - from: "package.json prebuild"
      to: "scripts/validate-taxonomy.ts"
      via: "npm pre<script> hook (wired in plan 01-01) now resolves to a real script"
      pattern: "tsx scripts/validate-taxonomy.ts"
---

<objective>
Make the taxonomy validation genuinely build-blocking and prove it. Create `scripts/validate-taxonomy.ts` — the prebuild gate wired in plan 01-01 — which imports `PAGES`, runs `pagesSchema.safeParse`, prints a readable `z.prettifyError` on failure, and `process.exit(1)` so `next build` aborts. Then prove the gate actually blocks via a negative fixture (a deliberately-broken page must fail the build), and add the phase-wide radius grep assertion that closes Crit 4. Finally confirm `npm run build` is green with the all-draft taxonomy.

Purpose: D-07/D-08 require the uniqueness bar + status-gated content rules to be a real build error, not advice. Without a negative fixture, "blocking" is unverified — VALIDATION.md Wave 0 mandates this. The grep assertion (Crit 4) is the QA-03 phase-gate check. This plan closes the loop: the validators (01-04) + the data (01-05) are exercised end-to-end through the actual `next build`.
Output: `scripts/validate-taxonomy.ts` (the gate) and `scripts/check-radius-literals.sh` (the Crit-4 grep); a verified green `npm run build`; a documented negative-fixture proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-taxonomy-data-model/01-CONTEXT.md
@.planning/phases/01-taxonomy-data-model/01-RESEARCH.md
@.planning/phases/01-taxonomy-data-model/01-PATTERNS.md
@.planning/phases/01-taxonomy-data-model/01-VALIDATION.md
@lib/services/registry.ts
@lib/services/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the prebuild gate scripts/validate-taxonomy.ts</name>
  <read_first>
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 442-457 (the build-blocking entry point — import PAGES + pagesSchema, safeParse, z.prettifyError on the error, process.exit(1); success prints the page count) and lines 269-287 (Pattern 4 — the prebuild hook semantics)
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 192-209 (scripts/validate-taxonomy.ts section — NO in-repo analog, copy from RESEARCH; the one place console.error/process.exit are correct; flag for code review)
    - lib/services/registry.ts (PAGES + validateTaxonomy exports) and lib/services/types.ts (pagesSchema export) — what the script imports; reconcile whether to call validateTaxonomy(PAGES) or pagesSchema.safeParse(PAGES) directly (use whichever the 01-04/01-05 summaries settled — both are acceptable; the script owns process.exit either way)
    - package.json (the prebuild script wired in plan 01-01 → tsx scripts/validate-taxonomy.ts) — this task creates the file that wiring points at
    - scripts/assert-site-shape.ts and scripts/assert-registry.ts (sibling build-time CLIs — same console-usage top-of-file comment convention)
  </read_first>
  <action>
    Create `scripts/validate-taxonomy.ts` — the genuinely-blocking prebuild gate (RESEARCH lines 442-457). Import `PAGES` from `@/lib/services/registry` and `pagesSchema` from `@/lib/services/types` (and `z` from zod for prettifyError). Run `pagesSchema.safeParse(PAGES)` (or call `validateTaxonomy(PAGES)` from the registry — match what the 01-05 summary exposed; whichever path, the SCRIPT owns the exit). On failure: print a clear header then `z.prettifyError(result.error)` (the Zod-4 human-readable, path-named output) and `process.exit(1)` so the non-zero exit aborts `next build`. On success: print a one-line confirmation including `PAGES.length`. Use Zod 4 idioms only (`z.prettifyError`, not v3 `formatError`). Add a top-of-file comment stating this is a build-time CLI where `console.error`/`process.exit` are intentional and correct (so code review does not false-positive the console usage against the no-console-in-production rule). Do NOT add fs writes or network calls (the script only imports local `@/lib` and prints to stdout/stderr — Information-Disclosure mitigation). The npm `prebuild` hook (already wired in 01-01) now resolves to this real file, so `npm run prebuild` becomes runnable.
  </action>
  <verify>
    <automated>npm run prebuild</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/validate-taxonomy.ts` imports `PAGES` (from registry) and `pagesSchema` (from types), runs `safeParse` (directly or via `validateTaxonomy`), and calls `process.exit(1)` on failure
    - `grep -c "z.prettifyError" scripts/validate-taxonomy.ts` returns 1 (Zod-4 readable error) and `grep -c "formatError" scripts/validate-taxonomy.ts` returns 0 (no Zod-3 idiom)
    - `npm run prebuild` exits 0 and prints the page count (the all-draft taxonomy validates — D-08, Crit 3)
    - A top-of-file comment flags the intentional build-time console/process.exit usage
    - The script contains no `fs.write`/`fetch`/`http` calls (only local imports + stdout)
  </acceptance_criteria>
  <done>scripts/validate-taxonomy.ts is the working prebuild gate: it validates PAGES via Zod, prints a readable error and exits non-zero on failure, exits 0 on the all-draft taxonomy, and the npm prebuild hook now runs it.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the gate blocks — negative fixture (duplicate keyword + short published intro)</name>
  <read_first>
    - .planning/phases/01-taxonomy-data-model/01-VALIDATION.md lines 46, 58, 644-646 (the negative-fixture requirement — a broken page with a duplicate keyword OR a <120-word published intro MUST make prebuild exit non-zero; without it "blocking" is unverified)
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 399-440 (publishedContentSchema word-count rule + the cross-record uniqueness superRefine — the two failure modes to exercise) and line 634 (the negative-case assertion in the test map)
    - lib/services/types.ts (pagesSchema + publishedContentSchema — the rules being exercised) and lib/services/registry.ts (PAGES — the array to temporarily perturb)
    - scripts/validate-taxonomy.ts (the gate from Task 1) — the command under test
  </read_first>
  <action>
    Prove the gate genuinely blocks both failure modes, then leave the tree green. Do this WITHOUT committing a broken taxonomy: perform two temporary perturbations and capture the non-zero exits as evidence in the SUMMARY. (a) DUPLICATE KEYWORD: temporarily set a second node's `primaryKeyword` equal to an existing node's primary (for example give a sub-service the hub's `klimaattechniek Zoetermeer`), run `npm run prebuild`, confirm it exits non-zero and the prettified error names the offending page+field (cannibalization), then REVERT. (b) SHORT PUBLISHED INTRO: temporarily flip one node's `status` to `published` while its `intro` is empty/short (<120 words), run `npm run prebuild`, confirm it exits non-zero naming `intro` word-count, then REVERT. Capture both failing outputs (the prettified error text + the non-zero exit) in the SUMMARY as the negative-fixture evidence. After reverting both, run `npm run prebuild` once more and confirm it exits 0. If you prefer a repeatable harness over manual perturbation, create a tiny `scripts/assert-gate-blocks.ts` that constructs an in-memory broken `PAGES` clone, asserts `pagesSchema.safeParse(brokenClone).success === false` for BOTH the duplicate-keyword case and the short-published-intro case (using node:assert), and prints OK — this keeps the proof runnable without touching committed data. Do NOT leave any node `published` or any duplicate keyword in the committed files (the committed taxonomy stays all-draft and valid).
  </action>
  <verify>
    <automated>npm run prebuild</automated>
  </verify>
  <acceptance_criteria>
    - Negative case A (duplicate primaryKeyword) was shown to make `npm run prebuild` exit non-zero with a prettified error naming the offending page+field — evidence (the error text) recorded in the SUMMARY
    - Negative case B (a `published` node with a <120-word intro) was shown to make `npm run prebuild` exit non-zero naming the `intro` word-count rule — evidence recorded in the SUMMARY
    - After reverting both perturbations, `npm run prebuild` exits 0 (committed taxonomy is clean and all-draft)
    - No node in the committed `lib/services/*.ts` is `published`/`review` and no duplicate `primaryKeyword` exists (`grep -rc 'status: "published"' lib/services` totals 0)
    - If the optional harness was created, `npx tsx scripts/assert-gate-blocks.ts` exits 0 (both broken clones fail safeParse; uses node:assert)
  </acceptance_criteria>
  <done>The Zod gate is proven to block both the duplicate-keyword (cannibalization) and the short-published-intro (thin-content) failure modes, with evidence captured, and the committed taxonomy is left green and all-draft.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Crit-4 radius grep assertion and confirm the full build is green</name>
  <read_first>
    - .planning/phases/01-taxonomy-data-model/01-VALIDATION.md lines 47, 59 (Crit 4 / QA-03 — the exact grep: no "straal van 50"/"straal van 100"/"100 km"/"50km" in app or components; returns nothing) and lines 30-34 (Sampling Rate — per-wave npm run build proves static export still succeeds)
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 326-332 (the confirmed offending strings + the note that over-ons holds regio prose but no numeric radius literal) and line 635 (the Crit-4 grep assertion row)
    - app/tarieven/page.tsx + components/PricingTabs.tsx (fixed in plan 01-03 — the grep target after the fix) — confirm the literals are already gone before asserting
    - scripts/assert-site-shape.ts (the sibling shell/CLI convention)
  </read_first>
  <action>
    Create `scripts/check-radius-literals.sh` — a small executable bash assertion that runs `grep -rn "straal van 50\|straal van 100\|100 km\|50km" app components` and exits NON-ZERO if any match is found (i.e. the assertion passes only when grep finds nothing). Use the inverted-grep idiom so a found literal fails the check (for example: if grep matches, echo the offending lines and exit 1; else echo OK and exit 0). Make it executable. This is the QA-03 / Crit-4 phase-gate check; it is a standalone script (not wired into prebuild — the radius fix is verified separately from the taxonomy gate). Scope the grep to `app` and `components` (the only dirs with the literals per RESEARCH; over-ons holds regio prose, not a numeric radius literal, so it is correctly out of the match set). Then run the FULL build (`npm run build`) to confirm the prebuild gate + `next build` both succeed — proving the taxonomy validates AND the static export still pre-renders every route with `output: "export"` intact. Capture the successful build (exit 0) in the SUMMARY as the phase-gate evidence.
  </action>
  <verify>
    <automated>bash scripts/check-radius-literals.sh && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/check-radius-literals.sh` exists, is executable, and exits 0 (no radius literal found in app/ or components/) — and would exit non-zero if a "50km"/"100 km" literal were reintroduced
    - `grep -rn "straal van 50\|straal van 100\|100 km\|50km" app components` returns nothing (Crit 4 satisfied)
    - `npm run build` exits 0 — the prebuild gate passes AND `next build` completes the static export (no "cannot use X with output: export" error)
    - The successful `npm run build` output (prebuild OK + build complete) is recorded in the SUMMARY
  </acceptance_criteria>
  <done>The Crit-4 radius grep assertion passes (literals gone), and npm run build is green end-to-end — the taxonomy gate runs and the static export still pre-renders every route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build script → filesystem/stdout | `scripts/validate-taxonomy.ts` runs at build time. It imports local `@/lib` modules and writes only to stdout/stderr. No network egress, no fs writes outside the project. This is the relevant boundary to keep tight (Information-Disclosure). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-EXFIL | Information Disclosure | `scripts/validate-taxonomy.ts` | mitigate | The script only imports local `@/lib/services/*` and prints to stdout/stderr — no network calls, no fs writes outside the project. Reviewed in code review (acceptance criterion forbids fs.write/fetch/http). |
| T-06-GATE | Tampering (data-integrity) | the prebuild gate's blocking guarantee | mitigate | The negative fixture (Task 2) proves the gate actually exits non-zero on a duplicate keyword and a short published intro — so a future thin/cannibalizing page cannot silently ship. Without this proof, "blocking" would be an unverified assumption (VALIDATION.md Wave 0). |
| T-06-CONSOLE | (code-quality, not STRIDE) | intentional `console.error`/`process.exit` | accept | These are correct for a build-time CLI (not shipped runtime code); flagged with a top-of-file comment so code review does not false-positive them against the no-console-in-production rule. |

**No HIGH-severity items.** The build script's only surface is local imports + stdout (Information-Disclosure mitigated by review); the gate's blocking behavior is proven, not assumed.
</threat_model>

<verification>
- `npm run prebuild` exits 0 (all-draft taxonomy validates — D-08, Crit 3).
- Negative fixture: a duplicate keyword AND a short published intro each make `npm run prebuild` exit non-zero — proven and evidenced (Crit 2 & 3).
- `bash scripts/check-radius-literals.sh` exits 0 and `grep -rn "straal van 50\|straal van 100\|100 km\|50km" app components` returns nothing (Crit 4 / QA-03).
- `npm run build` exits 0 — prebuild gate + static export both succeed (phase gate, VALIDATION.md Sampling Rate).
- Committed taxonomy is all-draft and valid; no `published`/`review` node and no duplicate keyword remains.
</verification>

<success_criteria>
- The taxonomy validation is genuinely build-blocking (IA-08/IA-09) and proven to block via a negative fixture.
- `npm run prebuild` and `npm run build` are both green with the all-draft taxonomy.
- The Crit-4 radius grep finds no stale literal (QA-03).
- The build-time gate script imports only local modules and prints to stdout (no exfiltration surface).
</success_criteria>

<output>
Create `.planning/phases/01-taxonomy-data-model/01-06-SUMMARY.md` when done. Record: the passing `npm run build` output, the two negative-fixture failure outputs (as blocking-proof evidence), and confirmation the committed taxonomy is all-draft.
</output>
