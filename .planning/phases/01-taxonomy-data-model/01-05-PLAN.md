---
phase: 01-taxonomy-data-model
plan: 05
type: execute
wave: 3
depends_on: ["01-04"]
files_modified:
  - lib/services/brands.ts
  - lib/services/airconditioning.ts
  - lib/services/warmtepompen.ts
  - lib/services/wtw.ts
  - lib/services/mechanische-ventilatie.ts
  - lib/services/registry.ts
  - scripts/assert-registry.ts
autonomous: true
requirements: [IA-01, IA-09]
user_setup: []

must_haves:
  truths:
    - "A single PAGES: PageNode[] aggregates every routable page (1 hub + 4 pillars + 17 subs + ~5 static = ~27) — the one source nav/sitemap/JSON-LD read (IA-01, Crit 1, D-04)"
    - "urlFor(node) is the ONLY href builder, emitting lowercase, leading-slash, no-trailing-slash nested URLs /diensten/{pillar}/{sub} (D-01/D-02/D-03)"
    - "Each page node carries a unique primaryKeyword + searchIntent transcribed from the RESEARCH keyword map (IA-09, D-13); home and hub take DISTINCT head terms; the three low-demand pages take regional/long-tail primaries"
    - "Brands are normalized in brands.ts (Daikin once, referenced by both airco + warmtepompen installatie via brandIds); dealer/erkendInstallateur flags are false placeholders pending Phase 4 (D-06)"
  artifacts:
    - path: "lib/services/registry.ts"
      provides: "PAGES aggregate + urlFor() + findBySlug/findByType lookups + validateTaxonomy(); top-of-file no-barrel-exception comment"
      contains: "export function urlFor"
      min_lines: 40
    - path: "lib/services/brands.ts"
      provides: "Normalized brand registry (Daikin once); erkendInstallateur false placeholders"
      contains: "daikin"
    - path: "lib/services/airconditioning.ts"
      provides: "Airco pillar node + 4 sub-service nodes with keyword map values"
      contains: "installatie"
  key_links:
    - from: "lib/services/registry.ts PAGES"
      to: "Phase 2 generateStaticParams + nav, Phase 3 sitemap/JSON-LD"
      via: "single exported array consumed downstream"
      pattern: "export const PAGES"
    - from: "lib/services/airconditioning.ts + warmtepompen.ts installatie nodes"
      to: "lib/services/brands.ts"
      via: "brandIds reference resolved by Phase 2 BrandGrid"
      pattern: "brandIds"
    - from: "lib/services/registry.ts validateTaxonomy"
      to: "lib/services/types.ts pagesSchema"
      via: "safeParse returning a result object (no throw — prebuild script owns exit)"
      pattern: "pagesSchema"
---

<objective>
Build the taxonomy data layer: the normalized brand registry, the four per-pillar data files (each holding its pillar node + sub-service nodes with the transcribed keyword map), and the `registry.ts` aggregation module that assembles the unified `PAGES` array, exposes the sole `urlFor()` href builder + lookup helpers, and provides `validateTaxonomy()`. This is the single source of truth that directly satisfies IA-01 / success criterion 1 ("no parallel hardcoded lists exist") and carries the IA-09 keyword map.

Purpose: D-04/D-05 specify one discriminated-union registry assembled from per-pillar files (each under 800 lines as Phase 4 content lands). D-01/D-02/D-03 define the URL structure that `urlFor()` enforces. D-06 normalizes brands (Daikin appears under two pillars — the reason to normalize). D-13 puts a unique `primaryKeyword`/intent on each node; the RESEARCH keyword map is the authoritative source and resolves the two cannibalization risks (home-vs-hub head term; the three low-demand advies/inregelen pages). All shells ship `status: "draft"` with empty content so the build stays green (D-08).
Output: `lib/services/{brands,airconditioning,warmtepompen,wtw,mechanische-ventilatie,registry}.ts` plus `scripts/assert-registry.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-taxonomy-data-model/01-CONTEXT.md
@.planning/phases/01-taxonomy-data-model/01-RESEARCH.md
@.planning/phases/01-taxonomy-data-model/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@lib/services/types.ts
@lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brands.ts and the four per-pillar data files (with the keyword map)</name>
  <read_first>
    - lib/services/types.ts (the PageNode union, the variant interfaces, ContentShell, and the documented import-direction decision from plan 01-04) — every node here is typed against these
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 483-551 (THE KEYWORD MAP — the authoritative per-URL primaryKeyword + intent + secondary for hub, 4 pillars, all 17 subs; transcribe exactly) and lines 537-551 (static-page keyword targets + the cannibalization control summary that resolves home-vs-hub and the three flagged low-demand pages)
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 254-267 (Pattern 3 — normalized brands; airco installatie brandIds daikin/mitsubishi-electric/mitsubishi-heavy, warmtepompen installatie daikin/mitsubishi-ecodan, WTW/MV none)
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 111-160 (the pillar-file + brands.ts sections — DIENSTEN_DROPDOWN analog, slugs from D-02, draft status, brandIds rules, erkendInstallateur false placeholders, under-800-lines)
    - lib/constants.ts lines 30-49 (DIENSTEN_DROPDOWN typed-data-array analog: SCREAMING_SNAKE_CASE, named export, double quotes, trailing commas) and lines 1-13 (SITE as const — the keyed-object analog for brands)
    - .planning/codebase/CONVENTIONS.md lines 20-37, 98-102 (exported-constant naming, named exports, no-barrel)
  </read_first>
  <action>
    Create `lib/services/brands.ts`: an `as const` keyed object `BRANDS` with one entry per brand (`daikin`, `mitsubishi-electric`, `mitsubishi-heavy`, `mitsubishi-ecodan`), each shaped with id, name, logo, blurb (empty string), erkendInstallateur (false) per Pattern 3. Daikin appears ONCE (referenced by two pillars). Mark erkendInstallateur false as owner-verify-pending placeholders with a comment (never render an unverified "erkend installateur" claim — D-06/CONT-03). Logos are /images/brands/<id>.svg placeholder paths (real SVGs are a Phase 2 asset concern). Named export, SCREAMING_SNAKE_CASE.
    Create the four pillar files (`airconditioning.ts`, `warmtepompen.ts`, `wtw.ts`, `mechanische-ventilatie.ts`). Each exports its pillar node (type pillar) plus its sub-service nodes (type service), typed against the PageNode union from types.ts, as a SCREAMING_SNAKE_CASE export (for example AIRCONDITIONING_PAGES typed PageNode array). Slugs come from D-02 exactly: pillars airconditioning/warmtepompen/wtw/mechanische-ventilatie (wtw stays lowercase); airco and WP subs installatie/onderhoud/reparatie-storing/advies; WTW subs vervangen/onderhoud-reinigen/inregelen/storing/aanleggen; MV subs vervangen/onderhoud-reinigen/storing/aanleggen. TRANSCRIBE the keyword map from RESEARCH lines 491-535 exactly: each node primaryKeyword + searchIntent + optional secondaryKeywords (for example airco pillar primary airconditioning intent commercieel; airco installatie primary "airco laten installeren" intent transactioneel; airco reparatie-storing primary "airco storing" intent transactioneel; through all 17 subs). For the THREE flagged low-demand pages use the REGIONAL/long-tail primary from RESEARCH: airco advies primary "airco advies Zoetermeer"; warmtepomp advies primary "warmtepomp advies regio Den Haag"; wtw inregelen primary "wtw inregelen" (niche — pair with the meetrapport secondary). Set status draft on every node and give each a content ContentShell with EMPTY/placeholder body fields (h1 empty, intro empty, steps empty array, faqs empty array, localAngle empty) plus SEO metadata fields shaped to satisfy whatever plan 01-04 contentShellSchema requires for a draft (match the schema — if metaTitle/metaDescription are min-1 in the structure-only schema use short non-empty placeholders; if the schema relaxes them for draft, empty is fine). Set brandIds ONLY on the two Installatie service nodes: airco installatie brandIds daikin/mitsubishi-electric/mitsubishi-heavy; warmtepompen installatie brandIds daikin/mitsubishi-ecodan; WTW/MV subs get NO brandIds. Model the nav-relevant fields (navTitle/navDescription/icon) on pillar + service nodes so Phase 2 derives dropdowns. Annotate keyword strings as ASSUMED (owner/tool-validates before Phase 4 — A1) with a file-level comment. Keep each file under 800 lines. Do NOT import any pricing data from components/PricingTabs.tsx (out of scope — pricing stays there). Do NOT build URLs in these files (that is urlFor in registry).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "lib/services/" | (! grep -q .) && echo "pillar and brands files compile"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/brands.ts` exports `BRANDS` with a single `daikin` entry and `erkendInstallateur: false` on every brand (placeholder comment present)
    - Each pillar file exports a typed PageNode array named export in SCREAMING_SNAKE_CASE
    - The airco installatie node carries brandIds equal to the three airco brand ids (daikin, mitsubishi-electric, mitsubishi-heavy); the warmtepompen installatie node carries daikin + mitsubishi-ecodan
    - `grep -rn "brandIds" lib/services/wtw.ts lib/services/mechanische-ventilatie.ts` returns NOTHING (WTW/MV subs have no brands)
    - `grep -rn "airco advies Zoetermeer" lib/services/airconditioning.ts` matches AND `grep -rn "warmtepomp advies regio Den Haag" lib/services/warmtepompen.ts` matches (the regional primaries on the low-demand pages)
    - `grep -rc 'status: "published"' lib/services` totals 0 AND `grep -rc 'status: "review"' lib/services` totals 0 (every node is draft this phase)
    - `npx tsc --noEmit` reports no error in any `lib/services/*.ts` data file
  </acceptance_criteria>
  <done>brands.ts (normalized, Daikin once, placeholder flags) and the four pillar files exist with D-02 slugs, the transcribed keyword map (distinct primaries; regional primaries on the three low-demand pages), all-draft empty shells, and correct brandIds — compiling under strict mode.</done>
</task>

<task type="auto">
  <name>Task 2: Create registry.ts — PAGES aggregate, urlFor(), lookups, validateTaxonomy(), static pages</name>
  <read_first>
    - lib/services/types.ts (PageNode union + pagesSchema + the import-direction decision) and the four pillar files + brands.ts from Task 1 (the nodes being aggregated)
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 238-251 (Pattern 2 — urlFor exhaustive switch over node.type, enforcing lowercase/leading-slash/no-trailing-slash) and lines 537-544 (the ~5 static pages to register: home /, tarieven, over-ons, contact, privacy-beleid, with light keyword targets; home MUST take a head term DISTINCT from the hub klimaattechniek Zoetermeer — use "airco warmtepomp ventilatie Zoetermeer" per RESEARCH line 540)
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 164-189 (registry.ts section — the no-barrel-exception top-of-file comment, urlFor switch, validateTaxonomy returning result object not throwing, findBySlug/findByType lookups)
    - lib/forms.ts (the ok-result convention validateTaxonomy mirrors; no try/catch in the codebase)
    - .planning/codebase/CONVENTIONS.md lines 94-102 (utilities return plain objects; no-barrel rule — registry.ts is the documented exception)
  </read_first>
  <action>
    Create `lib/services/registry.ts` with a top-of-file comment documenting that this is a JUSTIFIED aggregation module and an explicit exception to the no-barrel rule (D-05) — it aggregates PAGES, builds URLs, and runs validation; it is NOT a generic re-export barrel. Define the hub node (type hub, segment diensten, primaryKeyword "klimaattechniek Zoetermeer" intent commercieel per RESEARCH) and the ~5 static nodes (type static): home (pathSegment empty string, primaryKeyword "airco warmtepomp ventilatie Zoetermeer" — DISTINCT from hub to avoid the build-error collision per RESEARCH Open-Q 2), tarieven, over-ons, contact, privacy-beleid with their light keyword targets from RESEARCH lines 541-544 (privacy-beleid is a noindex candidate — model with status draft, Phase 3 decides indexing). Assemble `export const PAGES` typed as a PageNode array by spreading the hub node + the four pillar files exports + the static nodes into ONE array (this is the aggregation that makes Crit 1 provable). Implement `export function urlFor(node)` returning string as an exhaustive switch over node.type: hub returns the diensten root; pillar returns the diensten root plus a slash and pillarSlug; service returns the diensten root plus pillarSlug plus a slash plus serviceSlug; static returns the site root for the empty path segment else a leading slash plus pathSegment — enforcing lowercase, leading slash, no trailing slash (D-03). This is the ONLY place hrefs are built. Add small pure lookups findByType and findBySlug in the getInitials/lerp kept-very-small style. Implement `export function validateTaxonomy(pages)` returning a discriminated ok-true or ok-false-with-error result by running pagesSchema.safeParse(pages) — do NOT throw (project precedent is the ok-result; the 01-06 prebuild script owns process.exit). If plan 01-04 chose the makePagesSchema(urlFor) factory, wire urlFor into it here; if it chose a local slug helper, ensure urlFor and the helper agree on the canonical URL. Named exports, double quotes, trailing commas. Do NOT re-export the pillar files wholesale as a barrel; export only PAGES, urlFor, the lookups, and validateTaxonomy.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "services/registry" | (! grep -q .) && echo "registry compiles"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/registry.ts` exports `PAGES`, `urlFor`, `validateTaxonomy`, and at least one lookup; the top-of-file comment states the no-barrel exception
    - `urlFor` is implemented as a `switch (node.type)` covering all four variants and is the only function building a /diensten path in the file
    - `grep -c "airco warmtepomp ventilatie Zoetermeer" lib/services/registry.ts` returns 1 (home head term, distinct from the hub)
    - `grep -c "klimaattechniek Zoetermeer" lib/services/registry.ts` returns 1 (hub head term — only on the hub, not duplicated)
    - `npx tsc --noEmit` reports no error in `lib/services/registry.ts`
  </acceptance_criteria>
  <done>registry.ts aggregates the full ~27-page PAGES array, exposes urlFor() as the sole href builder, provides lookups + a non-throwing validateTaxonomy(), and registers the hub + static pages with home distinct from the hub.</done>
</task>

<task type="auto">
  <name>Task 3: Add the registry assertion script (PAGES count, URL uniqueness, no trailing slash)</name>
  <read_first>
    - lib/services/registry.ts (PAGES, urlFor, validateTaxonomy — the exports the assertion imports) from Task 2
    - .planning/phases/01-taxonomy-data-model/01-VALIDATION.md lines 42-48 (Crit 1 / IA-01 row — assert PAGES length is ~27 and the no-parallel-route-list check) and lines 54-63 (Wave 0 — use Node 26 built-in node:test/node:assert; do NOT add jest/vitest)
    - scripts/assert-site-shape.ts (the sibling assertion script from plan 01-02 — same build-time-CLI shape, node:assert, top-of-file console-usage comment) — mirror its structure
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md line 632 (Crit 1 structural + grep assertion description)
  </read_first>
  <action>
    Create `scripts/assert-registry.ts`, mirroring the shape of scripts/assert-site-shape.ts (build-time CLI, node:assert, top-of-file comment noting the intentional console usage and the no-test-framework choice). Import PAGES, urlFor, validateTaxonomy from @/lib/services/registry. Assert four things: (1) PAGES length equals the expected full-surface count (1 hub + 4 pillars + 17 subs + 5 static = 27 — state the exact expected number in a comment; use a tight band only if the planner-counted statics differ); (2) every urlFor(node) is unique across PAGES (a Set of the URLs has size equal to the array length); (3) every URL starts with a leading slash, is lowercase, and only the home URL is exactly the site root while no other URL ends in a trailing slash; (4) validateTaxonomy(PAGES) is ok-true (the all-draft taxonomy validates). Print a one-line OK with the page count on success; rely on node:assert to exit non-zero on any failure. Run with tsx scripts/assert-registry.ts. This is the standalone Crit-1 structural check, separate from the 01-06 prebuild gate (the Zod content/uniqueness gate). Do NOT add jest/vitest.
  </action>
  <verify>
    <automated>npx tsx scripts/assert-registry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/assert-registry.ts` exists, imports `PAGES`/`urlFor`/`validateTaxonomy` from `@/lib/services/registry`, and uses `node:assert` (no jest/vitest)
    - `npx tsx scripts/assert-registry.ts` exits 0 and prints the page count (~27)
    - The script asserts URL uniqueness (Set size equals PAGES length) and the no-trailing-slash rule (only `/` is the root)
    - Temporarily duplicating a node's slug locally makes the script exit non-zero, then restore (proves the uniqueness assertion is live)
  </acceptance_criteria>
  <done>scripts/assert-registry.ts asserts PAGES count, URL uniqueness, the no-trailing-slash rule, and validateTaxonomy ok — exiting 0 against the built registry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new external trust boundary. This plan creates build-time TS data files, an aggregation module, and a build-time assertion script. No runtime input, no network, no secrets.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-DATA | Repudiation / Trust (data-integrity) | `BRANDS[].erkendInstallateur` flags | mitigate | Every brand's `erkendInstallateur` is modeled as `false` with an owner-verify-pending comment; no node renders a dealer/erkend claim this phase. Owner verifies real dealer status in Phase 4 (CONT-03). Prevents emitting an unverified credential. |
| T-05-KW | Trust (data-integrity) | per-node `primaryKeyword` strings | accept | Keyword strings are `[ASSUMED]` (RESEARCH A1) — annotated as data pending tool validation (Google Keyword Planner / Ahrefs) before Phase 4 content locks. Modeling them now does not emit a public claim; they are editable data. Owner/SEO validates before Phase 4 (VALIDATION.md Manual-Only). |
| T-05-SCOPE | Tampering | `lib/services/*` importing pricing data | mitigate | The acceptance criteria and action forbid importing `components/PricingTabs.tsx` data into the taxonomy (out of scope, Dec-2025 "pricing dilutes topical expertise"). A `price` field on any node or a PricingTabs import is the documented warning sign. |

**No HIGH-severity items.** Build-time data only; the only consideration is data-integrity of owner-pending/assumed facts, mitigated by placeholder-flagging and `[ASSUMED]` annotations.
</threat_model>

<verification>
- `npx tsx scripts/assert-registry.ts` exits 0 — PAGES is the full ~27-page surface, all URLs unique, no trailing slashes, validateTaxonomy ok (Crit 1).
- `grep -rn "brandIds" lib/services/wtw.ts lib/services/mechanische-ventilatie.ts` returns nothing; airco/WP installatie nodes carry the correct brandIds (D-06).
- Home (`airco warmtepomp ventilatie Zoetermeer`) and hub (`klimaattechniek Zoetermeer`) have distinct primaries — no build-error collision (RESEARCH Open-Q 2 resolved).
- `npx tsc --noEmit` compiles all `lib/services/*.ts` under strict mode.
- Manual-only (does NOT block this phase): owner/SEO validates the `[ASSUMED]` keyword strings in a keyword tool before Phase 4 (VALIDATION.md Manual-Only).
</verification>

<success_criteria>
- One `PAGES: PageNode[]` is the single source of truth for the full service surface (IA-01, Crit 1, D-04).
- `urlFor()` is the sole href builder enforcing the D-03 URL policy.
- The IA-09 keyword map is transcribed onto every node with distinct primaries (home vs hub resolved; three low-demand pages on regional primaries).
- Brands normalized with placeholder dealer flags (D-06).
</success_criteria>

<output>
Create `.planning/phases/01-taxonomy-data-model/01-05-SUMMARY.md` when done. Record the exact final PAGES count and confirm the home/hub head terms are distinct, for the 01-06 gate and the Phase 2 hand-off.
</output>
