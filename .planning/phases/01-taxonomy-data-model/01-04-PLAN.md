---
phase: 01-taxonomy-data-model
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/services/types.ts
autonomous: true
requirements: [IA-08, IA-09]
user_setup: []

must_haves:
  truths:
    - "A discriminated-union PageNode type (hub | pillar | service | static) is the typed contract every page node satisfies (D-04)"
    - "The content shell type bundles body content (h1, intro, steps[], faqs[], localAngle, brandIds?) AND per-page SEO metadata (metaTitle, metaDescription, ogImage) AND keyword fields in one shape (D-14/D-15)"
    - "Zod schemas exist that enforce structure always and content rules (intro >=120 words, steps non-empty, faqs 3-6) ONLY for review/published status (IA-08, D-07/D-08)"
    - "faqs and steps are structured arrays of {question,answer} / {title,body} (not prose) so Phase 3 JSON-LD and Phase 2 components consume them with zero re-parsing (D-15)"
  artifacts:
    - path: "lib/services/types.ts"
      provides: "PageType/SearchIntent/PageStatus unions, PageNode discriminated union + per-variant interfaces, ContentShell, and the Zod schemas (contentShellSchema, publishedContentSchema, pagesSchema)"
      contains: "pagesSchema"
      min_lines: 60
  key_links:
    - from: "lib/services/types.ts pagesSchema"
      to: "lib/services/registry.ts urlFor (uniqueness refinement needs canonical URL)"
      via: "schema factory or shared slug helper (import direction resolved here)"
      pattern: "superRefine"
    - from: "lib/services/types.ts ContentShell"
      to: "Phase 2 ServiceFAQ/ServiceSteps components + Phase 3 FAQPage JSON-LD"
      via: "structured faqs[]/steps[] arrays consumed without re-parsing"
      pattern: "faqs"
---

<objective>
Define the typed contract for the entire taxonomy in one file: the `PageNode` discriminated union (by `type: "hub" | "pillar" | "service" | "static"`), the per-variant interfaces, the `ContentShell` shape (body content + SEO metadata + keyword fields, one source per D-14), and the Zod schemas that turn the anti-thin-content uniqueness bar (IA-08) and status-gated content rules (D-08) into build-checkable validators. This is the **contracts-first** plan — plans 01-05/01-06 implement and validate against these types, so the executor receives the contracts here rather than reverse-engineering them.

Purpose: D-04 mandates one discriminated-union registry; the union lives here. D-07/D-08 require Zod refinements (intro ≥120 words, 3-6 FAQs, non-empty steps + local angle) that are status-gated so empty `draft` shells pass. D-14/D-15 fix the content-shell shape: structured `faqs[]`/`steps[]` arrays + prose `intro`/`localAngle` + per-page SEO metadata, all in one type. `zod` (installed in 01-01) is the validation engine; this repo has zero prior Zod usage, so the schemas are built from RESEARCH §Code Examples, not an in-repo analog.
Output: `lib/services/types.ts` — the shared types + Zod schemas. No data nodes yet (those are 01-05).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-taxonomy-data-model/01-CONTEXT.md
@.planning/phases/01-taxonomy-data-model/01-RESEARCH.md
@.planning/phases/01-taxonomy-data-model/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@lib/constants.ts
@components/PricingTabs.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the PageNode discriminated union, per-variant interfaces, and ContentShell</name>
  <read_first>
    - components/PricingTabs.tsx line 9 (`type Tab = "wtw" | "mv" | "airco";`) — the in-repo precedent for a string-union discriminant; apply the same idiom to PageType/SearchIntent/PageStatus
    - lib/constants.ts lines 23-28 (`interface DropdownItem { icon; title; description; href }`) — the in-repo `interface` style for domain object shapes; note `icon`/`title`/`description` are the nav-relevant fields CONTEXT says to model onto pillar/service nodes now (so Phase 2 derives dropdowns)
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 202-236 (Pattern 1 — the ILLUSTRATIVE discriminated-union shape: PageType, PageBase, HubPage/PillarPage/ServicePage/StaticPage, PageNode; brandIds only on service) and lines 32-35 (D-13/D-14/D-15 — keyword fields on node, shell = content + SEO, structured arrays)
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 69-107 (`lib/services/types.ts` — the TS-types section: discriminant + interface analogs, nav-relevant fields)
    - .planning/codebase/CONVENTIONS.md lines 25-29 (string-union `type` for unions, `interface` for object shapes, short descriptive domain type names)
  </read_first>
  <behavior>
    - `PageType` is the union "hub" | "pillar" | "service" | "static"; `SearchIntent` and `PageStatus` are string unions
    - `PageNode` is a discriminated union over `type` such that a `switch (node.type)` is exhaustively checkable
    - A `ServicePage` carries `pillarSlug` + `serviceSlug` + optional `brandIds?: string[]`; a `PillarPage` carries `pillarSlug`; `HubPage` and `StaticPage` carry their own path discriminants
    - Every node variant carries `primaryKeyword`, `searchIntent`, optional `secondaryKeywords?`, `status`, and a `content: ContentShell`
    - `ContentShell` has structured `faqs: {question,answer}[]` and `steps: {title,body}[]`, prose `intro`/`localAngle`, `h1`, and SEO `metaTitle`/`metaDescription`/`ogImage?`
  </behavior>
  <action>
    Create `lib/services/types.ts`. Define string-union `type`s: `PageType = "hub" | "pillar" | "service" | "static"`, `SearchIntent = "informationeel" | "commercieel" | "transactioneel" | "navigationeel"`, `PageStatus = "draft" | "review" | "published"` (mirror the `type Tab` idiom). Define `interface`s in the `DropdownItem` style: a `PageBase` with `status: PageStatus`, `primaryKeyword: string`, `searchIntent: SearchIntent`, `secondaryKeywords?: string[]`, and `content: ContentShell`, PLUS nav-relevant fields (`navTitle`/`navDescription`/`icon` — model these now so Phase 2 derives the dropdowns from the taxonomy). Then `HubPage extends PageBase` (`type: "hub"`, segment `"diensten"`), `PillarPage extends PageBase` (`type: "pillar"`, `pillarSlug: string`), `ServicePage extends PageBase` (`type: "service"`, `pillarSlug: string`, `serviceSlug: string`, `brandIds?: string[]`), `StaticPage extends PageBase` (`type: "static"`, `pathSegment: string` — `""` = home). Export `type PageNode = HubPage | PillarPage | ServicePage | StaticPage`. Define `interface ContentShell` with `h1: string`, `intro: string` (prose), `steps: { title: string; body: string }[]`, `faqs: { question: string; answer: string }[]`, `localAngle: string` (prose), `metaTitle: string`, `metaDescription: string`, `ogImage?: string` (D-14/D-15). Use named exports, double quotes, trailing commas. The discriminant fields must let `urlFor()` (built in 01-05) switch exhaustively. Do NOT put any concrete page data here (that's 01-05); do NOT import `zod` in Task 1 (Task 2 adds the schemas).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "services/types" | (! grep -q .) && echo "types.ts compiles"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/types.ts` exports `PageNode`, `PageType`, `SearchIntent`, `PageStatus`, `ContentShell` and the four variant interfaces
    - `grep -c '"hub" | "pillar" | "service" | "static"' lib/services/types.ts` returns 1 (the discriminant union)
    - `ServicePage` declares `brandIds?: string[]`; `PillarPage` and `HubPage` do NOT declare `brandIds`
    - `ContentShell` declares `faqs` as an array of `{question, answer}` and `steps` as an array of `{title, body}` (structured, not string)
    - `ContentShell` declares `metaTitle`, `metaDescription`, `ogImage?` (SEO metadata co-located per D-14)
    - `npx tsc --noEmit` reports no error in `lib/services/types.ts`
  </acceptance_criteria>
  <done>lib/services/types.ts exports a well-formed PageNode discriminated union and a ContentShell that co-locates structured content + SEO metadata + keyword fields, compiling under strict mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the Zod schemas — contentShellSchema, publishedContentSchema, pagesSchema (Zod 4)</name>
  <read_first>
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 366-440 (Code Examples — `faqSchema`/`stepSchema`/`contentShellSchema` lines 371-397; `publishedContentSchema` status-gated superRefine lines 399-417; `pagesSchema` cross-record uniqueness superRefine lines 419-440) and lines 360-364 (Pitfall 5 — Zod 4 gotchas: `.array().min(1)` not `.nonempty()`, write-only `path` in `ctx.addIssue`, `z.prettifyError()`)
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 97-107 (the Zod-schemas section — copy from RESEARCH, the import-direction note for `urlFor`↔types.ts) and lines 309-318 (No Analog Found — Zod has zero in-repo precedent; use RESEARCH, project style for naming only)
    - .planning/phases/01-taxonomy-data-model/01-VALIDATION.md lines 44-48 (Crit 2/IA-09 + Crit 3/IA-08 rows — what the schemas must enforce)
    - lib/services/types.ts (the ContentShell + PageNode types from Task 1 — the schemas must align with these and/or `z.infer` back to them)
    - $HOME/.claude/rules/typescript/coding-style.md (Input Validation section — "Use Zod for schema-based validation and infer types from the schema")
  </read_first>
  <behavior>
    - `contentShellSchema` validates STRUCTURE only: h1 non-empty, intro is a string, steps/faqs are arrays, metaTitle/metaDescription non-empty (so a `draft` empty shell passes)
    - `publishedContentSchema` adds status-gated rules via superRefine: intro word-count >= 120, steps length >= 1, faqs length within 3-6 — these fire ONLY for review/published pages
    - `pagesSchema` validates an array of page nodes and superRefine-checks cross-record uniqueness: duplicate canonical URL => issue; duplicate primaryKeyword (case-insensitive) => issue naming the offender (cannibalization)
    - A draft page with empty intro/steps/faqs passes pagesSchema; a published page with a 10-word intro fails; two pages sharing a primaryKeyword fail
  </behavior>
  <action>
    In `lib/services/types.ts`, add `import { z } from "zod";` and define the three Zod schemas per RESEARCH §Code Examples (verified against zod.dev for Zod 4.4.3). `faqSchema` = `z.object({ question: z.string().min(1), answer: z.string().min(1) })`; `stepSchema` = `z.object({ title: z.string().min(1), body: z.string().min(1) })`. `contentShellSchema` = structure-only object (h1 `.min(1)`, intro `z.string()`, steps `z.array(stepSchema)`, faqs `z.array(faqSchema)`, localAngle `z.string()`, metaTitle/metaDescription `.min(1)`, ogImage `.optional()`). `publishedContentSchema` = `contentShellSchema.superRefine((c, ctx) => …)` adding: intro word-count `c.intro.trim().split(/\s+/).filter(Boolean).length >= 120` else `ctx.addIssue({ code: "custom", path: ["intro"], message })`; steps `.length >= 1`; faqs length in 3..6 — these are the content rules. `pagesSchema` = `z.array(pageSchema).superRefine((pages, ctx) => …)` where `pageSchema` applies `publishedContentSchema` to a node's `content` ONLY when `status` is `review`/`published` (else `contentShellSchema`), and the array-level superRefine checks uniqueness using two `Map`s keyed by the canonical URL and the lowercased `primaryKeyword`, emitting `ctx.addIssue({ path: [i, "primaryKeyword"] })` / `[i, "slug"]` on a duplicate. MUST use Zod 4 idioms only: `.array().min(1)` NOT `.nonempty()`; the WRITE `path` property in `ctx.addIssue` (read-side `ctx.path` was removed); reserve `z.prettifyError()` for the 01-06 script. Export `contentShellSchema`, `publishedContentSchema`, `pagesSchema` (and `pageSchema` if separate). For the canonical-URL identity inside the uniqueness refinement, RESOLVE the import direction now: either (a) accept a `urlFor` function injected into a `makePagesSchema(urlFor)` factory, or (b) compute the URL from the node's own slug fields with a small local slug helper duplicated minimally — pick ONE and document the choice in a top-of-file comment; do NOT create a circular `types.ts`↔`registry.ts` import. Prefer the factory or a local slug helper so `types.ts` does not import `registry.ts`.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep "services/types" | (! grep -q .) && grep -q "superRefine" lib/services/types.ts && ! grep -q ".nonempty()" lib/services/types.ts && echo "Zod schemas compile, Zod-4 idioms OK"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/services/types.ts` imports `zod` and exports `contentShellSchema`, `publishedContentSchema`, `pagesSchema`
    - `grep -c "superRefine" lib/services/types.ts` returns at least 2 (status-gated content + cross-record uniqueness)
    - `grep -c ".nonempty()" lib/services/types.ts` returns 0 (Zod 4 idiom — uses `.array().min(1)` instead)
    - The uniqueness refinement uses `ctx.addIssue({ ... path: [...] })` with a write-side `path` (not `ctx.path`)
    - `types.ts` does NOT `import` from `./registry` (no circular import — uniqueness uses a factory or local slug helper, documented in a comment)
    - `npx tsc --noEmit` reports no error in `lib/services/types.ts`
  </acceptance_criteria>
  <done>lib/services/types.ts exports the three Zod 4 schemas: structure-only contentShellSchema, status-gated publishedContentSchema, and pagesSchema with cross-record URL/keyword uniqueness — compiling under strict mode with no Zod-3 idioms and no circular import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new external trust boundary. This plan defines build-time TS types and Zod schemas. The schemas validate the *taxonomy data* (V5 input validation, build-time only) — no user input is processed this phase (the form's user-input validation is Phase 5 QA-02, which reuses this same `zod`).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-VAL | Tampering (data-integrity, build-time) | `pagesSchema` / `publishedContentSchema` | mitigate | The schemas are the anti-thin-content + anti-cannibalization gate. Status-gating ensures empty `draft` shells pass while a `review`/`published` page with a too-short intro or a duplicate keyword fails the build — preventing a low-quality/cannibalizing page from shipping (Dec-2025 Core Update risk). The 01-06 negative fixture proves the gate actually blocks. |
| T-04-DEP | Tampering | `import { z } from "zod"` | accept | `zod` was supply-chain vetted in plan 01-01 (slopcheck `[OK]`, no postinstall). No additional install here — this plan only imports it. |

**No HIGH-severity items.** Build-time validation logic only; no auth/network/secrets; user-input validation is deferred to Phase 5.
</threat_model>

<verification>
- `npx tsc --noEmit` compiles `lib/services/types.ts` under strict mode (the union + schemas are well-formed).
- `grep "superRefine" lib/services/types.ts` matches (≥2) and `grep ".nonempty()"` returns 0 (Zod-4 idioms).
- `types.ts` does not import `registry.ts` (no circular dependency).
- Full schema behavior (draft passes, broken published fails, duplicate keyword fails) is proven by the 01-06 negative-fixture task — this plan delivers the validators; 01-06 exercises them via the prebuild gate.
</verification>

<success_criteria>
- A discriminated-union `PageNode` + `ContentShell` typed contract exists (D-04/D-14/D-15).
- Three Zod 4 schemas enforce structure always and content rules only for review/published (IA-08, D-07/D-08).
- Cross-record URL + keyword uniqueness is encoded for IA-09 (exercised in 01-06).
- `types.ts` compiles under strict mode with no Zod-3 idioms and no circular import.
</success_criteria>

<output>
Create `.planning/phases/01-taxonomy-data-model/01-04-SUMMARY.md` when done. Record the resolved import-direction decision (factory vs local slug helper) so plan 01-05's registry wires `urlFor` consistently.
</output>
