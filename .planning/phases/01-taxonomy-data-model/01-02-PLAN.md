---
phase: 01-taxonomy-data-model
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/constants.ts
  - scripts/assert-site-shape.ts
autonomous: true
requirements: [SEO-08]
user_setup: []

must_haves:
  truths:
    - "NAP (name, address, phone, service area) resolves from one SITE constant with the full structured field set (SEO-08, Crit 5)"
    - "SITE.serviceRadiusKm === 60 — the single corrected radius value (D-09)"
    - "SITE.name === 'TPS klimaattechniek' (D-10)"
    - "SITE exposes geo.lat/lng, country, province, serviceAreas[] so Phase 3 JSON-LD and Phase 5 Maps pin read fields directly (D-12)"
  artifacts:
    - path: "lib/constants.ts"
      provides: "Extended SITE single-source-of-truth with full structured NAP + radius + geo + serviceAreas"
      contains: "serviceRadiusKm"
    - path: "scripts/assert-site-shape.ts"
      provides: "Node assertion that the full NAP field set is present and typed (Crit 5)"
      contains: "serviceAreas"
  key_links:
    - from: "lib/constants.ts SITE.serviceRadiusKm"
      to: "app/tarieven/page.tsx + components/PricingTabs.tsx radius copy (plan 01-03)"
      via: "SITE import consumed by visible radius copy"
      pattern: "serviceRadiusKm"
    - from: "lib/constants.ts SITE.geo + SITE.address parts"
      to: "Phase 3 JSON-LD / Phase 5 Maps pin"
      via: "field-by-field schema.org mapping (modeled now, consumed later)"
      pattern: "geo"
---

<objective>
Extend the existing `SITE` constant in `lib/constants.ts` into the complete, structured single source of truth for NAP (name, address, phone, service area) — the value every visible NAP string, Phase 3 JSON-LD field, and Phase 5 Maps pin will read. This fully satisfies SEO-08 in one pass and corrects the brand name (D-10) and service radius (D-09) at the source. Add a small Node assertion script that proves the full field set exists (Crit 5).

Purpose: SEO-08 requires NAP to resolve from one constants source. D-12 specifies the full structured set (address parts, country, province, geo, radius, serviceAreas) modeled so downstream phases just read fields. D-09 sets the corrected radius (60 km) that plan 01-03 then propagates into copy. The `geo.lat/lng` and `serviceAreas[]` values are owner-verify-pending and must be flagged as such inline so no unverified claim is emitted.
Output: Extended `SITE` object in `lib/constants.ts` (EXTEND in place — never parallel); a new `scripts/assert-site-shape.ts` structural assertion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-taxonomy-data-model/01-CONTEXT.md
@.planning/phases/01-taxonomy-data-model/01-RESEARCH.md
@.planning/phases/01-taxonomy-data-model/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@lib/constants.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SITE with the full structured NAP field set</name>
  <read_first>
    - lib/constants.ts (lines 1-13 — the current `SITE` object: name, tagline, phone, phoneDisplay, email, address, postcode, city, kvk, btw, whatsappUrl, closed with `as const`) — EXTEND this exact object, do NOT create a parallel one
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 33-66 (`lib/constants.ts` EXTEND SITE — the precise what-to-change/add list and the "extend in place, never parallel" rule)
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 26-30 (D-09/D-10/D-11/D-12 summaries), lines 459-479 (schema.org NAP modeling target — the field-by-field mapping SITE must mirror), lines 573-574 (A2 serviceAreas + A3 geo owner-verify-pending)
    - .planning/codebase/CONVENTIONS.md lines 20-37 (SCREAMING_SNAKE_CASE exported constants, `interface` for shapes, double quotes, trailing commas, `as const`)
    - CLAUDE.md (business-data guardrail — phone/email/address/KvK/BTW/radius come ONLY from SITE)
  </read_first>
  <behavior>
    - SITE.name === "TPS klimaattechniek" (was "TPS Ventilatie")
    - SITE.serviceRadiusKm === 60 (number, not string)
    - SITE.country === "NL" and SITE.province === "Zuid-Holland"
    - SITE.geo has numeric lat and lng (placeholder values, flagged owner-verify-pending)
    - SITE.serviceAreas is a non-empty string array seeded with Zoetermeer, Den Haag, Leidschendam-Voorburg, Pijnacker-Nootdorp, Lansingerland, Delft, Gouda, Leiden (flagged owner-review-pending)
    - SITE.email is UNCHANGED ("info@tpsventilatie.nl") and SITE.whatsappUrl is UNCHANGED (domain stays for launch)
    - SITE retains `as const`
  </behavior>
  <action>
    Modify the existing `SITE` object in `lib/constants.ts` in place. Change `name` from `"TPS Ventilatie"` to `"TPS klimaattechniek"` (D-10). ADD these fields (D-12): `country: "NL"`, `province: "Zuid-Holland"`, `geo: { lat: <placeholder number>, lng: <placeholder number> }`, `serviceRadiusKm: 60` (D-09), and `serviceAreas: [...]` seeded with the eight municipalities from D-11 (Zoetermeer, Den Haag, Leidschendam-Voorburg, Pijnacker-Nootdorp, Lansingerland, Delft, Gouda, Leiden). Add an inline comment on `geo` marking the lat/lng as owner-verify-pending placeholders (today's Maps pin is a placeholder per CONCERNS.md; Phase 5 QA-05 sets the real value) and an inline comment on `serviceAreas` marking the list owner-review-pending (never claim unserved areas — D-11). Choose flat-vs-nested layout for the address: SITE already has flat `address`/`postcode`/`city`, so KEEP those flat and add `country`/`province` as sibling flat fields; group only `geo` as a nested object (it maps to schema.org GeoCoordinates). Do NOT change `email`, `whatsappUrl`, `kvk`, `btw`, `phone`, `phoneDisplay`, `tagline`, or the domain. Keep `as const`, double quotes, trailing commas. Do NOT add any service/route data to this file (that lives in `lib/services/`); do NOT modify `NAV_LINKS`, `DIENSTEN_DROPDOWN`, or `TARIEVEN_DROPDOWN` (those become taxonomy-derived in Phase 2).
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -v "node_modules" | (! grep -i "constants") && echo "tsc OK on constants.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'name: "TPS klimaattechniek"' lib/constants.ts` returns 1
    - `grep -c 'serviceRadiusKm: 60' lib/constants.ts` returns 1
    - `grep -c 'country: "NL"' lib/constants.ts` returns 1 and `grep -c 'province: "Zuid-Holland"' lib/constants.ts` returns 1
    - `lib/constants.ts` contains a `geo:` object with numeric `lat`/`lng` and an owner-verify-pending comment
    - `lib/constants.ts` contains `serviceAreas:` with at least the 8 seed towns and an owner-review-pending comment
    - `grep -c 'info@tpsventilatie.nl' lib/constants.ts` returns 1 (email/domain unchanged)
    - `npx tsc --noEmit` reports no error originating in `lib/constants.ts` (strict mode compiles)
  </acceptance_criteria>
  <done>SITE holds the full structured NAP set (name corrected, radius 60, country/province, geo placeholder, serviceAreas seed) as one `as const` object, with owner-verify-pending fields flagged, and tsc compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add the SITE-shape assertion script (Crit 5)</name>
  <read_first>
    - lib/constants.ts (the SITE object as just extended in Task 1) — the object the assertion reads
    - .planning/phases/01-taxonomy-data-model/01-VALIDATION.md lines 42-50 (Crit 5 / SEO-08 row — the exact field list to assert: name, address, postcode, city, province, country, geo.lat, geo.lng, serviceRadiusKm, serviceAreas[]) and lines 54-63 (Wave 0 — SITE-shape assertion, use Node 26 built-in node:test/node:assert, do NOT add jest/vitest)
    - .planning/phases/01-taxonomy-data-model/01-RESEARCH.md lines 636 (Crit 5 structural assertion via node:test) and lines 621-624 (no test framework — Node 26 built-ins)
    - lib/forms.ts (the `@/` import idiom for importing from lib) — same alias resolution applies under tsx
  </read_first>
  <action>
    Create `scripts/assert-site-shape.ts` (the `scripts/` dir does not exist yet — create it). Import `SITE` from `@/lib/constants` and use Node 26's built-in `node:assert` (no jest/vitest — out of scope per REQUIREMENTS) to assert presence and correct type of the full NAP field set: `name` (non-empty string), `address`, `postcode`, `city`, `province`, `country` (non-empty strings), `geo.lat` + `geo.lng` (numbers), `serviceRadiusKm` (number, and assert it equals 60), and `serviceAreas` (a non-empty array of strings). On success, print a one-line OK. This script exits non-zero (assertion throws) if any field is missing/mistyped. Use `console.log`/`console.error` here intentionally — this is a build-time CLI assertion, not shipped runtime code (flag with a top-of-file comment so code review does not false-positive the console usage and the no-test-framework choice). Run it with `tsx scripts/assert-site-shape.ts`. This is a standalone assertion, NOT wired into `prebuild` (the prebuild gate in 01-06 validates the taxonomy; this script validates SITE shape and is run on demand / in the phase gate).
  </action>
  <verify>
    <automated>npx tsx scripts/assert-site-shape.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/assert-site-shape.ts` exists and imports `SITE` from `@/lib/constants`
    - `npx tsx scripts/assert-site-shape.ts` exits 0 and prints an OK line
    - The script asserts `serviceRadiusKm === 60` and that `serviceAreas` is a non-empty array (temporarily break a SITE field locally → script exits non-zero, then restore)
    - The script uses `node:assert` only (no jest/vitest import)
    - A top-of-file comment notes the intentional build-time console usage
  </acceptance_criteria>
  <done>scripts/assert-site-shape.ts asserts the full SITE NAP field set with node:assert and exits 0 against the extended SITE.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces no new external trust boundary. It edits a build-time TS constant and adds a build-time assertion script. No runtime input, no network, no secrets.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-DATA | Repudiation / Trust (data-integrity, not classic STRIDE) | `SITE.geo.lat/lng`, `SITE.serviceAreas` | mitigate | Model `geo` as placeholder numbers and `serviceAreas` as a seed list, BOTH flagged owner-verify/owner-review-pending inline. Never render these as verified facts in this phase; Phase 5 QA-05 fixes the real geo pin and the owner curates the town list (D-11). Prevents emitting an unverified location/coverage claim. |
| T-02-NAME | Repudiation | `SITE.name` rename → external Google Business Profile / KvK | accept | The rename is code-only this phase. GBP/KvK alignment is an explicit owner action verified in Phase 3 SEO-07 (RESEARCH Runtime State Inventory). Out of scope here; flagged to owner. |

**No HIGH-severity items.** No auth, no input, no network, no secrets — the only consideration is data-integrity of owner-pending facts, mitigated by placeholder-flagging.
</threat_model>

<verification>
- `npx tsx scripts/assert-site-shape.ts` exits 0 — full NAP field set present and typed (Crit 5).
- `grep -c "serviceRadiusKm: 60" lib/constants.ts` returns 1 (D-09 value at source — consumed by plan 01-03).
- `grep -c 'name: "TPS klimaattechniek"' lib/constants.ts` returns 1 (D-10).
- `npx tsc --noEmit` compiles the extended SITE under strict mode.
- Manual-only (does NOT block this phase): owner reviews `serviceAreas` town list; owner/Phase 5 supplies real `geo.lat/lng` (VALIDATION.md Manual-Only).
</verification>

<success_criteria>
- NAP resolves from one SITE source with the complete structured field set (SEO-08, Crit 5).
- `SITE.serviceRadiusKm === 60`, `SITE.name === "TPS klimaattechniek"`.
- `geo` and `serviceAreas` present and flagged owner-pending.
- SITE-shape assertion passes; tsc compiles.
</success_criteria>

<output>
Create `.planning/phases/01-taxonomy-data-model/01-02-SUMMARY.md` when done. Note which SITE fields are owner-verify-pending (geo, serviceAreas) for the Phase 3/4/5 hand-off.
</output>
