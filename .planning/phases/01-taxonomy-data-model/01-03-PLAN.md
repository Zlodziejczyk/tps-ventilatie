---
phase: 01-taxonomy-data-model
plan: 03
type: execute
wave: 2
depends_on: ["01-02"]
files_modified:
  - app/tarieven/page.tsx
  - components/PricingTabs.tsx
autonomous: true
requirements: [QA-03]
user_setup: []

must_haves:
  truths:
    - "The '50km' literal in app/tarieven/page.tsx is gone; the radius reads from SITE.serviceRadiusKm (QA-03, D-09)"
    - "Both '100 km' literals in components/PricingTabs.tsx are gone; the radius reads from SITE.serviceRadiusKm (QA-03, D-09)"
    - "The 50/100 km inconsistency no longer exists anywhere in app/ or components/ (Crit 4)"
    - "Visible radius copy reads 'tot 60 km vanuit Zoetermeer' via SITE interpolation (D-09)"
  artifacts:
    - path: "app/tarieven/page.tsx"
      provides: "SITE-sourced radius copy (one occurrence fixed)"
      contains: "SITE.serviceRadiusKm"
    - path: "components/PricingTabs.tsx"
      provides: "SITE-sourced radius copy (both occurrences fixed)"
      contains: "SITE.serviceRadiusKm"
  key_links:
    - from: "app/tarieven/page.tsx + components/PricingTabs.tsx"
      to: "lib/constants.ts SITE.serviceRadiusKm"
      via: "import { SITE } from '@/lib/constants'"
      pattern: "SITE.serviceRadiusKm"
---

<objective>
Eliminate the service-radius inconsistency at the consuming sites: replace the hardcoded "straal van 50km" in `app/tarieven/page.tsx` and BOTH hardcoded "straal van 100 km" occurrences in `components/PricingTabs.tsx` with copy sourced from `SITE.serviceRadiusKm` (set to 60 in plan 01-02). This is the literal embodiment of the single-source-of-truth guardrail and fully closes QA-03 / success criterion 4.

Purpose: QA-03 requires the 50-vs-100 km contradiction to be fixed *at the source* so it can never drift. RESEARCH confirmed neither coded value was correct (owner says 60). The radius now lives once in `SITE`; these three copy strings become derived. Both PricingTabs lines must change or the criterion-4 grep fails.
Output: `app/tarieven/page.tsx` and `components/PricingTabs.tsx` with SITE-sourced radius copy and the `SITE` import added to each (neither currently imports it).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-taxonomy-data-model/01-CONTEXT.md
@.planning/phases/01-taxonomy-data-model/01-RESEARCH.md
@.planning/phases/01-taxonomy-data-model/01-PATTERNS.md
@lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the 50km literal in app/tarieven/page.tsx with SITE-sourced copy</name>
  <read_first>
    - app/tarieven/page.tsx line 62 (exact current string: `<p>Wij zijn werkzaam in een straal van 50km rondom onze hoofdlocatie. Buiten deze regio geldt een kleine toeslag.</p>`) AND lines 1-7 (the import block — confirms `SITE` is NOT currently imported; `@/lib`/`@/components` idiom is already used there)
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 213-224 (`app/tarieven/page.tsx:62` MODIFY — add the `import { SITE } from "@/lib/constants";`, interpolate `{SITE.serviceRadiusKm}`, owner's exact phrasing "tot 60 km vanuit Zoetermeer")
    - lib/constants.ts (the extended SITE with `serviceRadiusKm: 60` from plan 01-02) — the field being read
    - .planning/codebase/CONVENTIONS.md lines 46-60 (import order: next internals → third-party → `@/lib/*` → `@/components`)
  </read_first>
  <action>
    Add `import { SITE } from "@/lib/constants";` to `app/tarieven/page.tsx` in the correct import-order position (with the other `@/lib`/`@/components` imports, after next internals). Replace the line-62 paragraph so the radius is interpolated from `{SITE.serviceRadiusKm}` and the phrasing follows the owner's exact wording "tot 60 km vanuit Zoetermeer" — e.g. the sentence becomes "Wij zijn werkzaam tot {SITE.serviceRadiusKm} km vanuit Zoetermeer. Buiten deze regio geldt een kleine toeslag." Do NOT re-hardcode `60`; interpolate the field. The literal `"straal van 50km"` / `"50km"` must be entirely gone. Finalize the exact Dutch sentence within the "tot 60 km vanuit Zoetermeer" constraint; keep the surrounding `<p>` JSX and the "kleine toeslag" clause intact. Touch nothing else in the file.
  </action>
  <verify>
    <automated>grep -rn "50km\|straal van 50" app/tarieven/page.tsx; test $(grep -c "SITE.serviceRadiusKm" app/tarieven/page.tsx) -ge 1 && test $(grep -c 'import { SITE }' app/tarieven/page.tsx) -eq 1 && ! grep -q "50km" app/tarieven/page.tsx && echo "tarieven radius OK"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "50km" app/tarieven/page.tsx` returns 0 (the literal is gone)
    - `grep -c "straal van 50" app/tarieven/page.tsx` returns 0
    - `grep -c "SITE.serviceRadiusKm" app/tarieven/page.tsx` returns at least 1
    - `grep -c 'import { SITE } from "@/lib/constants"' app/tarieven/page.tsx` returns 1
    - The radius copy reads "tot {SITE.serviceRadiusKm} km vanuit Zoetermeer" (owner's phrasing)
  </acceptance_criteria>
  <done>app/tarieven/page.tsx imports SITE and renders "tot 60 km vanuit Zoetermeer" via SITE.serviceRadiusKm; the 50km literal is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Replace BOTH 100 km literals in components/PricingTabs.tsx with SITE-sourced copy</name>
  <read_first>
    - components/PricingTabs.tsx lines 438-444 and 515-521 (the two identical info-banner blocks containing `Werkgebied: straal van 100 km vanuit Zoetermeer.` at lines 442 and 519) AND lines 1-9 (imports — confirms `SITE` is NOT imported; line 9 is the `type Tab` discriminant; it is a `"use client"` component)
    - .planning/phases/01-taxonomy-data-model/01-PATTERNS.md lines 228-240 (`components/PricingTabs.tsx:442,519` MODIFY — add `import { SITE } from "@/lib/constants";`, replace BOTH occurrences, SITE is safe in a client component, do NOT touch pricing data arrays)
    - lib/constants.ts (extended SITE with `serviceRadiusKm: 60`)
  </read_first>
  <action>
    Add `import { SITE } from "@/lib/constants";` to `components/PricingTabs.tsx` (correct import-order position; `SITE` is a plain module constant and is safe to import into a `"use client"` component — no server-only code). Replace BOTH info-banner strings (line 442 AND line 519 — they are identical) so the radius reads from `{SITE.serviceRadiusKm}` with the owner's phrasing, e.g. "Alle prijzen zijn inclusief BTW, voorrijkosten en klein materiaal. Werkgebied: tot {SITE.serviceRadiusKm} km vanuit Zoetermeer." Update BOTH occurrences — missing one fails the criterion-4 grep. Do NOT re-hardcode `100`/`60`. Do NOT touch the surrounding pricing data arrays (`WTW_UNITS`, `MV_ONDERHOUD`, etc.) — pricing stays in PricingTabs and out of taxonomy scope; only the radius copy changes. Keep the `<Icon name="info" />` banner markup intact.
  </action>
  <verify>
    <automated>grep -rn "100 km\|straal van 100" components/PricingTabs.tsx; test $(grep -c "SITE.serviceRadiusKm" components/PricingTabs.tsx) -eq 2 && test $(grep -c 'import { SITE }' components/PricingTabs.tsx) -eq 1 && ! grep -q "100 km" components/PricingTabs.tsx && echo "PricingTabs radius OK (both occurrences)"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "100 km" components/PricingTabs.tsx` returns 0 (both literals gone)
    - `grep -c "straal van 100" components/PricingTabs.tsx` returns 0
    - `grep -c "SITE.serviceRadiusKm" components/PricingTabs.tsx` returns exactly 2 (both banners fixed)
    - `grep -c 'import { SITE } from "@/lib/constants"' components/PricingTabs.tsx` returns 1
    - The pricing data arrays (`WTW_UNITS`, `MV_ONDERHOUD`) are unchanged (git diff shows only the import + two banner lines changed)
  </acceptance_criteria>
  <done>components/PricingTabs.tsx imports SITE and renders "tot 60 km vanuit Zoetermeer" in both info banners via SITE.serviceRadiusKm; both 100 km literals are gone.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundary. This plan edits two visible-copy strings to read from an existing build-time constant. No runtime input, no network, no secrets.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-DATA | Repudiation / Trust (data-integrity) | radius copy shown to prospects | mitigate | Radius copy now derives from the single owner-confirmed `SITE.serviceRadiusKm = 60`, eliminating the contradictory 50/100 km claims that could mislead a prospect about coverage. The criterion-4 grep asserts the old literals are gone, preventing a stale value from surviving. |

**No HIGH-severity items.** Pure copy-sourcing change; no auth/input/network/secrets surface.
</threat_model>

<verification>
- `grep -rn "straal van 50\|straal van 100\|100 km\|50km" app components` returns NOTHING (Crit 4 — the phase-wide radius grep).
- `grep -c "SITE.serviceRadiusKm" app/tarieven/page.tsx` ≥ 1 and `grep -c "SITE.serviceRadiusKm" components/PricingTabs.tsx` == 2.
- Both files now import `SITE` from `@/lib/constants`.
- Note on `app/over-ons/page.tsx`: RESEARCH Open-Question 3 asked whether over-ons needs a SITE-sourced radius too. Confirmed during planning — `app/over-ons/page.tsx:63` contains regio prose ("regio Zoetermeer en omstreken, inclusief Den Haag, Leiden, Delft en Rotterdam") but NO numeric radius literal, so it is NOT part of the Crit-4 grep target and is intentionally left untouched here (town-list editorial belongs to Phase 4; do not edit it in this phase).
</verification>

<success_criteria>
- The 50 km / 100 km inconsistency no longer exists anywhere in `app/` or `components/` (QA-03, Crit 4).
- Both consuming files read the radius from `SITE.serviceRadiusKm`.
- Copy reads "tot 60 km vanuit Zoetermeer" (D-09 phrasing).
</success_criteria>

<output>
Create `.planning/phases/01-taxonomy-data-model/01-03-SUMMARY.md` when done.
</output>
