---
phase: 05-lead-capture-form-security-launch-qa
plan: 05
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - lib/useEnableHeavyMotion.ts
  - app/page-sections/HeroSection.tsx
  - components/LazyMap.tsx
  - app/contact/page.tsx
autonomous: true
requirements: [QA-05, QA-06, QA-07]
must_haves:
  truths:
    - "An SSR-safe useEnableHeavyMotion hook returns false on the server and on first client render, then flips to true only after mount when the viewport is desktop (>=768px) AND prefers-reduced-motion is no-preference — so the WebGL aurora / canvas particles are NEVER mounted on mobile and a static fallback renders instead"
    - "The hero gates SoftAurora through that hook (renders a cheap CSS gradient fallback otherwise); the reusable hook is the seam Phase 6 inherits for its own hero motion"
    - "The contact map is corrected to the verified business location from SITE.geo (52.04822769870841, 4.502050197039296) — replacing the wrong placeholder pin (52.0623, 4.4932) — and is lazy-mounted (IntersectionObserver) so it costs nothing until scrolled near"
    - "LCP images use preload (Next 16) rather than the deprecated priority prop; no <Image> uses a custom quality value that is not allowlisted in next.config images.qualities; all images already use next/image (no raw <img>)"
  artifacts:
    - path: "lib/useEnableHeavyMotion.ts"
      provides: "SSR-safe desktop + reduced-motion gate hook (mobile/SSR => static fallback)"
      contains: "matchMedia"
    - path: "components/LazyMap.tsx"
      provides: "IntersectionObserver-mounted Google Maps iframe at the corrected SITE.geo coordinates"
      contains: "IntersectionObserver"
  key_links:
    - from: "app/page-sections/HeroSection.tsx"
      to: "lib/useEnableHeavyMotion.ts"
      via: "gate SoftAurora mount on desktop+motion"
      pattern: "useEnableHeavyMotion"
    - from: "components/LazyMap.tsx"
      to: "lib/constants.ts SITE.geo"
      via: "embed URL built from the verified coordinates"
      pattern: "SITE.geo"
---

<objective>
The mobile launch-QA trio that protects Core Web Vitals and trust (QA-05/06/07). Build an SSR-safe `useEnableHeavyMotion` hook and use it to STOP mounting the WebGL aurora (and any canvas particles) on mobile — the likeliest INP offender — rendering a cheap gradient fallback instead, while keeping desktop motion unless `prefers-reduced-motion`. Fix the contact map to the real business location from `SITE.geo` (the live iframe points at the wrong placeholder coordinates) and lazy-mount it so it costs nothing above the fold. Modernize image usage for the now-active Image Optimization (05-01): LCP images use `preload` (not the deprecated `priority`), and any custom `quality` props are allowlisted. The hook is the reusable seam Phase 6 inherits.

Purpose: Make the highest-traffic surfaces fast and correct on mobile — no main-thread WebGL where it hurts INP, a map that points at the actual business, and next-gen responsive images — so SEO-10's CWV gate (verified in 05-06) can pass.
Output: `lib/useEnableHeavyMotion.ts`, a gated hero, `components/LazyMap.tsx` + the corrected contact map, and `preload`/quality-correct images.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-CONTEXT.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-RESEARCH.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: SSR-safe useEnableHeavyMotion hook + gate the hero aurora (QA-06)</name>
  <files>lib/useEnableHeavyMotion.ts, app/page-sections/HeroSection.tsx</files>
  <read_first>
    - 05-RESEARCH.md §"Pattern 4: SSR-safe motion gate" (the exact hook: init `false`, in useEffect read `matchMedia(min-width:768px)` AND `matchMedia(prefers-reduced-motion: no-preference)`, listen for changes, set enabled = desktop && motionOk) + §"Pitfall 5: Hydration mismatch" + §"Anti-Patterns" (mounting the WebGL aurora unconditionally is the INP offender)
    - app/page-sections/HeroSection.tsx (the component that mounts `<SoftAurora />` — grep to confirm the import + where it renders; this is the one currently-live heavy-motion component) + components/SoftAurora.tsx (the WebGL/OGL component being gated) + lib/useParticleEngine.ts / components/FocalParticles.tsx / components/AmbientParticles.tsx (canvas particles — RESEARCH notes these are NOT currently mounted on any route, so gating the aurora is the concrete target; if any IS mounted, gate it the same way)
    - app/globals.css (confirm a cheap gradient utility exists or add a small `.aurora-fallback` class / use a Tailwind gradient for the static fallback)
  </read_first>
  <action>
    Create `lib/useEnableHeavyMotion.ts` (`"use client"`) exporting `useEnableHeavyMotion(minWidth = 768): boolean` exactly per RESEARCH Pattern 4: state inits `false` (so SSR + first client render = static fallback, guaranteeing no hydration mismatch AND that mobile never mounts the heavy component); in `useEffect`, create `window.matchMedia('(min-width:'+minWidth+'px)')` and `window.matchMedia('(prefers-reduced-motion: no-preference)')`, define `update = () => setEnabled(desktop.matches && motionOk.matches)`, call it once, add `change` listeners on both, and return a cleanup removing them. Then in the hero component, call `const heavy = useEnableHeavyMotion()` and render `{heavy ? <SoftAurora .../> : <div className="aurora-fallback" aria-hidden />}` where `.aurora-fallback` is a cheap CSS/Tailwind gradient approximating the aurora's resting colors (no canvas, no JS). Do the same gate for any canvas-particle component that is actually mounted. Do NOT change SoftAurora's internals — only its mount condition. Keep the hero a client component (it already is).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const h=fs.readFileSync('lib/useEnableHeavyMotion.ts','utf8'); const bad=[]; if(!/matchMedia/.test(h))bad.push('hook does not use matchMedia'); if(!/min-width/.test(h))bad.push('hook does not gate on desktop min-width'); if(!/prefers-reduced-motion/.test(h))bad.push('hook does not check prefers-reduced-motion'); if(!/useState\(\s*false\s*\)/.test(h))bad.push('hook does not init false (SSR/mobile fallback)'); if(bad.length){console.error(bad.join('; '));process.exit(1)} console.log('useEnableHeavyMotion OK')"</automated>
    <automated>node -e "const fs=require('fs'); const f='app/page-sections/HeroSection.tsx'; const s=fs.readFileSync(f,'utf8'); if(!/useEnableHeavyMotion/.test(s)){console.error('hero does not gate motion via useEnableHeavyMotion');process.exit(1)} if(!/SoftAurora/.test(s)){console.error('expected SoftAurora reference in hero');process.exit(1)} console.log('hero aurora gated OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/useEnableHeavyMotion.ts` returns `false` on SSR/first render and flips true only on desktop (>=768px) with motion allowed; it listens for media-query changes.
    - The hero renders `<SoftAurora>` only when the hook is true, else a static gradient fallback — so mobile mounts NO WebGL/canvas.
    - Any actually-mounted canvas-particle component is gated the same way.
  </acceptance_criteria>
  <done>Both verifies print OK; heavy motion is desktop-and-motion-only. No-`<canvas>`-on-mobile is confirmed in DevTools on preview (05-06).</done>
</task>

<task type="auto">
  <name>Task 2: Corrected, lazy-mounted contact map (QA-05)</name>
  <files>components/LazyMap.tsx, app/contact/page.tsx</files>
  <read_first>
    - 05-RESEARCH.md §"Pattern 6: Lazy map" (the corrected embed: programmatic keyless `https://www.google.com/maps?q=${lat},${lng}&z=15&output=embed` from SITE.geo as the autonomous default; the official GBP "Embed a map" iframe is the optional agency swap) + §"Assumptions Log A1" (verify output=embed renders on preview)
    - app/contact/page.tsx (the existing Google Maps `<iframe>` ~line 116 with the WRONG hardcoded coords 52.0623, 4.4932 and a fake place id — replace it; NOTE 05-03 swaps the form on this page — touch ONLY the map block here, leave the form to 05-03)
    - lib/constants.ts (`SITE.geo` = { lat: 52.04822769870841, lng: 4.502050197039296 } — the verified pin; the map MUST derive from this, never a hardcoded literal)
  </read_first>
  <action>
    Create `components/LazyMap.tsx` (`"use client"`, named export `LazyMap`): render a lightweight placeholder (a surface-toned box, optional static label/skeleton — no iframe yet) and use an `IntersectionObserver` (or a small "mount on first near-viewport" effect) to swap in the real `<iframe>` only once it scrolls near. Build the iframe `src` from `SITE.geo` as `https://www.google.com/maps?q=${SITE.geo.lat},${SITE.geo.lng}&z=15&output=embed` (keyless, exact coordinates); set `loading="lazy"`, a title, and an accessible label. Then in `app/contact/page.tsx` replace the existing hardcoded map `<iframe>` (the 52.0623/4.4932 placeholder) with `<LazyMap />`. Remove the wrong literal coordinates entirely. Add a brief comment that the agency MAY swap the programmatic embed for the official GBP "Embed a map" iframe (verified business name card) if preferred — same component, different `src`. Do not modify the contact form (05-03 owns it).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const m=fs.readFileSync('components/LazyMap.tsx','utf8'); const c=fs.readFileSync('app/contact/page.tsx','utf8'); const bad=[]; if(!/IntersectionObserver|useEffect/.test(m))bad.push('LazyMap not lazy-mounted'); if(!/SITE\.geo/.test(m))bad.push('LazyMap does not derive coords from SITE.geo'); if(!/output=embed/.test(m))bad.push('LazyMap missing output=embed src'); if(/52\.0623|4\.4932/.test(c)||/52\.0623|4\.4932/.test(m))bad.push('wrong placeholder coords 52.0623/4.4932 still present'); if(!/LazyMap/.test(c))bad.push('contact page does not use LazyMap'); if(bad.length){console.error(bad.join('; '));process.exit(1)} console.log('lazy map + corrected pin OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/LazyMap.tsx` builds its iframe `src` from `SITE.geo` (`output=embed`), mounts the iframe only on intersection, and sets `loading="lazy"`.
    - `app/contact/page.tsx` renders `<LazyMap />`; the old 52.0623/4.4932 placeholder coordinates are gone.
  </acceptance_criteria>
  <done>The verify prints OK; the map points at the verified location and defers its cost. Pin correctness is eyeballed on preview (05-06).</done>
</task>

<task type="auto">
  <name>Task 3: Modernize images for the active optimizer — preload + qualities (QA-07 component)</name>
  <files>app/page-sections/HeroSection.tsx</files>
  <read_first>
    - 05-RESEARCH.md §"Pitfall 3: priority deprecated → use preload" + §"Pitfall 2: qualities required (default [75])" + §"State of the Art" (priority→preload; AVIF opt-in done in 05-01) — and the note that there are ZERO raw `<img>` tags (all images already use `next/image`), so this is modernization, not migration
    - the LCP `<Image>` usages: grep the repo for `next/image` + `priority` (e.g. components/ServiceHero.tsx, the hero image in app/page-sections/HeroSection.tsx, pillar hero) — these are the above-the-fold images whose `priority` should become `preload`
    - next.config.ts (05-01 — `images.formats` now serves AVIF/WebP; `images.qualities` default [75])
  </read_first>
  <action>
    Audit `next/image` usages for two Next-16 modernizations: (1) For the LCP image on key pages (the home hero in `app/page-sections/HeroSection.tsx`, and — if trivially in scope — the `components/ServiceHero.tsx` hero), replace the deprecated `priority` prop with `preload`. Keep `sizes`/`fill` as-is. (2) For any `<Image quality={N}>` where N is not 75, either drop the prop (use the default) or confirm 05-01 added N to `images.qualities` — do not leave an un-allowlisted custom quality (it throws in Next 16). Limit edits to the LCP-critical images first (RESEARCH discretion: prioritize LCP images); list any deferred non-critical images in the SUMMARY. Do NOT convert files or add a loader — Vercel's optimizer handles WebP/AVIF + srcset automatically now that `unoptimized` is gone. (Scope note: this task's `files_modified` lists the home hero; if the executor also modernizes `components/ServiceHero.tsx`, add it to the plan's files in the SUMMARY — keep edits surgical and avoid files another wave-2 plan is editing.)
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const f='app/page-sections/HeroSection.tsx'; const s=fs.readFileSync(f,'utf8'); if(/<Image[^>]*\bpriority\b/.test(s)){console.error('home hero <Image> still uses deprecated priority — switch to preload');process.exit(1)} console.log('home hero image uses preload (or no priority) OK')"</automated>
    <automated>node -e "const cp=require('child_process'); let out=''; try{out=cp.execSync('grep -rEl \"quality=\\\\{?[0-9]\" app components 2>/dev/null || true').toString();}catch(e){} if(out.trim()){console.log('NOTE: files with explicit <Image quality> to confirm are allowlisted in next.config images.qualities:\\n'+out);} else {console.log('no explicit <Image quality> props — default [75] OK');}"</automated>
  </verify>
  <acceptance_criteria>
    - The home hero LCP `<Image>` uses `preload` (or no `priority`), not the deprecated `priority` prop.
    - No `<Image>` uses a custom `quality` value absent from `images.qualities` (else 05-01 allowlisted it).
    - No raw `<img>` regressions introduced; no custom loader added.
  </acceptance_criteria>
  <done>The verifies print OK; LCP images are preload-correct and quality-safe. AVIF/WebP + srcset delivery is confirmed in the Network panel on preview (05-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client device capability → render path | Viewport/motion preference decides whether heavy WebGL mounts; a wrong gate degrades mobile INP (CWV risk) |
| third-party Google Maps embed → page | The map iframe loads google.com in an embedded frame (lazy, no API key, no PII passed) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-motion | DoS (perf/CWV) | Unconditional WebGL aurora / canvas on mobile → INP regression | mitigate | `useEnableHeavyMotion` keeps heavy motion desktop-and-motion-only; mobile/SSR mounts a static gradient; proven by "no `<canvas>` mounts" + INP<200ms on preview (05-06) |
| T-5-mappin | Spoofing (trust) | Map pinned to the wrong location undermines local trust + GBP/NAP consistency | mitigate | Coordinates derive from the verified `SITE.geo`; the placeholder literals are removed; verify-grep blocks 52.0623/4.4932 |
| T-5-thirdparty | Information Disclosure | Map embed leaking data / heavy third-party cost above the fold | mitigate | Keyless `output=embed` (no API key, no user data passed); `loading="lazy"` + IntersectionObserver defer the cost |
| T-5-SC | Tampering (supply chain) | package installs | accept | N/A — zero packages installed in this plan |
</threat_model>

<verification>
- Verifies are pure Node string reads / greps (OneDrive-safe): hook shape, hero gating, lazy map + corrected coords, preload on the LCP image, and a quality-prop audit.
- Manual proof on the Vercel preview (05-06): DevTools mobile emulation shows NO `<canvas>`/WebGL mounting on mobile + a static fallback; `prefers-reduced-motion` honored on desktop; the map pin matches `SITE.geo`; the Network panel shows AVIF/WebP + `srcset`; Lighthouse "next-gen formats" passes; mobile INP < 200ms (SEO-10, gated in 05-06).
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- Heavy motion is gated to desktop + allowed-motion via a reusable SSR-safe hook (mobile gets a static fallback); the contact map points at the verified `SITE.geo` and is lazy-mounted; LCP images use `preload` and serve AVIF/WebP — clearing the path for SEO-10's CWV gate.
</success_criteria>

<output>
Create `.planning/phases/05-lead-capture-form-security-launch-qa/05-05-SUMMARY.md` when done.
</output>
</content>
