---
phase: 05-lead-capture-form-security-launch-qa
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - package.json
  - package-lock.json
  - .env.example
  - .planning/PROJECT.md
autonomous: true
requirements: [QA-01, QA-07]
must_haves:
  truths:
    - "next.config.ts no longer contains output: \"export\" and no longer contains images.unoptimized; the build runs in Next.js default hybrid mode (SSG pages prerender, route handlers run as serverless functions)"
    - "next.config.ts opts AVIF in explicitly via images.formats: [\"image/avif\", \"image/webp\"] (AVIF is NOT served by default — default is webp-only)"
    - "project D-03 trailingSlash: false is preserved unchanged in next.config.ts"
    - "@upstash/ratelimit and @upstash/redis are in package.json dependencies (durable serverless rate-limit infra for 05-02); zod/next/react already present"
    - "the GHL webhook env var is renamed in .env.example from NEXT_PUBLIC_GHL_WEBHOOK_URL to server-only GHL_WEBHOOK_URL, and the two Upstash vars (UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN) are documented"
    - "the static-export-vs-hybrid decision (QA-01) is resolved to HYBRID and logged in .planning/PROJECT.md Key Decisions (the ⚠️ Revisit row is replaced with the resolved outcome)"
  artifacts:
    - path: "next.config.ts"
      provides: "Hybrid hosting config: no output:export, no unoptimized, AVIF/WebP formats, trailingSlash:false preserved"
      contains: "image/avif"
    - path: ".env.example"
      provides: "Server-only GHL_WEBHOOK_URL + Upstash vars (no NEXT_PUBLIC_ on the secret)"
      contains: "GHL_WEBHOOK_URL"
    - path: ".planning/PROJECT.md"
      provides: "QA-01 hybrid decision logged in Key Decisions (the formal go/no-go gate result)"
      contains: "hybrid"
  key_links:
    - from: "next.config.ts (drop output:export)"
      to: "app/api/lead/route.ts (05-02)"
      via: "hybrid mode restores request-reading Route Handlers"
      pattern: "route handler"
    - from: ".env.example GHL_WEBHOOK_URL"
      to: "app/api/lead/route.ts process.env.GHL_WEBHOOK_URL (05-02)"
      via: "server-only secret, no NEXT_PUBLIC_"
      pattern: "GHL_WEBHOOK_URL"
---

<objective>
THE decision gate, resolved and made real. This is Wave 1 foundation — the single enabling change the whole phase depends on. Remove `output: "export"` and `images.unoptimized` from `next.config.ts` (Next.js default hybrid: ~22 pages stay statically prerendered, route handlers become serverless functions, Image Optimization turns on), opt AVIF in explicitly, and **preserve project D-03 `trailingSlash: false`**. Install the durable rate-limit infra (`@upstash/ratelimit` + `@upstash/redis`) that 05-02's secure route uses. Rename the GHL webhook env var to a **server-only** `GHL_WEBHOOK_URL` in `.env.example` (kills the client-exposed-secret risk at the config layer) and document the Upstash vars. Finally, **log the QA-01 static-vs-hybrid decision in `.planning/PROJECT.md`** — the formal go/no-go this phase owns (D-01).

Purpose: Flip the one switch that unblocks server-side form security (QA-02), reliable notification (LEAD-02), and image optimization (QA-07) — and record the decision in the project's decision ledger so it can never silently regress.
Output: Hybrid `next.config.ts`, two new deps in `package.json`/`package-lock.json`, a server-only `.env.example`, and a resolved QA-01 row in PROJECT.md.
</objective>

<phase_source_audit>
> Multi-source coverage audit for ALL of Phase 5 (recorded once, here in the Wave-1 lead plan). Every source item is COVERED by a plan. No silent omissions.

**GOAL (ROADMAP §Phase 5 — 5 success criteria):**
| Goal criterion | Covered by |
|----------------|-----------|
| 1. Prospect submits short offerte form (+ reassurance + AVG consent); owner notified within seconds (real E2E) | 05-03 (form) + 05-02 (route forward) + 05-06 (live E2E proof) |
| 2. Form fails safely (visible error + phone/WhatsApp fallback); lead path secured (server secret, Zod, honeypot, rate limit) | 05-03 (fail-safe) + 05-02 (security) + 05-01 (secret relocated to server env) |
| 3. Static-vs-hybrid resolved + logged in PROJECT.md; next.config supports the secure POST route + WebP/AVIF optimization | 05-01 |
| 4. Floating contact affordance site-wide; every CTA/`tel:`/`mailto:`/`wa.me` verified | 05-04 |
| 5. Mobile CWV pass (INP<200ms, good LCP); aurora/particles gated on mobile/reduced-motion; Maps pin correct | 05-05 (motion + map) + 05-06 (CWV gate) |

**REQ (REQUIREMENTS.md — 14 phase requirements):**
| Req | Covered by |
|-----|-----------|
| LEAD-01 offerte form + reassurance + short field set | 05-03 |
| LEAD-02 submit → instant owner notification (GHL) | 05-02 (route→GHL) + 05-03 (client trigger) + 05-06 (live proof) |
| LEAD-03 site-wide floating contact affordance | 05-04 |
| LEAD-04 every CTA/tel/mailto/wa.me verified | 05-04 |
| LEAD-05 visible error + phone/WhatsApp fallback, no silent hang | 05-03 |
| LEAD-06 AVG consent + GHL named processor in privacy | 05-03 |
| QA-01 resolve + log static-vs-hybrid in PROJECT.md | 05-01 |
| QA-02 secure path: server secret, Zod, honeypot, rate limit | 05-02 (+ 05-06 validation) |
| QA-04 network error handling on submit | 05-03 |
| QA-05 fix placeholder Maps pin | 05-05 |
| QA-06 gate WebGL aurora + particles on mobile/reduced-motion | 05-05 |
| QA-07 build-time image optimization (WebP/AVIF) | 05-01 (config) + 05-05 (component modernization) |
| QA-08 E2E lead-notification test — real submit → owner notified | 05-06 |
| SEO-10 mobile CWV pass (INP<200ms, good LCP) | 05-06 |

**RESEARCH (05-RESEARCH.md features/constraints):** Hybrid flip first (changes whole build) → 05-01. Secure Route Handler (Zod v4 + honeypot + @upstash/ratelimit) → 05-02. Shared lead-schema → 05-02. Thin client caller (lib/forms.ts) → 05-03. Reusable OfferteForm (extend ContactForm state machine) → 05-03. Layout-level sticky bar adapted to REAL tokens (glass-nav, not the non-existent --glass-strong) → 05-04. SSR-safe useEnableHeavyMotion hook → 05-05. Lazy map from SITE.geo → 05-05. Image modernization (qualities allowlist, priority→preload) → 05-05. Vercel-preview-only validation (OneDrive deadlock; never push main) → all plans' verifies + 05-06. Upstash vs in-memory open question → 05-02 (graceful-degrade) + 05-06 (provision checkpoint). @vercel/kv deprecated (do NOT use) → 05-01 dep choice. All COVERED.

**CONTEXT (D-01…D-17):** D-01 hybrid flip→05-01; D-02 WhatsApp+email notify→05-06(GHL config)+05-02(forward); D-03 server secret+env→05-01(env)+05-06(Vercel/GHL); D-04 live test on preview/never push main→05-06; D-05 field set→05-03; D-06 dienst dropdown+prefill→05-03; D-07 fail-safe→05-03; D-08 AVG consent→05-03; D-09 reassurance copy→05-03; D-10 reusable component→05-03; D-11 sticky bar→05-04; D-12 dismissal persists session→05-04; D-13 link sweep→05-04; D-14 motion gating→05-05; D-15 map fix→05-05; D-16 images→05-01(config)+05-05(component); D-17 CWV gate→05-06. All 17 COVERED.

**Exclusions (not gaps):** all CONTEXT Deferred Ideas (out-of-area postcode hint, full GHL CRM/nurture, Static Maps API key, per-town location pages); Phase 6 homepage rebuild (only consumes this phase's form + bar). No MISSING items → no source-audit escalation.
</phase_source_audit>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-CONTEXT.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-RESEARCH.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip next.config.ts to hybrid + enable AVIF/WebP image optimization</name>
  <files>next.config.ts</files>
  <read_first>
    - next.config.ts (current 11-line file: `output: "export"`, `trailingSlash: false`, `images: { unoptimized: true }` — see exactly what is there before editing)
    - 05-RESEARCH.md §"Pattern 1: Hybrid flip (the gate)" (the target config) + §"Pitfall 2: `qualities` is now required in Next 16" (default [75]) + §"Pitfall 3: `priority` is deprecated → use `preload`"
    - 05-CONTEXT.md §D-01 (hybrid is the resolved gate; preserve trailingSlash:false + apex canonical) + §D-16 (remove unoptimized; WebP/AVIF)
  </read_first>
  <action>
    Rewrite `next.config.ts` so the `NextConfig` object: (a) DROPS the `output: "export"` line entirely (this restores request-reading Route Handlers + Image Optimization — Next.js default hybrid); (b) DROPS `images.unoptimized` and instead sets `images.formats` to the two-element array `["image/avif", "image/webp"]` (AVIF must be opted in explicitly — the default is webp-only); (c) KEEPS `trailingSlash: false` unchanged (project D-03 — independent of output mode). Before finishing, grep the repo for any existing `<Image ... quality={` usages: if any custom quality value other than the default 75 is used, add `images.qualities` listing 75 plus that value (Next 16 requires custom qualities to be allowlisted); if no custom quality props exist, do NOT add `qualities` (the [75] default is correct). Add a short comment on the formats line noting AVIF is an explicit opt-in. Do NOT add `export const dynamic` anywhere and do NOT add a `loader` — Vercel's default loader is what we want.
    CRITICAL (OneDrive): do NOT run `next build` / `tsx` / `tsc` locally — the build is validated on Vercel CI via a pushed preview branch (see 05-06). `npm install` (Task 2) is safe locally.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('next.config.ts','utf8'); const errs=[]; if(/output\s*:\s*[\x22\x27]export[\x22\x27]/.test(s))errs.push('output:export still present'); if(/unoptimized/.test(s))errs.push('images.unoptimized still present'); if(!/formats/.test(s)||!/image\/avif/.test(s)||!/image\/webp/.test(s))errs.push('images.formats avif+webp missing'); if(!/trailingSlash\s*:\s*false/.test(s))errs.push('trailingSlash:false not preserved'); if(errs.length){console.error(errs.join('; '));process.exit(1)} console.log('next.config hybrid+images OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` contains NO `output: "export"` and NO `images.unoptimized`.
    - `next.config.ts` sets `images.formats` to `["image/avif", "image/webp"]`.
    - `trailingSlash: false` is still present (project D-03 preserved).
    - If any `<Image quality={N}>` with N≠75 exists in the codebase, `images.qualities` includes N (else `qualities` is absent).
  </acceptance_criteria>
  <done>The verify prints OK; the config is hybrid with AVIF/WebP and trailingSlash preserved. (Build green is proven on Vercel preview in 05-06, never locally.)</done>
</task>

<task type="auto">
  <name>Task 2: Install the durable rate-limit dependencies (@upstash/ratelimit + @upstash/redis)</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - package.json (current dependencies — confirm zod ^4.x, next 16.2.1, react 19.x are already present; you are ADDING only the two Upstash packages)
    - 05-RESEARCH.md §"Standard Stack → Supporting" (versions: @upstash/ratelimit 2.0.8, @upstash/redis 1.38.0) + §"Package Legitimacy Audit" (slopcheck clean, no postinstall scripts) + the explicit "Do NOT use @vercel/kv — deprecated" note
  </read_first>
  <action>
    Run `npm install @upstash/ratelimit @upstash/redis` (this is safe on the OneDrive mount — only `next build`/`tsx`/`tsc` deadlock, not `npm install`). This adds both to `dependencies` in `package.json` and updates `package-lock.json`. Do NOT install `@vercel/kv` (maintainer-deprecated per RESEARCH). Do NOT run any build/test command afterward. If `npm install` is unexpectedly slow or hangs on the OneDrive mount, note it in the SUMMARY and fall back to adding the two dependency entries to `package.json` by hand at the researched versions (`@upstash/ratelimit` ^2.0.8, `@upstash/redis` ^1.38.0) so 05-02 can import them once installed in CI.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); const d={...p.dependencies,...p.devDependencies}; const miss=['@upstash/ratelimit','@upstash/redis'].filter(k=>!d[k]); if(d['@vercel/kv']){console.error('@vercel/kv must NOT be added (deprecated)');process.exit(1)} if(miss.length){console.error('missing deps: '+miss.join(', '));process.exit(1)} console.log('upstash deps present: ratelimit '+d['@upstash/ratelimit']+', redis '+d['@upstash/redis'])"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` dependencies include `@upstash/ratelimit` and `@upstash/redis`.
    - `@vercel/kv` is NOT a dependency.
    - `package-lock.json` is updated (lockfile reflects the new packages).
  </acceptance_criteria>
  <done>The verify prints the two Upstash versions; no `@vercel/kv`; lockfile updated.</done>
</task>

<task type="auto">
  <name>Task 3: Make the GHL secret server-only in .env.example + document Upstash vars</name>
  <files>.env.example</files>
  <read_first>
    - .env.example (current — declares `NEXT_PUBLIC_GHL_WEBHOOK_URL=` and `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`)
    - 05-RESEARCH.md §"Runtime State Inventory" (rename rule: NEXT_PUBLIC_GHL_WEBHOOK_URL → GHL_WEBHOOK_URL; the old Vercel var must be deleted post-cutover; GOOGLE_SITE_VERIFICATION is unrelated and stays) + §"Recommended Project Structure" (.env.example diff)
    - 05-CONTEXT.md §D-03 (secret server-side, no NEXT_PUBLIC_)
  </read_first>
  <action>
    Edit `.env.example`: rename `NEXT_PUBLIC_GHL_WEBHOOK_URL` to `GHL_WEBHOOK_URL` (the `NEXT_PUBLIC_` prefix is what leaks a value into the client bundle — removing it is the core of QA-02's secret relocation). Keep it value-less (`GHL_WEBHOOK_URL=`) with a one-line comment that it is the GoHighLevel inbound webhook URL, server-only, set in Vercel for Preview + Production. Add the two Upstash vars `UPSTASH_REDIS_REST_URL=` and `UPSTASH_REDIS_REST_TOKEN=` with a comment that they come from the Vercel Marketplace "Upstash Redis" integration and back the `/api/lead` rate limiter (optional — the route degrades to honeypot-only if absent; see 05-02). Leave `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` untouched (unrelated, legitimately public). Add a comment noting the actual Vercel dashboard env edits + the deletion of the OLD public var happen in 05-06 (this file documents the contract; it does not set live values).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.env.example','utf8'); const errs=[]; if(/NEXT_PUBLIC_GHL_WEBHOOK_URL/.test(s))errs.push('NEXT_PUBLIC_GHL_WEBHOOK_URL still present (secret would leak to client)'); if(!/^GHL_WEBHOOK_URL=/m.test(s))errs.push('server-only GHL_WEBHOOK_URL missing'); if(!/UPSTASH_REDIS_REST_URL/.test(s)||!/UPSTASH_REDIS_REST_TOKEN/.test(s))errs.push('Upstash vars not documented'); if(!/NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION/.test(s))errs.push('GSC var was wrongly removed'); if(errs.length){console.error(errs.join('; '));process.exit(1)} console.log('.env.example server-only contract OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `.env.example` contains `GHL_WEBHOOK_URL=` and NOT `NEXT_PUBLIC_GHL_WEBHOOK_URL`.
    - `.env.example` documents `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN`.
    - `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION` is preserved.
  </acceptance_criteria>
  <done>The verify prints OK; the secret is server-only by contract; Upstash vars documented.</done>
</task>

<task type="auto">
  <name>Task 4: Log the QA-01 static-vs-hybrid decision in PROJECT.md Key Decisions</name>
  <files>.planning/PROJECT.md</files>
  <read_first>
    - .planning/PROJECT.md §"Key Decisions" (the table; the last row is `Static-export vs hybrid for a secure form route | … | ⚠️ Revisit (resolve in comms/QA planning)` — THIS is the row to resolve) + §"Constraints" line about `output: "export"` "open decision"
    - 05-CONTEXT.md §D-01 (the resolved decision text + that it MUST be logged in PROJECT.md Key Decisions as part of this phase) + ROADMAP §Phase 5 "Decision Gate"
  </read_first>
  <action>
    Update the `Static-export vs hybrid for a secure form route` row's Outcome cell in the Key Decisions table from `⚠️ Revisit (resolve in comms/QA planning)` to a RESOLVED outcome: state the decision is **Hybrid** — drop `output: "export"`; add one server Route Handler (`app/api/lead/route.ts`) holding the GHL webhook secret server-side; all other ~22 pages stay SSG; `trailingSlash: false` and the apex canonical preserved; resolved in Phase 5 (QA-01), 2026-06-29. Include the marker `✅` and the word `hybrid` in the cell. Also update the §Constraints line that calls `output: "export"` an "open decision" to reflect that the hybrid decision is now made (Phase 5). Update the document footer `*Last updated:*` line to note Phase 5 planning / QA-01 resolution on 2026-06-29. Do not alter unrelated rows.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('.planning/PROJECT.md','utf8'); const errs=[]; if(/Revisit \(resolve in comms\/QA planning\)/.test(s))errs.push('QA-01 row still marked ⚠️ Revisit'); const row=(s.split('\n').find(l=>/Static-export vs hybrid/.test(l))||''); if(!/hybrid/i.test(row)||!/✅/.test(row))errs.push('QA-01 row not resolved to ✅ hybrid'); if(errs.length){console.error(errs.join('; '));process.exit(1)} console.log('QA-01 decision logged OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The Key Decisions `Static-export vs hybrid` row Outcome no longer says "⚠️ Revisit" — it states the resolved **hybrid** decision with a ✅ marker and the Phase 5 / QA-01 reference.
    - The §Constraints "open decision" wording is updated to reflect the resolution.
    - No unrelated Key Decisions rows changed.
  </acceptance_criteria>
  <done>The verify prints OK; QA-01 is satisfied — the decision gate is formally recorded in the project ledger.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client bundle ↔ server secret | Renaming `NEXT_PUBLIC_GHL_WEBHOOK_URL` → `GHL_WEBHOOK_URL` moves the webhook out of any client-readable surface at the config layer (the live Vercel var cutover + old-var deletion is 05-06) |
| dev machine → npm registry | This plan installs two packages (`@upstash/*`) — the only supply-chain surface in the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-secret | Information Disclosure | GHL webhook URL readable from the client bundle via the `NEXT_PUBLIC_` prefix | mitigate (begin) | THIS plan renames the var to server-only `GHL_WEBHOOK_URL` in `.env.example`; 05-02 reads it server-side only; 05-06 sets the live Vercel var + **deletes** the old public one |
| T-5-SC | Tampering (supply chain) | `npm install @upstash/ratelimit @upstash/redis` | mitigate | RESEARCH §Package Legitimacy Audit: slopcheck 0.2.0 clean, 0 phantom packages, NO postinstall scripts on either Upstash package; pinned to researched versions; `@vercel/kv` deliberately excluded (deprecated) |
| T-5-config | Denial of Service (build) | A page silently going dynamic after the flip → unexpected serverless render | accept/verify | RESEARCH Pitfall 1 + A5: all ~22 routes currently use `generateStaticParams`/no dynamic API → stay SSG; proven by reading the Vercel build output in 05-06 (`○/●` static, `ƒ` only for `/api/lead`) |
</threat_model>

<verification>
- All four task verifies are pure Node string/JSON reads (safe on OneDrive — no `tsx`/`next build`).
- The hybrid build itself is proven GREEN ON VERCEL preview/CI in 05-06 (the de-facto test run) — never `npm run build` locally (OneDrive deadlock).
- The live Vercel env cutover (set `GHL_WEBHOOK_URL`, delete the old `NEXT_PUBLIC_` var) + Upstash provisioning are dashboard actions tracked in 05-06.
</verification>

<success_criteria>
- `next.config.ts` is hybrid (no output:export, no unoptimized), serves AVIF/WebP, and preserves `trailingSlash: false`.
- `@upstash/ratelimit` + `@upstash/redis` are installed; `@vercel/kv` is not.
- The webhook secret is server-only by contract in `.env.example`; Upstash vars documented.
- QA-01 is logged in PROJECT.md as a resolved **hybrid** decision — the gate this phase owns is closed.
</success_criteria>

<output>
Create `.planning/phases/05-lead-capture-form-security-launch-qa/05-01-SUMMARY.md` when done.
</output>
</content>
</invoke>
