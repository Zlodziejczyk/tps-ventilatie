---
phase: 05-lead-capture-form-security-launch-qa
plan: 06
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-03", "05-04", "05-05"]
files_modified:
  - .planning/phases/05-lead-capture-form-security-launch-qa/05-VALIDATION.md
autonomous: false
requirements: [QA-08, SEO-10, QA-02, LEAD-02]
must_haves:
  truths:
    - "The live infra exists: Vercel holds a server-only GHL_WEBHOOK_URL for Preview+Production, the OLD NEXT_PUBLIC_GHL_WEBHOOK_URL Vercel var is DELETED, the Upstash Redis integration (or the accepted honeypot-only fallback) is in place, and the agency-configured GoHighLevel workflow fans a form submit out to WhatsApp + email"
    - "A Vercel PREVIEW deploy (pushed non-main branch) builds GREEN with the hybrid output: ~22 content routes prerender as static (○/●) and only /api/lead is a function (ƒ); the built client bundle does NOT contain the GHL webhook string (secret absent from the browser)"
    - "The secure path is proven on preview: curl with malformed JSON => 400, missing required field => 400, honeypot-filled => 200 with NO GHL delivery, a clean valid body => 200; and a forced 502 / offline submit shows the visible error + Bel/WhatsApp/retry (never stuck 'sending')"
    - "QA-08 is proven: one real clean submit on the preview notifies the owner via WhatsApp + email within seconds; SEO-10 is proven: PageSpeed/Lighthouse mobile on the preview shows INP < 200ms and good LCP"
    - "main / production is never pushed and vercel --prod is never run during this phase — all proof is on the preview; owner sign-off remains the launch gate"
  artifacts:
    - path: ".planning/phases/05-lead-capture-form-security-launch-qa/05-VALIDATION.md"
      provides: "Per-requirement validation statuses flipped to green after preview proof"
      contains: "nyquist_compliant"
  key_links:
    - from: "Vercel preview deploy"
      to: "GoHighLevel workflow (WhatsApp + email)"
      via: "real submit → /api/lead → GHL → owner notified (QA-08)"
      pattern: "GHL"
    - from: "PageSpeed/Lighthouse mobile (preview URL)"
      to: "SEO-10 CWV gate"
      via: "INP < 200ms + good LCP"
      pattern: "INP"
---

<objective>
The launch-readiness gate (QA-08, SEO-10) — and the live proof for QA-02 / LEAD-02 that only a real deploy can give. This plan stands up the out-of-git runtime state (Vercel env cutover, Upstash provisioning, the agency GoHighLevel workflow), deploys a **preview** (a pushed non-`main` branch — local `next build` deadlocks on OneDrive), then proves the whole lead path end-to-end on that preview: a green hybrid build with the right static/function split, the webhook secret ABSENT from the client bundle, the secure-path contract (curl matrix), the fail-safe error affordance, the real submit → WhatsApp + email notification, and mobile Core Web Vitals (INP < 200ms, good LCP). `autonomous: false` — it depends on real dashboards, a real notification, and a real human. **Never push `main`; never `vercel --prod`.**

Purpose: Turn "the code is written" into "the lead path demonstrably works and is fast and secure" — on a preview, with the owner's sign-off as the final launch gate.
Output: live infra in place, a green preview with all QA proofs recorded, and `05-VALIDATION.md` statuses flipped to green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-CONTEXT.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-RESEARCH.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-VALIDATION.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Stand up the live infra (Vercel env cutover · Upstash · GoHighLevel workflow)</name>
  <files></files>
  <read_first>
    - 05-RESEARCH.md §"Runtime State Inventory" (the three out-of-git items: (1) re-add the webhook as server-only GHL_WEBHOOK_URL + DELETE the old NEXT_PUBLIC_ var; (2) provision Upstash Redis Marketplace integration → UPSTASH_REDIS_REST_URL/_TOKEN; (3) build the GHL workflow) + §"Environment Availability" (GHL workflow has NO fallback; Upstash has the honeypot-only fallback) + §"Open Questions Q1" (Upstash vs zero-infra)
    - 05-CONTEXT.md §D-02 (notify = WhatsApp + email) + §D-03 (env scoped Preview+Production; agency configures GHL) + §D-04 (never push main / never vercel --prod)
    - .env.example (05-01 — the exact var names the live env must match: GHL_WEBHOOK_URL, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN)
  </read_first>
  <what-built>
    All code is merged on the working branch: hybrid config (05-01), the secure route (05-02), the form (05-03), the bar (05-04), motion/map/images (05-05). What is missing is the runtime state that lives in dashboards, not git.
  </what-built>
  <how-to-verify>
    The agency (Oskar) confirms, in the Vercel + Upstash + GoHighLevel dashboards: (1) `GHL_WEBHOOK_URL` is set for Preview + Production (server-only, no NEXT_PUBLIC_) AND the old `NEXT_PUBLIC_GHL_WEBHOOK_URL` var is DELETED; (2) EITHER the Upstash Redis Marketplace integration is added (providing the two REST vars) OR the team accepts honeypot-only best-effort limiting for launch (the route degrades gracefully — record the choice); (3) a GoHighLevel workflow is live: inbound webhook trigger → notify owner by WhatsApp AND email.
  </how-to-verify>
  <action>
    Claude posts this checklist and waits — these are human/dashboard actions, not repo edits: ① Vercel → Settings → Environment Variables: add `GHL_WEBHOOK_URL` (the GHL inbound webhook URL) for Preview + Production; DELETE the legacy `NEXT_PUBLIC_GHL_WEBHOOK_URL`. ② Decide rate-limit infra (Open-Q1): add the Vercel Marketplace "Upstash Redis" integration (sets `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` on Preview+Prod) for durable limiting, OR accept honeypot-only for launch (the route works either way). ③ GoHighLevel: build/confirm the workflow (form/inbound-webhook trigger → WhatsApp message + email to the owner). Claude records the chosen rate-limit path and the GHL workflow status in the SUMMARY.
  </action>
  <resume-signal>Type "infra ready: env cutover done (old var deleted), ratelimit=&lt;upstash|honeypot-only&gt;, GHL workflow live" — or list what's still pending.</resume-signal>
  <acceptance_criteria>
    - Vercel has server-only `GHL_WEBHOOK_URL` (Preview+Prod); the old `NEXT_PUBLIC_GHL_WEBHOOK_URL` is deleted.
    - The rate-limit path is decided and recorded (Upstash provisioned, or honeypot-only accepted).
    - A GoHighLevel workflow notifies the owner via WhatsApp + email on submit.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Preview deploy + build/secret/secure-path verification (QA-01 build gate, QA-02)</name>
  <files></files>
  <read_first>
    - 05-RESEARCH.md §"Pitfall 1: A page silently opts out of SSG after the flip" (read the Vercel build output — all ~22 routes static `○/●`, only `/api/lead` a function `ƒ`) + §"Validation Architecture → Phase Requirements → Validation Map" (the QA-01/QA-02 rows: build output + curl matrix + client-bundle grep) + §"Pitfall 4: Rate limiter blocks the QA-08 test" (use distinct test data / wait the window)
    - 05-VALIDATION.md §"Per-Task Verification Map" (QA-01, QA-02 rows) + §"Sampling Rate" (push a non-main branch → Vercel preview build is the de-facto test run)
    - 05-CONTEXT.md §"Specifics" (never push main; validate on Vercel CI/preview; no local next build — OneDrive deadlock)
  </read_first>
  <what-built>
    The full Phase-5 code on a working branch, ready to deploy as a Vercel preview (serverless functions run on previews — a true end-to-end environment).
  </what-built>
  <how-to-verify>
    Push the working branch (a NON-`main` branch) so Vercel builds a preview, or run `vercel` (preview, NOT `--prod`). Then: (a) the build is GREEN and the build output shows the ~22 content routes as static (`○`/`●`) and ONLY `/api/lead` as a function (`ƒ`) — no content route silently became `ƒ (Dynamic)`; (b) grep the deployed client bundle / view-source for the GHL webhook string — it MUST be ABSENT (secret server-only); (c) curl the preview `/api/lead`: malformed JSON → 400, a body missing a required field → 400, a honeypot-filled body (`website` non-empty) → 200 but NO GHL delivery, a clean valid body → 200.
  </how-to-verify>
  <action>
    Claude prepares the deploy: confirm the branch is NOT `main`, push it (or invoke the `vercel` preview deploy — never `--prod`), and capture the preview URL + the build route map. Claude then runs (or relays for the operator to run) the curl matrix against the preview `/api/lead` and the client-bundle grep for the webhook string, and reads the Vercel build output for the static/function split. Record each result (build green + route split, secret-absent, the four curl outcomes) in the SUMMARY. If a content route shows as dynamic `ƒ`, investigate (a stray `cookies()/headers()`/uncached fetch) before proceeding. Do NOT push `main`; do NOT run `vercel --prod`.
  </action>
  <resume-signal>Type "preview verified: &lt;preview-url&gt;, build green + only /api/lead is ƒ, webhook string absent from bundle, curl 400/400/200-no-deliver/200 all pass" — or list failures.</resume-signal>
  <acceptance_criteria>
    - A green Vercel PREVIEW build exists; ~22 routes are static and only `/api/lead` is a function (QA-01 build gate).
    - The GHL webhook string is absent from the client bundle (QA-02 secret).
    - The curl matrix passes: 400 (malformed), 400 (invalid), 200-without-delivery (honeypot), 200 (valid) (QA-02 contract).
    - `main` was not pushed; `--prod` was not run.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: QA-08 live notification + fail-safe + the manual preview matrix</name>
  <files></files>
  <read_first>
    - 05-VALIDATION.md §"Manual-Only Verifications" (owner-notified-within-seconds; fail-safe error affordance; motion gating; maps pin; next-gen formats) + §"Per-Task Verification Map" (LEAD-02/QA-08, LEAD-05/QA-04, LEAD-03/04, QA-05, QA-06, QA-07 rows)
    - 05-CONTEXT.md §D-04 (the live test on a preview) + §D-07 (fail-safe) + §D-11/D-13 (bar + link sweep) + §D-14/D-15/D-16 (motion/map/images)
    - 05-RESEARCH.md §"Pitfall 4" (rate-limiter can block repeated test submits — use distinct data / wait the 10-min window, or temporarily widen on preview)
  </read_first>
  <what-built>
    The verified green preview from Task 2, with live GHL/Upstash from Task 1 — ready for the human-in-the-loop proofs that no CLI can substitute.
  </what-built>
  <how-to-verify>
    On the preview URL: (1) QA-08 — submit ONE clean valid lead → the owner receives a WhatsApp push AND an email within seconds (confirm BOTH channels). (2) LEAD-05/QA-04 — force the route to fail (offline / a temporarily bad webhook / devtools-blocked request) → the form shows the visible error with one-tap Bel + WhatsApp + retry and NEVER stays stuck on "sending"; submitting without the consent checkbox is blocked. (3) LEAD-03/04 — the sticky bar is hidden at top, slides in past ~200px on several routes, the × dismisses for the session, it stacks under 560px and is reduced-motion-aware; every CTA/`tel:`/`mailto:`/`wa.me` resolves. (4) QA-06 — DevTools mobile emulation shows NO `<canvas>`/WebGL mounting + a static fallback; `prefers-reduced-motion` honored on desktop. (5) QA-05 — the map pin matches `SITE.geo` (the real business). (6) QA-07 — the Network panel shows images served as AVIF/WebP with `srcset`.
  </how-to-verify>
  <action>
    Claude assembles the manual test matrix (the six checks above with exact steps + the preview URL) and posts it; the human runs them (relaying the QA-08 channel confirmation from the owner where needed) and returns pass/fail per item. For QA-08, use distinct test data or wait out the rate-limit window so a clean submit reaches both channels. Claude records every result in the SUMMARY. Any failure loops back to the owning plan (fail-safe → 05-03, bar → 05-04, motion/map/images → 05-05, secret/route → 05-02) and is re-previewed before this gate completes. No repo file changes in this task.
  </action>
  <resume-signal>Type "QA matrix: notify=&lt;ok both channels&gt;, failsafe=&lt;ok&gt;, bar/links=&lt;ok&gt;, motion=&lt;ok&gt;, map=&lt;ok&gt;, images=&lt;ok&gt;" — or list failures + the plan to fix.</resume-signal>
  <acceptance_criteria>
    - QA-08: a real submit notifies the owner via WhatsApp + email within seconds (both confirmed).
    - LEAD-05/QA-04: forced failure shows the visible error + Bel/WhatsApp/retry (never stuck "sending"); no-consent submit is blocked.
    - LEAD-03/04, QA-05, QA-06, QA-07 manual checks all pass on the preview (or are fixed and re-verified).
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: SEO-10 mobile Core Web Vitals gate (INP < 200ms + good LCP)</name>
  <files></files>
  <read_first>
    - 05-VALIDATION.md §"Manual-Only Verifications" (mobile INP<200ms + good LCP via PSI/Lighthouse on the preview) + §"Phase gate" (preview green + QA-08 proven + mobile INP<200ms/good LCP before owner sign-off)
    - 05-RESEARCH.md §"Phase Requirements → SEO-10" (INP good ≤ 200ms; LCP good ≤ 2.5s) + §"Assumptions Log A6" (confirm thresholds in the PSI report) + the QA-06 link (motion gating is the main INP lever)
  </read_first>
  <what-built>
    The green preview with motion gating live (05-05) — the configuration that should let mobile INP pass.
  </what-built>
  <how-to-verify>
    Run PageSpeed Insights (or Lighthouse) in MOBILE mode against the preview URL (test the heaviest pages — home + a pillar page). Confirm INP < 200ms and LCP is "good" (≤ 2.5s); CLS ≤ 0.1. If INP is borderline, confirm no `<canvas>`/WebGL is mounting on mobile (QA-06) and that the LCP image uses `preload` + AVIF/WebP (QA-07).
  </how-to-verify>
  <action>
    Claude posts the exact PSI/Lighthouse steps (mobile, the specific preview URLs to test) and waits; the human runs them and returns the INP / LCP / CLS numbers. Claude records them in the SUMMARY and, if a metric fails, ties it back to the likely lever (motion gating 05-05 / image preload 05-05 / a heavy third-party) for a fix + re-test. This is a launch criterion — it must pass on the preview before owner sign-off.
  </action>
  <resume-signal>Type "CWV: INP=&lt;ms&gt;, LCP=&lt;s&gt;, CLS=&lt;n&gt; on &lt;pages&gt;" (pass if INP<200ms + LCP≤2.5s) — or list the failing metric.</resume-signal>
  <acceptance_criteria>
    - Mobile INP < 200ms and good LCP (≤ 2.5s) on the preview for the key pages (SEO-10).
    - Any failing metric is traced to a lever, fixed, and re-measured.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Flip the validation statuses to green + record launch readiness</name>
  <files>.planning/phases/05-lead-capture-form-security-launch-qa/05-VALIDATION.md</files>
  <read_first>
    - .planning/phases/05-lead-capture-form-security-launch-qa/05-VALIDATION.md (the Per-Task Verification Map status column — all pending; the Wave 0 Requirements checkboxes; the Validation Sign-Off checklist; the frontmatter nyquist_compliant:false / wave_0_complete:false)
    - the SUMMARY results from Tasks 1-4 (the recorded pass/fail per requirement)
  </read_first>
  <action>
    Update `05-VALIDATION.md` to reflect the proven state: in the Per-Task Verification Map, flip each requirement's Status from pending to green for every check that passed on the preview (QA-01, QA-02, LEAD-01, LEAD-02/QA-08, LEAD-05/QA-04, LEAD-06, LEAD-03, LEAD-04, QA-05, QA-06, QA-07, SEO-10); leave any unproven item pending with a note. Tick the Wave 0 Requirements items now satisfied (GHL workflow exists; Upstash provisioned or honeypot-only accepted). Tick the Validation Sign-Off checklist items that hold. Set frontmatter `wave_0_complete: true` and `nyquist_compliant: true` ONLY if all in-scope checks passed; otherwise leave them false and list the gaps. This file is a planning artifact (not shipped) — edits are safe on OneDrive.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='.planning/phases/05-lead-capture-form-security-launch-qa/05-VALIDATION.md'; const s=fs.readFileSync(p,'utf8'); if(!/nyquist_compliant/.test(s)){console.error('VALIDATION.md missing nyquist_compliant frontmatter');process.exit(1)} const head=s.split('## Wave 0')[0]; const pend=(head.match(/pending/g)||[]).length; console.log('VALIDATION updated; remaining pending rows in map: '+pend+' (informational — confirm each is intentional)')"</automated>
  </verify>
  <acceptance_criteria>
    - `05-VALIDATION.md` Per-Task statuses reflect the preview proofs (passed checks marked green; any unproven left pending with a note).
    - Frontmatter `nyquist_compliant` / `wave_0_complete` are `true` only if every in-scope check passed (else false + listed gaps).
  </acceptance_criteria>
  <done>The verify prints the remaining-pending count; the validation tracker is honest about what was proven on the preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dashboards (Vercel/Upstash/GHL) → running app | The lead path's secret + rate-limit + notification all live in dashboard state, invisible to git; this task makes them real and consistent with the code |
| preview deploy → public internet | The preview is publicly reachable; the same secure-route controls (05-02) apply, proven here by curl + bundle grep |
| production (`main`) | Deliberately untouched this phase — launch is owner-gated; only the preview is exercised |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-secret | Information Disclosure | The OLD `NEXT_PUBLIC_GHL_WEBHOOK_URL` Vercel var still readable client-side | mitigate (complete) | Task 1 DELETES the legacy public var + sets the server-only one; Task 2 greps the built client bundle to PROVE the webhook string is absent |
| T-5-payload / T-5-spam | Tampering / DoS | Server validation + honeypot/rate-limit not actually enforcing | verify | Task 2 curl matrix proves 400/400/200-no-deliver/200; rate-limit path (Upstash or honeypot-only) recorded in Task 1 |
| T-5-config | DoS (build) | A content route silently going dynamic after the flip | verify | Task 2 reads the Vercel build output — only `/api/lead` may be `ƒ`; any other `ƒ` is investigated |
| T-5-prod | Elevation (premature launch) | Pushing `main` / `vercel --prod` before owner sign-off | mitigate | Hard rule across every task: preview-only; `main`/prod untouched; owner sign-off is the launch gate |
| T-5-pii | Information Disclosure | Real PII in the QA-08 test submit | accept/handle | Use test/own data for the live submit; the route does not log PII; GHL is the named processor (LEAD-06) |
</threat_model>

<verification>
- This is the phase's validation plan — verification IS the work: the QA-01 build gate (Vercel build output), the QA-02 secret+contract (bundle grep + curl matrix), LEAD-02/QA-08 (live WhatsApp+email), LEAD-05/QA-04 (forced-failure error UI), QA-05/06/07 (manual preview matrix), and SEO-10 (mobile PSI/Lighthouse) are all proven on the Vercel PREVIEW.
- The only repo-file verify (Task 5) is a pure Node read of VALIDATION.md (OneDrive-safe).
- Hard constraint honored throughout: no local `next build`/`tsx`; never push `main`; never `vercel --prod`.
</verification>

<success_criteria>
- Live infra is in place (server-only secret with the old var deleted, rate-limit path chosen, GHL workflow live); a green preview proves the static/function split and the absent client secret; the secure-path curl matrix + fail-safe both pass; QA-08 notifies the owner via WhatsApp + email; mobile INP < 200ms + good LCP (SEO-10).
- `05-VALIDATION.md` reflects the proven state; `main`/production remains untouched, ready for owner sign-off as the launch gate.
</success_criteria>

<output>
Create `.planning/phases/05-lead-capture-form-security-launch-qa/05-06-SUMMARY.md` when done.
</output>
</content>
