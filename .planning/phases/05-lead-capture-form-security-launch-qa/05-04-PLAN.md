---
phase: 05-lead-capture-form-security-launch-qa
plan: 04
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - components/StickyContactBar.tsx
  - app/layout.tsx
autonomous: true
requirements: [LEAD-03, LEAD-04]
must_haves:
  truths:
    - "A single site-wide sticky contact bar (StickyContactBar) is rendered at app/layout.tsx body level — a direct child of <body>, outside any transform/filter/backdrop-filter/container-type ancestor (no container-trap) — so it is viewport-fixed on every page"
    - "The bar shows Bel · WhatsApp · Offerte, appears on scroll (~200px past top) so it never competes with a hero CTA, is dismissible (×) with dismissal persisted for the session (sessionStorage), is mobile-safe (padding-bottom: max(12px, env(safe-area-inset-bottom)), stacked < 560px), and is reduced-motion-aware"
    - "The bar's CSS is adapted to the REAL design tokens (the glass-nav utility / signature-gradient), NOT the sketch's non-existent --glass-strong/--glass-blur/--ease-spring; all hrefs source from SITE (tel:SITE.phone, SITE.whatsappUrl, /contact)"
    - "It is the ONLY always-on contact element — there is no second floating WhatsApp FAB; existing per-component WhatsApp/tel/mailto links (Footer, ServiceHero, CTABanner, MobileMenu, contact page) are swept and confirmed to resolve and source SITE (LEAD-04)"
  artifacts:
    - path: "components/StickyContactBar.tsx"
      provides: "Layout-level Sketch-003-D sticky contact bar (scroll-in, dismissible, mobile-safe, reduced-motion)"
      contains: "sessionStorage"
    - path: "app/layout.tsx"
      provides: "Body-level mount of <StickyContactBar /> (single always-on contact affordance)"
      contains: "StickyContactBar"
  key_links:
    - from: "app/layout.tsx <body>"
      to: "components/StickyContactBar.tsx"
      via: "direct body-level child (no container-trap)"
      pattern: "StickyContactBar"
    - from: "components/StickyContactBar.tsx"
      to: "lib/constants.ts SITE"
      via: "tel:/wa.me/Offerte hrefs from the single source"
      pattern: "SITE"
---

<objective>
The site-wide speed-to-contact lever (LEAD-03/04). Realize the validated **Sketch-003-D** sticky contact bar as `components/StickyContactBar.tsx`, mounted at `app/layout.tsx` body level so it is genuinely viewport-fixed on every page (the container-trap is the documented failure mode — keep it a direct `<body>` child). Bel · WhatsApp · Offerte; scroll-in ~200px past the top so it never fights a hero CTA; dismissible × persisted for the session; mobile-safe and reduced-motion-aware. Critically, **adapt the sketch CSS to the real tokens** — the sketch's `--glass-strong`/`--glass-blur`/`--ease-spring` do NOT exist in `globals.css`; use the `glass-nav` utility + an inline spring easing. Then run the LEAD-04 link sweep: this bar is the ONE always-on contact element (no separate WhatsApp FAB), and every existing CTA/`tel:`/`mailto:`/`wa.me` across the site is confirmed to resolve and source `SITE`. Phase 6 inherits this exact component.

Purpose: Put a calm, ever-present, non-nagging way to call/WhatsApp/request a quote on every page — the single shared contact affordance the whole site (and Phase 6) reuses.
Output: `components/StickyContactBar.tsx` + a body-level mount in `app/layout.tsx`, plus a verified link sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-CONTEXT.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-RESEARCH.md
@.planning/phases/05-lead-capture-form-security-launch-qa/05-VALIDATION.md
@.claude/skills/sketch-findings-tps-ventilatie/references/trust-and-contact.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the layout-level StickyContactBar (Sketch-003-D adapted to real tokens)</name>
  <files>components/StickyContactBar.tsx</files>
  <read_first>
    - .claude/skills/sketch-findings-tps-ventilatie/references/trust-and-contact.md (Sketch-003-D: the `.cbar` show/hide pattern, the scroll>200 JS, the mobile-safe rules, the container-trap warning, AND the explicit Phase-5 coordination note "build ONE shared bar, not two")
    - 05-RESEARCH.md §"Pattern 5: Layout-level sticky bar — adapt, don't copy, the sketch CSS" (the CRITICAL token mismatch: --glass-strong/--glass-blur/--ease-spring DON'T exist → use `glass-nav` utility + inline cubic-bezier; the Tailwind-class skeleton; the container-trap "verified safe" note — overflow:clip on html/body does NOT trap fixed positioning) + §"Pitfall 5: Hydration mismatch" (init state to hidden; read sessionStorage/scroll only in useEffect)
    - app/globals.css (confirm the real tokens: the `glass-nav` utility ~rgba(255,255,255,0.85)+backdrop-filter blur(20px) saturate(1.2), and `signature-gradient`; confirm `--glass-strong` etc. are ABSENT)
    - lib/constants.ts (`SITE.phone`, `SITE.phoneDisplay`, `SITE.whatsappUrl`) + components/Icon.tsx (Icon wrapper for the phone/WhatsApp glyphs — never raw Material Symbols spans)
  </read_first>
  <action>
    Create `components/StickyContactBar.tsx` (`"use client"`, named export `StickyContactBar`). State: `show` (init `false`) and `dismissed` (init `false`) — both start hidden to avoid hydration mismatch. In a `useEffect`: if `sessionStorage.getItem("cbar-dismissed") === "1"` set `dismissed` and return; else attach a passive `scroll` listener setting `show = window.scrollY > 200` (call once on mount), and clean up on unmount. Render nothing when `dismissed`. Markup: a `fixed inset-x-0 bottom-0` element with a high z-index, the `glass-nav` utility for the glass surface (NOT `--glass-strong`), an upward shadow, `transition-transform` with `motion-reduce:transition-none` and an inline spring `cubic-bezier(0.22,1,0.36,1)`, transform `translateY(125%)` when hidden / `none` when shown, and inline `paddingBottom: "max(12px, env(safe-area-inset-bottom))"`. Inner row: a short message ("Direct contact?") + actions **Bel** (`tel:${SITE.phone}`, show `SITE.phoneDisplay`), **WhatsApp** (`SITE.whatsappUrl`, `target="_blank" rel="noopener"`), **Offerte** (link to `/contact` — the simplest action target per RESEARCH Open-Q3; inline-modal is deferred to Phase 6), and a `×` close button that sets `dismissed`, writes `sessionStorage.setItem("cbar-dismissed","1")`. Mobile: under 560px stack the inner column and let buttons go full-width (Tailwind `max-[560px]:` classes or a small responsive block). Use the `Icon` wrapper for glyphs. All hrefs MUST come from `SITE` — no hardcoded numbers. Keep within Atmospheric Clarity (no 1px borders; `on-surface` text). Do NOT paste the sketch's `--glass-strong`/`--ease-spring` CSS.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('components/StickyContactBar.tsx','utf8'); const need=['use client','StickyContactBar','scrollY','200','sessionStorage','cbar-dismissed','SITE','tel:','whatsapp','/contact','env(safe-area-inset-bottom)','motion-reduce']; const miss=need.filter(k=>!new RegExp(k.replace(/[.*+?^${}()|[\]\\\\]/g,'\\\\$&'),'i').test(s)); const bad=[]; if(/--glass-strong|--glass-blur|--ease-spring/.test(s))bad.push('uses non-existent sketch tokens — adapt to glass-nav'); if(!/glass-nav/.test(s))bad.push('does not use the real glass-nav utility'); if(miss.length)bad.push('missing: '+miss.join(', ')); if(bad.length){console.error(bad.join('; '));process.exit(1)} console.log('StickyContactBar shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/StickyContactBar.tsx` is a client component using `glass-nav` (not the sketch's missing tokens), `position: fixed` at the bottom, scroll-in at >200px, a `×` that persists dismissal in `sessionStorage` ("cbar-dismissed"), `env(safe-area-inset-bottom)` padding, a < 560px stacked layout, and `motion-reduce` handling.
    - Bel/WhatsApp/Offerte hrefs all derive from `SITE` (+ `/contact`); no hardcoded contact values.
    - State initializes hidden (no hydration mismatch).
  </acceptance_criteria>
  <done>The verify prints OK; the bar is token-correct and behavior-complete. Viewport-fixed behavior + scroll-in are confirmed visually on preview (05-06).</done>
</task>

<task type="auto">
  <name>Task 2: Mount the bar at body level + sweep all contact links (LEAD-04)</name>
  <files>app/layout.tsx</files>
  <read_first>
    - app/layout.tsx (the `<body>` renders JsonLd, `<Navbar/>`, `{children}`, `<Footer/>`, `<Analytics/>`, `<SpeedInsights/>` — mount `<StickyContactBar/>` as a direct `<body>` child, e.g. right after `<Footer/>`; the body itself has NO transform/filter, so it is a valid fixed-positioning context)
    - 05-RESEARCH.md §"Link sweep targets (LEAD-04 / D-13)" (the enumerated usage sites: Footer, ServiceHero, CTABanner, MobileMenu, contact page — all already source SITE; there is NO standalone WhatsApp FAB to remove)
    - components/Footer.tsx, components/ServiceHero.tsx, components/CTABanner.tsx, components/MobileMenu.tsx, app/contact/page.tsx (the five sweep sites — read to confirm each `tel:`/`mailto:`/`wa.me`/CTA resolves and pulls from `SITE`)
  </read_first>
  <action>
    Import and mount `<StickyContactBar />` in `app/layout.tsx` as a direct child of `<body>` (place it after `<Footer />`, before/after `<Analytics />` — order among non-visual siblings doesn't matter; what matters is that NO ancestor between `<body>` and the bar applies transform/filter/backdrop-filter/perspective/contain/container-type). Then perform the LEAD-04 link sweep as a verification pass over the five enumerated sites: confirm every `tel:` uses `SITE.phone`, every `mailto:` uses `SITE.email`, every WhatsApp link uses `SITE.whatsappUrl`, and that there is exactly ONE always-on contact element (this new bar) — no leftover/duplicate floating WhatsApp FAB. If the sweep finds a hardcoded contact value or a broken/placeholder href, fix it to read from `SITE` (small surgical edits, noted in the SUMMARY); if everything already sources `SITE` (expected per RESEARCH), record the sweep result in the SUMMARY with the list of verified sites. Do NOT introduce a second sticky/floating element.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const l=fs.readFileSync('app/layout.tsx','utf8'); if(!/StickyContactBar/.test(l)){console.error('StickyContactBar not mounted in layout');process.exit(1)} if(!/import[^;]*StickyContactBar/.test(l)){console.error('StickyContactBar not imported');process.exit(1)} console.log('bar mounted in layout OK')"</automated>
    <automated>node -e "const fs=require('fs'); const files=['components/Footer.tsx','components/ServiceHero.tsx','components/CTABanner.tsx','components/MobileMenu.tsx','app/contact/page.tsx']; let hard=[]; for(const f of files){ if(!fs.existsSync(f))continue; const s=fs.readFileSync(f,'utf8'); const m=s.match(/(?:href=|=)\x22(?:tel:|mailto:)(?!\x24\{|\x22\s*\+)[^\x22]+\x22/g); if(m)hard.push(f+': '+m.join(', ')); } if(hard.length){console.error('possible hardcoded tel:/mailto: (should derive from SITE) -> '+hard.join(' | '));process.exit(1)} console.log('contact-link sweep: no hardcoded tel:/mailto: literals OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` imports and renders `<StickyContactBar />` as a direct `<body>` child (single always-on contact element).
    - The sweep confirms all `tel:`/`mailto:`/`wa.me`/CTA links across the five sites resolve and source `SITE` (no hardcoded contact literals; any found are fixed).
    - No second floating WhatsApp FAB exists.
  </acceptance_criteria>
  <done>Both verifies print OK; one shared bar is live site-wide and the link sweep is clean. Cross-page fixed behavior is confirmed on preview (05-06).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout shell → every page | The bar is global chrome; a positioning/container-trap bug would silently break the affordance site-wide (functional risk, not AppSec) |
| outbound contact links → user device | `tel:`/`wa.me`/`mailto:` hand off to the OS; `wa.me` opens a third-party origin (`rel="noopener"`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-containertrap | DoS (usability) | A `position:fixed` bar nested in a transformed/container-typed ancestor → not actually viewport-fixed | mitigate | Mount as a direct `<body>` child; RESEARCH verified `overflow:clip` on html/body does NOT trap fixed positioning; confirmed visually across pages on preview (05-06) |
| T-5-linkrot | Repudiation (lost lead) | A broken/placeholder/hardcoded contact link | mitigate | LEAD-04 sweep over the 5 enumerated sites; all values from `SITE`; verify-grep flags hardcoded `tel:`/`mailto:` literals |
| T-5-tabnabbing | Tampering | `wa.me` opened without `rel="noopener"` | mitigate | WhatsApp link uses `target="_blank" rel="noopener"` |
| T-5-SC | Tampering (supply chain) | package installs | accept | N/A — this plan installs zero packages (RESEARCH §Package Legitimacy Audit applies only to the Upstash deps in 05-01) |
</threat_model>

<verification>
- Verifies are pure Node string reads (OneDrive-safe): bar shape + token correctness, layout mount, and a hardcoded-contact-link sweep across the five sites.
- Manual proof on the Vercel preview (05-06): the bar is hidden at top, slides in past ~200px on multiple routes (home/pillar/contact), the × dismisses for the session, it stacks under 560px, respects reduced-motion, and is genuinely viewport-fixed (not trapped) — and there is exactly one always-on contact element.
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- One layout-level sticky contact bar (Bel · WhatsApp · Offerte) is live site-wide, token-correct, scroll-in, dismissible-for-session, mobile-safe, and reduced-motion-aware — with no container-trap and no duplicate FAB.
- Every CTA/`tel:`/`mailto:`/`wa.me` link is verified working and sourced from `SITE` (LEAD-04).
</success_criteria>

<output>
Create `.planning/phases/05-lead-capture-form-security-launch-qa/05-04-SUMMARY.md` when done.
</output>
</content>
