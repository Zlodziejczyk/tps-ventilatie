---
phase: 09-measurement-foundation
plan: 04
type: execute
wave: 2
depends_on: ["09-02"]
files_modified:
  - scripts/gsc/thresholds.ts
  - scripts/measure-indexation.ts
  - docs/measurements/README.md
  - docs/measurements/gsc/ (first dated reading, e.g. 2026-09-2x.json)
autonomous: true
requirements: [MEAS-05]
must_haves:
  truths:
    - "A weekly reading asks Google, per URL, about all 27 sitemap URLs and records verdict, coverageState, indexingState, robotsTxtState, pageFetchState, lastCrawlTime and Google's canonical vs ours (D-19), plus the sitemap's submitted count (D-10) — so a stuck page is named, not bucketed"
    - "The URL list comes from sitemapEntries() and its length is compared to INDEXABLE_FLOOR — never a hand-maintained list (D-19, P8 D-05)"
    - "Thresholds are a pure function with named, derived constants — ramp anchored on 2026-09-16 (≥10 from week 2, ≥20 from week 4, ≥25 from week 8) plus immediate flags for lost indexation, robots not ALLOWED and canonical mismatch (D-20) — and they are proven to bite on fabricated readings before the first real run"
    - "The script exits 1 when a flag trips and 0 when clean, prints every flag, never prints token material, and supports a labelled simulated breach so the alert path can be exercised without waiting for a real one (D-21 contract)"
    - "verify-indexation.ts (what we serve) and measure-indexation.ts (what Google concluded) stay distinct, and the README says a disagreement between them is the highest-value signal (D-23)"
    - "The first real reading is committed under docs/measurements/gsc/ and docs/measurements/README.md explains each flag in plain Dutch, with the rule that a threshold is never lowered to get green"
  artifacts:
    - path: "scripts/gsc/thresholds.ts"
      provides: "RAMP_ANCHOR, RAMP, evaluateReading() — pure"
      contains: "RAMP_ANCHOR"
    - path: "scripts/measure-indexation.ts"
      provides: "weekly measurement CLI"
      contains: "sitemapEntries"
    - path: "docs/measurements/README.md"
      provides: "flag vocabulary + thresholds + how to run"
      contains: "lost-indexation"
  key_links:
    - from: "scripts/measure-indexation.ts"
      to: "lib/seo/policy.ts sitemapEntries() + lib/seo/invariants.ts INDEXABLE_FLOOR"
      via: "URL list and count-vs-floor check (D-19)"
      pattern: "sitemapEntries"
    - from: "scripts/measure-indexation.ts"
      to: "scripts/gsc/api.ts inspectUrl / getSitemap + scripts/gsc/auth.ts getAccessToken"
      via: "sequential per-URL inspection with the 09-02 client"
      pattern: "inspectUrl"
    - from: "scripts/measure-indexation.ts"
      to: "scripts/gsc/thresholds.ts evaluateReading()"
      via: "previous reading (newest dated file) + current reading → flags → exit code"
      pattern: "evaluateReading"
---

<objective>
Build the recurring half of MEAS-05: the weekly reading that asks Google what it concluded about each of the
27 URLs (D-19), the thresholds that turn readings into named flags (D-20), and the committed record plus the
plain-language README the owner and the next engineer will read when an alert fires. 09-05 puts it on a
schedule.

Purpose: the whole point of D-18…D-23 is that nobody has to remember. A reading that no threshold interprets
is a log; a threshold that has never been seen firing is a hope. This plan delivers both, proven.
Output: `scripts/gsc/thresholds.ts`, `scripts/measure-indexation.ts`, `docs/measurements/README.md`, the first
committed reading.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-measurement-foundation/09-CONTEXT.md
@.planning/phases/09-measurement-foundation/09-RESEARCH.md
@.planning/phases/09-measurement-foundation/09-PATTERNS.md
@.planning/phases/09-measurement-foundation/09-VALIDATION.md
@.planning/phases/09-measurement-foundation/09-02-SUMMARY.md
@lib/seo/invariants.ts
@lib/seo/policy.ts
@scripts/assert-gate-blocks.ts
@scripts/verify-indexation.ts
@scripts/gsc/auth.ts
@scripts/gsc/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/gsc/thresholds.ts as a pure evaluator with derived constants, and prove it bites (D-20)</name>
  <files>scripts/gsc/thresholds.ts</files>
  <read_first>
    - lib/seo/invariants.ts (the pattern to copy: pure function returning a violation list, injectable inputs, a named constant whose derivation is stated, a load-bearing failure message)
    - scripts/assert-gate-blocks.ts (the proof habit: perturb, assert the specific code; content-addressed, never index-addressed)
    - .planning/phases/09-measurement-foundation/09-RESEARCH.md §Pattern 2 (threshold semantics, enum values, `isIndexed = verdict === "PASS"`, why `robots-not-allowed` needs `lastCrawlTime`)
    - scripts/gsc/api.ts (the `IndexStatusResult` type from 09-02 — reuse it, do not redeclare)
  </read_first>
  <action>
    Create `scripts/gsc/thresholds.ts` — pure: no I/O, no `Date.now()`, no imports from the app beyond types.
    Export types `UrlReading` (`url` + the `IndexStatusResult` fields from `./api`, or `error: string` when
    the inspection failed), `Reading` (`taken`, `property`, `floor`, `urls: UrlReading[]`, optional
    `sitemap: { submitted, errors, lastDownloaded }`, optional `analytics: { visits7d }`), and
    `MeasurementFlag` (`code` union `"below-ramp" | "lost-indexation" | "robots-not-allowed" |
    "canonical-mismatch" | "count-vs-floor" | "simulated-breach"`, `url?`, `message`). Export the constants
    with their derivation in a comment: `RAMP_ANCHOR = "2026-09-16"` (the date the sitemap was submitted and
    processed at 27 — D-10 — so "week N" means N weeks after Google was told the pages exist) and
    `RAMP = [{ fromWeek: 2, min: 10 }, { fromWeek: 4, min: 20 }, { fromWeek: 8, min: 25 }]` (D-20; floors, and
    the message must say a shortfall means pages are stuck — find which, do not edit the rung). Export
    `isIndexed(u)` (`verdict === "PASS"`), `weeksSince(anchorIso, todayIso)` (floor of days/7), and
    `evaluateReading(prev: Reading | undefined, curr: Reading, todayIso: string): MeasurementFlag[]`:
    (1) `count-vs-floor` when `curr.urls.length !== curr.floor`; (2) `below-ramp` when the highest rung with
    `fromWeek <= weeksSince(...)` has `min > indexedCount` — message names the rung, the count and the date;
    (3) per URL `lost-indexation` when the same URL in `prev` was PASS and now is not (match by `url` string);
    (4) `robots-not-allowed` when `robotsTxtState !== "ALLOWED"` **and** `lastCrawlTime` is present (an
    uncrawled URL reports UNSPECIFIED and is informational only); (5) `canonical-mismatch` when
    `googleCanonical` is present and differs from `url` (exact string compare; sitemap URLs carry no trailing
    slash except `/`). Flags are appended, never early-returned. Keep the file under 150 lines.

    Prove it with `npx tsx -e` (these become the verify): (a) 12 PASS of 27 on `2026-10-15` → exactly one
    `below-ramp` whose message contains `20`; (b) prev has `…/diensten` PASS, curr has it `FAIL` →
    `lost-indexation` with `url` ending `/diensten`; (c) 27 PASS on `2026-09-22` with no prev → `[]`;
    (d) one URL `DISALLOWED` with `lastCrawlTime` → `robots-not-allowed`; the same without `lastCrawlTime` →
    no such flag; (e) `googleCanonical` differing on one URL → `canonical-mismatch` naming that URL.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx -e "import { evaluateReading, RAMP_ANCHOR, RAMP } from './scripts/gsc/thresholds'; const mk=(n:number,over:any={})=>({taken:'t',property:'p',floor:27,urls:Array.from({length:27},(_,i)=>({url:'https://www.tpsklimaattechniek.nl/u'+i,verdict:i<n?'PASS':'FAIL',coverageState:'x',indexingState:'INDEXING_ALLOWED',robotsTxtState:'ALLOWED',pageFetchState:'SUCCESSFUL',lastCrawlTime:'2026-09-20T00:00:00Z',...(over[i]||{})}))}); const a=evaluateReading(undefined,mk(12),'2026-10-15'); if(a.filter(f=>f.code==='below-ramp').length!==1||!/20/.test(a.find(f=>f.code==='below-ramp')!.message)) throw new Error('below-ramp proof failed: '+JSON.stringify(a)); const prev=mk(27); const cur=mk(27); cur.urls[3].verdict='FAIL'; const b=evaluateReading(prev,cur,'2026-09-22'); if(!b.some(f=>f.code==='lost-indexation'&&f.url===cur.urls[3].url)) throw new Error('lost-indexation proof failed'); const c=evaluateReading(undefined,mk(27),'2026-09-22'); if(c.length!==0) throw new Error('clean proof failed: '+JSON.stringify(c)); const d1=mk(27,{5:{robotsTxtState:'DISALLOWED'}}); if(!evaluateReading(undefined,d1,'2026-09-22').some(f=>f.code==='robots-not-allowed')) throw new Error('robots proof failed'); const d2=mk(27,{5:{robotsTxtState:'DISALLOWED',lastCrawlTime:undefined}}); if(evaluateReading(undefined,d2,'2026-09-22').some(f=>f.code==='robots-not-allowed')) throw new Error('uncrawled robots must not flag'); const e=mk(27,{7:{googleCanonical:'https://www.tpsklimaattechniek.nl/other'}}); if(!evaluateReading(undefined,e,'2026-09-22').some(f=>f.code==='canonical-mismatch'&&f.url===e.urls[7].url)) throw new Error('canonical proof failed'); if(RAMP_ANCHOR!=='2026-09-16'||RAMP.length!==3) throw new Error('constants wrong'); console.log('thresholds proven: below-ramp, lost-indexation, clean, robots (crawled/uncrawled), canonical')"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('scripts/gsc/thresholds.ts','utf8');if(/Date\.now\(\)|new Date\(\)\s*[^)]|readFileSync|fetch\(/.test(s)){console.error('thresholds must be pure (no I/O, no clock)');process.exit(1)}if(!/RAMP_ANCHOR\s*=\s*['\"]2026-09-16['\"]/.test(s)){console.error('RAMP_ANCHOR missing');process.exit(1)}console.log('thresholds purity OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/gsc/thresholds.ts` exports `RAMP_ANCHOR = "2026-09-16"`, `RAMP` with three rungs (10/20/25 at weeks 2/4/8), `isIndexed`, `weeksSince`, `evaluateReading`, and the `MeasurementFlag` union with the six codes.
    - The file contains no I/O or clock calls.
    - All five proofs pass in one `tsx -e` run, including the negative case (uncrawled DISALLOWED does not flag).
  </acceptance_criteria>
  <done>The thresholds exist as an invariant that has been observed failing — the only kind this project trusts.</done>
</task>

<task type="auto">
  <name>Task 2: Write scripts/measure-indexation.ts and docs/measurements/README.md, then take and commit the first reading (D-19, D-21, D-23, D-10)</name>
  <files>scripts/measure-indexation.ts, docs/measurements/README.md, docs/measurements/gsc/ (first reading)</files>
  <read_first>
    - scripts/verify-indexation.ts (CLI shape; the D-23 counterpart — read its header so the two headers describe each other correctly)
    - scripts/gsc/api.ts (`inspectUrl`, `getSitemap`, `PROPERTY_NEW`), scripts/gsc/auth.ts (`getAccessToken`), scripts/gsc/thresholds.ts (Task 1)
    - lib/seo/policy.ts (`sitemapEntries()` — the URL list, D-19) and lib/seo/invariants.ts (`INDEXABLE_FLOOR`)
    - .planning/phases/09-measurement-foundation/09-RESEARCH.md §Pattern 2 (quota, sequential spacing), §Pattern 5 (what the workflow expects from exit codes and the simulated breach), §Pitfall 8 (never log token material)
    - docs/seo-owner-runbook.md §6 (the "Melding | Betekenis" table idiom the README copies)
  </read_first>
  <action>
    Create `scripts/measure-indexation.ts` (`npx tsx scripts/measure-indexation.ts [--out docs/measurements/gsc] [--property sc-domain:…] [--simulate-breach]`,
    env `MEASURE_SIMULATE_BREACH=1` equivalent to the flag). Header: what it asks ("what did Google conclude
    about each URL we intend to be indexed"), that it deliberately imports `sitemapEntries()` (D-19 — the
    opposite choice from `verify-indexation.ts`, which imports only the floor; both are decisions, explain
    both), that it is run weekly by GitHub Actions (D-21) and answers a different question than
    `verify-indexation.ts` (D-23: a disagreement between the two is the most valuable signal). Steps:
    (1) URL list = `sitemapEntries().map(e => e.url)`; (2) token via `getAccessToken()`; (3) inspect each URL
    **sequentially** with a 150 ms pause via `inspectUrl(token, property, url)`; a per-URL API error is
    recorded as `{ url, error: "HTTP <status>" }` and the run continues — but if every URL errors, exit 2
    with the 403 hint (the service account is not a Full user on the property); (4) `getSitemap(token,
    property, <CANONICAL_ORIGIN>/sitemap.xml)` → `{ submitted (web), errors, lastDownloaded }` (D-10);
    (5) when `VERCEL_TOKEN` is set, the 7-day visits count from `https://api.vercel.com/v1/query/web-analytics/visits/count`
    (project/team ids from `.vercel/project.json` values as constants) → `analytics.visits7d`; omit otherwise;
    (6) previous reading = the newest `YYYY-MM-DD.json` in `--out` whose date is before today; (7) write
    `<out>/<today UTC>.json` (2-space JSON, keys in a stable order: `taken, property, floor, sitemap,
    analytics, urls`); with `--simulate-breach` write nothing and append a `simulated-breach` flag instead;
    (8) `evaluateReading(prev, curr, today)` → print a table (url · verdict · coverageState · lastCrawlTime ·
    canonical ok?) then every flag with its code, then `indexed N/27, ramp rung in force: …`; exit 0 when no
    flags, 1 when any flag (including simulated), 2 on usage/auth failure. Never print the token; never print
    the response body of a failed token exchange.

    Create `docs/measurements/README.md` (Dutch): what `gsc/<datum>.json` is and who writes it (09-05 adds the
    workflow section); the D-23 sentence about the two scripts; the flag table `code | betekenis | wat te doen`
    for `below-ramp`, `lost-indexation`, `robots-not-allowed`, `canonical-mismatch`, `count-vs-floor`,
    `simulated-breach`; the ramp table with the actual dates (2026-09-30 ≥10, 2026-10-14 ≥20, 2026-11-11 ≥25,
    derived from `RAMP_ANCHOR`); how to run locally (`GSC_SERVICE_ACCOUNT_JSON_FILE` from `.env.local`); and
    in bold: **verlaag nooit een drempel om groen te krijgen — zoek uit welke pagina vastzit** (the P8 D-06
    lesson).

    Run `npx tsx scripts/measure-indexation.ts` once. Expected: a reading with 27 URLs, `sitemap.submitted`
    27, exit 0 with no flags in week 1 (no ramp rung applies before 2026-09-30; regressions need a previous
    reading). If Google reports a `robots-not-allowed` or `canonical-mismatch` on a crawled URL, that is a real
    finding: keep the flag, commit the reading, and record the finding in the summary — do not soften the rule.
    Commit the reading with the script.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('scripts/measure-indexation.ts','utf8');const bad=[];for(const t of ['sitemapEntries','INDEXABLE_FLOOR','inspectUrl','getSitemap','evaluateReading','getAccessToken','simulate']){if(!s.includes(t))bad.push(t)}if(/https:\/\/www\.tpsklimaattechniek\.nl\/diensten/.test(s))bad.push('hand-typed URL present (D-19 forbids a parallel list)');if(/console\.log\([^)]*token/i.test(s))bad.push('logs token');if(bad.length){console.error(bad.join('; '));process.exit(1)}const r=require('fs').readFileSync('docs/measurements/README.md','utf8');for(const t of ['lost-indexation','below-ramp','canonical-mismatch','robots-not-allowed','2026-09-30','2026-10-14','2026-11-11','verify-indexation']){if(!r.includes(t)){console.error('README lacks '+t);process.exit(1)}}console.log('measure script + README shape OK')"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx scripts/measure-indexation.ts --simulate-breach; test $? -eq 1 && echo "simulated breach exits 1 as designed"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx scripts/measure-indexation.ts; RC=$?; test $RC -eq 0 -o $RC -eq 1 && node -e "const fs=require('fs');const d='docs/measurements/gsc/';const f=fs.readdirSync(d).filter(x=>/^\d{4}-\d{2}-\d{2}\.json$/.test(x)).sort().pop();if(!f){console.error('no reading written');process.exit(1)}const r=JSON.parse(fs.readFileSync(d+f,'utf8'));if(r.urls.length!==27||r.floor!==27||!r.sitemap||r.sitemap.submitted!==27){console.error('reading shape wrong: '+JSON.stringify({n:r.urls.length,floor:r.floor,sitemap:r.sitemap}));process.exit(1)}console.log('first reading OK: '+f+' — '+r.urls.filter(u=>u.verdict==='PASS').length+'/27 PASS')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/measure-indexation.ts` imports `sitemapEntries`, `INDEXABLE_FLOOR`, `inspectUrl`, `getSitemap`, `getAccessToken`, `evaluateReading`; contains no hand-typed page URL; never logs token material; supports `--simulate-breach` (exit 1, no file written).
    - A dated reading exists in `docs/measurements/gsc/` with 27 URL entries, `floor: 27` and `sitemap.submitted: 27`; the run's exit code and any real flags are recorded in the summary.
    - `docs/measurements/README.md` explains the six flag codes, the three ramp dates, the two-scripts distinction and the never-lower rule.
  </acceptance_criteria>
  <done>Week-one truth from Google is committed, interpreted by proven thresholds, and explained in the owner's language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo data → measurement | the URL list must be the governed collection, never a copy that can drift |
| Google API → reading file | untrusted response fields; only typed enum values are interpreted |
| script → CI logs (public) | the access token and key must never be printed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-parallel-list | Tampering | URL list | mitigate | `sitemapEntries()` import + `count-vs-floor` flag; verify rejects a hand-typed page URL |
| T-9-snapshot-assert | Repudiation | thresholds | mitigate | floors and relations only; rungs named with derivation; proofs in the verify |
| T-9-token-log | Information disclosure | script output | mitigate | no token in logs; token-exchange body never printed; verify greps |
| T-9-partial-run | Denial of service | per-URL API errors | mitigate | continue on per-URL error, record it; exit 2 only when every URL fails |
| T-9-quota | DoS | URL Inspection | accept | 27 sequential calls/week vs 600 QPM / 2,000 QPD |
| T-9-vercel-token | Information disclosure | optional analytics count | mitigate | token-gated, omitted when absent, never persisted |
| T-9-SC | Tampering | npm installs | accept (n/a) | none |
</threat_model>

<verification>
- Branch `gsd/phase-9-measurement-foundation`; push after the plan → Vercel preview READY (TypeScript gate for `thresholds.ts` and the CLI). After this plan the branch is **merged fast-forward into `main`** (09-05 requires the workflows on the default branch).
- `npm run prebuild` stays green; nothing here joins it.
- All verifies run in seconds; the real measurement run is one network round-trip per URL.
</verification>

<success_criteria>
- Thresholds are pure, derived, and proven to fire on fabricated readings.
- The weekly script produces a 27-URL reading from Google, interprets it, and exits with the documented codes.
- The first reading and the README are committed; the branch is ready to merge.
</success_criteria>

<output>
Create `.planning/phases/09-measurement-foundation/09-04-SUMMARY.md` when done.
</output>
