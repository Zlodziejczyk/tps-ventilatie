---
phase: 09-measurement-foundation
plan: 06
type: execute
wave: 4
depends_on: ["09-01", "09-02", "09-03", "09-05"]
files_modified:
  - docs/baseline/2026-09-16/serp/serp-baseline.md
  - docs/baseline/2026-09-16/serp/serp-example-footer.png
  - docs/baseline/2026-09-16/README.md
  - docs/baseline/2026-09-16/gsc/ (re-export of the new-domain performance files if the first export was empty)
  - docs/baseline/2026-09-16/vercel/analytics-after-24h.md
  - docs/seo-owner-runbook.md
  - .planning/REQUIREMENTS.md
  - package.json
autonomous: true
requirements: [MEAS-04, MEAS-05, MEAS-06]
must_haves:
  truths:
    - "The 24 taxonomy-derived queries were checked logged-out and Zoetermeer-geolocated for BOTH domains, recording a top-20 position or an explicit zero for each, with the exact URL template and the footer check written down so the milestone-close retake is mechanical (D-13)"
    - "The baseline directory is complete before Phase 10 starts: dns/, gsc/, gbp/, serp/ and a manifest whose rows equal the files, each with a taken timestamp; the new-domain GSC export was re-taken once data existed (D-16, D-17, D-27)"
    - "Vercel Analytics is confirmed REPORTING after at least 24 hours of traffic — a positive visitor count from the API/MCP — not merely enabled (MEAS-06, D-24)"
    - "The owner runbook describes what was done and where the evidence lives instead of telling the owner to do it (D-01 reconcile), documents the weekly workflow and the alert vocabulary, states the never-remove-TXT rule (D-07), and REQUIREMENTS no longer lists the nameserver move as out of scope (D-26)"
    - "npm scripts expose the four CLIs (measure, verify:measurement, baseline:gsc, snapshot:dns) and prebuild is byte-for-byte unchanged"
    - "Gates (a)–(d) of D-25 are complete and recorded; Phase 10 is unblocked"
  artifacts:
    - path: "docs/baseline/2026-09-16/serp/serp-baseline.md"
      provides: "24 × 2 positions/zeroes + method"
      contains: "uule="
    - path: "docs/baseline/2026-09-16/README.md"
      provides: "complete manifest"
      contains: "serp/serp-baseline.md"
    - path: "docs/seo-owner-runbook.md"
      provides: "done-state §2/§3, automated §6, new §7"
      contains: "measure-indexation"
  key_links:
    - from: "docs/baseline/2026-09-16/serp/serp-baseline.md"
      to: "docs/baseline/2026-09-16/gsc/serp-queries.json"
      via: "the query list is read from the exported JSON, never retyped"
      pattern: "serp-queries.json"
    - from: "package.json scripts"
      to: "scripts/measure-indexation.ts, scripts/verify-measurement.ts, scripts/export-gsc-performance.ts, scripts/snapshot-dns.sh"
      via: "npm run aliases (not prebuild)"
      pattern: "verify:measurement"
---

<objective>
Finish the baseline with its only non-machine part — the geolocated SERP snapshot with honest zeroes (D-13) —
run the completeness gate (D-17), confirm analytics is reporting after a day of traffic (MEAS-06), and
reconcile the documents that still describe this phase's work as an owner to-do list (D-01) or the nameserver
move as out of scope (D-26). This is the tail of the phase; when it lands, Phase 10 may start.

Purpose: "prove the negative" — a GSC export can only show queries the site already ranks for; the recorded
zeroes are the before-side of the milestone's central claim. And documentation that says "you must do X" after
X was done is how the next person redoes it.
Output: `serp/serp-baseline.md`, a complete manifest, `analytics-after-24h.md`, the reconciled runbook and
REQUIREMENTS row, four npm script aliases.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-measurement-foundation/09-CONTEXT.md
@.planning/phases/09-measurement-foundation/09-RESEARCH.md
@.planning/phases/09-measurement-foundation/09-VALIDATION.md
@.planning/phases/09-measurement-foundation/09-01-SUMMARY.md
@.planning/phases/09-measurement-foundation/09-02-SUMMARY.md
@.planning/phases/09-measurement-foundation/09-03-SUMMARY.md
@.planning/phases/09-measurement-foundation/09-05-SUMMARY.md
@docs/baseline/2026-09-16/README.md
@docs/baseline/2026-09-16/gsc/serp-queries.json
@docs/seo-owner-runbook.md
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Take the geolocated, logged-out SERP baseline for 24 queries × 2 domains with honest zeroes (D-13; Chrome per D-01)</name>
  <files>docs/baseline/2026-09-16/serp/serp-baseline.md, docs/baseline/2026-09-16/serp/serp-example-footer.png</files>
  <read_first>
    - docs/baseline/2026-09-16/gsc/serp-queries.json (09-02 — the list; read it, do not retype it)
    - .planning/phases/09-measurement-foundation/09-RESEARCH.md §Pitfall 12 (`uule` + `gl=nl&hl=nl&pws=0`, the footer self-check, Guest window as the cleaner surface) and §Pattern 4 (why zeroes matter)
    - .planning/phases/09-measurement-foundation/09-CONTEXT.md D-13 (15–25 queries, both domains, honest zeroes, method written down)
  </read_first>
  <action>
    Compute the `uule` for the canonical location `Zoetermeer,South Holland,Netherlands` (v1 form: `w+CAIQICI`,
    then the base64-alphabet character at index = length of the canonical name, then base64 of the name; a
    Node one-liner does it — record the exact one-liner and the resulting value in the method section). URL
    template: `https://www.google.nl/search?q=<query URL-encoded>&gl=nl&hl=nl&pws=0&num=20&uule=<value with + as %2B>`.
    In Chrome (D-01) — a Guest window if the user opens one, otherwise the signed-in profile with `pws=0` —
    load the first query and **verify the SERP footer shows "Zoetermeer" as the location**; screenshot it to
    `serp/serp-example-footer.png`. If the footer shows another location, try the canonical name in the SerpApi
    UULE generator page and repeat; if it still fails, record the method as "geolocation not confirmed" and
    still capture positions — never silently record unverified geolocation as verified.
    For each of the queries in `serp-queries.json`, in order: load the URL, `find`/`read_page` the organic
    results, and record for each domain the 1-based organic position within the top 20 or the literal
    `niet in top 20`, plus whether a TPS entry appears in the local pack (`ja`/`nee`). Pace ~5 s between
    queries; if Google shows a CAPTCHA, wait and continue — do not script it.
    Write `docs/baseline/2026-09-16/serp/serp-baseline.md` (Dutch): `taken:` UTC, `Methode` (browser state,
    URL template, `uule` value, footer check result, num=20, one row per query), then the table
    `# | query | tpsklimaattechniek.nl | tpsventilatie.nl | local pack | opmerking`, and a totals line
    (how many zeroes per domain). Read the query list from the JSON when building the table so counts match.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const fs=require('fs');const q=JSON.parse(fs.readFileSync('docs/baseline/2026-09-16/gsc/serp-queries.json','utf8')).queries;const s=fs.readFileSync('docs/baseline/2026-09-16/serp/serp-baseline.md','utf8');const missing=q.filter(x=>!s.includes(x));if(missing.length){console.error('queries missing from the SERP table: '+missing.join(' | '));process.exit(1)}for(const t of ['taken:','uule=','pws=0','gl=nl','Zoetermeer','niet in top 20']){if(!s.includes(t)){console.error('serp-baseline lacks '+t);process.exit(1)}}const rows=(s.match(/^\|\s*\d+\s*\|/gm)||[]).length;if(rows<q.length){console.error('table has '+rows+' rows, expected '+q.length);process.exit(1)}if(!fs.existsSync('docs/baseline/2026-09-16/serp/serp-example-footer.png')){console.error('footer screenshot missing');process.exit(1)}console.log('SERP baseline OK: '+rows+' rows, method recorded')"</automated>
    <human-check>Open the footer screenshot: the location line reads Zoetermeer (or the method section explicitly says geolocation could not be confirmed). Spot-check two rows against a fresh SERP.</human-check>
  </verify>
  <acceptance_criteria>
    - `serp-baseline.md` has one numbered row per query in `serp-queries.json`, values for both domains (position or `niet in top 20`), a `taken:` line, and a method section containing the URL template with `uule=`, `pws=0`, `gl=nl`, and the footer-check result.
    - `serp-example-footer.png` exists.
  </acceptance_criteria>
  <done>The absences are recorded; the milestone's before-side exists and can be re-taken mechanically.</done>
</task>

<task type="auto">
  <name>Task 2: Run the completeness gate (D-17), re-take the new-domain GSC export if it was empty (D-16), confirm analytics is reporting after 24 h (MEAS-06), finalize the manifest (D-27)</name>
  <files>docs/baseline/2026-09-16/README.md, docs/baseline/2026-09-16/gsc/ (re-export), docs/baseline/2026-09-16/vercel/analytics-after-24h.md</files>
  <read_first>
    - docs/baseline/2026-09-16/README.md (09-01 — placeholders to resolve)
    - .planning/phases/09-measurement-foundation/09-02-SUMMARY.md (whether the new-domain export printed the Pitfall 9 "0 rows" notice) and 09-03-SUMMARY.md (when analytics was enabled)
    - scripts/export-gsc-performance.ts (re-run only the new property: `--property sc-domain:tpsklimaattechniek.nl`)
    - scripts/verify-measurement.ts (the probe is part of the gate)
  </read_first>
  <action>
    (1) If 09-02's export of `sc-domain:tpsklimaattechniek.nl` had 0 rows by query, and at least 72 h have
    passed since verification (2026-09-16), re-run
    `npx tsx scripts/export-gsc-performance.ts --property sc-domain:tpsklimaattechniek.nl --out docs/baseline/2026-09-16/gsc`
    and record the new row counts; if it is still 0 rows, that is the honest baseline (a brand-new property
    with no impressions yet) — record it as such with the date, do not wait indefinitely.
    (2) Analytics after ≥24 h: Vercel MCP `get_web_analytics` (count, last 24 h and last 7 d) → expected a
    positive visitor/page-view count; write `docs/baseline/2026-09-16/vercel/analytics-after-24h.md` with
    `taken:`, the counts, and the plan's reporting window (Hobby 1 month / Pro 12 months — whichever the
    dashboard shows) so the "durable record" question from CONTEXT's deferred list is answered in writing.
    If a short-lived `VERCEL_TOKEN` is available, also run `VERCEL_TOKEN=… npx tsx scripts/verify-measurement.ts https://www.tpsklimaattechniek.nl`
    so the count check passes rather than skips, and paste the line.
    (3) Finalize `README.md`: replace every `⬜` placeholder row with the real row (dns ×7, gsc — the 09-02
    exports, `serp-queries.json`, the 09-03 screenshots and status files —, gbp, serp, vercel), each with
    `taken:`; add a `Retake at milestone close` section: `bash scripts/snapshot-dns.sh … --out docs/baseline/<date>/dns`,
    `npx tsx scripts/export-gsc-performance.ts --out docs/baseline/<date>/gsc`, the SERP method from
    `serp/serp-baseline.md`, and the GBP transcription steps; and a closing line stating that D-25 gates
    (a)–(d) are complete on this date and Phase 10 is unblocked.
    (4) The gate itself is the verify below — run it and paste its output into the summary.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx scripts/verify-measurement.ts https://www.tpsklimaattechniek.nl && node -e "const fs=require('fs');const p=require('path');const d='docs/baseline/2026-09-16';const need={dns:2,gsc:6,gbp:2,serp:1,vercel:3};for(const [dir,min] of Object.entries(need)){const n=fs.existsSync(p.join(d,dir))?fs.readdirSync(p.join(d,dir)).length:0;if(n<min){console.error(dir+'/ has '+n+' files, need >= '+min);process.exit(1)}}const r=fs.readFileSync(p.join(d,'README.md'),'utf8');if(/⬜/.test(r)){console.error('README still has placeholder rows');process.exit(1)}const files=[];(function walk(x){for(const e of fs.readdirSync(x,{withFileTypes:true})){const f=p.join(x,e.name);if(e.isDirectory())walk(f);else if(e.name!=='README.md')files.push(p.relative(d,f))}})(d);const unlisted=files.filter(f=>!r.includes(f));if(unlisted.length){console.error('files not in manifest: '+unlisted.join(', '));process.exit(1)}for(const t of ['D-26','D-27','Phase 10','serp/serp-baseline.md','taken']){if(!r.includes(t)){console.error('README lacks '+t);process.exit(1)}}const a=fs.readFileSync(p.join(d,'vercel/analytics-after-24h.md'),'utf8');if(!/taken:/.test(a)||!/(bezoekers|visitors|pageviews|paginaweergaven)[^\n]*\b[1-9]\d*\b/i.test(a)){console.error('analytics-after-24h.md lacks a labelled positive count (bezoekers/visitors …: N)');process.exit(1)}console.log('baseline COMPLETE: '+files.length+' files, all in the manifest; measurement probe green')"</automated>
  </verify>
  <acceptance_criteria>
    - `verify-measurement.ts` is green against production at gate time.
    - Every file under `docs/baseline/2026-09-16/` (excluding README) appears in the README manifest; no `⬜` placeholders remain; dns ≥2, gsc ≥6, gbp ≥2, serp ≥1, vercel ≥3 files.
    - `analytics-after-24h.md` records a positive count with `taken:`.
    - The README contains the retake recipe and the D-25 completion statement naming Phase 10.
  </acceptance_criteria>
  <done>Success criterion 3 — "baseline artefact committed" — is true in one directory a responder can point at, and the retake is a recipe.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the runbook (§2/§3 done-state, §6 automated, new §7), the REQUIREMENTS out-of-scope row (D-26), and register the npm scripts</name>
  <files>docs/seo-owner-runbook.md, .planning/REQUIREMENTS.md, package.json</files>
  <read_first>
    - docs/seo-owner-runbook.md (§2 GSC, §3 Vercel Analytics, §6 probe — the sections to rewrite; keep §1/§4/§5 untouched)
    - .planning/REQUIREMENTS.md (the "Out of Scope" table row "Moving legacy DNS nameservers")
    - package.json (`scripts` — add aliases; `prebuild` must not change)
    - docs/measurements/README.md (09-04/09-05 — link to it rather than duplicating the flag table)
    - .planning/phases/09-measurement-foundation/09-CONTEXT.md D-01 (the reconcile reason), D-07, D-26
  </read_first>
  <action>
    `docs/seo-owner-runbook.md`: rewrite §2 as `## 2. Google Search Console — uitgevoerd (Phase 9)`: what
    exists (2 Domain properties verified 2026-09-16 via DNS at dd24; `https://www.tpsklimaattechniek.nl/`
    verified via HTML tag; the two legacy URL-prefix properties' status from
    `docs/baseline/2026-09-16/gsc/legacy-url-prefix-status.md`), the sitemap processed at 27, indexing
    requested for the 5 URLs, where the token lives (Vercel env `NEXT_PUBLIC_GOOGLE_SITE_VERIFICATION`,
    Production) — and remove every owner checkbox from §2. Add the D-07 rule in bold: the
    `google-site-verification` TXT records on both domains are never removed, also not after Phase 10.
    Rewrite §3 as `## 3. Vercel Web Analytics + Speed Insights — uitgevoerd (Phase 9)`: enabled on the date
    from 09-03, evidence under `docs/baseline/2026-09-16/vercel/`, reporting window per plan, GA4 still out —
    no checkboxes. Update §6: the probe now runs automatically after every production deployment
    (`.github/workflows/verify-indexation.yml`) and a red run opens an `indexation-alert` issue; manual runs
    still work. Add `## 7. Wekelijkse indexatiemeting (GitHub Actions)`: what runs when, where readings live,
    link to `docs/measurements/README.md` for the flag vocabulary and thresholds, the never-lower rule, the
    60-day re-arm, key rotation. Update the `Last updated` line.
    `.planning/REQUIREMENTS.md`: in the Out of Scope table, change the "Moving legacy DNS nameservers" row's
    reason to state it was superseded on 2026-09-16 by Phase 9 D-26 (delegation moved to dd24 with a mirrored
    zone; rollback in `docs/baseline/2026-09-16/README.md`).
    `package.json`: add `"measure": "tsx scripts/measure-indexation.ts"`, `"verify:measurement": "tsx scripts/verify-measurement.ts"`,
    `"baseline:gsc": "tsx scripts/export-gsc-performance.ts"`, `"snapshot:dns": "bash scripts/snapshot-dns.sh"`.
    Do not touch `prebuild` (these need network and secrets). Commit on `main`; the push deploys and the
    post-deploy probe run should appear — note it in the summary.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const fs=require('fs');const r=fs.readFileSync('docs/seo-owner-runbook.md','utf8');const sec=(a,b)=>{const i=r.indexOf(a);const j=r.indexOf(b,i+1);return r.slice(i,j<0?undefined:j)};const s2=sec('## 2.','## 4.');if(/- \[ \]/.test(s2)){console.error('runbook §2/§3 still contain owner checkboxes');process.exit(1)}for(const t of ['measure-indexation','verify-indexation.yml','indexation-alert','docs/measurements/README.md','## 7.','2026-09-16']){if(!r.includes(t)){console.error('runbook lacks '+t);process.exit(1)}}if(!/nooit/i.test(s2)){console.error('runbook §2 lacks the never-remove rule');process.exit(1)}const req=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');if(!/Moving legacy DNS nameservers[^\n]*D-26/.test(req)){console.error('REQUIREMENTS row not reconciled');process.exit(1)}const pkg=JSON.parse(fs.readFileSync('package.json','utf8'));for(const k of ['measure','verify:measurement','baseline:gsc','snapshot:dns']){if(!pkg.scripts[k]){console.error('missing npm script '+k);process.exit(1)}}if(pkg.scripts.prebuild!=='tsx scripts/validate-taxonomy.ts && tsx scripts/assert-registry.ts && tsx scripts/assert-site-shape.ts && tsx scripts/assert-no-forbidden-claims.ts && tsx scripts/assert-gate-blocks.ts && tsx scripts/assert-metadata-seam.ts && tsx scripts/assert-seo.ts && bash scripts/check-radius-literals.sh'){console.error('prebuild changed');process.exit(1)}console.log('runbook, REQUIREMENTS row and npm scripts reconciled; prebuild untouched')" && npm run prebuild >/dev/null 2>&1 && echo "prebuild green"</automated>
  </verify>
  <acceptance_criteria>
    - Runbook §2 and §3 contain no `- [ ]` checkboxes, describe the done-state with evidence paths, and §2 states the never-remove rule; §6 mentions `verify-indexation.yml`; §7 exists and links `docs/measurements/README.md`.
    - The REQUIREMENTS out-of-scope row for nameservers references D-26.
    - `package.json` has the four new scripts and an unchanged `prebuild`; `npm run prebuild` is green.
  </acceptance_criteria>
  <done>The documents agree with reality: this phase's work is recorded as done, the weekly loop is explained, and the one requirement row that reality overtook says so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Chrome (signed-in) → Google SERP | personalisation and location must be neutralised or the baseline lies |
| docs → future readers | stale owner-todo prose causes re-doing or undoing (e.g. removing a TXT to "clean up") |
| `package.json` → Vercel build | any change to `prebuild` alters the build gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-serp-method-drift | Repudiation | SERP baseline | mitigate | URL template + `uule` + footer check recorded; query list read from JSON; verify asserts every query is present |
| T-9-unverified-geo | Repudiation | geolocation | mitigate | footer screenshot; explicit "not confirmed" wording if it fails, never silent |
| T-9-docs-stale | Tampering (by omission) | runbook / REQUIREMENTS | mitigate | section-scoped greps for checkboxes; D-26 row check |
| T-9-txt-cleanup | Denial of service | legacy verification | mitigate | D-07 rule in runbook §2 in bold + baseline README + probe check |
| T-9-prebuild | Tampering | build gate | mitigate | verify compares `prebuild` byte-for-byte and runs it |
| T-9-SC | Tampering | npm installs | accept (n/a) | none |
</threat_model>

<verification>
- On `main` (after 09-05); each commit deploys and the post-deploy probe runs — the last deployment's `verify-indexation` run should be green and is noted in the summary.
- The completeness gate (Task 2 verify) is the phase's MEAS-04 acceptance; paste its output into the summary and into the phase verification.
- Nothing here changes app code; `npm run prebuild` is run once at the end.
</verification>

<success_criteria>
- SERP baseline recorded for every shortlist query on both domains with the method written down.
- Baseline directory complete and manifested; analytics confirmed reporting after 24 h; D-25 (a)–(d) declared complete — Phase 10 unblocked.
- Runbook, REQUIREMENTS row and npm scripts reconciled; `prebuild` unchanged and green.
</success_criteria>

<output>
Create `.planning/phases/09-measurement-foundation/09-06-SUMMARY.md` when done.
</output>
