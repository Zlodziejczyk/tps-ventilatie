---
phase: 09-measurement-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/snapshot-dns.sh
  - docs/baseline/2026-09-16/README.md
  - docs/baseline/2026-09-16/dns/tpsventilatie.nl-pre-switch-2026-09-16T175840Z.txt
  - docs/baseline/2026-09-16/dns/tpsklimaattechniek.nl-2026-09-16T175843Z.txt
  - docs/baseline/2026-09-16/dns/dd24-records-to-enter.md
  - docs/baseline/2026-09-16/dns/dd24-mirror-verification.txt
  - docs/baseline/2026-09-16/dns/dd24-zone-table.jpg
  - docs/baseline/2026-09-16/dns/ (two live post-switch snapshots, timestamped at execution)
  - docs/baseline/2026-09-16/gsc/sitemap-success-27.jpg
  - docs/baseline/2026-09-16/gsc/legacy-domain-property-verified.jpg
  - docs/baseline/2026-09-16/gbp/gbp-state.md
  - docs/baseline/2026-09-16/gbp/ (profile + knowledge-panel screenshots)
autonomous: true
requirements: [MEAS-04, MEAS-03]
must_haves:
  truths:
    - "A committed, re-runnable scripts/snapshot-dns.sh writes a timestamped, dig-based, diffable record set per zone (SOA, NS, A, MX, TXT/SPF, DMARC, DKIM, autoconfig, autodiscover, DS) and has no write path to any zone (D-14)"
    - "The pre-migration DNS state of BOTH zones is committed under docs/baseline/2026-09-16/dns/ — including the pre-switch cyberfolks view of tpsventilatie.nl (SOA serial 2024020301) and the 33/33 dd24 mirror verification — instead of dying in an ephemeral scratchpad (D-12, D-26, D-27)"
    - "The baseline README records D-26 (legacy delegation moved to dd24, with the exact rollback) and D-27 (the date rule), marks D-03 as superseded, and states the D-06/D-07 rule: legacy verification completes before Phase 10 and its TXT record is never removed"
    - "GBP state is transcribed as six named fields (name, primary category, secondary categories, website URL, service area, review count + rating) next to a provenance screenshot, so Phase 11 can diff values rather than images (D-15); nothing in GBP is edited"
    - "The two GSC screenshots taken on 2026-09-16 — sitemap Success with 27 discovered (D-10) and the legacy Domain property verified (D-05/D-06) — are in the baseline as MEAS-03 evidence"
  artifacts:
    - path: "scripts/snapshot-dns.sh"
      provides: "D-14 zone snapshot CLI"
      contains: "+noall +answer"
    - path: "docs/baseline/2026-09-16/README.md"
      provides: "manifest + decision records D-26/D-27 + D-07 rule"
      contains: "D-26"
    - path: "docs/baseline/2026-09-16/dns/dd24-mirror-verification.txt"
      provides: "33/33 PASS pre-switch mirror proof"
      contains: "PASS"
    - path: "docs/baseline/2026-09-16/gbp/gbp-state.md"
      provides: "transcribed GBP fields (D-15)"
      contains: "Beoordeling"
  key_links:
    - from: "scripts/snapshot-dns.sh"
      to: "docs/baseline/2026-09-16/dns/<zone>-<UTC>.txt"
      via: "dig @authoritative-NS, header-compatible with the 2026-09-16 files so diff works"
      pattern: "DNS snapshot"
    - from: "docs/baseline/2026-09-16/README.md"
      to: "every file in dns/, gsc/, gbp/"
      via: "manifest table rows (bestand · wat · taken · bron · opnieuw maken)"
      pattern: "taken"
---

<objective>
Start the pre-migration baseline (MEAS-04) with the parts that exist today only in an ephemeral scratchpad or
in a live system that Phase 10/11 will change: both DNS zones (D-14), the decision record for the legacy
delegation move (D-26), and the current Google Business Profile state (D-15). This plan writes the baseline's
skeleton and its manifest; 09-02 adds the GSC exports, 09-03 the verification screenshots, 09-06 the SERP
baseline and the completeness gate (D-17).

Purpose: the DNS evidence of 2026-09-16 (pre-switch zone, mirror verification) is sitting in
`/private/tmp` and will be garbage-collected; the GBP fields are the values Phase 11's rename puts at risk
(34 reviews, 4,9). Capture them while they are still true.
Output: `scripts/snapshot-dns.sh`, `docs/baseline/2026-09-16/{README.md,dns/,gsc/,gbp/}`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-measurement-foundation/09-CONTEXT.md
@.planning/phases/09-measurement-foundation/09-RESEARCH.md
@.planning/phases/09-measurement-foundation/09-PATTERNS.md
@.planning/phases/09-measurement-foundation/09-VALIDATION.md
@scripts/check-radius-literals.sh
@docs/seo-owner-runbook.md
@lib/constants.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/snapshot-dns.sh (D-14) and take the live post-switch snapshot of both zones</name>
  <files>scripts/snapshot-dns.sh, docs/baseline/2026-09-16/dns/ (two timestamped live snapshots)</files>
  <read_first>
    - scripts/check-radius-literals.sh (bash house style: purpose header, `set -euo pipefail`, non-zero exit)
    - /private/tmp/claude-501/-Users-brickpro-macos-Library-CloudStorage-OneDrive-Personal-work-Polaris360-Projects-TPS-Ventilatie/29fcd5a0-956d-4f2b-ac40-6646ac18e664/scratchpad/dns-snapshot-2026-09-16/tpsventilatie.nl.txt (the exact output format to reproduce — header lines and `dig +noall +answer` rows; if the directory is gone, use the format block quoted in 09-PATTERNS.md §snapshot-dns)
    - .planning/phases/09-measurement-foundation/09-RESEARCH.md §Pattern 6 (record set) and §Pitfall 13 (this phase never writes DNS)
    - .planning/phases/09-measurement-foundation/09-CONTEXT.md D-14 and the D-26 addendum (what the legacy zone should now show)
  </read_first>
  <action>
    Create `scripts/snapshot-dns.sh` per D-14: usage `bash scripts/snapshot-dns.sh <zone> [<zone>...] [--out <dir>]`,
    default `--out` = `docs/baseline/$(date -u +%F)/dns`. Header comment: what it is (read-only zone snapshot),
    why (Phase 10's rollback is "revert A records" and needs a diffable before/after), how to re-run, and that
    the output format is deliberately byte-compatible with the 2026-09-16 snapshot so `diff` works.

    For each zone: resolve the authoritative NS list with `dig +short NS`, pick the alphabetically first as the
    query target, and write `<out>/<zone>-<YYYYmmddTHHMMSSZ>.txt` containing, in this order: the three header
    lines (`# DNS snapshot <zone>  taken <UTC ISO>  queried @<ns>`, `# registry delegation: <NS list>`,
    `# DS: <DS records> (empty = DNSSEC off)`), a blank line, then `dig @<ns> +noall +answer` for the apex
    `SOA NS A MX TXT`, `A` for `www mail ftp smtp pop`, `TXT` for `_dmarc`, `TXT` for `x._domainkey` and
    `titan1._domainkey`, `CNAME` for `autoconfig`, `SRV` for `_autodiscover._tcp`. Exit non-zero with a message
    if a zone resolves no NS. Use `set -euo pipefail`; no `dig` write operations, no registrar calls — the
    script must contain nothing but reads (D-14, §Pitfall 13).

    Run it: `bash scripts/snapshot-dns.sh tpsventilatie.nl tpsklimaattechniek.nl --out docs/baseline/2026-09-16/dns`.
    Expected in the legacy file: NS `ns1/2/3.domaindiscount24.net` (D-26), `A 195.78.67.39` for apex and www
    (WordPress still live, D-06), `MX 10 mail.tpsventilatie.nl.`, the SPF still containing `a` (Phase 10 tidies
    it — untouched here), `TXT "google-site-verification=DvCnCNBbXd73JTab3-DsDmq_KgkQmlCZ7onK6OqDkoI"` (D-07),
    DMARC `p=none`, the `x._domainkey` DKIM record. Expected in the new-domain file: TXT
    `google-site-verification=eXe-fYdK-V_ZCSunAaB7sVfj-fPiWtqtjlKPnWlwCZk`, Titan MX + SPF + `titan1._domainkey`,
    `www` CNAME to `vercel-dns`. If any expectation is not met, stop and report — the zone changed since
    2026-09-16 and that is a finding, not something to paper over.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && T=$(mktemp -d) && bash scripts/snapshot-dns.sh tpsventilatie.nl tpsklimaattechniek.nl --out "$T" && ls "$T"/*.txt | wc -l | grep -q '^ *2$' && grep -l 'google-site-verification=DvCnCNBb' "$T"/tpsventilatie.nl-*.txt && grep -l 'google-site-verification=eXe-fYdK' "$T"/tpsklimaattechniek.nl-*.txt && grep -q '^# DNS snapshot tpsventilatie.nl' "$T"/tpsventilatie.nl-*.txt && grep -q 'domaindiscount24' "$T"/tpsventilatie.nl-*.txt && grep -q '195.78.67.39' "$T"/tpsventilatie.nl-*.txt && ! grep -Ei 'nsupdate|update add|delete ' scripts/snapshot-dns.sh && echo "snapshot-dns OK (read-only, both zones, headers compatible)"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && ls docs/baseline/2026-09-16/dns/tpsventilatie.nl-*.txt docs/baseline/2026-09-16/dns/tpsklimaattechniek.nl-*.txt | wc -l | grep -qE '^ *[2-9]' && echo "live snapshots committed under the baseline"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/snapshot-dns.sh` exists, starts with a purpose header, uses `set -euo pipefail`, and contains `+noall +answer` and the three header line prefixes (`# DNS snapshot`, `# registry delegation:`, `# DS:`).
    - Running it against both zones into a temp dir produces exactly two `.txt` files whose contents include the two `google-site-verification=` TXT records, the dd24 NS names for the legacy zone, and `195.78.67.39`.
    - The script contains no DNS write verbs (`nsupdate`, `update add`, `delete `).
    - Two live snapshot files exist under `docs/baseline/2026-09-16/dns/`.
  </acceptance_criteria>
  <done>A re-runnable, read-only zone snapshot exists and the post-switch state of both zones is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Rescue the 2026-09-16 evidence and write the baseline README with the D-26/D-27 decision records</name>
  <files>docs/baseline/2026-09-16/README.md, docs/baseline/2026-09-16/dns/tpsventilatie.nl-pre-switch-2026-09-16T175840Z.txt, docs/baseline/2026-09-16/dns/tpsklimaattechniek.nl-2026-09-16T175843Z.txt, docs/baseline/2026-09-16/dns/dd24-records-to-enter.md, docs/baseline/2026-09-16/dns/dd24-mirror-verification.txt, docs/baseline/2026-09-16/dns/dd24-zone-table.jpg, docs/baseline/2026-09-16/gsc/sitemap-success-27.jpg, docs/baseline/2026-09-16/gsc/legacy-domain-property-verified.jpg</files>
  <read_first>
    - /private/tmp/claude-501/-Users-brickpro-macos-Library-CloudStorage-OneDrive-Personal-work-Polaris360-Projects-TPS-Ventilatie/29fcd5a0-956d-4f2b-ac40-6646ac18e664/scratchpad/dns-snapshot-2026-09-16/ (5 files) and .../scratchpad/gsc-evidence-2026-09-16/ (2 jpg) — the evidence to copy
    - .planning/phases/09-measurement-foundation/09-CONTEXT.md — D-03, D-06, D-07, D-10, D-11, D-12, D-17 and the executed-state addendum (D-26 with its rollback, D-27)
    - .planning/phases/09-measurement-foundation/09-RESEARCH.md §Executed-state delta (the table of what each evidence file proves)
    - docs/seo-owner-runbook.md (Dutch, owner-readable section style and the "Melding | Betekenis" table idiom to reuse for the manifest)
  </read_first>
  <action>
    Copy the seven evidence files into the baseline, renaming only the pre-switch legacy snapshot to
    `dns/tpsventilatie.nl-pre-switch-2026-09-16T175840Z.txt` (so it cannot be confused with the post-switch
    file Task 1 wrote); keep the other names. Do not edit their contents. If the scratchpad directory no longer
    exists: the two GSC screenshots are re-taken in Chrome (GSC → Sitemaps for the new property; Settings →
    Ownership verification for `sc-domain:tpsventilatie.nl`) and the README states that the pre-switch view of
    the legacy zone (SOA 2024020301, `ns1.cyberfolks.pl`) could not be recovered — never fabricate it.

    Write `docs/baseline/2026-09-16/README.md` in Dutch (matching the runbook), with English decision IDs:
    (1) `Doel` — what this directory is, the D-17/D-27 date rule (directory = date the pre-migration state was
    first captured; later artefacts carry their own `taken:`), and that the milestone-close retake gets its own
    dated directory; (2) `Manifest` — a table `bestand | wat bewijst het | taken (UTC) | bron | opnieuw maken`
    with one row per file now present (dns ×7 incl. Task 1's two live files, gsc ×2) and placeholder rows
    marked `⬜ 09-02` / `⬜ 09-03` / `⬜ 09-06` for the GSC exports, the verification screenshots and the SERP
    baseline — 09-06 turns every placeholder into a real row or removes it; (3) `Beslissingen` — D-26 verbatim
    in substance: delegation moved to dd24 on 2026-09-16, why (authoritative zone at cyberfolks, no login held,
    Tomasz unreachable), what was mirrored (6×A, MX, SPF, DMARC, DKIM byte-identical, autoconfig CNAME,
    autodiscover SRV; 33/33 PASS), and the **rollback**: dd24 → Whois/Nameserver tab → type "Externe
    nameserver" → `ns1/ns2/ns3.cyberfolks.pl` (cyberfolks keeps its zone; registry TTL ~1 h, resolver NS cache
    up to 14400 s); the residual risk that only DKIM selector `x` was discoverable (DMARC `p=none` fails soft;
    check `s=` in a real `info@tpsventilatie.nl` header); the consequence for Phase 10 (A repoint + SPF `a` drop
    now at dd24). Then D-27, and a one-line note that D-03 (TXT pasted by the user at the legacy registrar) is
    superseded by D-26. (4) `Regels` — D-06 "legacy verificatie is afgerond vóór Phase 10 iets verplaatst" and
    D-07 in bold: **verwijder het `google-site-verification` TXT-record op tpsventilatie.nl nooit**, ook niet
    na de migratie (180-dagen redirect-vloer); the same for the new domain's TXT. (5) `Wat de GSC-screenshots
    bewijzen` — sitemap Success 27 = D-10 (27 = `INDEXABLE_FLOOR`), legacy Domain property verified = D-05/D-06;
    indexing was requested for `/diensten` + 4 pillars on 2026-09-16 (D-11) — the URL-inspection evidence
    screenshot lands in 09-03.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const fs=require('fs');const d='docs/baseline/2026-09-16';const must=['dns/tpsventilatie.nl-pre-switch-2026-09-16T175840Z.txt','dns/tpsklimaattechniek.nl-2026-09-16T175843Z.txt','dns/dd24-records-to-enter.md','dns/dd24-mirror-verification.txt','dns/dd24-zone-table.jpg','gsc/sitemap-success-27.jpg','gsc/legacy-domain-property-verified.jpg','README.md'];const missing=must.filter(f=>!fs.existsSync(d+'/'+f));if(missing.length){console.error('missing: '+missing.join(', '));process.exit(1)}const r=fs.readFileSync(d+'/README.md','utf8');for(const t of ['D-26','D-27','D-07','cyberfolks.pl','2024020301','taken','INDEXABLE_FLOOR']){if(!r.includes(t)){console.error('README lacks '+t);process.exit(1)}}const pass=(fs.readFileSync(d+'/dns/dd24-mirror-verification.txt','utf8').match(/^PASS /gm)||[]).length;if(pass<33){console.error('mirror verification has '+pass+' PASS lines, expected >=33');process.exit(1)}console.log('baseline evidence + README OK ('+pass+' PASS lines)')"</automated>
  </verify>
  <acceptance_criteria>
    - All seven evidence files exist under `docs/baseline/2026-09-16/` with unchanged contents (mirror verification still has ≥33 `PASS` lines; the pre-switch file still contains `2024020301`).
    - `README.md` contains `D-26`, `D-27`, `D-07`, the rollback nameservers `cyberfolks.pl`, the token `taken`, and `INDEXABLE_FLOOR`, plus placeholder rows for 09-02/09-03/09-06.
    - The README names D-03 as superseded and states the never-remove-TXT rule in bold.
  </acceptance_criteria>
  <done>The 2026-09-16 evidence is durable and the two execution-time decisions are recorded where an incident responder will find them.</done>
</task>

<task type="auto">
  <name>Task 3: Snapshot the Google Business Profile state (D-15) via Chrome (D-01) — read-only</name>
  <files>docs/baseline/2026-09-16/gbp/gbp-state.md, docs/baseline/2026-09-16/gbp/ (screenshots)</files>
  <read_first>
    - lib/constants.ts `SITE` (name, phone, address, serviceAreas, openingHours — the on-site NAP to compare against)
    - docs/seo-owner-runbook.md §1 (the GBP fields the runbook expects to align; do NOT act on its checklist — Phase 11 edits, this phase reads)
    - .planning/phases/09-measurement-foundation/09-CONTEXT.md D-01, D-02 (GBP access: Thomas owner, Oskar admin — a read needs nothing more), D-15
    - .planning/phases/09-measurement-foundation/09-RESEARCH.md §Pitfall 11 (Chrome quirks)
  </read_first>
  <action>
    Load the `claude-in-chrome` tools (one ToolSearch for `tabs_context_mcp, tabs_create_mcp, navigate, computer,
    read_page, find`), call `tabs_context_mcp`, open a new tab. Steps with expected results (D-01):
    1. `https://business.google.com/` → the TPS profile is listed (Oskar is administrator). Expected: profile
       card showing the current name (expected still the old brand "TPS Ventilatie"), rating and review count.
    2. Open the profile → "Bedrijfsinformatie" / "Info". Expected: name, primary category, secondary
       categories, website URL (expected the old domain), service area list, phone, address, opening hours.
    3. Screenshot the profile header and the info view into `docs/baseline/2026-09-16/gbp/gbp-profile-<UTC>.png`
       (use the computer tool's screenshot; save via the Bash copy of the capture if the tool returns a file,
       otherwise `read_page` + a screenshot of the visible region — one image per view).
    4. `https://www.google.nl/search?q=TPS+Ventilatie+Zoetermeer` → the knowledge panel. Expected: same name,
       rating 4,9 and 34 reviews (values as of 2026-09-16; record whatever is shown). Screenshot to
       `gbp/knowledge-panel-<UTC>.png`.
    Transcribe into `docs/baseline/2026-09-16/gbp/gbp-state.md` (Dutch): a `taken:` UTC line, then a table with
    exactly these labelled rows — `Naam`, `Primaire categorie`, `Secundaire categorieën`, `Website-URL`,
    `Servicegebied`, `Aantal reviews`, `Beoordeling` — plus `Telefoon`, `Adres`, `Openingstijden` for
    completeness, and a `Afwijkingen t.o.v. SITE` list (name ≠ `SITE.name`, URL ≠ `CANONICAL_ORIGIN`, any
    service-area difference vs `SITE.serviceAreas`) as findings handed to Phase 11 — no edits are made in GBP.
    If any field is not visible in the UI, write `niet zichtbaar` rather than guessing.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const fs=require('fs');const p='docs/baseline/2026-09-16/gbp/gbp-state.md';const s=fs.readFileSync(p,'utf8');for(const l of ['taken:','Naam','Primaire categorie','Secundaire categorie','Website-URL','Servicegebied','Aantal reviews','Beoordeling','Afwijkingen']){if(!s.includes(l)){console.error('gbp-state.md lacks '+l);process.exit(1)}}const shots=fs.readdirSync('docs/baseline/2026-09-16/gbp').filter(f=>/\.(png|jpe?g)$/i.test(f));if(shots.length<1){console.error('no GBP screenshot');process.exit(1)}console.log('gbp-state OK, '+shots.length+' screenshot(s)')"</automated>
    <human-check>Open the screenshot and gbp-state.md side by side: every transcribed value matches the image; the `Afwijkingen` list names the old brand name and old website URL if GBP still shows them.</human-check>
  </verify>
  <acceptance_criteria>
    - `docs/baseline/2026-09-16/gbp/gbp-state.md` contains a `taken:` line and all seven D-15 field labels plus `Afwijkingen`.
    - At least one screenshot (`.png`/`.jpg`) exists in `docs/baseline/2026-09-16/gbp/`.
    - No GBP field was edited (the task is read-only; Phase 11 owns edits).
  </acceptance_criteria>
  <done>Phase 11 has the values at risk (34 reviews, 4,9, name, categories, URL, service area) recorded as diffable text with provenance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → live DNS zones | both zones carry live mail; this phase must never write to either |
| ephemeral scratchpad → committed docs | evidence exists only in `/private/tmp` until copied |
| Chrome (signed-in Google session) → GBP | admin session can edit the profile; this task only reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-9-dns-write | Tampering | `scripts/snapshot-dns.sh` | mitigate | script is `dig`-only; verify greps reject DNS write verbs; no registrar automation anywhere in the phase |
| T-9-evidence-loss | Repudiation | scratchpad evidence | mitigate | copied verbatim in Task 2; README states what could not be recovered rather than fabricating |
| T-9-gbp-edit | Tampering | GBP via Chrome | mitigate | read-only steps; no form submission; Phase 11 owns edits (D-15) |
| T-9-pii | Information disclosure | GBP screenshots (owner phone/address) | accept | already public NAP on the live site and knowledge panel |
| T-9-SC | Tampering | npm installs | accept (n/a) | no packages installed by this plan |
</threat_model>

<verification>
- Work on branch `gsd/phase-9-measurement-foundation`; push after the plan — the Vercel preview build must stay READY (nothing here touches app code, but the branch is the phase's landing zone until 09-04 merges).
- All verifies are `bash`/`node -e` runs in seconds — OneDrive-safe; no `next build`, `tsc` or `eslint` locally.
- Execution is inline (orchestrator implements; subagents starve on this mount) — sequential, not parallel.
- Nothing in this plan writes to DNS, GSC or GBP.
</verification>

<success_criteria>
- `scripts/snapshot-dns.sh` runs green against both zones and its output diffs cleanly against the 2026-09-16 files.
- The pre-switch legacy zone, the dd24 mirror plan + verification, both live snapshots and the two GSC screenshots are committed under `docs/baseline/2026-09-16/`.
- The README carries D-26 with its rollback, D-27, the superseded D-03, and the D-06/D-07 never-remove rule.
- GBP state is transcribed with provenance and the on-site deviations are listed for Phase 11.
</success_criteria>

<output>
Create `.planning/phases/09-measurement-foundation/09-01-SUMMARY.md` when done.
</output>
