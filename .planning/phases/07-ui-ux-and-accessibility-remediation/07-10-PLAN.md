---
phase: 07-ui-ux-and-accessibility-remediation
plan: 10
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - components/ServiceHero.tsx
  - components/ServiceCard.tsx
  - components/ServiceFAQ.tsx
autonomous: true
requirements: [UI-15]
must_haves:
  truths:
    - "The primary conversion CTAs measured at 40px in the 2026-07-01 mobile re-audit reach a >=44px hit-height: the ServiceHero 'Bekijk'/offerte signature-gradient button (was px-7 py-3 ~40px) and the ServiceCard 'Bekijk [dienst]' signature-gradient button (was px-5 py-2.5 ~40px) each get min-h-[44px] with centered content — RelatedServices is covered transitively because it renders ServiceCard"
    - "The FAQ disclosure toggles (native <summary> in ServiceFAQ, ~24px) reach a >=44px tap height via min-h-[44px] (the summary is already flex items-center, so min-height centers the row) — the disclosure behavior and chevron are unchanged"
    - "UI-15 is the honest ID for this item — the 2026-07-01 CONTEXT addendum mislabelled it 'UI-11', which already denotes the footer refresh (07-12); this plan uses UI-15 to remove the collision. Only hit-area sizing changes; gradient, text, radius, focus rings, and active:scale behavior are preserved"
    - "Inline text links flagged at ~40px (e.g. '(34 reviews op Google)') are WCAG 2.5.x-exempt inline text per the original audit — left alone unless trivially co-located; not a gate"
  key_links:
    - from: "components/ServiceCard.tsx signature-gradient button"
      to: "WCAG 2.5.5/2.5.8 target size (>=44px)"
      via: "inline-flex items-center justify-center min-h-[44px]"
      pattern: "min-h-\\[44px\\]"
---

<objective>
Extend the A11Y-04 ≥44px target-size pass to the primary conversion buttons the extended mobile re-audit (2026-07-01) measured at 40px (UI-15): the `ServiceHero` offerte/"Bekijk" signature-gradient button, the `ServiceCard` "Bekijk [dienst]" button (also rendered by `RelatedServices`), and the FAQ `<summary>` disclosure toggles (~24px). These are money buttons — they must be at least as tappable as the sticky-bar controls fixed in 07-04. Add `min-h-[44px]` with centered content; keep the gradient, label, radius, and focus/active behavior identical. (This item was mislabelled "UI-11" in the CONTEXT addendum, colliding with the footer refresh — tracked here as **UI-15**.)

Purpose: The highest-intent CTAs and the FAQ toggles meet the touch-target floor, matching the A11Y-04 treatment for non-conversion controls.
Output: `min-h-[44px]` (centered) on the ServiceHero + ServiceCard signature-gradient buttons and the ServiceFAQ `<summary>` toggles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: signature-gradient conversion buttons to >=44px hit-height (ServiceHero + ServiceCard)</name>
  <files>components/ServiceHero.tsx, components/ServiceCard.tsx</files>
  <read_first>
    - 07-CONTEXT.md ADDENDUM §"UI-11 · Primary 'Bekijk [dienst]' CTAs are 40px tall" (signature-gradient buttons on /diensten + pillar/service via ServiceHero/ServiceCard/RelatedServices; extend the A11Y-04 >=44px pass; likely one shared button-height token) — tracked as UI-15 here
    - components/ServiceHero.tsx line ~143 — the CTA `<a>`/`<Link>` `className="inline-flex items-center gap-2 signature-gradient text-on-primary px-7 py-3 rounded-lg font-semibold transition-transform active:scale-95 shadow-md focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` (already inline-flex items-center — just needs min-height). NOTE 07-01 edits a DIFFERENT element in this file (the WhatsApp button in HeroFacts); this plan depends_on 07-01 so edits don't collide
    - components/ServiceCard.tsx line ~61 — the CTA `className="signature-gradient text-on-primary px-5 py-2.5 rounded-lg font-semibold text-sm transition-transform active:scale-95 focus-visible:ring-2 focus-visible:ring-primary focus-visible:ring-offset-2"` (NOT flex — add inline-flex + centering so min-height renders). This is the button RelatedServices renders too, so fixing it here covers RelatedServices
    - CLAUDE.md — design-system tokens; no #000; keep the signature-gradient look
  </read_first>
  <action>
    Raise both signature-gradient conversion buttons to a ≥44px hit-height, glyph/label size unchanged:
    - `components/ServiceHero.tsx` (~line 143): add `min-h-[44px]` to the CTA button's className (it is already `inline-flex items-center`, so the min-height centers the content). Change nothing else.
    - `components/ServiceCard.tsx` (~line 61): add `inline-flex items-center justify-center min-h-[44px]` to the CTA button's className so the 44px min-height takes effect and the label stays centered. Keep `px-5 py-2.5 rounded-lg font-semibold text-sm` and the gradient/focus/active classes.
    Do not change the gradient, text, radius, focus rings, or active:scale — only add the min-height (and the flex/centering on ServiceCard). RelatedServices needs no edit (it renders ServiceCard).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const bad=[];const sh=fs.readFileSync('components/ServiceHero.tsx','utf8');if(!/min-h-\[44px\]/.test(sh))bad.push('ServiceHero CTA missing min-h-[44px]');const sc=fs.readFileSync('components/ServiceCard.tsx','utf8');if(!/min-h-\[44px\]/.test(sc))bad.push('ServiceCard CTA missing min-h-[44px]');if(!/signature-gradient/.test(sh)||!/signature-gradient/.test(sc))bad.push('signature-gradient look was lost');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('conversion CTA >=44px OK (ServiceHero + ServiceCard/RelatedServices)')"</automated>
  </verify>
  <acceptance_criteria>
    - `ServiceHero.tsx` CTA carries `min-h-[44px]`; `ServiceCard.tsx` CTA carries `inline-flex items-center justify-center min-h-[44px]`.
    - The signature-gradient, label, radius, and focus/active classes are unchanged; RelatedServices inherits the fix via ServiceCard.
  </acceptance_criteria>
  <done>Verify prints OK; on the preview (07-08) the "Bekijk" CTAs measure ≥44px in smallTargets checks.</done>
</task>

<task type="auto">
  <name>Task 2: FAQ <summary> disclosure toggles to >=44px tap height (ServiceFAQ)</name>
  <files>components/ServiceFAQ.tsx</files>
  <read_first>
    - 07-CONTEXT.md ADDENDUM §"UI-11 ... FAQ <summary> toggles ~24px" (raise to a real tap target) — tracked as UI-15
    - components/ServiceFAQ.tsx line ~37 — `<summary className="flex items-center justify-between gap-4 cursor-pointer list-none font-semibold text-on-surface">` (native details/summary; already flex items-center so a min-height centers the row)
  </read_first>
  <action>
    In `components/ServiceFAQ.tsx`, add `min-h-[44px]` to the `<summary>` className (keep `flex items-center justify-between gap-4 cursor-pointer list-none font-semibold text-on-surface`). The summary is already a flex row with centered items, so the min-height gives it a ≥44px tap target without moving the text or the chevron. Do not change the details/summary structure or the toggle behavior.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/ServiceFAQ.tsx','utf8');const bad=[];if(!/<summary className=\"[^\"]*min-h-\[44px\]/.test(s))bad.push('summary missing min-h-[44px]');if(!/<summary/.test(s))bad.push('summary element missing');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('FAQ summary >=44px OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The `<summary>` in `ServiceFAQ.tsx` carries `min-h-[44px]`; the existing flex/justify classes and toggle behavior are unchanged.
  </acceptance_criteria>
  <done>Verify prints OK; FAQ toggles present a ≥44px tap target on the preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Hit-area sizing utilities on existing buttons/summaries — no input, data flow, secrets, or auth touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10-none | — | Tailwind min-height/flex utilities on CTAs + summary | accept | No attack surface: presentational sizing only; hrefs, gradient, and disclosure behavior unchanged. |
</threat_model>

<verification>
- Verifies are pure Node string reads (OneDrive-safe): the ServiceHero + ServiceCard CTAs and the ServiceFAQ summary each carry `min-h-[44px]`, gradient look preserved.
- Manual proof on the Vercel preview (07-08): the "Bekijk [dienst]" CTAs and FAQ toggles measure ≥44px in the target-size checks.
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- The primary conversion CTAs (ServiceHero, ServiceCard/RelatedServices) and the FAQ `<summary>` toggles present ≥44px tap targets with no visual regression — closing UI-15 (the correctly-numbered mobile-CTA item).
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-10-SUMMARY.md` when done.
</output>
