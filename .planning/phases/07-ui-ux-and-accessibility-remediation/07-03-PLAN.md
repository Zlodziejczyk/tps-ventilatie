---
phase: 07-ui-ux-and-accessibility-remediation
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - app/layout.tsx
  - app/page.tsx
  - app/contact/page.tsx
  - app/tarieven/page.tsx
  - app/privacy-beleid/page.tsx
  - app/over-ons/page.tsx
  - app/diensten/page.tsx
  - app/diensten/[pillar]/page.tsx
  - app/diensten/[pillar]/[service]/page.tsx
autonomous: true
requirements: [A11Y-03]
must_haves:
  truths:
    - "A visually-hidden-until-focus skip link ('Naar inhoud', href=#main) is the FIRST focusable element of every page — rendered once as the first child of <body> in app/layout.tsx with sr-only + focus:not-sr-only so it appears on keyboard focus and is invisible otherwise; the re-audit skipLink probe (matches text 'naar inhoud' + href starting '#') returns true on every route"
    - "Every route's existing single <main> landmark carries id=\"main\" (and tabIndex={-1} so it accepts focus when skipped to) — all 8 <main> elements across app/ get the anchor target; no nested/duplicate <main> is introduced (the skip link lives in layout, the target stays the per-page main)"
    - "Zero visual change: sr-only hides the link until focused; existing <main className=\"pt-28 pb-20\"> spacing is preserved (attributes added, classes untouched)"
  key_links:
    - from: "app/layout.tsx (skip link href=#main)"
      to: "each page's <main id=main>"
      via: "in-page anchor to the main landmark"
      pattern: "id=\"main\""
---

<objective>
Add the keyboard skip-link the audit found missing on these nav-heavy pages (A11Y-03, WCAG 2.4.1 Bypass Blocks). Render a single "Naar inhoud" link as the first child of `<body>` in `app/layout.tsx`, visually hidden until focused (`sr-only` → `focus:not-sr-only`), pointing at `#main`; then give every route's existing `<main>` `id="main"` (+ `tabIndex={-1}`) as the target. The `<main>` landmark already exists on all 8 routes — this wires the bypass without introducing a nested/duplicate landmark.

Purpose: Let keyboard and screen-reader users jump past the fixed Navbar + mega-menu straight to content on every page — a core AA navigation gate.
Output: a skip link in `app/layout.tsx` and `id="main"` on the `<main>` of all 8 page files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skip link as the first child of &lt;body&gt; (app/layout.tsx)</name>
  <files>app/layout.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"A11Y-03" (add a visually-hidden-until-focus "Naar inhoud" link at the top of app/layout.tsx before <Navbar>, sr-only + focus:not-sr-only; <main> already exists on every route)
    - app/layout.tsx — `<body className="bg-background text-on-surface font-body">` renders `<JsonLd/>`, then `<Navbar/>`, `{children}`, `<Footer/>`, `<StickyContactBar/>`. The skip link must be the FIRST child of <body>, before <JsonLd/> and <Navbar/>
    - re-audit.mjs `PAGE_AUDIT` skipLink check (~line 155): true when an `<a>` has href starting `#` AND text containing "naar inhoud" (or "skip"/"content") — so the anchor text "Naar inhoud" + `href="#main"` satisfies it
  </read_first>
  <action>
    In `app/layout.tsx`, insert a skip link as the very first child inside `<body>` (before `<JsonLd .../>`): an `<a href="#main">Naar inhoud</a>` classed `sr-only focus:not-sr-only focus:absolute focus:left-4 focus:top-4 focus:z-[10000] focus:rounded-lg focus:bg-surface-container-lowest focus:px-4 focus:py-2 focus:font-semibold focus:text-on-surface focus:shadow-lg focus:outline-none focus:ring-2 focus:ring-primary`. This keeps it invisible until keyboard focus, then paints an on-brand focused chip above the fixed navbar (z above Navbar's z-50 and the sticky bar). Do not change the `<body>` classes, `<Navbar/>`, or anything else.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/layout.tsx','utf8');const bad=[];if(!/href=\"#main\"/.test(s))bad.push('no href=#main skip link');if(!/Naar inhoud/.test(s))bad.push('skip link text is not \"Naar inhoud\"');if(!/sr-only/.test(s)||!/focus:not-sr-only/.test(s))bad.push('skip link missing sr-only/focus:not-sr-only');const bodyIdx=s.indexOf('<body');const aIdx=s.indexOf('href=\"#main\"');const navIdx=s.indexOf('<Navbar');if(!(aIdx>bodyIdx&&aIdx<navIdx))bad.push('skip link is not before <Navbar> as first body child');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('layout skip link OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` renders `<a href="#main" class="sr-only focus:not-sr-only ...">Naar inhoud</a>` as the first child of `<body>`, before `<Navbar/>`.
    - The link is invisible until focused, then visibly painted above the navbar.
  </acceptance_criteria>
  <done>Verify prints OK; re-audit `skipLink=true` on every route (07-08); tabbing from page load reveals "Naar inhoud" first.</done>
</task>

<task type="auto">
  <name>Task 2: id="main" + tabIndex={-1} on all 8 &lt;main&gt; landmarks</name>
  <files>app/page.tsx, app/contact/page.tsx, app/tarieven/page.tsx, app/privacy-beleid/page.tsx, app/over-ons/page.tsx, app/diensten/page.tsx, app/diensten/[pillar]/page.tsx, app/diensten/[pillar]/[service]/page.tsx</files>
  <read_first>
    - The 8 `<main>` sites (one per route, verified by grep): app/page.tsx:~16 `<main>`; app/contact/page.tsx:~14, app/tarieven/page.tsx:~15, app/privacy-beleid/page.tsx:~59, app/over-ons/page.tsx:~28, app/diensten/page.tsx:~19, app/diensten/[pillar]/page.tsx:~113, app/diensten/[pillar]/[service]/page.tsx:~54 (each `<main className="pt-28 pb-20">`)
    - Note: app/over-ons/page.tsx and app/diensten/page.tsx were edited in 07-02, and app/contact/page.tsx in 07-01 — read the CURRENT file before editing (this plan is wave 2, after those landed)
  </read_first>
  <action>
    In each of the 8 page files, add `id="main" tabIndex={-1}` to that file's single `<main>` opening tag, preserving any existing className. So `<main>` → `<main id="main" tabIndex={-1}>` on app/page.tsx, and `<main className="pt-28 pb-20">` → `<main id="main" tabIndex={-1} className="pt-28 pb-20">` on the other seven. Add nothing else; do not wrap `{children}` in a new `<main>` in layout (that would duplicate the landmark). `tabIndex={-1}` lets the main region receive focus when the skip link is activated.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const files=['app/page.tsx','app/contact/page.tsx','app/tarieven/page.tsx','app/privacy-beleid/page.tsx','app/over-ons/page.tsx','app/diensten/page.tsx','app/diensten/[pillar]/page.tsx','app/diensten/[pillar]/[service]/page.tsx'];const bad=[];for(const f of files){const s=fs.readFileSync(f,'utf8');const mains=(s.match(/<main[ >]/g)||[]).length;if(mains!==1)bad.push(f+': expected exactly 1 <main>, found '+mains);if(!/<main[^>]*id=\"main\"/.test(s))bad.push(f+': <main> missing id=\"main\"');if(!/<main[^>]*tabIndex=\{-1\}/.test(s))bad.push(f+': <main> missing tabIndex={-1}')}if(bad.length){console.error(bad.join('\\n'));process.exit(1)}console.log('all 8 <main id=main tabIndex=-1> OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 route `<main>` elements carry `id="main"` and `tabIndex={-1}`; existing classNames are preserved.
    - Exactly one `<main>` per file; no new/nested `<main>` added in layout.
  </acceptance_criteria>
  <done>Verify prints OK; activating the skip link moves focus into `#main` on each route (spot-checked on preview, 07-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds one static in-page anchor + an id/tabindex attribute — no input, network, secrets, or auth touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-none | — | Skip-link anchor + main landmark ids | accept | No attack surface: same-document `#main` anchor; no external navigation, no user data. |
</threat_model>

<verification>
- Verifies are pure Node string reads (OneDrive-safe): the layout skip link (first body child, sr-only/focus, href=#main, "Naar inhoud") and `id="main" tabIndex={-1}` on all 8 mains.
- Manual proof on the Vercel preview via `re-audit.mjs` (07-08): `skipLink=true` on every audited page; a manual Tab-from-load reveals "Naar inhoud" and Enter jumps focus to content.
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- Every page exposes a keyboard-reachable, visually-hidden-until-focus skip link to the existing `<main>` landmark (now `id="main"`), with no visual change — closing A11Y-03.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-03-SUMMARY.md` when done.
</output>
