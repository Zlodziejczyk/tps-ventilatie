---
phase: 07-ui-ux-and-accessibility-remediation
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - app/page-sections/HeroSection.tsx
  - app/page-sections/ServicesSection.tsx
  - app/page-sections/PricingSection.tsx
  - app/page-sections/WhyTPSSection.tsx
  - app/page-sections/ReviewsSection.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "The five homepage section files Phase 6 retired and left on disk are deleted: app/page-sections/HeroSection.tsx, ServicesSection.tsx, PricingSection.tsx, WhyTPSSection.tsx, ReviewsSection.tsx — all confirmed to have ZERO importers (app/page.tsx now renders app/page-sections/home/HomeHero+ProofBand+ImageBand+ClosingCTA). The live app/page-sections/home/ directory and the live components/ServiceHero.tsx are NOT touched"
    - "Deleting these files also removes the last live UI-13 raw-material-symbols violation outside CTABanner (PricingSection had raw spans at 64/103/132) and the stale WhyTPSSection heading skip (A11Y-02) at the source — both become moot once the files are gone"
    - "No dangling import remains anywhere in app/ or components/ after deletion; lib/reviews.ts only references ReviewsSection in a historical comment (not an import) and is left as-is"
  key_links:
    - from: "app/page.tsx (live homepage)"
      to: "app/page-sections/home/* (live sections)"
      via: "the retired app/page-sections/*.tsx are no longer referenced and are removed"
      pattern: "page-sections/home/"
---

<objective>
Delete the five retired homepage section files Phase 6 orphaned and left on disk (per STATE.md: "Phase 7 deletes the retired section files"). Phase 6 rebuilt `app/page.tsx` to render `HomeHero / ProofBand / ImageBand / ClosingCTA` from `app/page-sections/home/`, leaving `HeroSection.tsx`, `ServicesSection.tsx`, `PricingSection.tsx`, `WhyTPSSection.tsx`, and `ReviewsSection.tsx` unimported dead code in `app/page-sections/`. Remove them — this also erases the last UI-13 raw-icon violation (PricingSection) and the WhyTPSSection A11Y-02 skip at the source.

Purpose: A clean tree with no dead homepage sections — dead code is a maintenance and audit hazard (false-positive grep hits, stale patterns).
Output: the five files removed; zero dangling imports; the live `home/` sections and `components/ServiceHero.tsx` untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the five retired homepage section files (dead code from Phase 6)</name>
  <files>app/page-sections/HeroSection.tsx, app/page-sections/ServicesSection.tsx, app/page-sections/PricingSection.tsx, app/page-sections/WhyTPSSection.tsx, app/page-sections/ReviewsSection.tsx</files>
  <read_first>
    - STATE.md Session Continuity (Phase 6: "5 old sections + CTABanner retired, files left on disk for Phase-7 cleanup"; NEXT: "Phase 7 ... deletes the retired section files")
    - app/page.tsx — imports ONLY from `./page-sections/home/` (HomeHero, ProofBand, ImageBand, ClosingCTA); none of the five retired files are referenced
    - Confirm zero importers before deleting: `grep -rn "page-sections/\(HeroSection\|ServicesSection\|PricingSection\|WhyTPSSection\|ReviewsSection\)" app/ components/ lib/ --include="*.tsx" --include="*.ts"` returns only the historical comment in lib/reviews.ts (NOT an import) — safe to delete
    - DO NOT touch `app/page-sections/home/` (live) or `components/ServiceHero.tsx` (live, different file from the retired ServicesSection.tsx)
  </read_first>
  <action>
    First re-confirm there are no importers (run the grep above). Then delete the five retired files with `git rm`:
    `git rm app/page-sections/HeroSection.tsx app/page-sections/ServicesSection.tsx app/page-sections/PricingSection.tsx app/page-sections/WhyTPSSection.tsx app/page-sections/ReviewsSection.tsx`
    Do not delete anything under `app/page-sections/home/`, do not delete `components/ServiceHero.tsx`, and do not edit `lib/reviews.ts` (its ReviewsSection mention is a comment, not an import).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const cp=require('child_process');const dead=['HeroSection','ServicesSection','PricingSection','WhyTPSSection','ReviewsSection'];const bad=[];for(const f of dead){if(fs.existsSync('app/page-sections/'+f+'.tsx'))bad.push(f+'.tsx still exists');}if(!fs.existsSync('app/page-sections/home/HomeHero.tsx'))bad.push('live home/HomeHero.tsx was wrongly removed');if(!fs.existsSync('components/ServiceHero.tsx'))bad.push('live components/ServiceHero.tsx was wrongly removed');const imp=cp.execSync('grep -rn \"page-sections/\\\\(HeroSection\\\\|ServicesSection\\\\|PricingSection\\\\|WhyTPSSection\\\\|ReviewsSection\\\\)\" app components --include=*.tsx --include=*.ts || true').toString().trim();if(imp)bad.push('dangling import(s) remain:\\n'+imp);if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('retired section files deleted, no dangling imports, live files intact')"</automated>
  </verify>
  <acceptance_criteria>
    - None of `app/page-sections/{HeroSection,ServicesSection,PricingSection,WhyTPSSection,ReviewsSection}.tsx` exist.
    - `app/page-sections/home/*` and `components/ServiceHero.tsx` still exist.
    - `grep` for imports of the five files in `app/`/`components/` returns nothing (lib/reviews.ts comment is not an import).
  </acceptance_criteria>
  <done>Verify prints OK; the Vercel preview build (07-08) succeeds with no missing-module errors — proving nothing referenced the deleted files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Deletion of unimported dead files — no runtime code path, input, secrets, or auth touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11-orphan | Denial of Service (build break) | Deleting a file that is still referenced | mitigate | Pre-delete grep confirms zero importers; the verify re-greps and asserts the live home/ + ServiceHero files remain; the Vercel preview build (07-08) is the final proof. |
</threat_model>

<verification>
- Verify is a Node existence + grep check (OneDrive-safe): the five files are gone, the live `home/` sections + `components/ServiceHero.tsx` remain, and no import references them.
- Build gate = the Vercel preview build (07-08): a green build proves nothing imported the deleted files (a dangling import would fail the build). Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- The five retired homepage section files are deleted with zero dangling imports and no impact on the live homepage — a clean tree, with the PricingSection UI-13 and WhyTPSSection A11Y-02 sources removed for free.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-11-SUMMARY.md` when done.
</output>
