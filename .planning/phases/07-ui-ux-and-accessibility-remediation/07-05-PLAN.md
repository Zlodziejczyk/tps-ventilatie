---
phase: 07-ui-ux-and-accessibility-remediation
plan: 05
type: execute
wave: 3
depends_on: ["07-04"]
files_modified:
  - components/StickyContactBar.tsx
autonomous: true
requirements: [UI-08]
must_haves:
  truths:
    - "At full scroll the fixed sticky contact bar never occludes the last footer row (KvK/BTW) at 375px or 1440px — the document reserves bottom space equal to the bar's height via a normal-flow spacer sibling rendered inside StickyContactBar (fragment-wrapped), taller on <560px where the bar wraps to two rows"
    - "The spacer only exists while the bar can be shown (it returns null with the bar when dismissed), so a dismissed bar leaves no dead space; the fix is layout-only and single-file (no app/layout.tsx change, so no conflict with 07-03's layout edit)"
  key_links:
    - from: "components/StickyContactBar.tsx spacer"
      to: "footer bottom row clearance"
      via: "normal-flow spacer reserves bar-height at document bottom"
      pattern: "aria-hidden"
---

<objective>
Stop the site-wide fixed sticky contact bar from covering the footer's last row (KvK/BTW) at full scroll (UI-08). The bar is `position: fixed; bottom: 0` and wraps to two rows below 560px, so on a fully-scrolled page it overlaps the final footer line. Reserve matching bottom space by rendering a normal-flow spacer sibling inside `StickyContactBar` (so the document's scrollable bottom extends past the footer by the bar's height) — single-file, keeping `app/layout.tsx` untouched so this wave-3 plan never collides with 07-03's layout edit.

Purpose: The footer's legal/contact line must be fully readable — small trust + completeness nit from the audit (polish, not launch-gating).
Output: a bottom spacer in `components/StickyContactBar.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reserve bottom space so the sticky bar never occludes the footer (UI-08)</name>
  <files>components/StickyContactBar.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"UI-08" (ensure the layout has bottom padding = sticky-bar height so it never occludes the last footer row; mitigated by the dismiss X)
    - components/StickyContactBar.tsx — read the CURRENT file (07-04 enlarged its close button in wave 1). It early-returns `null` when `dismissed`; otherwise returns a single fixed `<div class="glass-nav fixed inset-x-0 bottom-0 ...">`. On `max-[560px]` the inner row switches to `flex-col` (two rows → taller bar). It is a direct `<body>` child rendered after `<Footer/>`
    - components/Footer.tsx — the last row is the KvK/BTW bar; that is the content that must stay clear of the fixed bar
  </read_first>
  <action>
    In `components/StickyContactBar.tsx`, wrap the returned markup in a fragment and render a normal-flow spacer as a sibling of the fixed bar: `<div aria-hidden="true" className="h-20 min-[560px]:h-16" />` (≈80px on narrow/wrapped layouts, ≈64px ≥560px — matched to the bar height incl. `env(safe-area-inset-bottom)`), placed so it sits in normal document flow at the very bottom (after the Footer). Because the whole component returns `null` when `dismissed`, the spacer disappears with the bar (no dead space when dismissed). Do not convert the bar itself away from `fixed`, and do not edit `app/layout.tsx`. If a tighter fit is wanted, the spacer height may be measured from the bar via a ref — but the static responsive height is the required, sufficient fix.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/StickyContactBar.tsx','utf8');const bad=[];if(!/h-20 min-\[560px\]:h-16/.test(s))bad.push('missing responsive bottom spacer height h-20 min-[560px]:h-16');if(!/aria-hidden/.test(s))bad.push('spacer should be aria-hidden');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('sticky bar footer-clearance spacer OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/StickyContactBar.tsx` renders an `aria-hidden` normal-flow spacer sized `h-20 min-[560px]:h-16` alongside the fixed bar (fragment-wrapped), and still returns `null` when dismissed.
    - `app/layout.tsx` is not modified by this plan.
  </acceptance_criteria>
  <done>Verify prints OK; on the preview (07-08) the footer KvK/BTW row is fully visible above the bar at 375px and 1440px at full scroll.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Adds one presentational spacer div — no input, network, secrets, or auth touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-none | — | Layout spacer | accept | No attack surface: inert `aria-hidden` spacer; no behavior change. |
</threat_model>

<verification>
- Verify is a pure Node string read (OneDrive-safe): the responsive `aria-hidden` spacer is present.
- Manual proof on the Vercel preview (07-08): scroll to the bottom at 375px and 1440px — the footer's KvK/BTW line is fully visible above the sticky bar; a dismissed bar leaves no gap.
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- The sticky contact bar never occludes footer content at full scroll on mobile or desktop, with no dead space when dismissed — closing UI-08.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-05-SUMMARY.md` when done.
</output>
