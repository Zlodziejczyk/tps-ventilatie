---
phase: 07-ui-ux-and-accessibility-remediation
plan: 09
type: execute
wave: 3
depends_on: ["07-02", "07-04", "07-06"]
files_modified:
  - components/Navbar.tsx
  - components/MobileMenu.tsx
  - app/diensten/page.tsx
autonomous: true
requirements: [UI-12]
must_haves:
  truths:
    - "The 'Nieuw' badge font is bumped from sub-11px to >=11px everywhere it renders: components/Navbar.tsx (was text-[9px]), components/MobileMenu.tsx (was text-[9px]), and app/diensten/page.tsx (was text-[10px]) — all become text-[11px] (readable per the mobile re-audit UI-12)"
    - "Only the font-size utility changes on the badge span — the bg-tertiary-fixed/text-on-tertiary-fixed colors, px/py padding, rounded-full, font-bold uppercase tracking-wider, and the 'Nieuw' text are all preserved; the badge stays a small pill, just legible"
    - "This plan runs after 07-02 (diensten), 07-04 (MobileMenu), and 07-06 (Navbar) so it never collides with their edits to the same files"
  key_links:
    - from: "components/Navbar.tsx 'Nieuw' badge"
      to: "WCAG-adjacent legibility (mobile re-audit UI-12)"
      via: "text-[9px] -> text-[11px]"
      pattern: "text-\\[11px\\]"
---

<objective>
Make the "Nieuw" badge legible (UI-12, from the 2026-07-01 mobile re-audit). The badge renders at 9px in the desktop nav and mobile menu and 10px on the diensten hub — below a readable floor. Bump each to `text-[11px]`, changing only the font-size utility so the pill's color, padding, shape, and copy stay identical.

Purpose: A "Nieuw" label that readers can actually read — a small polish item surfaced by the extended mobile audit at real device widths.
Output: `text-[11px]` on the "Nieuw" badge in `Navbar.tsx`, `MobileMenu.tsx`, and `diensten/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump the "Nieuw" badge font to >=11px in all three locations (UI-12)</name>
  <files>components/Navbar.tsx, components/MobileMenu.tsx, app/diensten/page.tsx</files>
  <read_first>
    - 07-CONTEXT.md ADDENDUM §"UI-12" (Navbar.tsx:187 + MobileMenu.tsx:80 `text-[9px]`, diensten/page.tsx:48 `text-[10px]`; bump to >=11px for readability)
    - components/Navbar.tsx line ~187 — `<span className="shrink-0 whitespace-nowrap bg-tertiary-fixed text-on-tertiary-fixed text-[9px] px-1.5 py-0.5 rounded-full font-bold uppercase tracking-wider">` wrapping "Nieuw"
    - components/MobileMenu.tsx line ~80 — the same badge span with `text-[9px]`
    - app/diensten/page.tsx line ~48 — `<span className="absolute top-3 right-3 z-10 bg-tertiary-fixed text-on-tertiary-fixed text-[10px] px-2 py-0.5 rounded-full font-bold uppercase tracking-wider">` wrapping "Nieuw"
  </read_first>
  <action>
    In each of the three files, change the "Nieuw" badge span's font-size utility to `text-[11px]`: `text-[9px]` → `text-[11px]` in `components/Navbar.tsx` and `components/MobileMenu.tsx`, and `text-[10px]` → `text-[11px]` in `app/diensten/page.tsx`. Change nothing else on those spans (keep `bg-tertiary-fixed`, `text-on-tertiary-fixed`, all padding, `rounded-full`, `font-bold uppercase tracking-wider`, and the "Nieuw" text). Do not touch any other `text-[9px]`/`text-[10px]` usage that is not the "Nieuw" badge.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const bad=[];for(const f of ['components/Navbar.tsx','components/MobileMenu.tsx']){const s=fs.readFileSync(f,'utf8');if(/text-\[9px\][^]*?Nieuw/.test(s)&&/text-\[9px\]/.test(s.slice(Math.max(0,s.indexOf('Nieuw')-400),s.indexOf('Nieuw'))))bad.push(f+': Nieuw badge still text-[9px]');}const d=fs.readFileSync('app/diensten/page.tsx','utf8');const near=d.slice(Math.max(0,d.indexOf('Nieuw')-400),d.indexOf('Nieuw'));if(/text-\[10px\]/.test(near))bad.push('diensten: Nieuw badge still text-[10px]');for(const f of ['components/Navbar.tsx','components/MobileMenu.tsx','app/diensten/page.tsx']){const s=fs.readFileSync(f,'utf8');const n=s.slice(Math.max(0,s.indexOf('Nieuw')-400),s.indexOf('Nieuw'));if(!/text-\[11px\]/.test(n))bad.push(f+': Nieuw badge is not text-[11px]');}if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('Nieuw badge >=11px OK in all three files')"</automated>
  </verify>
  <acceptance_criteria>
    - The "Nieuw" badge span in `Navbar.tsx`, `MobileMenu.tsx`, and `diensten/page.tsx` uses `text-[11px]`.
    - No `text-[9px]` / `text-[10px]` remains on the "Nieuw" badge; badge colors, padding, shape, and text are unchanged.
  </acceptance_criteria>
  <done>Verify prints OK; the "Nieuw" badge reads at 11px on the preview (07-08 eyeball).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Font-size utility change on a static label — no input, data flow, secrets, or auth touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-09-none | — | Tailwind text-size class on the "Nieuw" badge | accept | No attack surface: a single font-size utility swap on presentational badges. |
</threat_model>

<verification>
- Verify is a pure Node string read (OneDrive-safe): the "Nieuw" badge carries `text-[11px]` and no sub-11px size in all three files.
- Manual proof on the Vercel preview (07-08): the "Nieuw" badge is legible in the desktop nav, mobile menu, and diensten hub.
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- The "Nieuw" badge renders at >=11px everywhere (nav, mobile menu, diensten hub), legible with no other style change — closing UI-12.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-09-SUMMARY.md` when done.
</output>
