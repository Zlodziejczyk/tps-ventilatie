---
phase: 07-ui-ux-and-accessibility-remediation
plan: 12
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - components/Footer.tsx
autonomous: true
requirements: [UI-11]
must_haves:
  truths:
    - "The footer brand column shows the real PNG logo (next/image `/tps-logo.png`, the same asset the Navbar uses) instead of the text wordmark `<div>{SITE.name}</div>` — sized for the brand column, business name still available to SEO via alt text"
    - "The brand paragraph is rewritten from the ventilation-only 'Uw specialist in hoogwaardige ventilatieoplossingen…' to the 4-pillar klimaattechniek positioning aligned with SITE.tagline 'Specialist in gezond binnenklimaat' (airco, warmtepompen, WTW, mechanische ventilatie)"
    - "The Diensten column is rebuilt from the LIVE taxonomy — `pillars()` + `urlFor()` from lib/services/registry — so it lists the four real pillars (Airconditioning, Warmtepompen, WTW, Mechanische ventilatie) linking to their real /diensten/[pillar] URLs, replacing the four stale links that all pointed at /diensten (and were missing Warmtepompen)"
    - "The bottom-bar 1px line `border-t border-outline-variant/20` is removed in favour of Atmospheric-Clarity tonal layering (spacing + a surface-token step) — no 1px section borders per the design system"
    - "OWNER-BLOCKED, do NOT wait: Instagram/Facebook social icons are NOT added (IG/FB URLs still pending from the owner; they also feed JSON-LD sameAs) — ship the refresh without them, add later. The brand copy is owner-reviewed content (Claude drafts, owner approves) — flagged in the SUMMARY"
    - "07-02's heading re-rank (footer column headers h4->h2) is preserved — this plan runs after 07-02 (depends_on) and does not revert the h2 column headers"
  key_links:
    - from: "components/Footer.tsx Diensten column"
      to: "lib/services/registry pillars()/urlFor()"
      via: "live-taxonomy footer links (no hardcoded /diensten repeats)"
      pattern: "pillars\\(\\)"
---

<objective>
Refresh the site-wide footer to the klimaattechniek brand (UI-11, routed here from the Phase-6 discussion): swap the text wordmark for the real PNG logo, rewrite the ventilation-only tagline to the 4-pillar positioning, rebuild the Diensten column from the live taxonomy (real pillar URLs, including the missing Warmtepompen), and drop the 1px bottom-bar border for Atmospheric-Clarity tonal layering. Runs after 07-02's footer heading re-rank (h4→h2) so the two footer edits don't collide and the corrected heading levels are preserved. Instagram/Facebook icons are intentionally out of scope (owner-blocked on the IG/FB URLs) — ship without them.

Purpose: The footer is on every page; today it under-sells the brand (ventilation-only copy, stale links, no logo) and violates the no-1px-border rule. This aligns it with the klimaattechniek positioning and the live service taxonomy.
Output: `components/Footer.tsx` with the PNG logo, 4-pillar copy, live-taxonomy Diensten links, and no 1px border.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer brand column — real PNG logo + 4-pillar copy</name>
  <files>components/Footer.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"UI-11 · Footer brand refresh" (real PNG logo via next/image; rewrite copy to the 4-pillar klimaattechniek positioning aligned with SITE.tagline; do NOT wait on IG/FB social icons)
    - components/Footer.tsx — the Brand column: `<div className="font-headline font-bold text-2xl text-primary">{SITE.name}</div>` (the text wordmark) and the paragraph "Uw specialist in hoogwaardige ventilatieoplossingen voor een gezond en comfortabel binnenklimaat."
    - components/Navbar.tsx (~line 60) — the working `<Image src="/tps-logo.png" ... />` usage (next/image props: src, alt, width/height); mirror its pattern and sizing approach so the static export renders it (project uses `unoptimized` images)
    - lib/constants.ts — `SITE.name` (for the logo alt), `SITE.tagline` ("Specialist in gezond binnenklimaat"), `SITE.city`
    - CLAUDE.md — business data via the `SITE` constant; Dutch copy; Claude drafts content, owner reviews before publish
  </read_first>
  <action>
    In `components/Footer.tsx`:
    1) Add `import Image from "next/image";` and replace the text wordmark `<div className="font-headline font-bold text-2xl text-primary">{SITE.name}</div>` with a next/image logo: `<Image src="/tps-logo.png" alt={SITE.name} ... />` sized for the brand column (mirror Navbar's Image props/sizing — a modest height like ~40–48px with width auto/explicit; keep the project's image conventions). The business name is preserved for SEO/accessibility via the `alt`.
    2) Rewrite the brand paragraph to the 4-pillar klimaattechniek positioning aligned with `SITE.tagline`. Draft (owner-reviewable): "Specialist in gezond binnenklimaat — airconditioning, warmtepompen en ventilatie (WTW &amp; mechanisch). Advies, installatie en onderhoud in {SITE.city} en omgeving." Use the `SITE.tagline`/`SITE.city` constants where natural; keep the existing `text-on-surface-variant leading-relaxed` classes.
    Do NOT add Instagram/Facebook icons (owner-blocked). Keep the brand column's `space-y-6` layout.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/Footer.tsx','utf8');const bad=[];if(!/import Image from \"next\/image\"/.test(s))bad.push('missing next/image import');if(!/src=\"\/tps-logo\.png\"/.test(s))bad.push('logo <Image src=\"/tps-logo.png\"> not added');if(/font-headline font-bold text-2xl text-primary\">\s*\{SITE\.name\}/.test(s))bad.push('text wordmark <div>{SITE.name}</div> still present');if(/hoogwaardige ventilatieoplossingen/.test(s))bad.push('old ventilation-only copy still present');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('footer brand column (logo + copy) OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/Footer.tsx` imports `next/image` and renders `<Image src="/tps-logo.png" alt={SITE.name} .../>` in the brand column; the text wordmark `<div>{SITE.name}</div>` is gone.
    - The brand paragraph reflects the 4-pillar klimaattechniek positioning (aligned with SITE.tagline); the old "hoogwaardige ventilatieoplossingen" copy is gone.
    - No IG/FB social icons added.
  </acceptance_criteria>
  <done>Verify prints OK; footer shows the logo + klimaattechniek copy on the preview (07-08). Owner reviews the drafted copy.</done>
</task>

<task type="auto">
  <name>Task 2: Diensten column from the live taxonomy (real pillar URLs)</name>
  <files>components/Footer.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"UI-11" (rebuild the Diensten column from lib/services/ — add Warmtepompen; link real pillar URLs — instead of 4 stale links all pointing at /diensten)
    - components/Footer.tsx — the Diensten column: an `<h2 className="font-bold text-on-surface mb-6">Diensten</h2>` (h2 after 07-02) then a `<ul>` of four `<Link href="/diensten">` items (WTW Onderhoud, Unit Vervangen, Kanalen Reinigen, Airconditioning) — all pointing at `/diensten`, stale
    - lib/services/registry.ts — `pillars(): PillarPage[]` (line ~218) and `urlFor(node): string` (line ~197); app/page-sections/home/PillarGrid.tsx shows the live pattern: `import { pillars, urlFor } from ...` then `pillars().map(p => ...)` with `p.navTitle` and `urlFor(p)`
  </read_first>
  <action>
    In `components/Footer.tsx`, add `import { pillars, urlFor } from "@/lib/services/registry";` and replace the four hardcoded `<li><Link href="/diensten">…</Link></li>` items with a data-driven map over `pillars()`: `{pillars().map((p) => (<li key={p.slug}><Link href={urlFor(p)} className="hover:text-primary transition-colors">{p.navTitle}</Link></li>))}`. This yields the four live pillars (Airconditioning, Warmtepompen, WTW, Mechanische ventilatie) linking to their real `/diensten/[pillar]` URLs. Keep the `<h2>Diensten</h2>` header (from 07-02) and the `<ul className="space-y-4 text-on-surface-variant">` wrapper. Do not touch the Bedrijf/Contact columns.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/Footer.tsx','utf8');const bad=[];if(!/import \{ pillars, urlFor \} from \"@\/lib\/services\/registry\"/.test(s))bad.push('missing pillars/urlFor import');if(!/pillars\(\)\.map/.test(s))bad.push('Diensten column is not mapped from pillars()');if(!/urlFor\(/.test(s))bad.push('links not built via urlFor()');const stale=(s.match(/<Link href=\"\/diensten\"/g)||[]).length;if(stale>0)bad.push(stale+' stale hardcoded href=\"/diensten\" link(s) remain');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('footer Diensten column is live-taxonomy driven OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The Diensten column maps `pillars()` → `<Link href={urlFor(p)}>{p.navTitle}</Link>`; no hardcoded `href="/diensten"` list items remain.
    - The four live pillars (incl. Warmtepompen) render with real pillar URLs; the h2 header and column layout are unchanged.
  </acceptance_criteria>
  <done>Verify prints OK; footer Diensten links resolve to the four real pillar pages on the preview (07-08).</done>
</task>

<task type="auto">
  <name>Task 3: Drop the 1px bottom-bar border for tonal layering (design-system)</name>
  <files>components/Footer.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"UI-11" ("Drop the 1px border — replace the bottom-bar `border-t border-outline-variant/20` with Atmospheric-Clarity tonal layering") and §"Constraints" (no 1px section borders — use background color token shifts)
    - CLAUDE.md "Design System Rules" (never use `1px solid` borders for section separation — use background token shifts like bg-surface-container-*)
    - components/Footer.tsx — the bottom bar `<div className="max-w-7xl mx-auto mt-16 pt-8 border-t border-outline-variant/20 flex ...">` holding the copyright + KvK/BTW. Footer root is `bg-surface-container-low`
  </read_first>
  <action>
    In `components/Footer.tsx`, remove `border-t border-outline-variant/20` from the bottom-bar `<div>`. Provide separation via Atmospheric-Clarity tonal layering instead of a line: keep the `mt-16 pt-8` spacing, and if a visual break is still wanted, give the bottom-bar row a subtle surface-token step (e.g. wrap/shift it toward a neighbouring `bg-surface-container` tone) rather than a border. Do not introduce any `border` utility or `#000`. Keep the copyright + KvK/BTW content and the flex layout.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/Footer.tsx','utf8');const bad=[];if(/border-t border-outline-variant\/20/.test(s))bad.push('bottom-bar 1px border-t still present');if(/border-black|border-\[#000/.test(s))bad.push('forbidden black border introduced');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('footer bottom-bar border removed (tonal layering) OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The bottom bar no longer uses `border-t border-outline-variant/20` (no 1px section border).
    - Separation is achieved via spacing/tonal token, not a line; copyright + KvK/BTW content preserved.
  </acceptance_criteria>
  <done>Verify prints OK; the footer bottom bar reads as a tonal step, not a hairline, on the preview (07-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational footer edit (logo, copy, data-driven links from the trusted internal taxonomy, class change) — no user input, external data, secrets, or auth touched. The `next/image` src is a local static asset. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-12-links | Tampering | Diensten links built from lib/services registry | accept | Links derive from the internal typed taxonomy (`pillars()`/`urlFor()`), not user input; same trusted source the nav/home already use. |
| T-07-12-none | — | Static logo/copy/border classes | accept | No attack surface: local image asset, static copy, and Tailwind class changes. |
</threat_model>

<verification>
- Verifies are pure Node string reads (OneDrive-safe): the logo Image, the new 4-pillar copy, the pillars()/urlFor() Diensten map (no stale /diensten links), and no 1px border.
- Manual proof on the Vercel preview (07-08): footer shows the logo, klimaattechniek copy, four live pillar links, and a borderless (tonal) bottom bar; footer heading levels from 07-02 (h2 columns) intact.
- Owner reviews the drafted brand copy (Claude drafts, owner approves). IG/FB icons deferred (owner-blocked).
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- The site-wide footer carries the real PNG logo, 4-pillar klimaattechniek copy, live-taxonomy Diensten links (incl. Warmtepompen, real URLs), and no 1px border — closing UI-11, with IG/FB icons deferred and the copy owner-reviewed.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-12-SUMMARY.md` when done.
</output>
