---
phase: 07-ui-ux-and-accessibility-remediation
plan: 08
type: execute
wave: 4
depends_on: ["07-01", "07-02", "07-03", "07-04", "07-05", "07-06", "07-07"]
files_modified:
  - .planning/phases/07-ui-ux-and-accessibility-remediation/re-audit.mjs
autonomous: false
requirements: []
must_haves:
  truths:
    - "The re-audit harness runs against the CURRENT Vercel preview (branch pushed → preview built — the build gate; no local next build) and reports, for every audited page: contrast SOLID-bg failures = 0, headingIssues = [], h1Count = 1, and skipLink = true; and the previously-flagged controls (sticky close, carousel arrows) are gone from smallTargets"
    - "The harness BASE is updated to the new preview URL and OUT to the current session scratchpad; a playwright node_modules symlink is present per the ui-audit-harness memory; results are saved to the phase dir as evidence (audit.json + a short RESULTS note)"
    - "This is a human-in-the-loop gate (autonomous:false): it needs the push, the emitted preview URL, and a manual eyeball of UI-05/06/07 (Dutch badge, teal aurora, clean 375px pills), UI-08 (footer clearance), and UI-09 (keyboard mega-menu) which the automated checks don't fully cover"
  key_links:
    - from: "re-audit.mjs"
      to: "Vercel preview URL"
      via: "BASE constant → Playwright audit of 8 pages × 4 viewports"
      pattern: "const BASE ="
---

<objective>
Prove Phase 7 on the Vercel preview — the phase's verification gate (mirrors ROADMAP Success Criteria, all "verified on preview"). Push the branch so Vercel builds a fresh preview (the build gate — never a local `next build` on OneDrive), point `re-audit.mjs` at it, run the Playwright audit, and confirm the machine gates are green: 0 solid-bg AA contrast failures, 0 heading skips (one h1/page), skip-link present, and the flagged touch targets ≥44px. Then eyeball the items the harness can't fully assert (UI-05/06/07 hero look, UI-08 footer clearance, UI-09 keyboard mega-menu). `autonomous:false` — it needs the push, the preview URL, the harness setup, and a human pass.

Purpose: One consolidated, evidence-backed confirmation that A11Y-01…04 + UI-05…10 are actually fixed on the deployed preview before the phase is marked done.
Output: an updated `re-audit.mjs` (BASE/OUT), a saved `audit.json`, and a short RESULTS note recording the green gates.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
</context>

<tasks>

<task type="manual">
  <name>Task 1: Deploy the preview and re-run the audit harness — confirm all gates (A11Y-01..04, UI-05..10)</name>
  <files>.planning/phases/07-ui-ux-and-accessibility-remediation/re-audit.mjs</files>
  <read_first>
    - The "ui-audit-harness" memory (inline Playwright: the `playwright` node_modules must be symlinked from the npx cache; screenshots go to /private/tmp; the harness needs the current preview URL) and the "tps-deploy-and-integrations" memory (push a non-main branch → Vercel preview = the build gate around the OneDrive deadlock; never push main, never `vercel --prod`)
    - .planning/phases/07-ui-ux-and-accessibility-remediation/re-audit.mjs — `const BASE = "https://tps-ventilatie-...vercel.app"` (~line 4, stale — update to the new preview) and `const OUT = "/private/tmp/.../scratchpad"` (~line 5, a prior session path — update to THIS session's scratchpad). It audits TARGETS `/`, `/contact`, `/diensten`, `/diensten/airconditioning`, `/tarieven`, `/over-ons` + one discovered service page, desktop + mobile, and prints per-page `headingIssues`, `h1Count`, `skipLink`, `smallTargets`, and "contrast SOLID-bg failures (mobile)"
    - 07-CONTEXT.md §"Constraints" (gate: 0 solid-bg AA contrast fails, 0 heading skips, skip-link focusable, controls ≥44px)
  </read_first>
  <action>
    1) Ensure all wave 1–3 plans (07-01…07-07) are committed on this branch, then push the branch so Vercel builds a preview; capture the emitted preview URL (do NOT push main; do NOT `vercel --prod`).
    2) In `re-audit.mjs`, set `BASE` to the new preview URL and `OUT` to this session's scratchpad dir; ensure a `playwright` module is resolvable (symlink from the npx-cached install per the memory) and Chromium is available.
    3) Run `node .planning/phases/07-ui-ux-and-accessibility-remediation/re-audit.mjs`; read the console summary + `audit.json`.
    4) Confirm the gates on EVERY audited page: `contrast SOLID-bg failures = 0`, `headingIssues = []`, `h1Count = 1`, `skipLink = true`; and that `smallTargets` no longer lists the sticky-bar close or the review-carousel arrows (desktop). If any gate fails, stop and route the specific failure back to its plan (contrast→07-01, headings→07-02, skip-link→07-03, targets→07-04) rather than patching here.
    5) Manually verify the items the harness can't fully cover: UI-05 Dutch badge + UI-06 calm teal aurora + UI-07 clean 375px pills (hero screenshots), UI-08 footer KvK/BTW row clear of the sticky bar at 375px and 1440px, UI-09 keyboard-only mega-menu (Tab opens Diensten, Escape closes), and the mobile-menu chevron ≥44px with the menu open.
    6) Save `audit.json` into the phase dir and write a short RESULTS note listing the green gates + the preview URL as evidence.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const p='.planning/phases/07-ui-ux-and-accessibility-remediation/re-audit.mjs';const s=fs.readFileSync(p,'utf8');const m=s.match(/const BASE = \"([^\"]+)\"/);if(!m){console.error('cannot find BASE in re-audit.mjs');process.exit(1)}console.log('re-audit BASE = '+m[1]+' (confirm this is the CURRENT preview before trusting results)')"</automated>
  </verify>
  <acceptance_criteria>
    - The harness ran against the current preview; `audit.json` is saved in the phase dir.
    - Every audited page: 0 solid-bg contrast failures, `headingIssues=[]`, `h1Count=1`, `skipLink=true`; sticky close + carousel arrows absent from `smallTargets`.
    - Manual pass recorded for UI-05/06/07 (hero), UI-08 (footer clearance), UI-09 (keyboard mega-menu), and the mobile chevron.
  </acceptance_criteria>
  <done>All machine gates green in the audit output + the manual pass recorded, with the preview URL and audit.json saved as evidence. Any red gate is routed back to its source plan, not patched here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local harness → Vercel preview | Playwright drives a headless browser against the public preview URL; no credentials or user data are sent; the preview is a non-production deploy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08-deploy | Tampering | Accidentally promoting to production instead of a preview | mitigate | Push a non-main branch only (preview build); never `vercel --prod`; owner sign-off is the launch gate (per project constraints). |
| T-07-08-none | — | Read-only audit harness | accept | The harness only reads the rendered preview; no writes to the app, no secrets. |
</threat_model>

<verification>
- The gate itself IS the verification: the re-audit harness on the preview must show 0 contrast fails, 0 heading skips, skip-link true, and ≥44px targets, plus a manual pass for the visual/keyboard items.
- Build gate = the Vercel preview build (no local `next build`/`tsx` — OneDrive deadlock). Never push main; never `vercel --prod`.
</verification>

<success_criteria>
- On the Vercel preview, the re-audit reports zero solid-bg AA contrast failures, zero heading skips (one h1/page), a focusable skip-link, and ≥44px flagged controls; and the manual pass confirms the Dutch/teal hero, footer clearance, and keyboard mega-menu — Phase 7's success criteria are met with saved evidence.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-08-SUMMARY.md` when done.
</output>
