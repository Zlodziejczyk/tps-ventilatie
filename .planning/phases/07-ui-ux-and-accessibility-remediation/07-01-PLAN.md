---
phase: 07-ui-ux-and-accessibility-remediation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - components/CTABanner.tsx
  - components/ServiceHero.tsx
  - app/contact/page.tsx
autonomous: true
requirements: [A11Y-01, UI-10, UI-13]
must_haves:
  truths:
    - "The two WhatsApp CTA buttons (components/CTABanner.tsx + components/ServiceHero.tsx) render dark on-surface text/icon (#141D1F, Tailwind token text-on-surface) on the unchanged brand-green #25D366 background — measured contrast rises from 1.98:1 to ~8.5:1, clearing WCAG AA (>=4.5:1 text) — the recognizable WhatsApp green is kept (owner decision 2026-07-01: dark-text-on-green)"
    - "The contact-page WhatsApp icon (app/contact/page.tsx) glyph is darkened from green-on-green-tint (~1.89:1) to on-surface text on the SAME #25D366/10 tint row, clearing the >=3:1 non-text-contrast bar; the tint row background is left unchanged"
    - "No #000 black is introduced (design system uses on-surface #141D1F); the fix is class-only — geometry, layout, focus rings and hover behavior are untouched"
    - "CTABanner's three raw `material-symbols-outlined` spans (phone, chat, support_agent) are swapped to the `<Icon>` wrapper (UI-13, CLAUDE.md guardrail) — same glyphs, same inherited color, so the swap composes with the WhatsApp contrast fix on the same file (one edit per file). PricingSection's UI-13 spans are resolved separately by deleting that dead file in 07-11"
  key_links:
    - from: "components/CTABanner.tsx"
      to: "WCAG 2.1 AA (1.4.3 text contrast)"
      via: "dark on-surface text on brand-green WhatsApp button"
      pattern: "bg-\\[#25D366\\] text-on-surface"
    - from: "components/ServiceHero.tsx"
      to: "WCAG 2.1 AA (1.4.3 text contrast)"
      via: "dark on-surface text on brand-green WhatsApp button"
      pattern: "bg-\\[#25D366\\] text-on-surface"
---

<objective>
Kill the only launch-gating WCAG AA failure from the 2026-06-30 audit (A11Y-01) plus its sibling non-text-contrast nit (UI-10). Today white text/icon on the WhatsApp green `#25D366` measures **1.98:1** on the two CTA buttons, and the contact-page WhatsApp glyph is **1.89:1** green-on-green-tint. Per the locked owner decision (2026-07-01: dark-text-on-green — keeps the brand green), switch the button text/icon to the `text-on-surface` token (`#141D1F`) over the unchanged `#25D366`, lifting text contrast to ~8.5:1, and darken the contact icon glyph on its unchanged tint row to clear ≥3:1.

Purpose: Make every WhatsApp affordance legible and AA-compliant so the site can launch on the accessibility gate — without losing the recognizable WhatsApp green.
Output: `components/CTABanner.tsx`, `components/ServiceHero.tsx`, and `app/contact/page.tsx` with contrast-safe WhatsApp controls.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark on-surface text on the two WhatsApp CTA buttons (A11Y-01)</name>
  <files>components/CTABanner.tsx, components/ServiceHero.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"A11Y-01" (measured 1.98:1 white-on-#25D366; recommended fix = dark on-surface #141D1F on the green ≈ 8.5:1; apply to BOTH files identically) and §"Constraints" (no #000 — use the on-surface token)
    - components/CTABanner.tsx line 34 — the WhatsApp `<a href={SITE.whatsappUrl}>` currently `bg-[#25D366] text-white ...`; its child `<span className="material-symbols-outlined">chat</span>` inherits text color, so recoloring the text recolors the glyph too
    - components/ServiceHero.tsx line 82 — the second WhatsApp `<a>` in `HeroFacts()`, currently `bg-[#25D366] text-white ...` with an inheriting `<Icon name="chat" filled className="text-xl" />`
    - CLAUDE.md "Design System Rules" — colors come from MD3 tokens; `text-on-surface` = `#141D1F`
  </read_first>
  <action>
    In BOTH `components/CTABanner.tsx` (the `bg-[#25D366]` WhatsApp anchor) and `components/ServiceHero.tsx` (the `bg-[#25D366]` WhatsApp anchor inside `HeroFacts`), change the button text color class from `text-white` to `text-on-surface`. Leave everything else on those anchors exactly as-is: keep `bg-[#25D366]`, `font-bold`, padding, `rounded-xl`, `hover:opacity-90`, and the `focus-visible:ring-[#25D366] focus-visible:ring-offset-2` rings. Do NOT change the background color, the icon markup, or any geometry — the child `chat` glyph inherits `currentColor`, so it darkens with the text. Do not introduce `#000`/`text-black`.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');for(const f of ['components/CTABanner.tsx','components/ServiceHero.tsx']){const s=fs.readFileSync(f,'utf8');const bad=[];if(/bg-\[#25D366\]\s+text-white/.test(s))bad.push('still has text-white on the #25D366 button');if(!/bg-\[#25D366\]\s+text-on-surface/.test(s))bad.push('missing text-on-surface on the #25D366 button');if(/text-black|text-\[#000/.test(s))bad.push('introduced forbidden black text');if(bad.length){console.error(f+': '+bad.join('; '));process.exit(1)}console.log(f+': WhatsApp button contrast OK')}"</automated>
  </verify>
  <acceptance_criteria>
    - `components/CTABanner.tsx` and `components/ServiceHero.tsx` each render the WhatsApp button as `bg-[#25D366] text-on-surface` (dark glyph + label on the unchanged green).
    - No `text-white` remains on either `#25D366` button; no `#000`/`text-black` introduced.
    - Background, padding, rounding, hover and focus-ring classes are unchanged.
  </acceptance_criteria>
  <done>The verify prints OK for both files. Final proof: re-audit.mjs (07-08) reports 0 solid-bg contrast failures for the WhatsApp buttons at ~8.5:1 on the preview.</done>
</task>

<task type="auto">
  <name>Task 2: Darken the contact-page WhatsApp icon glyph (UI-10)</name>
  <files>app/contact/page.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"UI-10" (green glyph on 10% green tint ≈ 1.89:1; darken the glyph for ≥3:1; KEEP the tint row) and §"A11Y-01" note that the tint row's text already passes — only the icon color changes
    - app/contact/page.tsx lines 73-86 — the WhatsApp contact row: outer `<a>` has `bg-[#25D366]/10` (row tint) and an inner `<div className="... bg-[#25D366]/20 ...">` holding `<Icon name="chat" filled className="text-[#25D366]" />` — the green-on-green-tint glyph is the ~1.89:1 offender
  </read_first>
  <action>
    In `app/contact/page.tsx`, change the WhatsApp row's icon class from `text-[#25D366]` to `text-on-surface` on the `<Icon name="chat" filled ... />` element only. Leave the row's `bg-[#25D366]/10` tint and the inner `bg-[#25D366]/20` circle exactly as-is (they stay for brand recognition), and leave the phone/mail/address/hours rows untouched. The darkened glyph on the light-green tint clears ≥3:1 (dark on-surface on a ~10-20% green tint is well above the non-text bar).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/contact/page.tsx','utf8');const bad=[];if(/name=\"chat\"[^>]*className=\"text-\[#25D366\]\"/.test(s))bad.push('WhatsApp icon glyph still text-[#25D366]');if(!/name=\"chat\"[^>]*className=\"text-on-surface\"/.test(s))bad.push('WhatsApp icon glyph is not text-on-surface');if(!/bg-\[#25D366\]\/10/.test(s))bad.push('tint row bg-[#25D366]/10 was removed (should be kept)');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('contact WhatsApp icon contrast OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The contact-page WhatsApp `<Icon name="chat" filled>` uses `text-on-surface`, not `text-[#25D366]`.
    - The `bg-[#25D366]/10` tint row and `bg-[#25D366]/20` icon circle are preserved (brand tint kept).
    - No other contact rows are modified.
  </acceptance_criteria>
  <done>The verify prints OK. The glyph reads ≥3:1 against the tint on the preview re-audit (07-08).</done>
</task>

<task type="auto">
  <name>Task 3: Swap CTABanner's raw material-symbols spans to the Icon wrapper (UI-13)</name>
  <files>components/CTABanner.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"UI-13" (CTABanner lines 27/36/44 bypass the Icon wrapper — CLAUDE.md guardrail violation; renders fine but must use `<Icon>`; pairs naturally with A11Y-01 on the same file)
    - CLAUDE.md "Project Guardrails" (Icons: use the `components/Icon.tsx` wrapper — never raw Material Symbols spans)
    - components/CTABanner.tsx — three raw spans: line 27 `<span className="material-symbols-outlined">phone</span>` (tel button), line 36 `<span className="material-symbols-outlined">chat</span>` (WhatsApp button — its color is set by Task 1 via the parent `text-on-surface`), line 44 `<span className="material-symbols-outlined text-5xl mb-4 text-primary-fixed block">support_agent</span>` (support card glyph)
    - components/Icon.tsx — the wrapper: `<Icon name="..." filled={false} className="..." />` renders the outlined Material Symbol and inherits `currentColor`; the existing raw spans are all outlined (unfilled), so pass no `filled`
  </read_first>
  <action>
    In `components/CTABanner.tsx`, add `import { Icon } from "@/components/Icon";` and replace the three raw `<span className="material-symbols-outlined">…</span>` elements with the wrapper, preserving glyph + any extra classes: line 27 → `<Icon name="phone" />`, line 36 → `<Icon name="chat" />`, line 44 → `<Icon name="support_agent" className="text-5xl mb-4 text-primary-fixed block" />`. Do NOT add `filled` (the originals are outlined), do NOT change the parent anchors' color/geometry classes (Task 1 owns the WhatsApp button's `text-on-surface`), and leave the `phone`/`support_agent` glyph colors as they inherit today. After this, no `material-symbols-outlined` string remains in the file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/CTABanner.tsx','utf8');const bad=[];if(/material-symbols-outlined/.test(s))bad.push('a raw material-symbols-outlined span remains');if(!/import \{ Icon \} from \"@\/components\/Icon\"/.test(s))bad.push('missing Icon import');for(const n of ['phone','chat','support_agent'])if(!new RegExp('<Icon name=\"'+n+'\"').test(s))bad.push('missing <Icon name=\"'+n+'\"');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('CTABanner Icon-wrapper swap OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `components/CTABanner.tsx` imports `Icon` and uses `<Icon name="phone" />`, `<Icon name="chat" />`, and `<Icon name="support_agent" className="text-5xl mb-4 text-primary-fixed block" />`.
    - No `material-symbols-outlined` string remains in the file; glyphs render identically (outlined) and the WhatsApp button color still comes from Task 1's `text-on-surface`.
  </acceptance_criteria>
  <done>Verify prints OK; the CLAUDE.md raw-icon guardrail no longer fires on CTABanner (the only live UI-13 file; PricingSection is deleted in 07-11).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Presentational CSS-class change only — no user input, data flow, secrets, network, or auth is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-none | — | Tailwind text-color classes on existing WhatsApp links | accept | No attack surface introduced: markup/class-only edit; the `wa.me`/`tel` targets and link attributes are unchanged. |
</threat_model>

<verification>
- Verifies are pure Node string reads (OneDrive-safe): the two CTA buttons carry `bg-[#25D366] text-on-surface` (no `text-white`, no black), and the contact icon is `text-on-surface` on the preserved tint.
- Manual proof on the Vercel preview via `re-audit.mjs` (07-08): "contrast SOLID-bg failures" = 0 for the WhatsApp buttons; the contact glyph clears ≥3:1.
- Never run `next build`/`tsx` locally (OneDrive deadlock) — proof is on the preview.
- ⚠ SITE-WIDE CONSISTENCY (surface, do not silently change): Phase 6's homepage shipped a DIFFERENT AA-safe WhatsApp treatment — `ClosingCTA` + `StickyContactBar` use a neutral pill + primary glyph (not dark-on-green). This plan applies the locked owner decision (dark-text-on-green) to CTABanner/ServiceHero/contact per 2026-07-01. Result: two WhatsApp treatments coexist site-wide. This is intentional-per-decision but visually inconsistent — 07-08 flags it in the owner preview eyeball so the owner can pick one treatment for the whole site if desired. Do NOT unilaterally re-style the homepage CTAs here.
</verification>

<success_criteria>
- Both WhatsApp CTA buttons and the contact WhatsApp icon are dark-on-green / dark-on-tint and pass WCAG AA (text ≥4.5:1, glyph ≥3:1), the brand green is retained, and no black text is introduced — closing the only launch-gating a11y item (A11Y-01) and UI-10.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-01-SUMMARY.md` when done.
</output>
