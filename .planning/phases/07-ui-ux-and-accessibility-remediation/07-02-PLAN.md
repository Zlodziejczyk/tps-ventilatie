---
phase: 07-ui-ux-and-accessibility-remediation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/Footer.tsx
  - app/over-ons/page.tsx
  - app/diensten/page.tsx
autonomous: true
requirements: [A11Y-02]
must_haves:
  truths:
    - "Every audited page's heading outline nests with no level skips and exactly one h1 — the three still-live flagged skips are fixed: Footer column headers h4->h2 (footer landmark, safe from any preceding level), over-ons USP-card titles h3->h2 (so h1->h2->h2 nests), and the /diensten hub gains a screen-reader-only h2 section heading before the ServiceCard grid so the hub reads h1->h2->h3 instead of h1->h3"
    - "The audit's FOURTH skip (WhyTPSSection h2->h4) is NOT addressed here because Phase 6 retired that component — app/page-sections/WhyTPSSection.tsx is a dead file (no import), deleted in 07-11; the current homepage (HomeHero/ProofBand/PillarGrid/ImageBand/ClosingCTA) already nests h1->h2->h3 with no skip, verified in 07-08"
    - "Visual sizing is unchanged everywhere — only the heading tag/level changes; all existing Tailwind text-*/font-* classes are preserved (Tailwind Preflight makes unclassed h2/h3/h4 render identically), and the sr-only hub heading is invisible"
    - "components/ServiceCard.tsx is NOT changed: its h3 is correct on pillar pages (/diensten/[pillar]) and in RelatedServices where it sits under an h2 section — only the hub lacked a section heading, which the sr-only h2 supplies without regressing those pages"
  key_links:
    - from: "app/diensten/page.tsx"
      to: "components/ServiceCard.tsx (h3 titles)"
      via: "sr-only h2 section heading placed before the pillar-card grid so h1->h2->h3 nests"
      pattern: "sr-only"
---

<objective>
Fix the still-live heading-level skips from the 2026-06-30 audit (A11Y-02) so every page's outline nests without gaps, exactly one h1 each, with zero visual change. Three of the four original skips remain: the `Footer` column headers are h4 after page h2s, `/over-ons` jumps h1→h3 (USP cards render before the reviews h2), and the `/diensten` hub jumps h1→h3 because the `ServiceCard` pillar-card titles (h3) render with no section heading between them and the page h1. The fourth original skip — `WhyTPSSection` h2→h4 — is gone: Phase 6 retired that homepage section (dead file, deleted in 07-11), and the rebuilt homepage already nests cleanly. Re-rank the tags; for the hub, add a `sr-only` h2 section label (the honest fix — the hub genuinely lacks a heading for the card grid, and `ServiceCard`'s h3 is correct on the pillar/related pages).

Purpose: A correct heading outline is a core WCAG 2.1 AA (1.3.1) signal for screen-reader navigation and is a hard gate in the re-audit; sizing must not move.
Output: re-ranked headings in `Footer.tsx` and `over-ons/page.tsx`, and a `sr-only` section heading in `diensten/page.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Footer column headers h4 -> h2 (site-wide footer skip)</name>
  <files>components/Footer.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"A11Y-02" (h2->h4 in the Footer)
    - components/Footer.tsx — three column headers `<h4 className="font-bold text-on-surface mb-6">` ("Diensten", "Bedrijf", "Contact"); the "Brand" column is a `<div>` (leave it here — 07-12 handles the brand column's logo/copy refresh). Footer is a global `<footer>` landmark rendered after every page's content, so its first heading must not sit >1 level below whatever precedes it
    - NOTE: 07-12 (UI-11 footer refresh) also edits this file but runs AFTER this plan (it depends_on 07-02); do the heading re-rank first so 07-12 builds on the corrected tags
  </read_first>
  <action>
    In `components/Footer.tsx`, change all three column-title elements from `<h4 ...>` to `<h2 ...>` (open + close), keeping `className="font-bold text-on-surface mb-6"` unchanged. h2 is chosen because it never skips regardless of the host page's deepest preceding heading (going to a shallower/equal level is never a skip). Leave the brand `<div>` and the bottom KvK/BTW bar as-is.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/Footer.tsx','utf8');const h2=(s.match(/<h2 className=\"font-bold text-on-surface mb-6\">/g)||[]).length;const bad=[];if(/<h4[ >]/.test(s))bad.push('an <h4> remains');if(h2<3)bad.push('expected 3 column <h2> headers, found '+h2);if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('Footer headings OK ('+h2+' h2 columns)')"</automated>
  </verify>
  <acceptance_criteria>
    - The three footer column headers are `<h2 className="font-bold text-on-surface mb-6">`; no `<h4>` remains.
    - The brand block and bottom bar are unchanged; rendering is visually identical.
  </acceptance_criteria>
  <done>Verify prints OK; no page skips into the footer (re-audit headingIssues=[] site-wide in 07-08).</done>
</task>

<task type="auto">
  <name>Task 2: over-ons USP-card titles h3 -> h2 (about-page skip)</name>
  <files>app/over-ons/page.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"A11Y-02" (h1->h3 on /over-ons)
    - app/over-ons/page.tsx — after the `<h1>` (line ~38) the four USP cards render `<h3 className="font-bold text-on-surface">{item.title}</h3>` (line ~88) BEFORE the reviews `<h2>` (line ~104); that h1->h3 is the skip AND it precedes the h2. It is the only `<h3>` in the file
  </read_first>
  <action>
    In `app/over-ons/page.tsx`, change the USP-card title from `<h3 ...>` to `<h2 ...>` (open + close), keeping `className="font-bold text-on-surface"` unchanged. The outline becomes h1 → h2 (USP cards) → h2 (reviews) → h2 (CTABanner) — no skips. Do not touch the reviews section or CTABanner.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/over-ons/page.tsx','utf8');const bad=[];if(/<h3[ >]/.test(s))bad.push('an <h3> remains (USP title not re-ranked)');if(!/<h2 className=\"font-bold text-on-surface\">\{item.title\}<\/h2>/.test(s))bad.push('USP title is not <h2>{item.title}</h2> with original class');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('over-ons headings OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The USP-card title is `<h2 className="font-bold text-on-surface">{item.title}</h2>`; no `<h3>` remains in the file.
    - Reviews h2 and CTABanner are unchanged; rendering is visually identical.
  </acceptance_criteria>
  <done>Verify prints OK; /over-ons no longer skips h1→h3 (re-audit headingIssues=[] in 07-08).</done>
</task>

<task type="auto">
  <name>Task 3: /diensten hub sr-only section heading before the pillar grid (hub skip)</name>
  <files>app/diensten/page.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"A11Y-02" (h1->h3 on /diensten)
    - app/diensten/page.tsx — the hub renders `<h1>` (line ~30), then a bare `<section className="max-w-7xl mx-auto px-6 mb-24">` (line ~42) whose grid holds four `<ServiceCard>` (each title is h3), then the reviews `<h2>` (line ~75). The hub itself has NO heading between the h1 and the ServiceCard h3s — that is the h1->h3 skip
    - components/ServiceCard.tsx — the card title is `<h3 className="text-xl font-headline font-bold ...">` (line ~31). DO NOT change it: it is correct on app/diensten/[pillar]/page.tsx (sits under an h2, line ~128) and in components/RelatedServices.tsx (under an h2). Changing the shared component would risk those pages
    - components/RelatedServices.tsx + app/diensten/[pillar]/page.tsx — confirm ServiceCard's h3 nests under an h2 there, so leaving it as h3 is right
  </read_first>
  <action>
    In `app/diensten/page.tsx`, add a screen-reader-only section heading as the FIRST child inside the pillar-cards `<section>` (the one at line ~42, before the `<StaggerChildren>` grid): `<h2 className="sr-only">Onze vakgebieden</h2>`. This gives the card grid a real (invisible) section heading so the outline reads h1 → h2 (sr-only) → h3 (cards) → h2 (reviews) → h2 (CTABanner) with no skip. Do NOT modify `components/ServiceCard.tsx`, the grid, the reviews section, or the CTABanner. `sr-only` is a standard Tailwind utility (present in this project's globals) — visually hidden, exposed to assistive tech.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/diensten/page.tsx','utf8');const bad=[];if(!/<h2 className=\"sr-only\">/.test(s))bad.push('missing sr-only <h2> section heading');const h1=(s.match(/<h1[ >]/g)||[]).length;if(h1!==1)bad.push('expected exactly one h1, found '+h1);if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('diensten hub heading OK');const sc=fs.readFileSync('components/ServiceCard.tsx','utf8');if(!/<h3 /.test(sc)){console.error('ServiceCard should still use <h3> (unchanged)');process.exit(1)}console.log('ServiceCard still h3 (unchanged) OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `app/diensten/page.tsx` contains `<h2 className="sr-only">Onze vakgebieden</h2>` before the pillar-card grid; exactly one h1 remains.
    - `components/ServiceCard.tsx` is unchanged (still `<h3>`), so pillar and RelatedServices pages don't regress.
  </acceptance_criteria>
  <done>Verify prints OK; /diensten reads h1→h2→h3 with no skip, and /diensten/[pillar] stays clean (re-audit headingIssues=[] in 07-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Heading-level/markup edit only — no input, data flow, secrets, or auth touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-none | — | Static heading tags in server components | accept | No attack surface: tag-level and one sr-only label; no behavior change. |
</threat_model>

<verification>
- Verifies are pure Node string reads (OneDrive-safe): each flagged heading is re-ranked, ServiceCard stays h3, and the hub gains a sr-only h2; exactly one h1 per touched page.
- Manual proof on the Vercel preview via `re-audit.mjs` (07-08): `headingIssues=[]` and `h1Count=1` on `/`, `/diensten`, `/diensten/airconditioning`, `/over-ons`, and every audited route (the rebuilt homepage is included — its HomeHero/ProofBand/PillarGrid/ClosingCTA outline must also read clean).
- WhyTPSSection is intentionally NOT edited (dead file, deleted in 07-11) — do not resurrect it.
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- Every audited page's heading outline nests without level skips, with exactly one h1, and identical visual sizing — closing A11Y-02 — while `ServiceCard` (correct elsewhere) is left untouched.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-02-SUMMARY.md` when done.
</output>
