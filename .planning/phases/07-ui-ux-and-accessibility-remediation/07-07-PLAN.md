---
phase: 07-ui-ux-and-accessibility-remediation
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - app/page-sections/HeroSection.tsx
autonomous: true
requirements: [UI-05, UI-06, UI-07]
must_haves:
  truths:
    - "The homepage hero badge is all-Dutch — the English 'Clean Air Technology' is gone, replaced by a Dutch phrase aligned to the SITE slogan 'Specialist in gezond binnenklimaat' (UI-05)"
    - "The hero aurora reads calm and on-brand — SoftAurora color1/color2 AND the static gradient fallback use the brand teal/cyan family (around #006580), not the loud magenta/green bands (UI-06)"
    - "All three hero trust-pills lay out cleanly at 375px with no overflow/clipping/awkward wrap (UI-07)"
    - "PHASE-6 DEPENDENCY: this plan remediates the hero AFTER Phase 6 rebuilds it. The executor reads the CURRENT (post-Phase-6) app/page-sections/HeroSection.tsx first; for any of UI-05/06/07 the rebuild already satisfies, it asserts and skips; otherwise it applies the fix. Acceptance is outcome-based (Dutch badge, teal aurora, clean pills), robust to the rebuild"
  key_links:
    - from: "app/page-sections/HeroSection.tsx badge"
      to: "SITE slogan 'Specialist in gezond binnenklimaat'"
      via: "Dutch hero badge text"
      pattern: "gezond binnenklimaat"
---

<objective>
Bring the homepage hero on-brand and all-Dutch (UI-05/06/07) — a Dutch badge instead of "Clean Air Technology", a calmer brand-teal aurora instead of the loud magenta/green bands, and three trust-pills that lay out cleanly at 375px. **These three items live in the hero that Phase 6 rebuilds, and Phase 6 runs before Phase 7** — so this is a *verify-and-remediate* plan: read the post-Phase-6 hero, keep whatever the rebuild already got right, and fix only the residual gaps. Acceptance is outcome-based so it holds whether or not the rebuild already landed each fix.

Purpose: The highest-traffic page's hero must be on-brand, calm, Dutch, and clean on mobile — the last of the audit's polish items, gated to after the Phase-6 rebuild to avoid rework.
Output: a Dutch badge, teal/cyan aurora (props + fallback), and clean 375px pills in `app/page-sections/HeroSection.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify-and-remediate the post-Phase-6 hero — Dutch badge, teal aurora, clean 375px pills (UI-05/06/07)</name>
  <files>app/page-sections/HeroSection.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"UI-05" (badge "Clean Air Technology" → Dutch, align with SITE slogan "Specialist in gezond binnenklimaat"; confirm the badge survives the Phase-6 rebuild before editing), §"UI-06" (magenta/green aurora reads loud; SoftAurora color1/color2 + static fallback #a8dff0/#b8e8d0 → nudge to brand teal/cyan #006580 family), §"UI-07" (3 desktop pills, only 2 lay out cleanly at 375px — absolute bottom-8 + flex-wrap; likely resolved by the rebuild — verify), and the §"Sequencing note" (do UI-05/06/07 AFTER Phase 6's hero lands)
    - app/page-sections/HeroSection.tsx — the CURRENT file (post-Phase-6). In the pre-Phase-6 baseline: the badge text is "Clean Air Technology" (~line 81); `<SoftAurora color1="#a8dff0" color2="#b8e8d0" .../>` plus a static fallback `bg-gradient-to-br from-[#a8dff0]/50 via-[#b8e8d0]/40 ...`; three trust-pills in an `absolute bottom-8 ... flex flex-wrap` row. The rebuild may have changed these — read what is actually there now
    - lib/constants.ts — `SITE.tagline` = "Specialist in gezond binnenklimaat" (the Dutch slogan to align the badge with); brand teal is the primary/`#006580` family
    - Skill "sketch-findings-tps-ventilatie" — the validated hero design language ("Atmospheric Clarity, engineered", brand-teal aurora); load it to match the intended hero look
  </read_first>
  <action>
    Read the current `app/page-sections/HeroSection.tsx` (post-Phase-6). Then, for each item, assert-or-fix:
    (UI-05) If a hero badge still shows English (e.g. "Clean Air Technology"), replace the badge text with a concise Dutch phrase aligned to `SITE.tagline` — e.g. "Specialist in gezond binnenklimaat" (or a short "Gezond binnenklimaat" if the badge is size-constrained). No English copy remains in the hero badge.
    (UI-06) If the aurora still uses the loud light-blue/green pair, set `SoftAurora` `color1`/`color2` AND the static gradient fallback to the brand teal/cyan family (values in the `#006580` neighbourhood, e.g. a lighter teal `#3fa3bd`/`#8fd4e8` paired toward `#006580`), so the resting aurora reads calm brand-teal, not magenta/green. Keep all other SoftAurora props and the desktop-only motion gating (`useEnableHeavyMotion`) exactly as-is.
    (UI-07) If the three trust-pills still overflow/clip at 375px, make them lay out cleanly at 375px (e.g. allow a centered 2-row wrap without clipping, or tighten mobile gap/padding, or move them from absolute to static flow on mobile) — all three fully visible, no horizontal overflow. Keep the desktop layout.
    If the Phase-6 rebuild already satisfies any item, make no change for that item and record "already satisfied by Phase 6" in the SUMMARY. Do not touch other sections; keep the design-system rules (tokens, no #000, Icon wrapper).
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('app/page-sections/HeroSection.tsx','utf8');const bad=[];if(/Clean Air Technology/i.test(s))bad.push('UI-05: English badge \"Clean Air Technology\" still present');if(/#a8dff0|#b8e8d0/.test(s))bad.push('UI-06: loud aurora colors #a8dff0/#b8e8d0 still present — retune to the teal/#006580 family');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('hero UI-05/UI-06 source checks OK (UI-07 pills verified visually on preview)')"</automated>
  </verify>
  <acceptance_criteria>
    - UI-05: no English badge text remains; the hero badge is Dutch, aligned to the SITE slogan.
    - UI-06: `SoftAurora` colors and the static fallback are in the brand teal/cyan (~#006580) family — the old `#a8dff0`/`#b8e8d0` loud pair is gone; motion gating unchanged.
    - UI-07: at 375px all three trust-pills are fully visible with no overflow/clipping.
    - Any item already handled by the Phase-6 rebuild is noted as such in the SUMMARY.
  </acceptance_criteria>
  <done>Verify prints OK; on the preview (07-08) the hero badge is Dutch, the aurora reads calm brand-teal, and the 375px pills lay out cleanly with `horizScroll=false`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Copy + color-prop + responsive-layout change in a presentational hero — no input, network, secrets, or auth touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-none | — | Hero badge/aurora/pills | accept | No attack surface: static copy, color literals, and layout classes; the WebGL gate is unchanged. |
</threat_model>

<verification>
- Verify is a pure Node string read (OneDrive-safe): no English badge, no loud aurora literals.
- Manual proof on the Vercel preview (07-08): the hero badge is Dutch, the aurora reads calm brand-teal (screenshot), and at 375px all three pills lay out cleanly (`horizScroll=false`, no clipping).
- Phase-6 dependency: run this AFTER Phase 6's hero rebuild has landed on the branch; read the current hero before editing.
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- The homepage hero is on-brand and all-Dutch — Dutch badge, calmer brand-teal aurora, clean 375px trust-pills — applied to the Phase-6 hero with no regression, closing UI-05/UI-06/UI-07.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-07-SUMMARY.md` when done.
</output>
