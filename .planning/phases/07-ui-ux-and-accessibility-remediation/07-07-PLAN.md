---
phase: 07-ui-ux-and-accessibility-remediation
plan: 07
type: execute
wave: 3
depends_on: []
files_modified:
  - app/page-sections/home/HomeHero.tsx
  - app/globals.css
autonomous: true
requirements: [UI-05, UI-06, UI-07]
must_haves:
  truths:
    - "UI-05/06/07 target the LIVE homepage hero after Phase 6's rebuild — NOT the retired app/page-sections/HeroSection.tsx (dead file, deleted in 07-11). The live hero is app/page-sections/home/HomeHero.tsx; the aurora is a pure-CSS .aurora/.blob in app/globals.css (Phase 6 replaced the old SoftAurora WebGL)"
    - "UI-05 (Dutch badge): the HomeHero badge renders {SITE.tagline} ('Specialist in gezond binnenklimaat') — the English 'Clean Air Technology' died with HeroSection; no English chrome remains in the live hero"
    - "UI-06 (calm on-brand aurora): the .aurora .blob gradients in globals.css use the calm brand teal/cyan family (#a8dff0 / #b8e8d0 / #baeaff) — the loud magenta/green SoftAurora bands are gone from the homepage; verify no loud accent literals remain"
    - "UI-07 (clean mobile pills): the old absolute `bottom-8 flex-wrap` 3-pill row that broke at 375px no longer exists — HomeHero uses an in-flow flex-wrap proof bar; confirm no horizontal overflow/clipping at 375px on the preview (07-08)"
    - "This is verify-and-record: if all three are already satisfied by the Phase-6 rebuild (expected), make NO source change and record 'satisfied by Phase 6' in the SUMMARY; only touch HomeHero/globals.css if a residual gap is found"
  key_links:
    - from: "app/page-sections/home/HomeHero.tsx badge"
      to: "SITE slogan 'Specialist in gezond binnenklimaat'"
      via: "Dutch hero badge = {SITE.tagline}"
      pattern: "SITE.tagline"
---

<objective>
Close UI-05/06/07 against the LIVE post-Phase-6 homepage hero — a Dutch badge (not "Clean Air Technology"), a calm brand-teal aurora (not the loud magenta/green bands), and trust content that lays out cleanly at 375px. **Phase 6 rebuilt the homepage** (`app/page.tsx` now renders `HomeHero / ProofBand / ImageBand / ClosingCTA`), retiring the old `app/page-sections/HeroSection.tsx` (dead file, deleted in 07-11) and swapping the WebGL `SoftAurora` for a pure-CSS `.aurora`/`.blob` in `app/globals.css`. Investigation shows the rebuild already satisfies all three items — so this is a **verify-and-record** plan: assert the Dutch badge, the calm-teal aurora, and clean mobile layout on the live files; only edit if a residual gap remains. Acceptance is outcome-based so it holds whether Phase 6 landed each fix (it did) or not.

Purpose: The highest-traffic page's hero must be on-brand, calm, Dutch, and clean on mobile — retargeted to the live component set so we don't remediate a dead file.
Output: an assertion (and SUMMARY note) that UI-05/06/07 hold on the live hero; a residual fix in `HomeHero.tsx`/`globals.css` only if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify-and-record UI-05/06/07 on the live post-Phase-6 hero (Dutch badge, calm teal aurora, clean 375px layout)</name>
  <files>app/page-sections/home/HomeHero.tsx, app/globals.css</files>
  <read_first>
    - 07-CONTEXT.md §"UI-05" (badge → Dutch, align with SITE slogan "Specialist in gezond binnenklimaat"), §"UI-06" (aurora reads loud → nudge to brand teal/cyan), §"UI-07" (3 pills, only 2 lay out cleanly at 375px), and the §"Sequencing note" (UI-05/06/07 live in the hero Phase 6 rebuilds — do them after / fold into Phase 6)
    - app/page-sections/home/HomeHero.tsx — the LIVE hero. The badge renders `{SITE.tagline}` with the comment "Dutch badge (UI-05 — no English chrome)"; the aurora is `<div className="aurora" aria-hidden>` with `.blob b1/b2/b3` (comment "Pure-CSS brand-teal aurora"); trust content (proof bar, coverage line, availability pulse) is in normal flow with `flex-wrap`, NOT the old absolute `bottom-8` pill row
    - app/globals.css — `.aurora .b1/.b2/.b3` radial-gradient colors: `#a8dff0` / `#b8e8d0` / `#baeaff` (comment "no loud accent colors, no phantom sketch vars"); brand primary is `#006580`
    - lib/constants.ts — `SITE.tagline` = "Specialist in gezond binnenklimaat"
    - Skill "sketch-findings-tps-ventilatie" — the validated "Atmospheric Clarity, engineered" hero language (brand-teal aurora) that Phase 6 implemented; load it to confirm the intended look matches
  </read_first>
  <action>
    Read the LIVE hero (`app/page-sections/home/HomeHero.tsx`) and the aurora CSS (`app/globals.css`). Assert each item; edit ONLY if an assertion fails:
    (UI-05) The hero badge is Dutch (`{SITE.tagline}` / "Specialist in gezond binnenklimaat") and no English badge copy ("Clean Air Technology") appears in any live home section. If English chrome is found, replace it with the Dutch tagline.
    (UI-06) The `.aurora .blob` gradients use the calm brand teal/cyan family (`#a8dff0`/`#b8e8d0`/`#baeaff`) and there is NO loud magenta/green literal and NO `SoftAurora` WebGL on the homepage. If a loud literal is present, retune it toward the brand teal (`#006580`) family, keeping it calm/low-saturation.
    (UI-07) The hero trust content is in normal flow (`flex-wrap`), not the old absolute `bottom-8` pill row; nothing is set up to overflow at 375px. If a residual mobile-overflow pattern exists, fix it (allow clean wrap / tighten mobile gap).
    Record the outcome per item in the SUMMARY ("satisfied by Phase 6" vs "residual fix applied"). Keep design-system rules (tokens, no #000, Icon wrapper). Do not touch other sections.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const bad=[];const h=fs.readFileSync('app/page-sections/home/HomeHero.tsx','utf8');if(/Clean Air Technology/i.test(h))bad.push('UI-05: English badge \"Clean Air Technology\" present in live hero');if(!/SITE\.tagline/.test(h))bad.push('UI-05: badge no longer renders SITE.tagline');if(/SoftAurora/.test(h))bad.push('UI-06: SoftAurora WebGL is back on the homepage hero');const g=fs.readFileSync('app/globals.css','utf8');if(!/\.aurora \.b1/.test(g)&&!/\.blob/.test(g))bad.push('UI-06: CSS aurora .blob rules missing');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('hero UI-05/UI-06 source checks OK (UI-07 pills verified visually on preview in 07-08)')"</automated>
  </verify>
  <acceptance_criteria>
    - UI-05: the live HomeHero badge is Dutch (`SITE.tagline`); no English badge text anywhere in the home sections.
    - UI-06: the CSS aurora `.blob` gradients are the calm teal/cyan family; no loud magenta/green literal; no `SoftAurora` on the homepage.
    - UI-07: hero trust content is in-flow `flex-wrap` (no absolute `bottom-8` pill row); no 375px overflow pattern.
    - Each item's status ("satisfied by Phase 6" / "residual fix applied") is recorded in the SUMMARY.
  </acceptance_criteria>
  <done>Verify prints OK; on the preview (07-08) the hero badge is Dutch, the aurora reads calm brand-teal, and the 375px hero lays out with `horizScroll=false` — closing UI-05/06/07 against the live hero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Read-and-assert (copy/color/layout) on a presentational hero — no input, network, secrets, or auth touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07-none | — | Live hero badge/aurora/layout | accept | No attack surface: verify-only assertions, at most static copy/color/layout literals. |
</threat_model>

<verification>
- Verify is a pure Node string read (OneDrive-safe): live-hero source targets, not the dead HeroSection.tsx.
- Manual proof on the Vercel preview (07-08): the hero badge is Dutch, the aurora reads calm brand-teal (screenshot), and at 375px the hero lays out cleanly (`horizScroll=false`, no clipping).
- This plan retargets UI-05/06/07 to the LIVE homepage (HomeHero + globals.css) — the original 07-07 targeted the now-dead HeroSection.tsx.
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- The live homepage hero is on-brand and all-Dutch — Dutch badge, calm brand-teal CSS aurora, clean 375px layout — verified (or residual-fixed) on the Phase-6 component set, closing UI-05/UI-06/UI-07.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-07-SUMMARY.md` when done.
</output>
