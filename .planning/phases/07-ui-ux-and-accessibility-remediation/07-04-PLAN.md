---
phase: 07-ui-ux-and-accessibility-remediation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - components/StickyContactBar.tsx
  - components/MobileMenu.tsx
  - components/ReviewCarousel.tsx
autonomous: true
requirements: [A11Y-04]
must_haves:
  truths:
    - "The three sub-44px interactive controls from the audit are padded to a >=44x44 hit-area with the glyph size unchanged: the sticky-bar close button (was 32x32) becomes >=44x44, the mobile-menu accordion chevron toggles (were 40x42) become >=44x44, and the review-carousel prev/next arrows (were 40x40) become 44x44"
    - "Only hit-area/box sizing changes — icon glyph sizes, aria-labels, positions, and behavior are preserved; inline text links (breadcrumbs, '34 reviews', etc.) are left alone (WCAG 2.5.x-exempt per the audit)"
  key_links:
    - from: "components/StickyContactBar.tsx close button"
      to: "WCAG 2.5.5/2.5.8 target size"
      via: "h-11 w-11 (44px) hit area, 20px glyph unchanged"
      pattern: "h-11 w-11"
---

<objective>
Bring the three interactive controls the audit measured under 44px up to a ≥44×44 hit-area (A11Y-04, WCAG target-size) without changing how they look at the glyph level: the sticky-bar dismiss button (32×32), the mobile-menu accordion chevrons (40×42), and the review-carousel arrows (40×40). Pad the tap area; keep the icon sizes, labels, and positions.

Purpose: Reliable touch/pointer targets for the site's non-inline controls — a launch-hardening AA item.
Output: enlarged hit-areas on `StickyContactBar.tsx`, `MobileMenu.tsx`, and `ReviewCarousel.tsx`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-ux-and-accessibility-remediation/07-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sticky-bar close button 32x32 -> 44x44</name>
  <files>components/StickyContactBar.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"A11Y-04" (sticky-bar close 32x32; pad hit-area to >=44x44, keep glyph)
    - components/StickyContactBar.tsx — the dismiss `<button aria-label="Contactbalk sluiten">` (~line 52) currently `... absolute right-3 top-2 flex h-8 w-8 items-center justify-center rounded-full ...` holding `<Icon name="close" className="text-[20px]" />`
  </read_first>
  <action>
    In `components/StickyContactBar.tsx`, change the close button's `h-8 w-8` to `h-11 w-11` (44px) — keep `flex items-center justify-center rounded-full`, the `absolute right-3 top-2` position, the `aria-label`, and the `text-[20px]` glyph exactly as-is (the larger box centers the same 20px glyph). Do not touch the Bel/WhatsApp/Offerte action row.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/StickyContactBar.tsx','utf8');const bad=[];if(/aria-label=\"Contactbalk sluiten\"[\\s\\S]{0,160}h-8 w-8/.test(s))bad.push('close button still h-8 w-8');if(!/aria-label=\"Contactbalk sluiten\"[\\s\\S]{0,160}h-11 w-11/.test(s))bad.push('close button not h-11 w-11');if(!/name=\"close\" className=\"text-\[20px\]\"/.test(s))bad.push('close glyph size changed (should stay text-[20px])');if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('sticky close 44px OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The close button is `h-11 w-11` (≥44px) with an unchanged `text-[20px]` glyph, position, and aria-label.
  </acceptance_criteria>
  <done>Verify prints OK; re-audit `smallTargets` no longer flags the sticky close (07-08).</done>
</task>

<task type="auto">
  <name>Task 2: Mobile-menu accordion chevrons 40x42 -> >=44x44</name>
  <files>components/MobileMenu.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"A11Y-04" (mobile-menu accordion chevrons 40x42; pad to >=44x44, keep glyph)
    - components/MobileMenu.tsx — the per-pillar toggle `<button aria-expanded={isOpen} aria-label={...uitklappen/inklappen}>` (~line 85) is `className="p-2 rounded-lg text-on-surface-variant hover:bg-surface-container-high transition-colors cursor-pointer"` around an `<Icon name="expand_more" .../>`. The top "Sluit menu" close button (~line 41, `p-3`) already ~48px — leave it
  </read_first>
  <action>
    In `components/MobileMenu.tsx`, on the pillar accordion toggle `<button>` (the one with `aria-expanded`), add `flex items-center justify-center min-h-11 min-w-11` to guarantee a ≥44×44 hit-area while centering the existing `expand_more` glyph; keep `rounded-lg`, the hover, `cursor-pointer`, the `aria-expanded`/`aria-label`, and the Icon unchanged. Do not alter the "Sluit menu" button or the nav links.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/MobileMenu.tsx','utf8');const m=s.match(/aria-expanded=\{isOpen\}[\\s\\S]{0,260}?className=\"([^\"]*)\"/);const bad=[];if(!m){bad.push('could not find the accordion toggle button')}else{const cls=m[1];if(!/min-h-11/.test(cls)||!/min-w-11/.test(cls))bad.push('chevron toggle missing min-h-11/min-w-11 (got: '+cls+')')}if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('mobile chevron 44px OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The mobile accordion chevron toggle has `min-h-11 min-w-11` (≥44px) and centers its unchanged glyph; aria attributes preserved.
  </acceptance_criteria>
  <done>Verify prints OK; the chevron measures ≥44×44 (menu-open manual check on preview, 07-08 — the automated audit scans with the menu closed).</done>
</task>

<task type="auto">
  <name>Task 3: Review-carousel arrows 40x40 -> 44x44</name>
  <files>components/ReviewCarousel.tsx</files>
  <read_first>
    - 07-CONTEXT.md §"A11Y-04" (review-carousel arrows 40x40; pad to >=44)
    - components/ReviewCarousel.tsx — the prev `<button aria-label="Vorige review">` (~line 159) and next `<button aria-label="Volgende review">` (~line 166) are both `... w-10 h-10 rounded-full ... hidden lg:flex` around a chevron Icon
  </read_first>
  <action>
    In `components/ReviewCarousel.tsx`, change `w-10 h-10` to `w-11 h-11` (44px) on BOTH arrow buttons (prev + next), keeping every other class (`rounded-full`, colors, shadow, hover, `hidden lg:flex`, the `-translate-x-4`/`translate-x-4` offsets) and the chevron Icon unchanged.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/ReviewCarousel.tsx','utf8');const bad=[];const arrowBtns=(s.match(/aria-label=\"(Vorige|Volgende) review\"/g)||[]).length;if(arrowBtns!==2)bad.push('expected 2 arrow buttons, found '+arrowBtns);if(/w-10 h-10/.test(s))bad.push('an arrow still w-10 h-10');const w11=(s.match(/w-11 h-11/g)||[]).length;if(w11<2)bad.push('expected both arrows at w-11 h-11, found '+w11);if(bad.length){console.error(bad.join('; '));process.exit(1)}console.log('carousel arrows 44px OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Both carousel arrow buttons are `w-11 h-11` (44px); no `w-10 h-10` remains; glyphs and positions unchanged.
  </acceptance_criteria>
  <done>Verify prints OK; re-audit desktop `smallTargets` no longer flags the carousel arrows (07-08).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Hit-area sizing classes on existing buttons — no input, network, secrets, or auth touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-none | — | Button sizing utilities | accept | No attack surface: box-size class changes only; handlers/labels unchanged. |
</threat_model>

<verification>
- Verifies are pure Node string reads (OneDrive-safe): sticky close `h-11 w-11`, chevron `min-h-11 min-w-11`, carousel arrows `w-11 h-11` — glyphs unchanged.
- Manual proof on the Vercel preview via `re-audit.mjs` (07-08): desktop `smallTargets` no longer lists the sticky close or carousel arrows; the mobile-menu chevron is spot-checked with the menu open (the automated scan runs with it closed).
- Never run `next build`/`tsx` locally (OneDrive deadlock).
</verification>

<success_criteria>
- The sticky-bar close, mobile-menu chevrons, and carousel arrows all present a ≥44×44 hit-area with unchanged glyphs — closing A11Y-04.
</success_criteria>

<output>
Create `.planning/phases/07-ui-ux-and-accessibility-remediation/07-04-SUMMARY.md` when done.
</output>
