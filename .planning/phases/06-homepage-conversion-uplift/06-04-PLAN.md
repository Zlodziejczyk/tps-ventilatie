---
phase: 06-homepage-conversion-uplift
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - app/page-sections/home/HomeHero.tsx
  - app/page-sections/home/PillarGrid.tsx
autonomous: true
requirements: [D-04, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-18, D-19]
must_haves:
  truths:
    - "Above the fold: Dutch badge, exactly one h1 (gradient-accent span), proof bar (initials + 4,9 + short quote + 34 Google-reviews), data-sourced coverage line, compact offerte form — two columns stacking under ~860px (D-04/D-06/D-07/D-18)"
    - "All four pillars visible as an equal scannable grid with icons + Nieuw tag on Warmtepompen; primary click opens /diensten/[pillar], a distinct Offerte button pre-selects that service in the hero form and scrolls to it (D-08/D-09)"
    - "Each card shows sub-service chips linking to sub-service pages + brand chips; WTW + Mechanische Ventilatie render a neutral 'Diverse merken · merkonafhankelijk' chip, never fabricated brands (D-10/D-12/D-13)"
    - "Pure-CSS brand-teal aurora behind the hero (no WebGL/canvas); motion cheap+gated; trust pills clean at 375px (D-04/D-18/D-19)"
  artifacts:
    - path: "app/page-sections/home/HomeHero.tsx"
      provides: "client island: hero content + dienst state + formRef + compact OfferteForm + pillar routing"
      min_lines: 90
    - path: "app/page-sections/home/PillarGrid.tsx"
      provides: "4-pillar grid from taxonomy: primary link, Offerte button, sub-service chips, brand chips + neutral fallback"
      min_lines: 70
  key_links:
    - from: "app/page-sections/home/HomeHero.tsx"
      to: "components/OfferteForm.tsx"
      via: "variant=compact + controlled dienst"
      pattern: "variant"
    - from: "app/page-sections/home/PillarGrid.tsx"
      to: "lib/services/registry.ts"
      via: "pillars/childrenOf/brandsForPillar/urlFor"
      pattern: "brandsForPillar"
    - from: "app/page-sections/home/HomeHero.tsx"
      to: "PillarGrid onOfferte"
      via: "setDienst + formRef.scrollIntoView"
      pattern: "scrollIntoView"
---

<objective>
Build the hero + 4-pillar grid as ONE client island (HomeHero) that lifts a shared dienst state and a formRef, so a pillar Offerte click pre-selects that service in the compact hero form and scrolls to it (D-09) with NO query param / useSearchParams / Suspense — the page stays static. Hero = Sketch-001-D (Dutch badge, one gradient-accent h1, proof bar, data-sourced coverage line, pure-CSS aurora, compact OfferteForm). PillarGrid = Sketch-002-D equal grid rendered entirely from the taxonomy with the neutral WTW/MV brand fallback.

Output: app/page-sections/home/HomeHero.tsx + app/page-sections/home/PillarGrid.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-homepage-conversion-uplift/06-RESEARCH.md
@.planning/phases/06-homepage-conversion-uplift/06-PATTERNS.md
@.planning/phases/06-homepage-conversion-uplift/06-VALIDATION.md
@.claude/skills/sketch-findings-tps-ventilatie/references/hero-and-conversion.md
@.claude/skills/sketch-findings-tps-ventilatie/references/service-pillars.md
@app/page-sections/HeroSection.tsx
@app/page-sections/ServicesSection.tsx
@components/OfferteForm.tsx
@components/BrandGrid.tsx
@lib/services/registry.ts
@lib/constants.ts
@lib/reviews.ts
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: HomeHero island — badge, one h1, proof bar, coverage line, CSS aurora, compact form + state/ref</name>
  <files>app/page-sections/home/HomeHero.tsx</files>
  <read_first>
    - app/page-sections/HeroSection.tsx lines 1-8 ("use client" + named export convention) + 76-82 (badge Icon-in-pill) — DO NOT copy its SoftAurora/WebGL (Pitfall 6)
    - 06-RESEARCH.md Pattern 2 (state-lift + ref-scroll) + Container-query example (@container + @min-[860px]:grid-cols-[1.15fr_0.85fr]) + Gradient H1 example + Dutch score formatting (guard REVIEW_RATING null)
    - 06-PATTERNS.md "HomeHero.tsx" section + sketch-token translation table
    - lib/reviews.ts 13,135-136 (REVIEWS + REVIEW_RATING {4.9,34}, nullable) ; lib/initials.ts getInitials ; lib/constants.ts SITE.serviceAreas + serviceRadiusKm
    - components/OfferteForm.tsx new props variant/dienst/onDienstChange (06-02) ; app/globals.css .aurora/.blob/.fu (06-03)
  </read_first>
  <action>
    "use client" named export HomeHero. Hold const [dienst,setDienst]=useState("") + const formRef=useRef<HTMLDivElement>(null). Wrap hero in @container; two-column grid @min-[860px]:grid-cols-[1.15fr_0.85fr] items-start gap-10 collapsing to one column under 860px (D-04). Left: a Dutch badge pill (Icon + Dutch label, UI-05, no English); exactly ONE h1 with a signature-gradient bg-clip-text text-transparent accent span carrying the D-06 headline "Airco, warmtepomp & ventilatie — goed geregeld in Zoetermeer en heel Zuid-Holland"; a proof bar built from real REVIEWS names via getInitials monogram chips (NO avatars — D-07) + Dutch-formatted score (REVIEW_RATING guarded null -> toLocaleString nl-NL, else a "{REVIEWS.length}+ reviews" line) + a short real quote + "34 Google-reviews" (REVIEW_RATING?.count ?? REVIEWS.length); a coverage line composed from SITE.serviceAreas + SITE.serviceRadiusKm (never hardcode towns); a live pulse (StickyContactBar 63-65 motion-safe:animate-ping). Behind content: a decorative div.aurora aria-hidden with three .blob b1/b2/b3 (06-03 classes; pointer-events-none, absolute inset-0) — NO SoftAurora/ogl/canvas. Right: div ref={formRef} wrapping <OfferteForm variant="compact" dienst={dienst} onDienstChange={setDienst} />. Real tokens + Icon wrapper only. Pillar callback stub left for Task 3.
  </action>
  <acceptance_criteria>
    - rg -c "<h1" HomeHero.tsx equals 1 (A11Y-02).
    - rg -n "@container|@min-\[860px\]" (D-04), rg -n "variant" + "getInitials" + "serviceAreas|serviceRadiusKm" all present.
    - No-WebGL: search SoftAurora|ogl|AmbientParticles|FocalParticles|useParticleEngine|<canvas -> ZERO.
    - Guardrails: no phantom var(--gradient-ink|--glass-strong|--shadow-|--ease-clarity|--color-bg-tint|--color-text-faint), no material-symbols-outlined, no #000, no hardcoded phone.
    - Ties to 06-VALIDATION.md D-04/D-06/D-07/D-18/D-19.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "@container" app/page-sections/home/HomeHero.tsx</automated>
  </verify>
  <done>Badge + one gradient h1 + proof bar + coverage line + aurora + compact form; state/ref present; zero WebGL; guardrail greps empty.</done>
</task>

<task type="auto">
  <name>Task 2: PillarGrid — equal 4-card grid from taxonomy with brand chips + neutral WTW/MV fallback</name>
  <files>app/page-sections/home/PillarGrid.tsx</files>
  <read_first>
    - app/page-sections/ServicesSection.tsx 22, 84-89 (card-grid + Icon + feature-chip visual — but SOURCE from taxonomy)
    - components/BrandGrid.tsx 33-48 (text-word chip: brand-colored square via --brand + brand NAME; NEVER <img>, logo paths 404) ; BRAND_COLOR now from lib/services/brands.ts (06-01)
    - lib/services/registry.ts pillars() (icons ac_unit/heat_pump/hvac/air), childrenOf, brandsForPillar ([] for wtw + mechanische-ventilatie), urlFor
    - 06-RESEARCH.md "Neutral brand fallback (D-13)" + Pitfall 5 ; 06-PATTERNS.md "PillarGrid.tsx" ; service-pillars.md (independent heights, Nieuw tag, chips=links)
  </read_first>
  <action>
    "use client" named export PillarGrid taking { onOfferte: (pillarNavTitle: string) => void }. Map pillars() into an equal grid 4->2->1 cols with items-start (independent heights — D-08). Each card: pillar Icon (pillar.icon), navTitle as h3, a presentational "Nieuw" tag ONLY on Warmtepompen (markup, not taxonomy), navDescription; a PRIMARY Link href={urlFor(pillar)} -> /diensten/[pillar] (D-09) and a DISTINCT button onClick={() => onOfferte(pillar.navTitle)} labeled Offerte (pass navTitle so it matches the form dienstOptions exactly); sub-service chips from childrenOf(pillar.pillarSlug) each a Link href={urlFor(child)} (D-10); a brand-chip row from brandsForPillar(pillar.pillarSlug) — length>0 renders the BrandGrid text-word chips via BRAND_COLOR (brand-colored square + brand.name), length===0 renders a single neutral chip "Diverse merken · merkonafhankelijk" (D-13, no fabricated names). Chips-as-links, NO expand-drawer (D-11 default). Icon wrapper; tonal layering not 1px borders; text-on-surface not #000; whitespace-nowrap content-sized chips (clean at 375px — UI-07).
  </action>
  <acceptance_criteria>
    - rg -n "pillars\(\)|childrenOf|brandsForPillar|urlFor" PillarGrid.tsx shows all four taxonomy calls (nothing hardcoded).
    - rg -n "Diverse merken|merkonafhankelijk" shows the neutral fallback; rg -n "onOfferte" shows the gesture passes navTitle.
    - tsx assert (verify) proves brandsForPillar returns [] for wtw + mechanische-ventilatie (06-VALIDATION.md D-13).
    - No <img brand logo; guardrail greps (material-symbols-outlined / phantom vars / #000) empty.
  </acceptance_criteria>
  <verify>
    <automated>npx tsx -e "import('./lib/services/registry.ts').then(m=>{if(m.brandsForPillar('wtw').length===0&&m.brandsForPillar('mechanische-ventilatie').length===0){console.log('OK')}else{throw 0}})"</automated>
  </verify>
  <done>Four cards from taxonomy; primary link + Offerte button; sub-service + brand chips; WTW/MV neutral fallback proven; no fabricated brands, no logo images.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the pillar-to-form gesture (routeToOfferte) into HomeHero</name>
  <files>app/page-sections/home/HomeHero.tsx</files>
  <read_first>
    - 06-RESEARCH.md Pattern 2 routeToOfferte (setDienst + reduced-motion-aware scrollIntoView) + the note that onOfferte MUST receive pillar.navTitle to align with the form's pillars().map(p=>p.navTitle) options
    - app/page-sections/home/HomeHero.tsx (Task 1 dienst state + formRef) ; app/page-sections/home/PillarGrid.tsx (Task 2 onOfferte prop)
  </read_first>
  <action>
    In HomeHero add function routeToOfferte(pillarNavTitle: string){ setDienst(pillarNavTitle); const reduce = window.matchMedia("(prefers-reduced-motion: reduce)").matches; formRef.current?.scrollIntoView({ behavior: reduce ? "auto" : "smooth", block: "center" }); } and render <PillarGrid onOfferte={routeToOfferte} /> below the hero grid. The compact OfferteForm stays controlled by the same dienst state so the pre-select is visible. Final guardrail sweep: one h1, zero WebGL imports, zero phantom vars.
  </action>
  <acceptance_criteria>
    - rg -n "routeToOfferte|scrollIntoView" HomeHero.tsx shows the gesture; rg -n "PillarGrid" shows it wired with onOfferte.
    - rg -c "<h1" HomeHero.tsx still equals 1; no-WebGL search still ZERO.
    - Preview: clicking each pillar Offerte pre-selects that pillar in the hero dropdown AND scrolls to the form (06-VALIDATION.md D-09, manual/preview).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "routeToOfferte" app/page-sections/home/HomeHero.tsx</automated>
  </verify>
  <done>routeToOfferte wired; PillarGrid rendered with onOfferte; pre-select + scroll verified on preview; one h1; zero WebGL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/lead | The compact hero form submits untrusted lead fields to the Phase-5 secure route (inherited; not rebuilt here). |
| static data -> DOM | REVIEWS quotes, pillar navTitle/navDescription, brand names rendered as text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-XSS | Tampering | proof bar / pillar / brand text | mitigate | All content is static data rendered as text (React auto-escapes); NO dangerouslySetInnerHTML anywhere in the new sections. |
| T-06-04-V5 | Tampering | compact form submit | mitigate | Reuses OfferteForm (06-02) -> /api/lead server Zod validation + honeypot; no client-only path, no weakened schema. |
| T-06-04-END | Repudiation | WTW/MV brand chips | mitigate | brandsForPillar()===[] drives a neutral chip — no fabricated dealer endorsement (D-13/Pitfall 5). |
| T-06-04-D | DoS (perf) | aurora / motion | mitigate | Pure-CSS aurora gated to desktop+no-reduced-motion; zero WebGL/canvas on the homepage (SEO-10). |
| T-06-04-SC | Tampering (supply chain) | package installs | accept | Zero installs this phase. |
</threat_model>

<verification>
- Local: rg + the two tsx asserts (WTW/MV empty-brands) pass (OneDrive-safe; no next build/tsc).
- Preview (wave gate): hero stacks under 860px; pillar Offerte pre-selects + scrolls; compact submit -> POST /api/lead 200 + WhatsApp; aurora animates desktop-only; PageSpeed LCP = hero H1, INP < 200ms.
- Owner-review (manual, non-blocking): H1 + coverage-line copy (D-06) approvable; WTW/MV serviced-brand list may be supplied later to replace the neutral chip (D-13) — the neutral fallback ships in the meantime.
</verification>

<success_criteria>
- HomeHero: one gradient h1, badge, proof bar, data coverage line, gated CSS aurora, compact controlled form, routeToOfferte gesture; zero WebGL.
- PillarGrid: 4 taxonomy-sourced cards, primary link + Offerte button, sub-service + brand chips, WTW/MV neutral fallback.
- All guardrail + phantom-token greps empty; both tsx asserts print OK.
</success_criteria>

<output>
Create `.planning/phases/06-homepage-conversion-uplift/06-04-SUMMARY.md` when done.
</output>
