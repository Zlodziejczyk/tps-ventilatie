---
phase: 06-homepage-conversion-uplift
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/OfferteForm.tsx
  - lib/whatsapp.ts
autonomous: true
requirements: [D-03, D-09]
must_haves:
  truths:
    - "A caller can render OfferteForm variant=compact showing only Postcode + Telefoon + Dienst + condensed consent + submit (D-03)"
    - "When dienst/onDienstChange props are passed the dienst select is CONTROLLED, so a post-mount pillar click can change its value (D-09 enabler)"
    - "A compact submit still POSTs to /api/lead and passes the shared leadSchema (naam.min(2) via sentinel + consent literal(true)) so the silent GHL backup is captured (Pitfall 1)"
    - "Existing full-form usages (service pages) are unaffected — defaultDienst stays uncontrolled, naam/email/bericht still render, naam still required in full mode"
  artifacts:
    - path: "components/OfferteForm.tsx"
      provides: "OfferteForm with variant + controlled-dienst props + compact layout"
      contains: "variant"
      min_lines: 200
    - path: "lib/whatsapp.ts"
      provides: "buildWhatsAppLeadUrl with sentinel-Naam omission + COMPACT_LEAD_NAME export"
      contains: "COMPACT_LEAD_NAME"
  key_links:
    - from: "components/OfferteForm.tsx"
      to: "lib/forms.ts"
      via: "submitLead (silent /api/lead backup)"
      pattern: "submitLead"
    - from: "components/OfferteForm.tsx"
      to: "lib/whatsapp.ts"
      via: "buildWhatsAppLeadUrl + COMPACT_LEAD_NAME"
      pattern: "COMPACT_LEAD_NAME"
---

<objective>
Extend the Phase-5 OfferteForm (do NOT fork a second form — D-03) with a variant "full" | "compact" prop and an OPTIONAL controlled dienst/onDienstChange pair, so the Wave-2 hero can render a low-friction Postcode+Telefoon+Dienst quick-start form whose dropdown can be pre-selected by a pillar click (D-09). Resolve the load-bearing Open-Q1 mechanic in-plan (no owner block): the compact variant supplies a hidden owner-recognizable sentinel naam plus a condensed explicit AVG consent so the shared server leadSchema still validates and the durable GHL backup is never silently lost.

Purpose: one form, two layouts, one secure route — the hero conversion unit without weakening leadSchema.
Output: components/OfferteForm.tsx extended (backward compatible); lib/whatsapp.ts gains a COMPACT_LEAD_NAME sentinel export and omits the Naam line for it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-homepage-conversion-uplift/06-RESEARCH.md
@.planning/phases/06-homepage-conversion-uplift/06-PATTERNS.md
@.planning/phases/06-homepage-conversion-uplift/06-VALIDATION.md
@components/OfferteForm.tsx
@lib/whatsapp.ts
@lib/lead-schema.ts
@lib/forms.ts
@./CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add COMPACT_LEAD_NAME sentinel and omit its Naam line in buildWhatsAppLeadUrl</name>
  <files>lib/whatsapp.ts</files>
  <behavior>
    - buildWhatsAppLeadUrl with a real naam (Jan Jansen) STILL includes a "Naam: Jan Jansen" line (full-form message unchanged).
    - buildWhatsAppLeadUrl with naam === COMPACT_LEAD_NAME OMITS the Naam line entirely (compact quick-start message stays clean).
    - COMPACT_LEAD_NAME is exported and its length is >= 2 (so it passes leadSchema naam.min(2)).
  </behavior>
  <read_first>
    - lib/whatsapp.ts lines 16-33 (the full builder; line 22 always-pushes Naam; lines 26-30 the conditional-push idiom to mirror)
    - lib/lead-schema.ts lines 8-19 (naam.min(2) + consent literal(true) — the server contract the sentinel must satisfy)
    - 06-RESEARCH.md Open Q1 + Pitfall 1 (the naam+consent mechanic and why a blank/sentinel Naam must be omitted)
  </read_first>
  <action>
    Export const COMPACT_LEAD_NAME = "Snelaanvraag" from lib/whatsapp.ts (length 12, >= 2 — owner-recognizable, signals a hero quick-aanvraag). Change the always-present Naam line into a conditional push mirroring the existing email/bericht idiom: push the Naam line only when fields.naam is present, trimmed non-empty, AND not equal to COMPACT_LEAD_NAME. Leave Telefoon/Postcode/Dienst and the email/bericht conditionals exactly as they are. This keeps the full-form WhatsApp message identical and keeps the compact quick-start message free of a "Naam: Snelaanvraag" line.
  </action>
  <acceptance_criteria>
    - rg -n "export const COMPACT_LEAD_NAME" lib/whatsapp.ts returns one match.
    - tsx behavioral assert (verify block) proves: full naam keeps its Naam line, sentinel omits it, COMPACT_LEAD_NAME.length is 12. Pure module — OneDrive-safe; no next build/tsc.
    - Ties to 06-VALIDATION.md "D-03 compact form to /api/lead" row (backup integrity).
  </acceptance_criteria>
  <verify>
    <automated>npx tsx -e "import('./lib/whatsapp.ts').then(m=>{const f=decodeURIComponent(m.buildWhatsAppLeadUrl({naam:'Jan Jansen',telefoon:'0612345678',postcode:'1234AB',dienst:'Airco'}));const c=decodeURIComponent(m.buildWhatsAppLeadUrl({naam:m.COMPACT_LEAD_NAME,telefoon:'0612345678',postcode:'1234AB',dienst:'Airco'}));if(!(f.includes('Naam: Jan Jansen')&&!c.includes('Naam:')&&m.COMPACT_LEAD_NAME.length>=2))throw 0;console.log('OK')})"</automated>
  </verify>
  <done>Sentinel exported; real names still print a Naam line; the sentinel omits it; tsx assert prints OK.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend OfferteForm with variant + controlled dienst + compact layout</name>
  <files>components/OfferteForm.tsx</files>
  <behavior>
    - variant=compact renders Postcode + Telefoon + Dienst select + a condensed required consent + submit; it does NOT render naam/email/bericht inputs, but DOES include a hidden naam input valued COMPACT_LEAD_NAME so handleSubmit FormData read yields naam.length >= 2.
    - When dienst + onDienstChange props are provided, the select is controlled (value={dienst}, onChange calls onDienstChange) so setDienst("Warmtepompen") after mount moves the dropdown.
    - When they are omitted (existing service-page usages), the select stays uncontrolled via defaultValue={defaultDienst ?? ""} and naam/email/bericht render as today (full mode, naam required).
    - A compact submit builds the WhatsApp URL and fires submitLead(data) with consent:true and naam=COMPACT_LEAD_NAME (schema passes).
  </behavior>
  <read_first>
    - components/OfferteForm.tsx (whole file) — props interface lines 14-21, handleSubmit lines 32-56 (FormData read of naam/consent — REUSE unchanged), select lines 139-155, consent checkbox lines 171-185, submit block 187-199
    - 06-RESEARCH.md Pattern 1 (compact variant prop shape) + Pattern 2 (controlled select rationale) + Pitfall 3 (uncontrolled defaultValue will not move on a post-mount click)
    - 06-PATTERNS.md "components/OfferteForm.tsx — EXTEND" (backward-compatible prop table + controlled-vs-uncontrolled branch)
    - lib/whatsapp.ts COMPACT_LEAD_NAME (Task 1) — import the same literal for the hidden naam value
    - 06-RESEARCH.md Security Domain V5 (consent MUST be sent; no client secret)
  </read_first>
  <action>
    Widen OfferteFormProps to add variant "full" | "compact" (default "full"), dienst string optional, onDienstChange (v:string)=>void optional; keep defaultDienst. Import COMPACT_LEAD_NAME from @/lib/whatsapp. Make the dienst select controlled ONLY when dienst !== undefined (value={dienst} + onChange); otherwise keep the existing defaultValue={defaultDienst ?? ""} uncontrolled path untouched. Gate the naam/email/bericht field blocks behind variant === "full". In compact mode render (a) the Postcode input, (b) the Telefoon input, (c) the Dienst select, (d) a hidden input type=hidden name=naam value={COMPACT_LEAD_NAME}, (e) a condensed but explicit required consent checkbox linking to /privacy-beleid (reuse the existing consent markup, tightened), and (f) the existing submit + reassurance line "Vrijblijvend · geen kosten · AVG-proof" (D-03). Do NOT modify handleSubmit (it reads naam/consent from FormData — the hidden input + checkbox satisfy it) and do NOT weaken lib/lead-schema.ts. The honeypot hidden input (name="website") that the /api/lead route expects for spam control MUST render in BOTH modes — do NOT gate it behind variant === "full" (if it lives inside the full-only block, compact submissions would drop the field the route checks). Use only real tokens/utilities already in the file (inputClass, signature-gradient, Icon, text-on-surface-variant) — no phantom sketch vars, no raw material-symbols-outlined spans.
  </action>
  <acceptance_criteria>
    - rg -n "variant" components/OfferteForm.tsx shows the new prop AND the variant==="full" gate; rg -n "onDienstChange" shows the controlled onChange wiring.
    - rg -n "COMPACT_LEAD_NAME" components/OfferteForm.tsx shows the hidden-naam value is the shared sentinel (not a hardcoded string).
    - rg -n "submitLead" and rg -n name=consent both still present (compact submit uses the secure-route backup; consent not dropped).
    - rg -n "website" components/OfferteForm.tsx confirms the honeypot hidden input renders in the compact code path (NOT gated behind variant==="full").
    - Guardrails: grep -v '^\s*//' over the file finds no material-symbols-outlined and no phantom var(--gradient-ink|--glass-strong|--shadow-|--ease-clarity|--color-bg-tint|--color-text-faint).
    - Preview (Wave-2/3 gate): submit the hero compact form -> Network shows POST /api/lead -> 200 and WhatsApp opens (06-VALIDATION.md "D-03 compact form E2E").
  </acceptance_criteria>
  <verify>
    <automated>rg -n "COMPACT_LEAD_NAME" components/OfferteForm.tsx && rg -n "onDienstChange" components/OfferteForm.tsx && rg -n "type=.hidden." components/OfferteForm.tsx && rg -n "consent" components/OfferteForm.tsx && rg -n "website" components/OfferteForm.tsx</automated>
  </verify>
  <done>OfferteForm accepts variant + controlled dienst; compact mode shows 3 fields + consent + hidden sentinel naam; full mode unchanged; guardrail greps empty; preview submit returns POST /api/lead 200.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser form -> /api/lead | Untrusted lead fields (postcode/telefoon/dienst/consent/honeypot) cross into the server route. Owned by Phase-5; this plan MUST keep posting through it. |
| client -> WhatsApp deep link | Lead fields are encoded into a wa.me URL via buildWhatsAppLeadUrl (encodeURIComponent). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-V5 | Tampering | compact form submit | mitigate | Compact submit posts to the SAME /api/lead route with the server-authoritative Zod leadSchema + honeypot (Phase-5); no client-only validation. leadSchema is NOT weakened — the sentinel naam + explicit consent satisfy naam.min(2) + consent literal(true). |
| T-06-02-AVG | Info disclosure / compliance | consent capture | mitigate | Compact variant keeps a required explicit consent checkbox linking /privacy-beleid and sends consent:true — consent is NOT dropped for friction. |
| T-06-02-I | Info disclosure | client bundle | mitigate | The form imports nothing secret; the GHL webhook stays server-only in /api/lead (no NEXT_PUBLIC_). submitLead (lib/forms.ts) holds no secret. |
| T-06-02-XSS | Tampering | WhatsApp URL build | accept | buildWhatsAppLeadUrl uses encodeURIComponent on all fields; no dangerouslySetInnerHTML. |
| T-06-02-SC | Tampering (supply chain) | package installs | accept | Zero installs this phase (RESEARCH Package Legitimacy Audit) — no slopcheck gate applicable. |
</threat_model>

<verification>
- Both tsx asserts pass locally (pure modules; the only OneDrive-safe test form — no next build/tsc).
- Backward compatibility: existing service-page OfferteForm usages (full variant, uncontrolled defaultDienst) render and submit exactly as before — spot-check on the Wave-3 preview.
- Wave-1 gate: branch push -> Vercel preview build green (prebuild gates + full-route typecheck compiles the widened props).
- Security preserved (not newly built): compact submit is server-validated via the inherited /api/lead; consent carried; no secret client-side.
</verification>

<success_criteria>
- OfferteForm exposes variant + optional controlled dienst/onDienstChange; compact mode renders 3 visible fields + condensed consent + hidden sentinel naam.
- lib/whatsapp.ts exports COMPACT_LEAD_NAME and omits its Naam line; real names still print theirs.
- leadSchema untouched; full-form behavior unchanged; guardrail greps empty; both tsx asserts print OK.
</success_criteria>

<output>
Create `.planning/phases/06-homepage-conversion-uplift/06-02-SUMMARY.md` when done.
</output>
