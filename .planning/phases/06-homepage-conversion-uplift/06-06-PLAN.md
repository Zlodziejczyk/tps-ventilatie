---
phase: 06-homepage-conversion-uplift
plan: 06
type: execute
wave: 3
depends_on: ["06-04", "06-05"]
files_modified:
  - app/page-sections/home/ClosingCTA.tsx
  - app/page.tsx
autonomous: true
requirements: [D-01, D-02, D-16, D-17, D-18]
must_haves:
  truths:
    - "An engineered dark closing CTA band sits before the footer with Bel · WhatsApp · Offerte; the WhatsApp CTA is AA-safe (>=4.5:1 text / >=3:1 glyph) and uses the Icon wrapper (no raw material-symbols-outlined spans) — the two CTABanner bugs are fixed on restyle (D-17/A11Y-01)"
    - "app/page.tsx is a Server Component that keeps its metadata export and composes <main> = HomeHero -> ProofBand -> ImageBand -> ClosingCTA, retiring the 5 old sections + CTABanner import (D-01/D-02)"
    - "No pricing-preview section renders on the home page; pricing shows only as USP pills (D-02)"
    - "No second always-on contact element is added — the layout-level StickyContactBar is inherited untouched (D-16)"
  artifacts:
    - path: "app/page-sections/home/ClosingCTA.tsx"
      provides: "restyled dark closing CTA band, AA-safe WhatsApp, Icon wrapper, SITE-sourced links"
      min_lines: 30
    - path: "app/page.tsx"
      provides: "server homepage: metadata + <main> composing the 4 new sections"
      contains: "buildMetadata"
  key_links:
    - from: "app/page.tsx"
      to: "app/page-sections/home/*"
      via: "compose HomeHero/ProofBand/ImageBand/ClosingCTA"
      pattern: "HomeHero"
    - from: "app/page-sections/home/ClosingCTA.tsx"
      to: "lib/constants.ts SITE"
      via: "tel/whatsapp/offerte links"
      pattern: "SITE.whatsappUrl"
---

<objective>
Restyle the current CTABanner into the D-17 engineered dark closing band as a new ClosingCTA section — fixing its two shipped bugs (raw material-symbols-outlined spans -> Icon wrapper; AA-failing #25D366+white WhatsApp -> the proven StickyContactBar treatment / or dark-on-green 8.6:1). Then rewrite app/page.tsx to compose the four new sections, keep the metadata export, retire the five old sections + the CTABanner import (D-01/D-02), and confirm nothing adds a second always-on contact element (the StickyContactBar stays inherited from layout — D-16).

Output: app/page-sections/home/ClosingCTA.tsx + rewritten app/page.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-homepage-conversion-uplift/06-RESEARCH.md
@.planning/phases/06-homepage-conversion-uplift/06-PATTERNS.md
@.planning/phases/06-homepage-conversion-uplift/06-VALIDATION.md
@components/CTABanner.tsx
@components/StickyContactBar.tsx
@app/page.tsx
@app/layout.tsx
@lib/constants.ts
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ClosingCTA — restyle CTABanner into the dark band, fix Icon + AA WhatsApp bugs</name>
  <files>app/page-sections/home/ClosingCTA.tsx</files>
  <read_first>
    - components/CTABanner.tsx (whole file) — line 13 bg-on-primary-fixed dark band base to KEEP (this is the "gradient-ink" translation); lines 27/36/44 raw material-symbols-outlined spans to REPLACE with Icon; lines 30-38 the bg-[#25D366] text-white WhatsApp = 1.99:1 AA FAIL to FIX
    - components/StickyContactBar.tsx 83-99 (the proven AA-safe WhatsApp CTA: neutral bg-surface-container-high + Icon text-primary glyph; and the signature-gradient Offerte pill) — copy this treatment
    - 06-RESEARCH.md AA-safe WhatsApp example (#25D366 + text-on-surface = 8.6:1 alternative) + token table (--gradient-ink -> bg-on-primary-fixed) ; 06-PATTERNS.md "ClosingCTA.tsx" FIX 1 + FIX 2
    - lib/constants.ts SITE (phone, phoneDisplay, whatsappUrl) — all contact data via SITE, never hardcode
  </read_first>
  <action>
    Create app/page-sections/home/ClosingCTA.tsx as a SERVER component (CTABanner is server today — keep it). Base = the dark band bg-on-primary-fixed (D-17). Provide three actions all sourced from SITE: Bel (href={`tel:${SITE.phone}`}, Icon name="call") ; WhatsApp (href={SITE.whatsappUrl}, target=_blank rel=noopener) rendered AA-safe — EITHER neutral bg-surface-container-high with an Icon name="chat" filled text-primary glyph (the StickyContactBar treatment), OR bg-[#25D366] with text-on-surface (dark) = 8.6:1 — do NOT use #25D366 + text-white (1.99:1 FAIL) ; Offerte (a signature-gradient pill linking /contact, Icon name="request_quote"). ALL icons via the Icon wrapper — zero raw material-symbols-outlined spans. Dutch heading/subcopy (Claude drafts). Optionally wrap in AnimateOnScroll or a CSS .fu entrance (discretion, D-19). No #000; tonal/dark band not 1px borders.
  </action>
  <acceptance_criteria>
    - No-raw-icon gate: search ClosingCTA for material-symbols-outlined -> ZERO hits (FIX 1).
    - AA gate: the WhatsApp anchor must NOT combine bg-[#25D366] with text-white; rg -n "bg-\[#25D366\]" together with rg -n "text-white" on the SAME element must not co-occur (FIX 2). Prefer bg-surface-container-high + Icon text-primary, or bg-[#25D366] + text-on-surface.
    - rg -n "SITE.whatsappUrl|SITE.phone" present (contact via SITE); rg -n "Icon" present.
    - Ties to 06-VALIDATION.md D-17/A11Y-01.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "SITE.whatsappUrl" app/page-sections/home/ClosingCTA.tsx</automated>
  </verify>
  <done>Dark closing band with Bel/WhatsApp/Offerte from SITE; zero raw icon spans; AA-safe WhatsApp; ready to compose.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite app/page.tsx to compose the four new sections (server, keep metadata)</name>
  <files>app/page.tsx</files>
  <read_first>
    - app/page.tsx (current) — KEEP lines 7-12 metadata (buildMetadata(findBySlug("/")!)) ; REMOVE the 5 retired section imports (HeroSection/ServicesSection/PricingSection/WhyTPSSection/ReviewsSection) + the CTABanner import (D-01/D-02)
    - 06-PATTERNS.md "app/page.tsx — REWRITE" (keep metadata; compose <main> with the 4 new sections; page stays a Server Component — no "use client")
    - app/layout.tsx 7,74 (StickyContactBar mounted at layout level — inherited; render NOTHING new/always-on — D-16)
    - 06-RESEARCH.md Open Q3 (retired section files: remove imports now; deleting the files is optional/deferred — leave them in place, lint-safe)
  </read_first>
  <action>
    Rewrite app/page.tsx: keep the `export const metadata = buildMetadata(findBySlug("/")!)` line and its two imports. Replace the body with a Server Component returning <main> composing (in order) <HomeHero /> -> <ProofBand /> -> <ImageBand /> -> <ClosingCTA /> from app/page-sections/home/. Remove the five retired section imports + the CTABanner import (their value is absorbed: services->pillar grid, USPs->USP pills, reviews->proof cards; no pricing-preview section — D-01/D-02). Keep the page a Server Component (HomeHero is the only client island). Do NOT add any sticky bar / FAB — StickyContactBar is inherited from layout (D-16). Leave the retired section files on disk (Phase-7 cleanup); just drop their imports here.
  </action>
  <acceptance_criteria>
    - rg -n "buildMetadata" app/page.tsx still present (metadata preserved).
    - Retired-imports gone: search app/page.tsx for HeroSection|ServicesSection|PricingSection|WhyTPSSection|ReviewsSection|CTABanner -> ZERO hits (D-01/D-02).
    - rg -n "HomeHero|ProofBand|ImageBand|ClosingCTA" shows the 4 new sections composed; rg -n "\"use client\"" app/page.tsx -> ZERO (stays server); rg -n "<main" present (landmark kept).
    - No second contact element: app/page.tsx does not import/mount StickyContactBar or any FAB (D-16).
    - Ties to 06-VALIDATION.md D-01.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "buildMetadata" app/page.tsx</automated>
  </verify>
  <done>Home is a Server Component composing the 4 new sections in <main>; metadata preserved; 5 old sections + CTABanner imports removed; no second sticky bar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> tel:/wa.me | Closing CTA links built from SITE constants (no user input concatenated). |
| static compose -> DOM | page.tsx renders server sections; only HomeHero is a client island. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06-I | Info disclosure | closing CTA links | mitigate | tel:/wa.me/offerte hrefs come from SITE constants; no secret, no NEXT_PUBLIC_ webhook client-side. |
| T-06-06-XSS | Tampering | composed sections | accept | All sections render static/server content as text; no dangerouslySetInnerHTML. |
| T-06-06-A11Y | (quality gate) | WhatsApp contrast | mitigate | AA-safe WhatsApp treatment (>=4.5:1 text / >=3:1 glyph) replaces the 1.99:1 fail; verified on preview (A11Y-01). |
| T-06-06-SC | Tampering (supply chain) | package installs | accept | Zero installs this phase. |
</threat_model>

<verification>
- Local: rg asserts confirm metadata kept, retired imports gone, 4 new sections composed, no "use client" in page.tsx, AA WhatsApp treatment, no raw icon spans, no second sticky bar.
- Phase gate (preview — the OneDrive-safe canonical build): push branch -> Vercel preview build green (prebuild gates + full-route typecheck + 27 routes); then the behavioral gate: hero compact submit POST /api/lead 200 + WhatsApp; pillar Offerte pre-select+scroll; aurora desktop-only; 3 static cards; PageSpeed mobile INP < 200ms + LCP = hero H1; axe: AA contrast clean (WhatsApp >=4.5:1) + heading-order clean (exactly one h1).
- Wave-0/pipeline note: no test framework is added; validation = grep/tsx source asserts + the Vercel preview (no local next build/tsc — OneDrive deadlock).
- Owner sign-off (end-of-phase human gate, non-blocking to tasks): whole-home preview review of copy (H1/coverage), WTW/MV brands, and the closing band.
</verification>

<success_criteria>
- ClosingCTA: dark band, Bel/WhatsApp/Offerte from SITE, Icon wrapper, AA-safe WhatsApp; both CTABanner bugs fixed.
- app/page.tsx: Server Component, metadata preserved, composes the 4 new sections in <main>, retired imports removed, no pricing-preview section, no second sticky bar.
- Preview build green + behavioral/CWV/contrast/heading gates pass.
</success_criteria>

<output>
Create `.planning/phases/06-homepage-conversion-uplift/06-06-SUMMARY.md` when done.
</output>
