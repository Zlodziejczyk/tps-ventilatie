---
phase: 06-homepage-conversion-uplift
plan: 05
type: execute
wave: 2
depends_on: ["06-01", "06-03"]
files_modified:
  - app/page-sections/home/ProofBand.tsx
  - app/page-sections/home/ImageBand.tsx
autonomous: true
requirements: [D-05, D-14, D-15, D-18]
must_haves:
  truths:
    - "A dedicated proof section shows the big 4,9 score + CSS Google-G mark + '34 reviews', three static review cards (stars, quote, name, timeAgo), a keurmerken strip (BRL 100/200 + KvK), and USP pills — all as a server section (D-14)"
    - "Reviews render as a static 3-up grid, NOT the ReviewCarousel — no carousel JS, all three visible at once (D-15)"
    - "The staged home-hero.jpg renders as a full-width next/image band lower on the page, lazy and off the LCP path — no priority (D-05)"
    - "Headings nest without skips (h2/h3 only — the single h1 lives in the hero); numbers guard REVIEW_RATING null (D-18)"
  artifacts:
    - path: "app/page-sections/home/ProofBand.tsx"
      provides: "server proof band: score+CSS-G, 3 static cards, keurmerken, USP pills"
      min_lines: 70
    - path: "app/page-sections/home/ImageBand.tsx"
      provides: "server next/image full-width band, lazy, off LCP"
      min_lines: 12
  key_links:
    - from: "app/page-sections/home/ProofBand.tsx"
      to: "lib/reviews.ts + lib/initials.ts"
      via: "REVIEWS + REVIEW_RATING + getInitials"
      pattern: "getInitials"
    - from: "app/page-sections/home/ProofBand.tsx"
      to: "app/globals.css .gmark"
      via: "className gmark"
      pattern: "gmark"
    - from: "app/page-sections/home/ImageBand.tsx"
      to: "public/images/heroes/home-hero.jpg"
      via: "next/image loading=lazy, no priority"
      pattern: "home-hero"
---

<objective>
Build the Sketch-003-D proof/trust band as a pure server section (zero client JS) and wire the staged home-hero.jpg as a lazy full-width band lower on the page (off the LCP path — protects SEO-10). Reviews are STATIC 3-up cards (not the carousel, D-15); the Google score uses the CSS-G from plan 06-03; keurmerken are BRL 100/200 + KvK text chips (F-gassen/STEK dropped per owner); USP pills read the site's proven pattern.

Output: app/page-sections/home/ProofBand.tsx + app/page-sections/home/ImageBand.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-homepage-conversion-uplift/06-RESEARCH.md
@.planning/phases/06-homepage-conversion-uplift/06-PATTERNS.md
@.planning/phases/06-homepage-conversion-uplift/06-VALIDATION.md
@.claude/skills/sketch-findings-tps-ventilatie/references/trust-and-contact.md
@components/ReviewCarousel.tsx
@app/page-sections/WhyTPSSection.tsx
@lib/reviews.ts
@lib/constants.ts
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ProofBand — score+CSS-G, static 3-up review cards, keurmerken, USP pills (server)</name>
  <files>app/page-sections/home/ProofBand.tsx</files>
  <read_first>
    - components/ReviewCarousel.tsx 26-53 (ReviewCard + Stars visual to copy as a plain server subcomponent — STRIP the "use client" carousel machinery) ; getInitials now importable from lib/initials.ts (06-01)
    - app/page-sections/WhyTPSSection.tsx 7-28 (USP array pattern reading SITE.serviceRadiusKm — copy the icon-in-tile + label shape)
    - lib/reviews.ts 13,135-136 (REVIEWS 17 real; REVIEW_RATING {4.9,34}, nullable — guard) ; lib/constants.ts SITE.kvk (73722650)
    - trust-and-contact.md (proof section structure) ; app/globals.css .gmark (06-03) ; 06-RESEARCH.md Pattern 5 (static 3-up) + Dutch score formatting
  </read_first>
  <action>
    Create app/page-sections/home/ProofBand.tsx as a SERVER section (no "use client", no ReviewCarousel import — D-15). Render: a heading block with the big score (REVIEW_RATING guarded null -> value.toLocaleString("nl-NL",{minimumFractionDigits:1}) = "4,9"; if null omit the numeric score and show only a neutral "{REVIEWS.length}+ reviews op Google" line), the CSS Google-G via the .gmark class (06-03, no image), and the count "{REVIEW_RATING?.count ?? REVIEWS.length} reviews". A static 3-up grid (grid, 3 cols desktop -> 1 col mobile) of exactly three chosen short brand-clean REVIEWS (e.g. Christine te Kamp, Lois Lovelle, Albert Terstegs) using a local ReviewCard/Stars copied from ReviewCarousel (stars via Icon name="star" filled; quote line-clamp-5; initials monogram via getInitials). A keurmerken strip of text/CSS chips: "BRL 100", "BRL 200", and "KvK {SITE.kvk}" (F-gassen/STEK dropped). USP pills: "Geen voorrijkosten", "Reactie binnen 1 werkdag", "Gecertificeerd & verzekerd" (icon-in-tile, whitespace-nowrap, clean at 375px — UI-07). Headings h2/h3 only (single h1 is the hero — A11Y-02). Icon wrapper; tonal layering not 1px borders; text-on-surface not #000; static text only (React auto-escapes — no dangerouslySetInnerHTML).
  </action>
  <acceptance_criteria>
    - rg -n "getInitials" + "gmark" + "REVIEW_RATING" present; rg -n "BRL 100|BRL 200" + "kvk|KvK" present (keurmerken).
    - Not-a-carousel gate: search ProofBand for ReviewCarousel -> ZERO hits (D-15); rg -c "<h1" -> 0 (single h1 is the hero).
    - Guardrails: no material-symbols-outlined, no phantom vars, no #000, no dangerouslySetInnerHTML, no hardcoded phone.
    - Ties to 06-VALIDATION.md D-14 + D-15.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "gmark" app/page-sections/home/ProofBand.tsx</automated>
  </verify>
  <done>Proof band renders score + CSS-G + 3 static cards + BRL 100/200 + KvK + USP pills; no carousel; REVIEW_RATING guarded; no h1; guardrail greps empty.</done>
</task>

<task type="auto">
  <name>Task 2: ImageBand — lazy full-width home-hero.jpg, off the LCP path (server)</name>
  <files>app/page-sections/home/ImageBand.tsx</files>
  <read_first>
    - app/page-sections/WhyTPSSection.tsx 2, 66-71 (the repo's next/image usage) — but use intrinsic width/height, not fill
    - 06-RESEARCH.md Pattern 4 + Pitfall 4 (loading=lazy, NO priority, no CLS via width/height; keep it off the LCP path)
    - public/images/heroes/home-hero.jpg (staged, 203KB) ; next.config.ts formats avif/webp (hybrid — no unoptimized)
  </read_first>
  <action>
    Create app/page-sections/home/ImageBand.tsx as a SERVER component using next/image: src="/images/heroes/home-hero.jpg", a meaningful Dutch alt (e.g. "TPS-monteur installeert een klimaatsysteem"), width={1600} height={900} sizes="100vw", className="w-full h-auto object-cover", loading="lazy", and NO priority (priority would preload it onto the LCP path — Pitfall 4). Wrap it as a full-width band (rounded, tonal surface container) placed lower on the page near the trust band (D-05). No text/H1.
  </action>
  <acceptance_criteria>
    - rg -n "home-hero" ImageBand.tsx shows the staged image; rg -n "loading=\"lazy\"" present.
    - Off-LCP gate: search ImageBand for priority -> ZERO hits (Pitfall 4).
    - Preview: PageSpeed mobile confirms LCP element = hero H1 text, not this image; good LCP (06-VALIDATION.md D-05, manual/preview).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "home-hero" app/page-sections/home/ImageBand.tsx</automated>
  </verify>
  <done>ImageBand renders home-hero.jpg via next/image, lazy, no priority, intrinsic dimensions; sits lower on the page; LCP stays the hero H1 on preview.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static data -> DOM | Review quotes/names, keurmerken, USP labels rendered as text. No user input on this section. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05-XSS | Tampering | review quotes / copy | mitigate | Static data from lib/reviews.ts rendered as text (React auto-escapes); NO dangerouslySetInnerHTML. |
| T-06-05-R | Repudiation | score/count display | mitigate | REVIEW_RATING is real owner-finalized data (4,9/34); guarded for null so no fabricated rating renders. |
| T-06-05-D | DoS (perf) | image band | mitigate | next/image lazy, no priority, intrinsic dims -> off LCP path, no CLS (SEO-10). Static 3-up drops carousel JS. |
| T-06-05-SC | Tampering (supply chain) | package installs | accept | Zero installs this phase. |
</threat_model>

<verification>
- Local: rg asserts confirm CSS-G, keurmerken, no-carousel, single-h1-absent, lazy image, no priority.
- Preview (wave gate): three cards visible at once (no controls); Google-G renders; PageSpeed LCP = hero H1, good LCP; axe heading-order clean.
- Owner numbers finalized (STATE 2026-06-28): 4,9 / 34, BRL 100/200 + KvK — re-confirm exact live count at sign-off (ticks up).
</verification>

<success_criteria>
- ProofBand (server): score + CSS-G + 3 static cards + BRL 100/200 + KvK + USP pills; no ReviewCarousel; REVIEW_RATING guarded; no h1.
- ImageBand (server): home-hero.jpg via next/image, lazy, no priority; off the LCP path.
- Guardrail + phantom-token greps empty.
</success_criteria>

<output>
Create `.planning/phases/06-homepage-conversion-uplift/06-05-SUMMARY.md` when done.
</output>
