---
phase: 06-homepage-conversion-uplift
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
autonomous: true
requirements: [D-04, D-14, D-19]
must_haves:
  truths:
    - "A pure-CSS brand-teal aurora (drifting blurred blobs) animates only on desktop with no reduced-motion, and paints a static soft base everywhere else — zero client JS, off the LCP path (D-04/D-19)"
    - "A pure-CSS Google-'G' mark renders from a conic-gradient + mask with no image asset (D-14)"
    - "A cheap CSS fade-up entrance class exists as a lighter alternative to framer-motion for the rebuilt sections (D-19)"
    - "No phantom sketch tokens are introduced — only real MD3 tokens/brand tints"
  artifacts:
    - path: "app/globals.css"
      provides: "aurora keyframes+classes, .gmark Google-G, .fu fade-up — bare global CSS + keyframes"
      contains: "gmark"
  key_links:
    - from: "app/page-sections/home/HomeHero.tsx (Wave 2)"
      to: "app/globals.css .aurora"
      via: "className aurora/blob"
      pattern: "aurora"
    - from: "app/page-sections/home/ProofBand.tsx (Wave 2)"
      to: "app/globals.css .gmark"
      via: "className gmark"
      pattern: "gmark"
---

<objective>
Add the three pure-CSS primitives the Wave-2 sections consume, using the SAME CSS-first pattern the file already uses for `.air-pulse`/`.animate-fan-spin` (bare global classes + `@keyframes`, NOT `@theme` tokens and NOT `@utility`): (1) a brand-teal aurora (drifting blurred radial blobs) that animates only inside a desktop + no-reduced-motion media query and otherwise paints a static soft base; (2) the CSS Google-"G" `.gmark` (conic-gradient + mask, no image); (3) an optional cheap `.fu` fade-up entrance. All colors come from real MD3 tokens or the approved brand tints — zero phantom sketch vars.

Purpose: land the CSS foundation FIRST so HomeHero (aurora) and ProofBand (Google-G) reference classes that already exist.
Output: `app/globals.css` extended with `.aurora`/`.blob*` + drift keyframes, `.gmark`, `.fu` + fade keyframe, all reduced-motion-aware.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-homepage-conversion-uplift/06-RESEARCH.md
@.planning/phases/06-homepage-conversion-uplift/06-PATTERNS.md
@.planning/phases/06-homepage-conversion-uplift/06-VALIDATION.md
@.claude/skills/sketch-findings-tps-ventilatie/references/design-language.md
@.claude/skills/sketch-findings-tps-ventilatie/references/trust-and-contact.md
@app/globals.css
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure-CSS aurora (gated) + .fu fade-up to globals.css</name>
  <files>app/globals.css</files>
  <read_first>
    - app/globals.css lines 111-134 (existing bare @keyframes + bare .air-pulse/.animate-fan-spin classes — the EXACT pattern to mirror; do NOT add these as @theme or @utility) and lines 136-149 (the existing prefers-reduced-motion guard block — extend/mirror it)
    - 06-RESEARCH.md Pattern 3 (aurora keyframes + gating; brand tints #a8dff0 / #b8e8d0 / #baeaff; pointer-events none; blur 60px; animate only inside @media (min-width:768px) and (prefers-reduced-motion:no-preference))
    - design-language.md "Pure-CSS aurora" + "Fade-up entrance" CSS blocks (the sketch source — port through the token table, drop any --ease-* / --color-* sketch vars)
    - 06-PATTERNS.md sketch-token translation table (--ease-clarity -> cubic-bezier(0.25,0.1,0.25,1); never emit phantom vars)
  </read_first>
  <action>
    After the existing keyframes/classes block, add bare global CSS: an `.aurora` wrapper (position:absolute; inset:0; overflow:hidden; pointer-events:none) and `.aurora .blob` children (position:absolute; border-radius:9999px; filter:blur(60px); opacity ~.45; mix-blend-mode:multiply) with three blob variants painting radial-gradients in the brand tints #a8dff0 (b1), #b8e8d0 (b2), #baeaff (b3) — these are the calmer brand-teal tints (UI-06), NOT the loud magenta/green. Define `drift1/drift2/drift3` @keyframes (small translate3d + scale). Apply the drift animations ONLY inside `@media (min-width:768px) and (prefers-reduced-motion:no-preference)` so mobile/SSR/reduced-motion get the static gradient base with zero animation (D-04/D-19 — lowest INP, zero client JS). Also add a cheap `.fu` fade-up class (opacity:0; translateY(16px); animation forwards) + its `fu` keyframe using the cubic-bezier(0.25,0.1,0.25,1) ease, and null it out under prefers-reduced-motion (mirror lines 136-149). Use literal hex tints + the real cubic-bezier — no var(--gradient-*), no var(--ease-*), no var(--shadow-*), no var(--color-surface-2).
  </action>
  <acceptance_criteria>
    - rg -n "\.aurora|\.blob|@keyframes drift" app/globals.css shows the aurora wrapper, blob, and drift keyframes.
    - rg -n "min-width:768px" app/globals.css shows the desktop+motion gate wrapping the drift animations.
    - rg -n "\.fu" app/globals.css shows the fade-up class.
    - Phantom-token gate: grep -v '^\s*/\*' app/globals.css then search for var(--gradient-ink|--glass-strong|--shadow-|--ease-clarity|--color-bg-tint|--color-surface-2|--color-text-faint) finds ZERO hits.
    - Ties to 06-VALIDATION.md "D-04/D-19 CSS aurora, no WebGL" + "Pitfall 2 no token regressions".
  </acceptance_criteria>
  <verify>
    <automated>rg -n "@keyframes drift|min-width:768px" app/globals.css</automated>
  </verify>
  <done>Aurora blobs + drift keyframes + .fu exist; drift animations gated to desktop+no-reduced-motion; reduced-motion nulls them; zero phantom vars.</done>
</task>

<task type="auto">
  <name>Task 2: Add the CSS Google-'G' .gmark utility (no image)</name>
  <files>app/globals.css</files>
  <read_first>
    - trust-and-contact.md "CSS Google 'G' mark (no image)" block (conic-gradient from -45deg with the four Google hexes + radial mask cutting the center)
    - 06-RESEARCH.md "CSS Google-'G' mark, no image (D-14)" code example
    - app/globals.css lines 126-134 (bare-class placement precedent)
  </read_first>
  <action>
    Add a bare `.gmark` global class rendering the Google "G" purely in CSS: a small round element with background:conic-gradient(from -45deg,#ea4335 0 25%,#fbbc05 0 50%,#34a853 0 75%,#4285f4 0) and a -webkit-mask + mask radial-gradient cutting a transparent center hole (per the sketch). Fixed pixel size (e.g. 16px) so the consuming ProofBand can scale via a wrapper. No image, no SVG, no network asset (no 404 risk). This is the only Google-brand mark on the page.
  </action>
  <acceptance_criteria>
    - rg -n "\.gmark" app/globals.css returns the class; rg -n "conic-gradient" app/globals.css shows the four Google hexes.
    - rg -n "mask:" app/globals.css shows the center-cut mask (both -webkit-mask and mask present).
    - Ties to 06-VALIDATION.md "D-14" (proof band CSS-G).
  </acceptance_criteria>
  <verify>
    <automated>rg -n "\.gmark|conic-gradient" app/globals.css</automated>
  </verify>
  <done>`.gmark` renders the Google G from conic-gradient + mask, no asset; ProofBand can reference it in Wave 2.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | CSS-only additions. No input, no script, no network asset — the aurora and Google-G are decorative, statically authored styles. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-D | Denial of Service (perf) | aurora animation | mitigate | Drift animations run ONLY on desktop + no-reduced-motion; mobile/reduced-motion paint a static base (protects mobile INP / SEO-10). No WebGL/canvas. |
| T-06-03-I | Info disclosure | globals.css | accept | No secrets, no data — decorative CSS only. |
| T-06-03-SC | Tampering (supply chain) | package installs | accept | Zero installs this phase — no gate applicable. |
</threat_model>

<verification>
- Local: rg asserts confirm the classes, keyframes, the desktop+motion gate, and ZERO phantom vars.
- Preview (Wave-2/3 gate): aurora animates on desktop, is static on mobile/reduced-motion; the Google-G renders as a crisp G; PageSpeed mobile shows the hero H1 (not the aurora) as LCP, INP < 200ms.
- CSS added as bare classes + keyframes (the `.air-pulse` precedent) — Tailwind v4 compiles unchanged; no `@theme`/`@utility` churn.
</verification>

<success_criteria>
- `.aurora`/`.blob*` + drift keyframes exist and are gated to desktop+no-reduced-motion; `.fu` exists and respects reduced-motion.
- `.gmark` renders the Google G from conic-gradient + mask, no image.
- Zero phantom sketch vars in the file (Pitfall 2 gate green).
</success_criteria>

<output>
Create `.planning/phases/06-homepage-conversion-uplift/06-03-SUMMARY.md` when done.
</output>
