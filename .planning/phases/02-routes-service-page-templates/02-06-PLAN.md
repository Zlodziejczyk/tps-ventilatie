---
phase: 2
plan: 02-06
title: Content salvage port + phase verification
wave: 5
depends_on: [02-03, 02-04, 02-05]
autonomous: true
requirements: [IA-02, IA-03, IA-04, IA-05, IA-06, IA-07]
files_modified:
  - lib/services/airconditioning.ts
  - lib/services/wtw.ts
  - lib/services/mechanische-ventilatie.ts
  - scripts/assert-registry.ts
---

# 02-06 — Content salvage port + phase verification

## Objective

Seed the taxonomy draft shells with the **existing** `/diensten` copy (D-04 salvage —
the owner values this work), applying the D-05 watch-items, so the new pillar/sub-service
pages render with real content instead of empty shells. Then run the **whole-phase
verification gate**: extended registry asserts + a green production build with every route
pre-rendered + anti-drift greps. This plan closes the phase.

**Scope guard:** this is *mechanical salvage into `status: "draft"` shells*, NOT Phase-4
authoring. No new long-form copy, no FAQ writing, no owner-review claims. `status` stays
`"draft"` on every node (the ≥120-word / 3-6-FAQ uniqueness gate is status-gated and must
NOT fire). Warmtepompen stays empty (graceful omit, D-06). Phase 4 owns expansion + the
editorial sign-off.

## must_haves

- WTW `vervangen` carries the salvaged 10-step replacement sequence as `content.steps`
  (`{title, body}`, both non-empty — structure schema requires it); WTW `onderhoud-reinigen`
  / pillar intro carry the salvaged "when to replace" + USP prose in `content.intro`.
- MV `onderhoud-reinigen` carries the salvaged benefits + cleaning-method prose; the
  **dakventilator** content is folded into MV `onderhoud-reinigen` / `vervangen` intro
  (NOT a new route — D-05).
- Airco intros carry the salvaged service descriptions with **"Panasonic" dropped** (brands =
  Daikin / Mitsubishi Electric / Mitsubishi Heavy only — D-05).
- Every modified node keeps `status: "draft"`; `npm run build` prebuild taxonomy gate passes
  (no uniqueness/structure failure).
- `npx tsx scripts/assert-registry.ts` exits 0 with the added content-port spot assertions.
- `npm run build` exits 0 and all 22 service routes (1 hub + 4 pillar + 17 service) are
  pre-rendered in the export.
- Anti-drift greps clean (no Panasonic, no DienstenNav, no DIENSTEN_DROPDOWN, radius/NAP intact).

## Tasks

<task id="02-06-1" type="execute">
  <title>Port salvaged WTW + MV + Airco copy into draft shells (D-04/D-05)</title>
  <read_first>
    - app/diensten/page.tsx — NOTE: by now this is the lean hub (02-04). Recover the original section copy from git: `git show HEAD~:app/diensten/page.tsx` (WTW_REPLACEMENT_STEPS, the "Wanneer vervangen" block, MV_BENEFITS, MV_CLEANING_REASONS, the dakventilator block, the AIRCO_CARDS, and the "Daikin • Mitsubishi • Panasonic" line).
    - lib/services/wtw.ts, lib/services/mechanische-ventilatie.ts, lib/services/airconditioning.ts (draftShell content fields to populate; keep status "draft")
    - lib/services/types.ts (ContentShell: intro is free text; steps = {title,body} both required non-empty; localAngle free text)
    - .planning/phases/02-routes-service-page-templates/02-CONTEXT.md (D-04/D-05 watch-items)
  </read_first>
  <action>
    Recover the original `/diensten` copy from git history and port it into the matching
    taxonomy nodes' `content`, leaving `status: "draft"` everywhere:
    - **WTW `vervangen`** → `content.steps`: convert the 10 `WTW_REPLACEMENT_STEPS` strings to
      `StepItem[]` — `title` = a concise 1-3 word label, `body` = the original step phrase
      (verbatim). Both fields non-empty (structure schema requires it). Optionally seed
      `content.intro` with the "Wanneer uw WTW unit vervangen?" prose.
    - **WTW pillar / `onderhoud-reinigen`** → `content.intro`: the WTW USP lines ("Tot 95%
      warmteterugwinning…", vochtbalans, gecertificeerde monteurs) + the reinigen description.
    - **MV `onderhoud-reinigen`** → `content.intro`: MV_BENEFITS + the "roterende borstel"
      cleaning-method prose + MV_CLEANING_REASONS. **Fold the dakventilator block** (4-jaar/
      15-jaar/80%-energie facts) here and/or into MV `vervangen` intro — NOT a new route (D-05).
    - **Airco subs/pillar** → `content.intro`: the AIRCO_CARDS descriptions, mapped to the
      matching sub-service (Installatie/Onderhoud/Reparatie & storing/Advies). **Drop
      "Panasonic"** — brand mentions, where present, name only Daikin / Mitsubishi Electric /
      Mitsubishi Heavy.
    Do NOT touch `faqs` (Phase 4), do NOT raise any `status`, do NOT add owner-review/dealer
    claims. The before/after `kanalen-*.jpg` images already live in `public/images/work/` — a
    later content task may reference them, but image embedding is not required here.
  </action>
  <acceptance_criteria>
    - `lib/services/wtw.ts`: the `vervangen` node `content.steps` has 10 entries, each with non-empty `title` and `body`.
    - No occurrence of "Panasonic" in the Phase-2 port surface (`grep -rn "Panasonic" app/diensten lib/services` is empty). NOTE: a pre-existing "Panasonic" in `components/PricingTabs.tsx:613` is OUT of Phase-2 scope (Phase-1 pricing boundary / Phase-4 CONT-05) — do NOT edit it here.
    - Every modified node still has `status: "draft"` (grep: no `"review"`/`"published"` introduced).
    - No new MV route/slug was added (the dakventilator content lives inside existing MV nodes' content).
    - `npm run build`'s prebuild step (`tsx scripts/validate-taxonomy.ts`) passes — the status-gated uniqueness rule does NOT fire on the drafts.
  </acceptance_criteria>
</task>

<task id="02-06-2" type="execute">
  <title>Extend assert-registry with content-port + coverage spot checks</title>
  <read_first>
    - scripts/assert-registry.ts (existing asserts + the 02-01 lookup asserts)
    - lib/services/registry.ts (findService for spot lookups)
  </read_first>
  <action>
    Add assertions: WTW `vervangen` has `content.steps.length === 10` with every step's
    `title` and `body` non-empty; no node in `PAGES` has a `content.intro` containing
    "Panasonic"; `findService("warmtepompen","installatie")` still has `brandIds` length 2.
    Keep prior asserts intact.
  </action>
  <acceptance_criteria>
    - `npx tsx scripts/assert-registry.ts` exits 0.
    - The new assertions are present (grep: `Panasonic`, `steps.length`).
  </acceptance_criteria>
</task>

<task id="02-06-3" type="verify">
  <title>Phase verification — green build, route coverage, anti-drift greps</title>
  <read_first>
    - next.config.ts (output: export, trailingSlash:false — export emits one HTML file per route)
    - package.json (build = prebuild taxonomy gate then next build)
    - memory note: OneDrive — run `npm run build` in background + poll output; do not block in foreground
  </read_first>
  <action>
    Run the whole-phase gate (build in background, poll the log per the OneDrive constraint):
    1. `npm run build` → expect exit 0; the prebuild taxonomy gate passes and `next build`
       statically generates the dynamic routes. Capture the route summary.
    2. Confirm route coverage in `out/`: the hub + 4 pillar pages + 17 sub-service pages are
       present (spot-check at least `/diensten`, `/diensten/airconditioning`,
       `/diensten/airconditioning/installatie`, `/diensten/wtw/vervangen`,
       `/diensten/warmtepompen` exist as exported HTML).
    3. Anti-drift greps: `grep -rn "DienstenNav" app components` empty; `grep -rn
       "DIENSTEN_DROPDOWN" lib app components` empty; `grep -rn "Panasonic" app/diensten lib/services`
       empty (scoped to the Phase-2 port surface — the pre-existing `components/PricingTabs.tsx:613`
       Panasonic line is a known Phase-4 / CONT-05 pricing-copy item, intentionally NOT touched here);
       `serviceRadiusKm: 60` intact in `lib/constants.ts` and `check-radius-literals.sh` still passes.
    4. `npm run lint` clean.
  </action>
  <acceptance_criteria>
    - `npm run build` exits 0; build output shows the `/diensten/[pillar]` and
      `/diensten/[pillar]/[service]` routes statically generated (4 + 17).
    - The 5 spot-checked routes exist as exported HTML in `out/`.
    - All three anti-drift greps return empty; `check-radius-literals.sh` passes.
    - `npm run lint` exits 0.
  </acceptance_criteria>
</task>

<threat_model>
Content port edits typed in-repo data files (no user input, no secrets, no network); the
build is local/static. The prebuild Zod gate is the integrity control — it blocks malformed
or cannibalizing taxonomy from shipping. No XSS (React auto-escapes; no
`dangerouslySetInnerHTML`), no SSRF, no secret exposure. Compliance note (not security):
the port must not introduce unverified dealer/keurmerk claims — enforced by the "no
Panasonic / no erkend-installateur" criteria. Residual risk: **none (LOW)**.
</threat_model>

## Verification

This plan IS the phase verification. Phase 2 is done when 02-06-3's acceptance criteria all
pass: green build, 22 routes pre-rendered, anti-drift greens, lint clean.
