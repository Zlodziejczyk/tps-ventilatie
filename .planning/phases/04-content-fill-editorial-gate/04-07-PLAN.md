---
phase: 04-content-fill-editorial-gate
plan: 07
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/services/brands.ts
  - lib/services/registry.ts
autonomous: true
requirements: [CONT-03, CONT-05]
must_haves:
  truths:
    - "Each brand in brands.ts has a non-empty nl install-context blurb (data, forward-compatible); erkendInstallateur stays false for every brand until owner proof"
    - "Pricing transparency copy is present on the tarieven static node: all-in incl. BTW + voorrijkosten framing for Airco/WTW/MV, and Warmtepompen 'op maat via offerte' with inclusions"
    - "No brand blurb contains a dealer/'erkend installateur' claim; no Belgian 6% BTW anywhere"
  artifacts:
    - path: "lib/services/brands.ts"
      provides: "Filled brand blurbs (4 brands); erkendInstallateur all false"
      contains: "blurb"
    - path: "lib/services/registry.ts"
      provides: "tarieven static node content with transparency copy"
      contains: "voorrijkosten"
  key_links:
    - from: "brands.ts blurb"
      to: "BrandGrid (forward-compatible data) + intake §9 owner assets"
      via: "data fill; BrandGrid currently renders names only"
      pattern: "blurb"
---

<objective>
Fill the cross-cutting brand + pricing content. (1) Brand install blurbs in `brands.ts` (CONT-03, D-19) — nl install-context copy for Daikin / Mitsubishi Electric / Heavy / Ecodan as data; `erkendInstallateur` stays `false` (no dealer claim until owner proof); logos handled per the D-19 reality (text-chip fallback). (2) Pricing transparency copy (CONT-05, D-11) into the `tarieven` static node's `ContentShell` (in `registry.ts`) — all-in incl. BTW + voorrijkosten framing for Airco/WTW/MV, Warmtepompen "op maat via offerte" with inclusions; pricing DATA stays in `components/PricingTabs.tsx` (untouched). Honors D-11/D-12/D-19.

Purpose: Brand data and pricing-transparency framing in their sanctioned homes, without touching locked components or pricing data — the "Transparant" USP made literally true.
Output: filled `brands.ts` blurbs; transparency copy on the `tarieven` node in `registry.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md
@.planning/phases/04-content-fill-editorial-gate/04-RESEARCH.md
@.planning/phases/04-content-fill-editorial-gate/04-PATTERNS.md
@docs/anti-claim-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill brand install blurbs in brands.ts (data; dealer status stays gated)</name>
  <files>lib/services/brands.ts</files>
  <read_first>
    - lib/services/brands.ts (full — the `as const` BRANDS object 11-40, every `blurb: ""`, every `erkendInstallateur: false`, placeholder `logo` paths; `BrandId = keyof typeof BRANDS` at 42)
    - components/BrandGrid.tsx (full — IMPORTANT: it renders brand NAME text-chips only (line 46); it reads NEITHER `blurb` NOR `logo`. So blurbs are forward-compatible DATA — they do not render today. Do NOT modify this locked P2 component.)
    - docs/anti-claim-checklist.md (brand rule: naming a product you install is allowed; dealer/"erkend" claim is gated)
    - 04-RESEARCH.md §"Brand-Asset Sourcing" (315-324 — official-only assets, text-chip fallback; logo presence ≠ dealer claim) + §D-19 (63-65) + Pitfall 4 (382-386)
  </read_first>
  <action>
    Fill each brand's `blurb` (currently "") with a short nl install-context sentence — what the brand is and that TPS installs it (e.g. Daikin: marktleider in airconditioning en warmtepompen die TPS installeert; Mitsubishi Electric / Mitsubishi Heavy Industries: airco/klimaatsystemen TPS installeert; Mitsubishi Ecodan: lucht/water-warmtepomplijn TPS installeert). Naming a product you install is allowed; a dealer/"erkend installateur"/"officiële dealer" claim is NOT. Keep `erkendInstallateur: false` for ALL four (D-12/D-19 — flip only when intake §5 returns proof, handled in 04-09). Leave the `logo` placeholder paths as-is (harmless DATA — BrandGrid renders names only; real official SVGs arrive via intake §9 / Phase 6; do NOT pull aggregator logos, D-19). Preserve the `as const` object literal + `keyof` id type (do not convert to an array; do not add an id not referenced by a node's `brandIds`). Update the file header (6-9) to note blurbs are filled as forward-compatible data and `erkendInstallateur` remains gated. Add a code comment that brand install CONTENT also renders today via the Installatie node intros (04-03/04-04) since BrandGrid shows names only.
  </action>
  <verify>
    <automated>npx tsx -e "import { BRANDS } from './lib/services/brands.ts'; const ids=Object.keys(BRANDS); let bad=[]; for(const id of ids){const b=BRANDS[id]; if(!b.blurb||b.blurb.trim().length<10) bad.push(id+' empty blurb'); if(b.erkendInstallateur!==false) bad.push(id+' erkendInstallateur not false'); if(/erkend|dealer|gecertificeerd/i.test(b.blurb)) bad.push(id+' blurb has gated claim');} if(bad.length){console.error('FAIL: '+bad.join('; '));process.exit(1)} console.log('brands OK — '+ids.length+' blurbs filled, all erkendInstallateur false, no gated claim')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
    <automated>node -e "const s=require('fs').readFileSync('lib/services/brands.ts','utf8').toLowerCase(); if(/erkend\s+installateur|officiële dealer/.test(s)){console.error('dealer claim in brands.ts');process.exit(1)} console.log('brands.ts no-dealer-claim grep OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 4 brands have a non-empty (≥10 char) install-context `blurb`; none contains "erkend"/"dealer"/"gecertificeerd".
    - Every brand `erkendInstallateur` is `false`.
    - The `as const` shape + `BrandId` type are preserved; `logo` paths unchanged; BrandGrid (locked) not modified.
  </acceptance_criteria>
  <done>Both verify commands print OK; brand blurbs are filled as data, dealer status stays gated, no locked component changed.</done>
</task>

<task type="auto">
  <name>Task 2: Add pricing transparency copy to the tarieven static node</name>
  <files>lib/services/registry.ts</files>
  <read_first>
    - lib/services/registry.ts:29-43 (the static-node `draftShell` — takes h1/metaTitle/metaDescription only) + :87-102 (the `tarieven` static node, `content` via draftShell, currently no intro/steps/faqs body) + :29 (note: registry's draftShell is the 3-arg form)
    - lib/services/types.ts:41-50 (ContentShell) — the tarieven node is `status: "draft"` and STAYS draft here (statics index via policy.ts type-rule, but its body copy still helps; flipping is not required for a static). Confirm: do NOT flip tarieven to review (policy.ts:24 indexes statics by type already; the uniqueness Zod gate only bites review/published, so adding rich body to a draft static is safe and renders).
    - components/PricingTabs.tsx is the pricing DATA home (DO NOT touch — P1 Pitfall 3); this task adds only framing COPY to the taxonomy node
    - docs/anti-claim-checklist.md + 04-RESEARCH.md §"Pricing & BTW Facts" (292-301 — NL 21% standard, 9% labor nuance; NEVER Belgian 6%; WP = op maat via offerte) + §D-11 (51)
    - app/tarieven/page.tsx (how the tarieven page renders — check whether it reads the node's content.intro; if not, the transparency copy still belongs on the node as the single-source content home and is available to the page/Phase 6)
  </read_first>
  <action>
    Add pricing-transparency framing copy to the `tarieven` static node's `content` (87-102) by passing an `extra` object — but the registry `draftShell` (29-43) is the 3-arg form with NO `extra` param. Widen registry's `draftShell` to accept the same `extra?: { intro?; steps?; faqs?; localAngle? }` shape (mirror wtw.ts:31-46; import `FaqItem`/`StepItem` if you add steps/faqs — for tarieven an `intro` + optional `faqs` is enough). Then set the tarieven `intro` to transparency copy: all prijzen all-in **inclusief BTW én voorrijkosten** for Airco/WTW/MV (no verborgen kosten — sharpens the "Transparant" USP); Warmtepompen = **"prijs op maat via offerte"** with what's included (opname, materiaal, installatie, inbedrijfstelling) since there is no standard WP price list. Optionally add 2-3 pricing FAQs (zijn voorrijkosten inbegrepen, waarom warmtepomp op maat, is BTW inbegrepen). Formal `u` (D-14). Do NOT state Belgian 6% BTW (NL = 21%); do NOT duplicate the actual price numbers (those live in PricingTabs). Keep the tarieven node `status: "draft"` (statics index by type via policy.ts; no flip needed and the Zod uniqueness gate does not bite drafts). Do NOT touch `components/PricingTabs.tsx`.
  </action>
  <verify>
    <automated>npx tsx -e "import { PAGES } from './lib/services/registry.ts'; const t=PAGES.find(n=>n.type==='static'&&n.pathSegment==='tarieven'); const blob=JSON.stringify(t.content).toLowerCase(); if(!/voorrijkosten/.test(blob)||!/btw/.test(blob)){console.error('tarieven missing all-in BTW/voorrijkosten framing');process.exit(1)} if(!/op maat|offerte/.test(blob)){console.error('tarieven missing warmtepomp op-maat framing');process.exit(1)} if(/\b6\s*%\s*btw/.test(blob)){console.error('Belgian 6% btw present');process.exit(1)} console.log('tarieven transparency copy OK')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
    <automated>node -e "const a=require('fs').readFileSync('lib/services/registry.ts','utf8'); const b=require('fs').readFileSync('components/PricingTabs.tsx','utf8'); console.log('PricingTabs untouched check is git-diff scope'); if(!/voorrijkosten/i.test(a)){console.error('registry tarieven copy missing');process.exit(1)} console.log('registry tarieven copy present OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The `tarieven` static node `content` contains all-in incl. BTW + voorrijkosten framing AND the Warmtepompen "op maat via offerte" framing.
    - No Belgian "6% btw" literal; actual price numbers are NOT duplicated (they stay in PricingTabs.tsx, which is unchanged).
    - registry's `draftShell` widened to accept `extra` without breaking existing static/hub nodes (they pass no extra → defaults).
  </acceptance_criteria>
  <done>Both verify commands print OK; pricing-transparency copy lives on the tarieven node, pricing data untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| brand/pricing data → published trust copy | A dealer claim in a brand blurb or a wrong BTW framing crosses into the live site and damages the "Transparant" USP |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Spoofing (trust) | A brand blurb implying authorized-dealer status | mitigate | Blurbs are install-context only; `erkendInstallateur` stays false; verify check rejects "erkend"/"dealer"/"gecertificeerd" in blurbs |
| T-04-11 | Tampering (YMYL integrity) | Stating Belgian 6% BTW or a wrong all-in claim on tarieven | mitigate | NL 21% framing only (RESEARCH facts); verify rejects "6% btw"; the 04-01 prebuild grep also catches it if the node ever flips to review |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Brand-blurb + tarieven-copy checks via `npx tsx` (run on Vercel CI if OneDrive deadlocks local tsx).
- No-dealer-claim + registry-copy-present greps are pure Node reads (OneDrive-safe).
- `components/PricingTabs.tsx` must show NO diff (verify via `git diff --stat` at review — pricing data untouched).
- Whole-phase build GREEN ON VERCEL preview (never locally — OneDrive deadlock).
</verification>

<success_criteria>
- Brand blurbs are filled as forward-compatible data with dealer status gated (all `false`); no locked component changed.
- Pricing-transparency copy (all-in incl. BTW + voorrijkosten; WP op maat) lives on the tarieven node; pricing data stays in PricingTabs; no Belgian 6% BTW anywhere.
</success_criteria>

<output>
Create `.planning/phases/04-content-fill-editorial-gate/04-07-SUMMARY.md` when done.
</output>
