---
phase: 04-content-fill-editorial-gate
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/reviews.ts
  - app/page-sections/ReviewsSection.tsx
  - app/diensten/page.tsx
  - app/over-ons/page.tsx
  - app/page-sections/WhyTPSSection.tsx
  - lib/seo/jsonld.tsx
  - scripts/assert-seo.ts
autonomous: true
requirements: [CONT-06, CONT-07, CONT-08]
must_haves:
  truths:
    - "All three previously-scattered REVIEWS arrays read from one source (lib/reviews.ts); no duplicate review array remains"
    - "Home and key pages show a Google count + link sourced from the single reviews module (count derives from the module, not a hardcoded literal); a star score renders only when real rating data is present"
    - "businessJsonLd emits aggregateRating ONLY when REVIEW_RATING is non-null (gated until real Google data); it is omitted while null"
    - "Over-ons JSX no longer claims 'gecertificeerd' before proof (interim 'vakkundig'/'ervaren'); home USP/brand copy no longer contradicts the taxonomy"
  artifacts:
    - path: "lib/reviews.ts"
      provides: "Single consolidated reviews source (REVIEWS: Review[]) + gated REVIEW_RATING"
      exports: ["REVIEWS", "REVIEW_RATING"]
    - path: "lib/seo/jsonld.tsx"
      provides: "businessJsonLd with conditional aggregateRating spread"
      contains: "REVIEW_RATING"
  key_links:
    - from: "app/page-sections/ReviewsSection.tsx"
      to: "lib/reviews.ts"
      via: "import REVIEWS + REVIEW_RATING"
      pattern: "from \"@/lib/reviews\""
    - from: "app/diensten/page.tsx"
      to: "lib/reviews.ts"
      via: "import REVIEWS"
      pattern: "from \"@/lib/reviews\""
    - from: "app/over-ons/page.tsx"
      to: "lib/reviews.ts"
      via: "import REVIEWS"
      pattern: "from \"@/lib/reviews\""
    - from: "lib/seo/jsonld.tsx"
      to: "lib/reviews.ts"
      via: "import REVIEW_RATING, conditional spread"
      pattern: "REVIEW_RATING"
---

<objective>
Consolidate the three scattered `REVIEWS` arrays into one source (`lib/reviews.ts`), repoint all consumers, wire the reserved `aggregateRating` JSON-LD slot (gated on real Google data), and refresh the Over-ons + home USP copy to be literally true. Implements CONT-08 (reviews single source + Google score/count/link), CONT-07 (Over-ons refresh + 4 USPs), and the CONT-06/D-02 interim-cert discipline in JSX (the 04-01 grep gate scans only taxonomy `content`, so the JSX "gecertificeerd" instances are corrected here). Honors D-17 (single source + gated rating), D-18 (home = data/content only, NO layout change).

Purpose: One trust-data source feeding ReviewCarousel, the JSON-LD rating slot, and on-page score/count/link — eliminating 3-copy drift and pre-proof "gecertificeerd" claims.
Output: new `lib/reviews.ts`; repointed `ReviewsSection.tsx` / `diensten/page.tsx` / `over-ons/page.tsx`; conditional `aggregateRating` in `jsonld.tsx`; updated `assert-seo.ts` ratings assertion; corrected USP copy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md
@.planning/phases/04-content-fill-editorial-gate/04-RESEARCH.md
@.planning/phases/04-content-fill-editorial-gate/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lib/reviews.ts and repoint the three review consumers</name>
  <files>lib/reviews.ts, app/page-sections/ReviewsSection.tsx, app/diensten/page.tsx, app/over-ons/page.tsx</files>
  <read_first>
    - components/ReviewCarousel.tsx:6-10 (the `Review` interface to reuse: name/quote/timeAgo) and :17-24 (`getInitials` — over-ons can derive initials, drop its own field)
    - lib/constants.ts (the `SITE` single-source ethos; named export, SCREAMING_SNAKE_CASE) and lib/services/registry.ts:152-159 (the `PAGES` aggregation pattern to mirror)
    - app/page-sections/ReviewsSection.tsx (full — the 18-item `Review[]` at 6-115; count display at 162; hardcoded "4.9" at 160; carousel at 168)
    - app/diensten/page.tsx:14-30 (the verbatim-copied array + the comment flagging it for consolidation; render at 190)
    - app/over-ons/page.tsx:11-30 (the DIFFERENTLY-shaped untyped array `{ initials; name; quote }`, no `timeAgo`); render 100-126 (uses `review.initials` at 114; "Geverifieerde Klant" at 119)
    - 04-PATTERNS.md §"lib/reviews.ts" (120-150) + §"Reviews consumers — repoint" (232-240)
    - 04-RESEARCH.md §"Reviews & aggregateRating" → "Consolidation" (338-344 — KEEP verbatim quotes incl. the old "TPS Ventilatie" string; do NOT mass-replace)
  </read_first>
  <action>
    Create `lib/reviews.ts`: header comment (the ONE consolidated reviews source per D-17, replaces the 3 scattered arrays). `import type { Review } from "@/components/ReviewCarousel"` (reuse the shipped shape — do NOT invent a second type). Export `export const REVIEWS: Review[] = [...]` = the de-duped union of the three arrays, quotes KEPT VERBATIM (including any "TPS Ventilatie" string). The 18-item `ReviewsSection.tsx` array is the superset; the over-ons 3 are a subset (reconcile by dropping `initials` and adding a `timeAgo`; for the over-ons longer Jacqueline variant keep the fuller quote in the `{name,quote,timeAgo}` shape). Also export the gated rating `export const REVIEW_RATING: { value: number; count: number; url: string } | null = null;` with a comment that it stays null until the owner provides REAL Google score+count via intake §8 (D-13/D-17): when null the JSON-LD slot stays empty and on-page score/count/link render gracefully.
    Repoint consumers (D-18: data/source only — NO layout/visual change, no markup restructure):
    - `ReviewsSection.tsx`: delete the inline `const REVIEWS` (6-115); `import { REVIEWS, REVIEW_RATING } from "@/lib/reviews"`. Keep all existing markup. For the count at 162 and the hardcoded "4.9" at 160: render the star score ONLY when `REVIEW_RATING` is non-null (use `REVIEW_RATING.value`); while null, do NOT render a fabricated "4.9" literal — show the carousel + a neutral "{REVIEWS.length}+ reviews op Google" count. When `REVIEW_RATING` is set, make the Google line link to `REVIEW_RATING.url`.
    - `diensten/page.tsx`: delete the copied `const REVIEWS` (18-…) and the stale comment 14-17; `import { REVIEWS } from "@/lib/reviews"`; keep `<ReviewCarousel reviews={REVIEWS} />` at 190.
    - `over-ons/page.tsx`: delete the untyped `const REVIEWS` (11-30); `import { REVIEWS } from "@/lib/reviews"`. In the grid render (100-126), replace `review.initials` (114) with initials derived inline from `review.name` (mirror ReviewCarousel's `getInitials`); keep the "Geverifieerde Klant" label and markup. Use `REVIEWS.slice(0, 3)` to preserve the 3-card grid.
    Conventions: `@/lib/reviews` path; named exports, no default, no barrel; double quotes; trailing commas; NO `"use client"` in `lib/reviews.ts`.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/reviews.ts','utf8'); if(!/export const REVIEWS\s*:\s*Review\[\]/.test(s)||!/export const REVIEW_RATING/.test(s)||!/from ['\"]@\/components\/ReviewCarousel['\"]/.test(s)){console.error('reviews module shape wrong');process.exit(1)} console.log('lib/reviews.ts OK')"</automated>
    <automated>node -e "const f=['app/page-sections/ReviewsSection.tsx','app/diensten/page.tsx','app/over-ons/page.tsx']; const fs=require('fs'); for(const p of f){const s=fs.readFileSync(p,'utf8'); if(!/from ['\"]@\/lib\/reviews['\"]/.test(s)){console.error(p+' not repointed to @/lib/reviews');process.exit(1)} if(/const REVIEWS\s*[:=]/.test(s)){console.error(p+' still declares a local REVIEWS array');process.exit(1)}} console.log('all 3 consumers repointed, no local REVIEWS')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('app/over-ons/page.tsx','utf8'); if(/review\.initials/.test(s)){console.error('over-ons still uses review.initials (field dropped)');process.exit(1)} console.log('over-ons initials derived OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/reviews.ts` exists, reuses `Review` from `@/components/ReviewCarousel`, exports `REVIEWS: Review[]` (verbatim quotes) and `REVIEW_RATING` (null-gated).
    - `grep -L "@/lib/reviews"` finds none of the 3 consumers (all import it); no `const REVIEWS` array remains in any of the 3 files.
    - `over-ons/page.tsx` no longer references `review.initials`.
    - No layout/markup restructure on the home (D-18) — only the source and the conditional score.
  </acceptance_criteria>
  <done>All three verify commands print OK; one reviews source feeds all consumers; the fabricated "4.9" no longer renders unless real rating data is present.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the gated aggregateRating slot, fix over-ons/USP interim-cert copy, update assert-seo</name>
  <files>lib/seo/jsonld.tsx, scripts/assert-seo.ts, app/over-ons/page.tsx, app/page-sections/WhyTPSSection.tsx</files>
  <read_first>
    - lib/seo/jsonld.tsx:2-4 + :31-33 (the "NO aggregateRating in Phase 3" header note) + :34-74 (businessJsonLd — where to spread the slot) + :107-118 (faqJsonLd — the null-on-empty gated-builder pattern to mirror)
    - scripts/assert-seo.ts:56-61 (the assertions that ratings/review are ABSENT — must become conditional on REVIEW_RATING)
    - lib/reviews.ts (the `REVIEW_RATING` const created in Task 1)
    - app/over-ons/page.tsx:54 ("ervaren en gecertificeerde ventilatiespecialist" — D-02 violation in body prose) and :67 (the "Gecertificeerd" USP) and :119 ("Geverifieerde Klant")
    - app/page-sections/WhyTPSSection.tsx:7-28 (the home USP array) + :39 ("TPS Ventilatie" brand string) + :16 (brands "Itho Daalderop, Zehnder en Orcon" — NOT the taxonomy's Daikin/Mitsubishi brands)
    - 04-PATTERNS.md §"lib/seo/jsonld.tsx" (203-228) + §"USP copy refresh" (244-257)
    - 04-RESEARCH.md §"Certification / Keurmerk Facts" (303-313) + Open-Q1 (470-473 — default to interim "Vakkundig" until F-gassen/STEK proof) + §"CRITICAL policy finding" (328-331 — self-serving rating earns no Google stars; fill for AI/on-page)
  </read_first>
  <action>
    (a) `lib/seo/jsonld.tsx`: `import { REVIEW_RATING } from "@/lib/reviews"`. Inside `businessJsonLd()`'s returned object, add a conditional spread that emits `aggregateRating` ONLY when `REVIEW_RATING` is non-null (mirror the faqJsonLd null-on-empty discipline + the existing `sameAs`-omission rule — never emit empty/placeholder): `...(REVIEW_RATING && { aggregateRating: { "@type": "AggregateRating", ratingValue: REVIEW_RATING.value, reviewCount: REVIEW_RATING.count } })`. Update the header note (2-4, 31-33) to say the slot is now wired but gated on real Google data (D-17), and that self-serving ratings earn no Google star snippet — the slot serves AI/LLM + on-page display. Server-only `.tsx`, no client directive; all data via imported consts.
    (b) `scripts/assert-seo.ts`: change the two absent-assertions (60-61) to be conditional on the gate — assert `aggregateRating`/`review` are ABSENT when `REVIEW_RATING === null` (the current launch state), and present (typed `AggregateRating` with `ratingValue`) when non-null. Import `REVIEW_RATING` from `@/lib/reviews`. Leave the sitemap-count / "4 indexable statics" assertions (24-49) UNCHANGED here — those are updated in 04-09 when nodes actually publish.
    (c) `over-ons/page.tsx`: line 54 — replace "ervaren en gecertificeerde ventilatiespecialist" with literally-true interim wording per D-02 (e.g. "ervaren en vakkundige ventilatiespecialist"); line 67 USP — change `title: "Gecertificeerd"` + its `desc` to interim "Vakkundig" (e.g. title "Vakkundig", desc about opleiding/normen without the word "gecertificeerd"). Add a code comment: swap back to "Gecertificeerd" once intake §6 returns F-gassen/STEK proof (gated value, like the dealer badge). Refresh the three intro paragraphs (53-61) toward the "Verhaal van Thomas" framing (CONT-07) keeping the formal `u` voice; do NOT invent unverified facts — keep claims to ervaring/persoonlijk/transparant/regio. Keep all markup/layout.
    (d) `WhyTPSSection.tsx`: this section's USPs do NOT contain "Gecertificeerd" (they are ervaring/merk-onafhankelijk/voorrijkosten/snel) — leave the USP titles, but fix the two stale-content items per D-18 (copy only): line 39 "TPS Ventilatie" → "TPS klimaattechniek"; line 16 brand list "Itho Daalderop, Zehnder en Orcon" → align to the taxonomy brands the site actually positions (Daikin, Mitsubishi Electric/Heavy) OR neutralize to "alle grote merken" (these legacy ventilation brands are not in the taxonomy and contradict the airco/WP positioning). NO layout change.
    Run nothing locally (OneDrive). Validation is on Vercel CI + the Node string checks below.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/seo/jsonld.tsx','utf8'); if(!/REVIEW_RATING/.test(s)||!/aggregateRating/.test(s)||!/AggregateRating/.test(s)){console.error('aggregateRating slot not wired to REVIEW_RATING');process.exit(1)} console.log('jsonld aggregateRating gated OK')"</automated>
    <automated>node -e "const s=require('fs').readFileSync('scripts/assert-seo.ts','utf8'); if(!/REVIEW_RATING/.test(s)){console.error('assert-seo not updated for REVIEW_RATING');process.exit(1)} console.log('assert-seo conditional OK')"</automated>
    <automated>node -e "const o=require('fs').readFileSync('app/over-ons/page.tsx','utf8').toLowerCase(); if(/gecertificeerd/.test(o)){console.error('over-ons still contains gecertificeerd (D-02 violation)');process.exit(1)} console.log('over-ons interim-true OK')"</automated>
    <automated>node -e "const w=require('fs').readFileSync('app/page-sections/WhyTPSSection.tsx','utf8'); if(/TPS Ventilatie/.test(w)||/Itho Daalderop|Zehnder|Orcon/.test(w)){console.error('WhyTPSSection still has stale brand string/legacy brands');process.exit(1)} console.log('home USP copy aligned OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `businessJsonLd()` spreads `aggregateRating` only when `REVIEW_RATING` is non-null; omits it while null (verified by the jsonld check).
    - `scripts/assert-seo.ts` references `REVIEW_RATING` and conditionally asserts presence/absence; the sitemap-count assertions are left for 04-09.
    - `grep -i gecertificeerd app/over-ons/page.tsx` returns nothing (interim "vakkundig" everywhere in JSX).
    - `WhyTPSSection.tsx` contains neither "TPS Ventilatie" nor the legacy ventilation-brand names; no layout change.
  </acceptance_criteria>
  <done>All four verify commands print OK; the rating slot is gated, JSX cert claims are interim-true, and home trust copy no longer contradicts the taxonomy — all without a layout change (D-18).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviews data → JSON-LD + on-page display | A fabricated/edited review or an unsourced star rating crosses into structured data + visible trust copy (Google fake-review/policy risk) |
| client → site | No new input handling this plan; reviews are static build data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Spoofing/Repudiation (trust) | `aggregateRating` rendered without a real Google source | mitigate | `REVIEW_RATING` defaults `null` → slot omitted + no on-page score until owner supplies real data (intake §8, D-13); `assert-seo.ts` asserts the null-state absence |
| T-04-04 | Tampering (authenticity) | Editing verbatim customer review quotes | mitigate | Quotes kept VERBATIM incl. old brand string (RESEARCH Open-Q2); consolidation is a code move, not a content rewrite |
| T-04-05 | Tampering (content integrity) | Pre-proof "gecertificeerd" claim surviving in JSX (outside the 04-01 grep scope) | mitigate | THIS plan removes the JSX instances (over-ons:54,67) → interim "vakkundig"; verified by grep |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed this phase (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- All Node string checks pass locally (pure fs reads — OneDrive-safe; no `tsx`/`next build`).
- `npx tsx scripts/assert-seo.ts` validates ON VERCEL CI: in the launch (null) state it still asserts ratings absent; the existing "4 indexable statics" + sitemap-4 assertions remain valid because no node is `published` yet (those update in 04-09).
- Whole-phase build GREEN ON VERCEL preview deploy (never locally — OneDrive deadlock).
</verification>

<success_criteria>
- One reviews source (`lib/reviews.ts`) feeds ReviewCarousel on home + diensten + over-ons; no duplicate array remains.
- `aggregateRating` is wired but gated (omitted until real Google data); on-page score renders only when present.
- Over-ons + home trust copy is literally true (no pre-proof "gecertificeerd"; no stale brand contradictions) with zero layout change (D-18).
</success_criteria>

<output>
Create `.planning/phases/04-content-fill-editorial-gate/04-02-SUMMARY.md` when done.
</output>
