---
phase: 04-content-fill-editorial-gate
plan: 09
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04", "04-05", "04-06", "04-07", "04-08"]
files_modified:
  - lib/services/airconditioning.ts
  - lib/services/warmtepompen.ts
  - lib/services/wtw.ts
  - lib/services/mechanische-ventilatie.ts
  - lib/services/registry.ts
  - lib/services/brands.ts
  - lib/reviews.ts
  - scripts/assert-seo.ts
autonomous: false
requirements: [CONT-06, CONT-10]
must_haves:
  truths:
    - "Any owner-returned data from intake is mapped into the gated slots (brand dealer status, cert proof → keurmerk block, real Google score/count → REVIEW_RATING, story, NAP/hours, pricing confirmation)"
    - "Thomas has reviewed the whole site on a Vercel preview deploy (one consolidated pass over all 21 pages) and signed off"
    - "The owner-approved set of pillar/service nodes is batch-flipped review -> published; the status flip is the audit-trail git commit"
    - "scripts/assert-seo.ts sitemap-count / indexable-set assertions are updated to match the now-published set (the Phase-3 'exactly 4 statics' assertion no longer holds)"
  artifacts:
    - path: "scripts/assert-seo.ts"
      provides: "Indexable/sitemap assertions updated for the published taxonomy set"
      contains: "isIndexable"
  key_links:
    - from: "owner sign-off (preview)"
      to: "status: published"
      via: "batch git commit = audit trail (D-06)"
      pattern: "published"
    - from: "intake §8 Google score"
      to: "REVIEW_RATING (lib/reviews.ts)"
      via: "map real data into the gated const"
      pattern: "REVIEW_RATING"
---

<objective>
The hard editorial gate (CONT-10, D-05/D-06/D-07/D-09). With every pillar drafted and flipped to `review` (04-03..06) and the reviews/brands/pricing wired (04-02/07), this plan: (1) maps any owner-returned intake data into the gated slots; (2) drives the whole-site owner review on a Vercel preview deploy (one consolidated pass, D-07); (3) batch-flips the approved set `review → published` (D-09) — the git commit IS the audit trail (D-06); (4) updates `scripts/assert-seo.ts` so its indexable/sitemap assertions match the now-published set (the Phase-3 "exactly 4 statics" assertion no longer holds once pillars/services publish). Also lands CONT-06 finalization (cert/keurmerk block) IF the owner returned proof. `autonomous: false` — it waits on a real human's sign-off.

Purpose: No page goes live without owner editorial approval; the published set is exactly what Thomas signed off, and the index lever (status flip) is pulled cleanly with the SEO assertions kept honest.
Output: gated slots filled from intake (where returned); approved nodes at `status: "published"`; updated `assert-seo.ts`; a final green Vercel build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md
@.planning/phases/04-content-fill-editorial-gate/04-RESEARCH.md
@docs/owner-intake-spec.md
@docs/anti-claim-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map any owner-returned intake data into the gated slots</name>
  <files>lib/reviews.ts, lib/services/brands.ts, lib/services/airconditioning.ts, lib/services/warmtepompen.ts, lib/services/wtw.ts, lib/services/mechanische-ventilatie.ts, lib/services/registry.ts</files>
  <read_first>
    - docs/owner-intake-spec.md §4 "→ Code destination" column (the ASK→DERIVE→code map): §5 brands/dealerstatus → brands.ts `erkendInstallateur`; §6 certificeringen+proof → keurmerk/trust copy (CONT-06); §8 Google link + score/count + review consent → lib/reviews.ts `REVIEW_RATING`; §7 story/USPs → over-ons (already interim in 04-02 — only swap "Vakkundig"→"Gecertificeerd" if F-gassen/STEK proof returned); §1/2 NAP/hours → SITE (note: SITE is lib/constants.ts — only update if owner corrects values; that is a small constants edit) ; §4 pricing confirm → tarieven node
    - lib/reviews.ts (the null `REVIEW_RATING` to fill with REAL Google value/count/url if intake §8 returned them)
    - lib/services/brands.ts (`erkendInstallateur: false` — flip a brand to true ONLY with returned proof, D-12)
    - docs/anti-claim-checklist.md (the gate every newly-ungated claim must still pass)
    - 04-RESEARCH.md §"Certification / Keurmerk Facts" (303-313) + Open-Q1 (470-473) + A2 (461) — F-gassen/STEK unblocks "Gecertificeerd"; §"Reviews" technical aggregateRating requirements (333-336)
  </read_first>
  <action>
    For EACH owner return that arrived (this task is conditional on what Thomas submitted — if a given item did NOT come back, leave it gated and note it):
    - Google reviews (§8): if a real Google score + count + profile URL returned AND review consent is yes, set `REVIEW_RATING` in `lib/reviews.ts` to `{ value, count, url }` (real data only — D-13). This auto-activates the `aggregateRating` JSON-LD slot (04-02) and the on-page score/count/link.
    - Brand dealer status (§5): for any brand with returned proof of "erkend installateur"/dealer status, flip that brand's `erkendInstallateur` to `true` in `brands.ts`; otherwise leave `false`. Only with proof.
    - Certifications (§6, CONT-06): if F-gassen/STEK (or other) proof returned, you MAY now use the named keurmerk + swap the over-ons USP/body wording from interim "Vakkundig" back to "Gecertificeerd" (the gated value). Add the keurmerk trust copy where it belongs (over-ons / pillar trust framing) — display-gated on proof (D-12). If NO proof returned, leave interim "Vakkundig" and add NO named keurmerk.
    - Story/USPs (§7): refine the over-ons "Verhaal van Thomas" from his returned bullets/voice note (CONT-07) — keep formal `u`.
    - NAP/hours (§1/2) + pricing confirm (§4): if the owner corrected any value, update `SITE` (lib/constants.ts) and/or the tarieven node copy accordingly; if confirmed unchanged, no edit.
    Apply `docs/anti-claim-checklist.md` to every newly-ungated claim. If the owner has NOT responded by the time the rest of the phase is ready, this task maps what is available and the remaining slots stay gated (graceful partial launch, D-08) — the un-returned items do not block publishing the rest.
  </action>
  <verify>
    <automated>npx tsx -e "import { REVIEW_RATING } from './lib/reviews.ts'; import { BRANDS } from './lib/services/brands.ts'; const fs=require('fs'); const o=fs.readFileSync('app/over-ons/page.tsx','utf8').toLowerCase(); const certProof=/gecertificeerd/.test(o); for(const id of Object.keys(BRANDS)){ if(BRANDS[id].erkendInstallateur===true && !/* proof must exist */ true){} } if(REVIEW_RATING!==null){ if(typeof REVIEW_RATING.value!=='number'||typeof REVIEW_RATING.count!=='number'||!REVIEW_RATING.url){console.error('REVIEW_RATING set but malformed — must be real {value,count,url}');process.exit(1)} } console.log('intake-mapping invariants OK (rating '+(REVIEW_RATING?'set':'gated')+', cert '+(certProof?'ungated-with-proof':'interim')+')')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
    <automated>node -e "const s=require('fs').readFileSync('lib/services/brands.ts','utf8'); console.log('REMINDER: any erkendInstallateur:true must be backed by intake §5 proof recorded in the SUMMARY'); console.log('brands erkend flags: '+(s.match(/erkendInstallateur:\s*true/g)||[]).length+' true')"</automated>
  </verify>
  <acceptance_criteria>
    - `REVIEW_RATING` is either still `null` (no real data) or a well-formed `{ value:number, count:number, url:string }` from REAL Google data.
    - Any `erkendInstallateur: true` is backed by returned proof (recorded in the SUMMARY); otherwise all stay `false`.
    - "Gecertificeerd" appears in over-ons ONLY if F-gassen/STEK proof returned; otherwise interim "Vakkundig" persists.
    - Un-returned items remain gated (no fabricated data); the phase can still publish the rest.
  </acceptance_criteria>
  <done>The verify commands print OK; every ungated claim is backed by real owner data; missing returns stay safely gated.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Whole-site owner review on the Vercel preview deploy</name>
  <files></files>
  <read_first>
    - .planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md §D-05 (review surface = Vercel preview deploy) + §D-07 (one whole-site review of all 21 pages) + §D-09 (then batch-publish)
    - .planning/phases/04-content-fill-editorial-gate/04-VALIDATION.md §"Manual-Only Verifications" (the uniqueness-quality + sign-off + ISDE-in-context manual gates)
  </read_first>
  <what-built>
    All pillar/service nodes are at `review` (noindex, out of sitemap — safe to sit) and render on the latest Vercel preview deploy. Claude confirms the preview build is GREEN (prebuild Zod gate + assert-no-forbidden-claims pass on Vercel) and assembles the review surface: the preview URL + the list of all 21 pages (4 pillars + 17 subs) for Thomas to walk.
  </what-built>
  <how-to-verify>
    1. The Vercel preview deploy built GREEN (prebuild gates passed — no intro<120w, no missing step/faq, no forbidden claim).
    2. Thomas reviews ALL 21 rendered pages on the preview URL in one consolidated pass (D-07): copy reads true, ISDE/subsidie statements are correct in context (cross-check against the RESEARCH facts table + the cited rvo.nl links), no page is distinguishable from another only by its H1 (D-15), pricing/brand/cert framing is accurate.
    3. Thomas approves the set (or flags specific pages to revise — revisions loop back to the relevant pillar plan before this gate completes).
  </how-to-verify>
  <action>Claude confirms the latest Vercel preview deploy is GREEN (prebuild Zod gate + assert-no-forbidden-claims passed), then assembles the review surface for Thomas: the preview URL plus the explicit list of all 21 pages to walk (4 pillar URLs + the 17 sub-service URLs, derived via `urlFor` from the taxonomy). Claude posts this list and waits; the human relays it to Thomas and returns his sign-off / revise notes. No repo file changes in this task (publishing happens in Task 3).</action>
  <resume-signal>Type "approved: <list or 'all'>" with the pages Thomas signed off, or "revise: <pages + notes>".</resume-signal>
  <acceptance_criteria>
    - The preview deploy is GREEN and Thomas has done ONE whole-site review (D-07).
    - The approved page set is explicitly recorded (drives the batch flip in Task 3).
    - Any "revise" items are resolved (in their pillar plan) and re-previewed before sign-off completes.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Batch-flip the approved set to published and update assert-seo</name>
  <files>lib/services/airconditioning.ts, lib/services/warmtepompen.ts, lib/services/wtw.ts, lib/services/mechanische-ventilatie.ts, lib/services/registry.ts, scripts/assert-seo.ts</files>
  <read_first>
    - lib/seo/policy.ts:23-28 (`isIndexable` — hub/pillar/service index on `status === "published"`; flipping is the ONLY index lever) + :40-42 (`sitemapEntries`)
    - scripts/assert-seo.ts:24-49 (the assertions that currently hardcode "exactly the 4 static content pages" + "sitemap holds exactly 4 entries" — these BREAK once pillars/services publish and MUST be updated) + :32-39 (the noindex-set assertions that assume draft services)
    - lib/services/registry.ts (the hub node HUB_PAGE 47-62 — decide whether the hub flips to published too; Phase 3 left it draft/noindex. Flip it only if Thomas approved the /diensten hub copy)
    - .planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md §D-06 (the status-flip commit is the audit trail) + §D-09 (batch flip)
  </read_first>
  <action>
    For every node in the OWNER-APPROVED set (from Task 2), flip its `status` from `"review"` to `"published"` in its pillar file (and the hub node in registry.ts if the hub was approved). Leave any non-approved node at `review` (it stays noindex/out-of-sitemap — graceful partial launch, D-08). This single commit is the audit trail (D-06) — use a clear message naming the published set.
    Then update `scripts/assert-seo.ts` so it stays honest with the new reality:
    - Replace the hardcoded `["/", "/contact", "/over-ons", "/tarieven"]` indexable-set assertion (26-30) with one that derives the EXPECTED indexable set from `PAGES.filter(isIndexable)` and asserts it equals the statics-plus-published-set (or, more robustly, assert every `published` pillar/service IS indexable and every `draft`/`review` node is NOT — keyed on status, not a frozen list).
    - Update the sitemap-count assertion (42-43) from `=== 4` to match the published set size (or derive it from `PAGES.filter(isIndexable).length`).
    - Update the noindex-set assertions (33-39): keep privacy-beleid noindex; the draft-service example must point at a node that is STILL draft/review (or assert the rule by status rather than a specific now-published node).
    Keep the geoRadius / canonical-origin / aggregateRating(conditional, from 04-02) assertions intact. Do NOT run locally (OneDrive) — the final build validates on Vercel.
  </action>
  <verify>
    <automated>npx tsx -e "import { PAGES } from './lib/services/registry.ts'; import { isIndexable, sitemapEntries } from './lib/seo/policy.ts'; const pub=PAGES.filter(n=>n.status==='published'); const idx=PAGES.filter(isIndexable); for(const n of PAGES){ if((n.type==='pillar'||n.type==='service'||n.type==='hub')){ const should=n.status==='published'; if(isIndexable(n)!==should){console.error('index lever drift: '+n.primaryKeyword+' status='+n.status+' indexable='+isIndexable(n));process.exit(1)} } } if(sitemapEntries().length!==idx.length){console.error('sitemap count != indexable count');process.exit(1)} console.log('index lever OK — '+pub.length+' published, '+idx.length+' indexable, sitemap aligned')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
    <automated>node -e "const s=require('fs').readFileSync('scripts/assert-seo.ts','utf8'); if(/\[\s*\x22\/\x22,\s*\x22\/contact\x22,\s*\x22\/over-ons\x22,\s*\x22\/tarieven\x22\s*\]/.test(s)){console.error('assert-seo still hardcodes the 4-static indexable set — must be status-derived now');process.exit(1)} console.log('assert-seo updated for published set OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Every owner-approved pillar/service (and the hub, if approved) is `status: "published"`; non-approved nodes remain `review` (noindex).
    - `isIndexable` returns true for exactly the published pillar/service/hub set + the statics (minus privacy-beleid); `sitemapEntries().length` equals the indexable count.
    - `scripts/assert-seo.ts` no longer hardcodes the 4-static set — it derives indexability/sitemap-count by status.
    - The status-flip commit names the published set (the D-06 audit trail).
  </acceptance_criteria>
  <done>The approved set is published, the index lever is pulled cleanly, assert-seo matches reality, and the final build is GREEN on Vercel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| review → published (index lever) | The status flip admits pages to the index + sitemap; publishing an un-reviewed or false-claim page is the terminal risk |
| owner-returned data → ungated claims | Intake data (dealer status, cert proof, Google rating) crosses from form to live trust copy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Elevation / Repudiation (editorial integrity) | A page reaching `published` without owner sign-off | mitigate | Only the explicitly-approved set is flipped (Task 2 records it); the status-flip commit is the audit trail (D-06); non-approved nodes stay noindex |
| T-04-03 | Spoofing (trust) | Activating `aggregateRating` / dealer / cert without real proof | mitigate | Task 1 sets these ONLY from returned proof/data; un-returned slots stay gated; checklist applied |
| T-04-15 | Tampering (SEO integrity) | `assert-seo.ts` left asserting the stale "4 statics" set → CI red OR a silent index drift | mitigate | Task 3 rewrites the assertions to be status-derived; the index-lever verify proves isIndexable matches status |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- The index-lever invariant (isIndexable == status, sitemap count == indexable count) and the assert-seo-not-hardcoded check via `npx tsx`/Node (run on Vercel CI if OneDrive deadlocks local tsx).
- The whole-site review is a human-action gate on the Vercel preview deploy (D-05/D-07) — no CLI substitute for owner sign-off.
- FINAL whole-phase build GREEN ON VERCEL preview/CI (prebuild Zod gate + assert-no-forbidden-claims + assert-seo all pass) — never run locally (OneDrive deadlock). Optional fallback: copy the repo to /tmp for a local build if a pre-push sanity check is wanted.
</verification>

<success_criteria>
- Owner-returned data is mapped into the gated slots (or left gated if not returned); the owner has done one whole-site review; the approved set is published (status flip = audit trail); `assert-seo.ts` is status-derived; the final Vercel build is green. No page is live without editorial approval (CONT-10).
</success_criteria>

<output>
Create `.planning/phases/04-content-fill-editorial-gate/04-09-SUMMARY.md` when done.
</output>
