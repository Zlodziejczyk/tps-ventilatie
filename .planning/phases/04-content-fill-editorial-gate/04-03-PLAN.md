---
phase: 04-content-fill-editorial-gate
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/services/airconditioning.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-04, CONT-09]
must_haves:
  truths:
    - "The airconditioning pillar page and all 4 sub-service pages have a unique intro >=120 words, >=1 service-specific step, and 3-6 distinct FAQs"
    - "No airconditioning node claims any ISDE/subsidie (airco = koeling = geen subsidie); the airco-no-subsidy fact is stated where users would ask"
    - "Installatie copy names the brands TPS installs (Daikin/Mitsubishi Electric/Heavy) as installed equipment, with NO dealer/'erkend installateur' claim"
    - "All 5 airconditioning nodes are flipped draft -> review after clearing the Zod gate + the D-13 checklist"
  artifacts:
    - path: "lib/services/airconditioning.ts"
      provides: "Filled ContentShell for 1 pillar + 4 sub-service nodes; all status:review"
      contains: "faqs"
  key_links:
    - from: "airconditioning.ts faqs[]"
      to: "ServiceFAQ + FAQPage JSON-LD"
      via: "Phase 2/3 components read content.faqs"
      pattern: "question"
---

<objective>
Fill the complete Airconditioning silo (pillar + 4 sub-services: installatie, onderhoud, reparatie-storing, advies) with unique Dutch copy per D-03 (complete topical silo) and D-15 (per-page intent angle). Each node gets a ≥120-word intro, ≥1 service-specific step, 3-6 distinct FAQs (D-16 allocation), and a regio `localAngle`. The defining YMYL fact for this pillar: airconditioning is cooling → NO ISDE/subsidie (CONT-04, D-13) — stated plainly where users ask. Installatie carries brand install content (CONT-03) naming Daikin/Mitsubishi Electric/Heavy as installed equipment without a dealer claim. Implements CONT-01/02/04/09; honors D-01/D-02/D-08/D-14/D-15/D-16/D-20.

Purpose: A complete, unique, accurate airco silo that clears the build-enforced uniqueness bar and the D-13 anti-claim gate, ready for owner review.
Output: filled `lib/services/airconditioning.ts` with all 5 nodes at `status: "review"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md
@.planning/phases/04-content-fill-editorial-gate/04-RESEARCH.md
@.planning/phases/04-content-fill-editorial-gate/04-PATTERNS.md
@docs/anti-claim-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill the airconditioning PILLAR node + widen draftShell for faqs</name>
  <files>lib/services/airconditioning.ts</files>
  <read_first>
    - lib/services/airconditioning.ts (full — the pillar node 31-50 with its partial intro at 46-48; the local `draftShell` 13-28 that accepts only `{ intro?; localAngle? }` — NO `steps`/`faqs` param yet)
    - lib/services/types.ts:31-50 (`FaqItem`, `StepItem`, `ContentShell` shape) + :144-167 (`publishedContentSchema` — the ≥120w / ≥1 step / 3-6 faq gate that bites at review/published)
    - lib/services/wtw.ts:31-46 (the WIDEST `draftShell` to mirror — accepts `{ intro?; steps?; localAngle? }`; widen airco's to also accept `faqs?`) and :13-29 (the `const X_STEPS: StepItem[]` named-const pattern)
    - docs/anti-claim-checklist.md (the pre-review gate — airco section: NO ISDE/subsidie anywhere)
    - 04-RESEARCH.md §"Airconditioning — ISDE: NO" (286-290 — the exact honest phrasing "Voor airconditioning (koeling) is geen ISDE-subsidie beschikbaar.") + §"Architecture Patterns" Pattern 2 angle map (196-209) + Pattern 3 FAQ allocation (211-224)
    - lib/constants.ts SITE.serviceAreas (the regio names for localAngle — Zoetermeer + the 60km towns; never city pages, D-15)
  </read_first>
  <action>
    First widen the local `draftShell` in `airconditioning.ts` (13-28) to accept `extra?: { intro?: string; steps?: StepItem[]; faqs?: FaqItem[]; localAngle?: string }` and default `steps: extra?.steps ?? []`, `faqs: extra?.faqs ?? []` (follow wtw.ts:31-46; add `FaqItem`/`StepItem` to the `import type` from "./types"). Keep `draftShell` local (no shared helper — the established convention).
    Then fill the PILLAR node (slug "airconditioning"): EXPAND the intro (currently ~35 words at 46-48) to ≥120 unique words — airco as year-round comfort (koelen in zomer, verwarmen in winter via warmtepomp-airco), the 4 services overview, the brands TPS installs (Daikin, Mitsubishi Electric, Mitsubishi Heavy — installed equipment, NOT a dealer claim), and a Zoetermeer + regio framing; formal `u` voice (D-14); interim "vakkundige"/"ervaren" never "gecertificeerd" (D-02). Add ≥1 pillar-level `step` (e.g. the typical traject: opname → advies → installatie → service). Add 3-6 DECISION-level FAQs (D-16 pillar allocation): include the airco-subsidie question answered honestly ("Krijg ik subsidie voor airconditioning?" → "Voor airconditioning (koeling) is er geen ISDE-subsidie; die geldt voor warmtepompen. Een airco die óók verwarmt kan wel energiezuinig zijn — vraag ons gerust om advies."), plus geluid, kosten-indicatie (route to /tarieven, don't duplicate pricing), and garantie. Set a `localAngle` with Zoetermeer + regio signals from SITE.serviceAreas (no city pages). Light D-20 cross-sell where natural (e.g. one sentence linking to warmtepompen for heating interest) IN the intro or an FAQ answer — note in a code comment that `relatedOverride` does not exist (locked infra), so cross-sell is in-copy only. Do NOT add euro amounts; do NOT claim ISDE for airco anywhere.
  </action>
  <verify>
    <automated>npx tsx -e "import { AIRCONDITIONING_PAGES } from './lib/services/airconditioning.ts'; const p=AIRCONDITIONING_PAGES.find(n=>n.type==='pillar'); const w=p.content.intro.trim().split(/\s+/).filter(Boolean).length; const f=p.content.faqs.length; const s=p.content.steps.length; if(w<120||s<1||f<3||f>6){console.error('pillar gate fail: words='+w+' steps='+s+' faqs='+f);process.exit(1)} const blob=JSON.stringify(p.content).toLowerCase(); if(/isde|subsidie/.test(blob)&&!/geen\s+isde|geen\s+subsidie/.test(blob)){console.error('airco pillar mentions subsidie without the geen-disclaimer');process.exit(1)} console.log('airco pillar OK w='+w+' s='+s+' f='+f)" 2>&1 || echo "NOTE: if tsx deadlocks on OneDrive, validate on Vercel CI instead"</automated>
  </verify>
  <acceptance_criteria>
    - `draftShell` in airconditioning.ts accepts `faqs?` (and `steps?`); imports `FaqItem`/`StepItem`.
    - The pillar node intro is ≥120 words, has ≥1 step and 3-6 FAQs.
    - The pillar content contains no bare ISDE/subsidie claim for airco — any subsidie mention is the explicit "geen ISDE-subsidie voor koeling" framing.
    - Brands are named as installed equipment only; no "erkend installateur"/dealer/"gecertificeerd" string.
  </acceptance_criteria>
  <done>The airco pillar node clears the Zod gate and the airco-subsidie check; brand mentions are install-context only.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the 4 airconditioning sub-service nodes and flip the silo to review</name>
  <files>lib/services/airconditioning.ts</files>
  <read_first>
    - lib/services/airconditioning.ts (the 4 service nodes: installatie 51-75 with brandIds at 65, onderhoud 76-96, reparatie-storing 97-117, advies 118-138 — each has a ~1-sentence intro to EXPAND)
    - lib/services/wtw.ts:13-29 (the `WTW_REPLACEMENT_STEPS` named-const pattern to mirror for service-specific steps) and the filled wtw service nodes 69-128 (reference shape for steps usage)
    - docs/anti-claim-checklist.md (per-node pre-review gate)
    - 04-RESEARCH.md Pattern 2 angle map (196-209): Installatie=proces/merken/site survey/single-vs-multi-split; Onderhoud=preventie/checklist/filters/frequentie; Storing=symptomen(lekkage/lawaai/niet koelen)/spoed/diagnose; Advies=keuze/onafhankelijk/capaciteit. Pattern 3 FAQ allocation (211-224): sub-service = TASK FAQs.
  </read_first>
  <action>
    For EACH of the 4 service nodes write a unique ≥120-word intro leading with its own intent angle (D-15), ≥1 service-specific `step` (declare per-service `const X_STEPS: StepItem[]` named consts above the array, wtw.ts style — e.g. installatie steps: opname/positiebepaling → montage binnen+buitenunit → leidingwerk+vacuümtrekken → testen+oplevering), and 3-6 distinct TASK FAQs (no FAQ duplicated across nodes or with the pillar — D-16 one-canonical-home). Concrete, service-specific detail, never swapped boilerplate (the test: swapping two H1s must NOT make pages indistinguishable):
    - installatie: proces + Single/Multi-split + brands (Daikin/Mitsubishi as installed equipment, NO dealer claim) + site survey; TASK FAQs (hoe lang duurt een installatie, single vs multi-split, binnen-/buitenunit plaatsing, geluid buitenunit). Brand install content here satisfies CONT-03 in rendered copy.
    - onderhoud: preventie + checklist (filters reinigen, druk/koudemiddel controleren, condensafvoer) + frequentie + energie/hygiëne; TASK FAQs (hoe vaak onderhoud, wat zit erin, zelf doen vs vakman).
    - reparatie-storing: snelheid + symptomen (lekkage, lawaai, niet koelen, foutcode) + spoed + diagnose; TASK FAQs (airco koelt niet — oorzaken, wat kost een reparatie → route to consult/tarieven, hoe snel kunt u komen).
    - advies: onafhankelijke keuze + capaciteitsberekening + vrijblijvende opname; TASK FAQs (welke airco voor mijn situatie, airco of warmtepomp → light D-20 cross-sell to warmtepompen, capaciteit bepalen). NO ISDE for airco anywhere; route any euro amounts to a consult; formal `u`; interim "vakkundig"/"ervaren".
    After all 5 nodes (pillar from Task 1 + these 4) clear the Zod gate and you have applied `docs/anti-claim-checklist.md`, flip every node's `status` from `"draft"` to `"review"` in this file (D-08/D-09 — review = noindex, safe to sit; the prebuild gate + assert-no-forbidden-claims run on the next Vercel deploy).
  </action>
  <verify>
    <automated>npx tsx -e "import { AIRCONDITIONING_PAGES as P } from './lib/services/airconditioning.ts'; let bad=[]; for(const n of P){const c=n.content; const w=c.intro.trim().split(/\s+/).filter(Boolean).length; if(w<120||c.steps.length<1||c.faqs.length<3||c.faqs.length>6) bad.push(n.primaryKeyword+' (w='+w+' s='+c.steps.length+' f='+c.faqs.length+')'); if(n.status!=='review') bad.push(n.primaryKeyword+' status='+n.status);} const allFaqs=P.flatMap(n=>n.content.faqs.map(f=>f.question.toLowerCase().trim())); const dup=allFaqs.filter((q,i)=>allFaqs.indexOf(q)!==i); if(dup.length) bad.push('dup FAQ: '+dup.join(' | ')); if(bad.length){console.error('FAIL: '+bad.join('; '));process.exit(1)} console.log('airco silo OK — 5 nodes review, gate passed, no dup FAQ')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
    <automated>node -e "const s=require('fs').readFileSync('lib/services/airconditioning.ts','utf8').toLowerCase(); if(/erkend\s+installateur|gecertificeerd/.test(s)){console.error('forbidden claim in airconditioning.ts');process.exit(1)} if(/\b6\s*%\s*btw/.test(s)){console.error('Belgian 6% btw literal present');process.exit(1)} console.log('airco anti-claim grep OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 airconditioning nodes: intro ≥120 words, ≥1 step, 3-6 FAQs; every node `status: "review"`.
    - No FAQ question is duplicated across the 5 nodes (one canonical home, D-16).
    - `grep -iE "erkend installateur|gecertificeerd|6 *% *btw" lib/services/airconditioning.ts` returns nothing.
    - No ISDE/subsidie claim for airco (only the explicit "geen subsidie" framing if mentioned).
  </acceptance_criteria>
  <done>Both verify commands print OK (or the build is GREEN on Vercel CI if tsx deadlocks locally); the airco silo is complete, unique, anti-claim-clean, and at `review`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored airco content → published YMYL page | A false subsidie/dealer/cert claim on airco pages crosses into the live site at the status flip |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering (YMYL integrity) | An ISDE/subsidie claim wrongly applied to airco (cooling) | mitigate | The airco-no-subsidy fact is the authored default; `docs/anti-claim-checklist.md` + the per-pillar verify check; airco names no euro amounts |
| T-04-07 | Spoofing (trust) | Brand names read as a dealer/"erkend" endorsement | mitigate | Brands named as installed equipment only; `erkendInstallateur` stays false (brands.ts, 04-07); grep gate catches "erkend installateur"/"gecertificeerd" |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Per-node Zod-gate + no-dup-FAQ checks via `npx tsx` (run on Vercel CI if OneDrive deadlocks the local tsx).
- Anti-claim grep over the file is a pure Node read (OneDrive-safe).
- The status flip to `review` is validated by the prebuild Zod gate + `scripts/assert-no-forbidden-claims.ts` on the next Vercel preview deploy; whole-phase build GREEN ON VERCEL (never locally).
</verification>

<success_criteria>
- The airconditioning silo (5 nodes) has unique ≥120-word intros, service-specific steps, 3-6 distinct FAQs, regio localAngle, brand install content (no dealer claim), and the airco-no-subsidy fact.
- All 5 nodes are at `status: "review"` and pass the build-enforced uniqueness + anti-claim gates.
</success_criteria>

<output>
Create `.planning/phases/04-content-fill-editorial-gate/04-03-SUMMARY.md` when done.
</output>
