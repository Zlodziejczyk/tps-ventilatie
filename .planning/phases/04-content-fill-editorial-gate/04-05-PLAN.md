---
phase: 04-content-fill-editorial-gate
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/services/wtw.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-04, CONT-09]
must_haves:
  truths:
    - "The live D-02/D-13 violation at wtw.ts:65 ('gecertificeerde monteurs') is replaced with literally-true interim wording ('vakkundige'/'ervaren')"
    - "The WTW pillar + all 5 sub-service pages have a unique intro >=120 words (ported ~55w intros EXPANDED), >=1 step, and 3-6 distinct FAQs (currently ZERO FAQs anywhere)"
    - "ISDE/subsidie content is accurate (WTW = yes from 2026 ONLY with the isolatiemaatregel precondition + rendement/capaciteit thresholds), cites the official rvo.nl ventilatie URL, amounts routed to a consult"
    - "All 6 WTW nodes are flipped draft -> review after clearing the Zod gate + the D-13 checklist"
  artifacts:
    - path: "lib/services/wtw.ts"
      provides: "Filled/expanded ContentShell for 1 pillar + 5 sub-service nodes; all status:review; no 'gecertificeerd'"
      contains: "isolatie"
  key_links:
    - from: "wtw.ts ISDE copy"
      to: "official RVO ventilatie source"
      via: "cited rvo.nl ventilatie URL + isolatie precondition"
      pattern: "rvo\\.nl"
---

<objective>
Complete the WTW silo (pillar + 5 sub-services: vervangen, onderhoud-reinigen, inregelen, storing, aanleggen). WTW is PARTIALLY ported: some intros are ~55 words (BELOW the 120w gate) and NO node has any FAQs. First fix the live D-02/D-13 violation at wtw.ts:65 ("gecertificeerde monteurs" → "vakkundige"/"ervaren"). Then EXPAND every intro to ≥120 words, add ≥1 step where missing, and add 3-6 distinct FAQs to every node (all net-new). The ISDE story (CONT-04, D-10) is the genuinely-new 2026 differentiator: WTW qualifies from 2026 ONLY in combination with an isolatiemaatregel, plus rendement/capaciteit thresholds — state conditions, cite the rvo.nl ventilatie URL, route amounts to a consult. Implements CONT-01/02/04/09; honors D-01/D-02/D-08/D-10/D-14/D-15/D-16/D-20.

Purpose: Turn the half-ported WTW pages into a complete, unique, accurate silo with the violation fixed — clearing the build-enforced gates, ready for owner review.
Output: filled/expanded `lib/services/wtw.ts` with all 6 nodes at `status: "review"` and no "gecertificeerd" string.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md
@.planning/phases/04-content-fill-editorial-gate/04-RESEARCH.md
@.planning/phases/04-content-fill-editorial-gate/04-PATTERNS.md
@docs/anti-claim-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the wtw.ts:65 violation, expand the pillar + add pillar FAQs/ISDE, widen draftShell for faqs</name>
  <files>lib/services/wtw.ts</files>
  <read_first>
    - lib/services/wtw.ts (full — pillar 48-68 with the VIOLATION at 65 ("gecertificeerde monteurs") and a ~55w intro to EXPAND; the local `draftShell` 31-46 accepts `{ intro?; steps?; localAngle? }` — NO `faqs?` yet; `WTW_REPLACEMENT_STEPS` 13-29)
    - lib/services/types.ts:31-50 + :144-167 (the gate)
    - docs/anti-claim-checklist.md (WTW ISDE rule: yes-2026 + isolatie precondition; never blanket "ventilatie is gesubsidieerd")
    - 04-RESEARCH.md §"WTW / Balansventilatie — ISDE: YES from 2026" (262-273 — centrale WTW min rendement 85% + capaciteit 125 m³/h; decentrale 80% + 80 m³/h; **THE insulation precondition** "gecombineerd met een of meer isolatiemaatregelen", aanvraag binnen 24 maanden na de isolatiemaatregel, bouwjaar vóór 1-1-2019; **no euro amounts in copy**) + cite URL https://www.rvo.nl/subsidies-financiering/isde/woningeigenaren/ventilatie
    - 04-PATTERNS.md §"D-13 fix required (wtw.ts:65)" (84) + Pattern 1 (181-194)
    - lib/constants.ts SITE.serviceAreas
  </read_first>
  <action>
    Widen the local `draftShell` (31-46) to add `faqs?: FaqItem[]` to its `extra` param (default `faqs: extra?.faqs ?? []`; add `FaqItem` to the `import type` from "./types"). 
    FIX THE VIOLATION: in the pillar intro (65), replace "gecertificeerde monteurs" with "vakkundige monteurs" (or "ervaren monteurs") — literally true until cert proof (D-02/D-13). While editing, EXPAND that pillar intro from ~55 to ≥120 unique words: WTW/warmteterugwinning explained (tot ~95% warmteterugwinning, gezonde lucht + energiebesparing, vochtbalans), the full service set (vervangen/onderhoud/inregelen/storing/aanleggen), Zoetermeer + regio framing; formal `u` (D-14). Add ≥1 pillar-level step (e.g. opname → advies → uitvoering → inregelen+oplevering) — distinct from the per-service `WTW_REPLACEMENT_STEPS`. Add 3-6 DECISION FAQs (D-16): the WTW-subsidie question is the canonical home HERE — answer accurately: WTW kan vanaf 2026 in aanmerking komen voor ISDE, MAAR alleen in combinatie met een isolatiemaatregel en bij de juiste rendement/capaciteit; cite the rvo.nl ventilatie URL literally; route amounts to a consult. Plus geluid (fluisterstil/dB), levensduur (15-20 jaar), and luchtkwaliteit/gezondheid. Set `localAngle` (Zoetermeer + regio, no city pages). Light D-20 cross-sell in-copy only (note `relatedOverride` absent — locked infra). NEVER state a euro bedrag; NEVER claim a blanket "ventilatie is gesubsidieerd" (must pair with isolatie); NO "gecertificeerd"/"erkend installateur".
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('lib/services/wtw.ts','utf8').toLowerCase(); if(/gecertificeerd/.test(s)){console.error('wtw.ts STILL contains gecertificeerd (the :65 violation must be fixed)');process.exit(1)} console.log('wtw.ts gecertificeerd removed OK')"</automated>
    <automated>npx tsx -e "import { WTW_PAGES as P } from './lib/services/wtw.ts'; const p=P.find(n=>n.type==='pillar'); const c=p.content; const w=c.intro.trim().split(/\s+/).filter(Boolean).length; if(w<120||c.steps.length<1||c.faqs.length<3||c.faqs.length>6){console.error('pillar gate fail w='+w+' s='+c.steps.length+' f='+c.faqs.length);process.exit(1)} const blob=JSON.stringify(c).toLowerCase(); if(!/rvo\.nl/.test(blob)){console.error('WTW ISDE must cite rvo.nl');process.exit(1)} if(/subsidie|isde/.test(blob)&&!/isolatie/.test(blob)){console.error('WTW subsidie mention must include the isolatie precondition');process.exit(1)} console.log('WTW pillar OK w='+w+' cites rvo.nl + isolatie precondition')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -i gecertificeerd lib/services/wtw.ts` returns nothing (the :65 violation is fixed).
    - `draftShell` accepts `faqs?`; imports `FaqItem`.
    - Pillar intro ≥120 words (expanded from ~55), ≥1 step, 3-6 FAQs.
    - The WTW subsidie content cites rvo.nl AND includes the isolatie precondition; no euro amount.
  </acceptance_criteria>
  <done>The violation is gone; the WTW pillar clears the gate with accurate 2026 ISDE (isolatie precondition) citing the RVO ventilatie source.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the 5 WTW sub-service nodes, add FAQs everywhere, flip the silo to review</name>
  <files>lib/services/wtw.ts</files>
  <read_first>
    - lib/services/wtw.ts (the 5 service nodes: vervangen 69-90 (intro ~50w + has WTW_REPLACEMENT_STEPS), onderhoud-reinigen 91-111 (intro present), inregelen 112-128 (EMPTY intro/steps/faqs), storing 129-145 (EMPTY), aanleggen 146-162 (EMPTY)) — NONE have FAQs
    - lib/services/wtw.ts:13-29 (WTW_REPLACEMENT_STEPS — reuse for vervangen; declare new named-const StepItem[] for inregelen/storing/aanleggen)
    - docs/anti-claim-checklist.md
    - 04-RESEARCH.md Pattern 2 angle map (196-209): Vervangen=levensduur(15-20jr)/efficiëntiewinst/sequence; Onderhoud-reinigen=preventie/checklist/filters-ventielen/frequentie; Inregelen=meetrapport/balans/per-ruimte/meetapparatuur; Storing=symptomen(lawaai)/snelheid/diagnose; Aanleggen=nieuwbouw/renovatie/kanaalroutes/bouwbesluit. Pattern 3 (211-224 — sub = TASK FAQs; ISDE canonical home is the pillar)
  </read_first>
  <action>
    For EACH of the 5 service nodes ensure a unique ≥120-word intro (EXPAND vervangen + onderhoud-reinigen which are short; WRITE inregelen/storing/aanleggen from empty), ≥1 service-specific `step` (vervangen reuses `WTW_REPLACEMENT_STEPS`; declare per-service `const X_STEPS: StepItem[]` for inregelen (meten → ventielen instellen per ruimte → meetrapport), storing (diagnose → onderdeel/oorzaak → herstel → controle), aanleggen (ontwerp/kanaalplan → kanalen plaatsen → unit monteren → inregelen+oplevering)), and 3-6 distinct TASK FAQs (no duplication across nodes or with the pillar — D-16; the subsidie DECISION FAQ lives on the pillar — aanleggen/vervangen may link to it, not restate it):
    - vervangen: levensduur (15-20 jaar) + efficiëntiewinst + de vervang-sequence; TASK FAQs (hoe lang duurt vervangen, wanneer vervangen vs onderhoud, wat kost het → consult/tarieven).
    - onderhoud-reinigen: preventie + checklist (filters, ventilatoren, kanalen, ventielen) + frequentie + hygiëne; TASK FAQs (hoe vaak onderhoud, wat wordt gereinigd, zelf filters vervangen).
    - inregelen: meetrapport + per-ruimte balans + meetapparatuur + waarom het ertoe doet; TASK FAQs (wat is inregelen, waarom een meetrapport, hoe vaak opnieuw inregelen).
    - storing: symptomen (lawaai, trillen, niet afzuigen) + snelheid + diagnose; TASK FAQs (wtw maakt lawaai — oorzaken, hoe snel kunt u komen, wat kost storingsbezoek → consult).
    - aanleggen: nieuwbouw/renovatie + kanaalroutes + bouwbesluit-normen + when it applies; TASK FAQs (balansventilatie in bestaande woning, hoe lang duurt aanleg, ISDE bij aanleg → short pointer to the pillar's full conditions). Formal `u`; interim "vakkundig"/"ervaren"; NO euro amounts; cite rvo.nl only where ISDE conditions are stated.
    After all 6 nodes clear the gate and you have applied `docs/anti-claim-checklist.md`, flip every node `status` "draft" → "review" (D-08/D-09).
  </action>
  <verify>
    <automated>npx tsx -e "import { WTW_PAGES as P } from './lib/services/wtw.ts'; let bad=[]; for(const n of P){const c=n.content; const w=c.intro.trim().split(/\s+/).filter(Boolean).length; if(w<120||c.steps.length<1||c.faqs.length<3||c.faqs.length>6) bad.push(n.primaryKeyword+' (w='+w+' s='+c.steps.length+' f='+c.faqs.length+')'); if(n.status!=='review') bad.push(n.primaryKeyword+' status='+n.status);} const allFaqs=P.flatMap(n=>n.content.faqs.map(f=>f.question.toLowerCase().trim())); const dup=allFaqs.filter((q,i)=>allFaqs.indexOf(q)!==i); if(dup.length) bad.push('dup FAQ: '+dup.join(' | ')); if(bad.length){console.error('FAIL: '+bad.join('; '));process.exit(1)} console.log('WTW silo OK — 6 nodes review, gate passed, no dup FAQ')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
    <automated>node -e "const s=require('fs').readFileSync('lib/services/wtw.ts','utf8').toLowerCase(); if(/erkend\s+installateur|gecertificeerd/.test(s)){console.error('forbidden claim');process.exit(1)} if(/\b6\s*%\s*btw/.test(s)){console.error('Belgian 6% btw literal');process.exit(1)} console.log('WTW anti-claim grep OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 WTW nodes: intro ≥120 words, ≥1 step, 3-6 FAQs; every node `status: "review"`.
    - No FAQ duplicated across the 6 nodes (the ISDE conditions live once on the pillar).
    - `grep -iE "erkend installateur|gecertificeerd|6 *% *btw" lib/services/wtw.ts` returns nothing.
  </acceptance_criteria>
  <done>Both verify commands print OK (or build GREEN on Vercel CI if tsx deadlocks); the WTW silo is complete, expanded, anti-claim-clean, and at `review`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored WTW/ISDE content → published YMYL page | An over-promised "ventilatie is gesubsidieerd" (without the isolatie precondition) or a surviving "gecertificeerd" claim crosses into the live site |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering (YMYL integrity) | WTW subsidy stated without the mandatory isolatie precondition (over-promise) | mitigate | Conditions sourced from RESEARCH verified table; verify check requires "isolatie" wherever subsidie/isde appears; cite rvo.nl; amounts to consult |
| T-04-05 | Tampering (content integrity) | The live "gecertificeerde monteurs" (wtw.ts:65) reaching a published page | mitigate | THIS plan fixes it → "vakkundige"; both the file grep here AND the 04-01 prebuild grep gate catch any regression |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- The dedicated `grep -i gecertificeerd` check (pure Node read, OneDrive-safe) proves the :65 violation is fixed.
- Per-node Zod-gate + cite-rvo.nl + isolatie-precondition + no-dup-FAQ checks via `npx tsx` (run on Vercel CI if OneDrive deadlocks).
- Status flip validated by the prebuild Zod gate + `assert-no-forbidden-claims.ts` on the next Vercel preview; whole-phase build GREEN ON VERCEL.
</verification>

<success_criteria>
- The wtw.ts:65 "gecertificeerd" violation is fixed; the WTW silo (6 nodes) has unique ≥120-word intros (ported intros expanded), service-specific steps, 3-6 distinct FAQs, regio localAngle, and accurate 2026 ISDE content (isolatie precondition, rvo.nl cite, amounts to consult).
- All 6 nodes at `status: "review"` and passing the build-enforced gates.
</success_criteria>

<output>
Create `.planning/phases/04-content-fill-editorial-gate/04-05-SUMMARY.md` when done.
</output>
