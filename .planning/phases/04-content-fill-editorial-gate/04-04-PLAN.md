---
phase: 04-content-fill-editorial-gate
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/services/warmtepompen.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-04, CONT-09]
must_haves:
  truths:
    - "The warmtepompen pillar page and all 4 sub-service pages (net-new, currently empty) have a unique intro >=120 words, >=1 service-specific step, and 3-6 distinct FAQs"
    - "ISDE/subsidie content is accurate (WP = yes), states the durable conditions, cites the official rvo.nl warmtepomp URL, and routes euro amounts to a free consult (no bedragen in copy)"
    - "Installatie copy names Daikin / Mitsubishi Ecodan as installed equipment with NO dealer/'erkend installateur' claim"
    - "All 5 warmtepompen nodes are flipped draft -> review after clearing the Zod gate + the D-13 checklist"
  artifacts:
    - path: "lib/services/warmtepompen.ts"
      provides: "Filled ContentShell for 1 pillar + 4 sub-service nodes (net-new); all status:review"
      contains: "rvo.nl"
  key_links:
    - from: "warmtepompen.ts ISDE copy"
      to: "official RVO source"
      via: "cited rvo.nl warmtepomp URL in copy"
      pattern: "rvo\\.nl"
---

<objective>
Fill the complete Warmtepompen silo — currently FULLY EMPTY / net-new (the hardest content plan) — with unique Dutch copy: pillar + 4 sub-services (installatie, onderhoud, reparatie-storing, advies). The centerpiece is accurate ISDE content (CONT-04, D-10): WP qualifies → state the durable conditions, cite the official rvo.nl warmtepomp URL, route euro amounts to a free consult. Each node gets a ≥120-word intro, ≥1 service-specific step, 3-6 distinct FAQs (D-16), and a regio `localAngle`. Installatie carries brand install content (CONT-03) for Daikin/Mitsubishi Ecodan without a dealer claim. Implements CONT-01/02/04/09; honors D-01/D-02/D-08/D-10/D-14/D-15/D-16/D-20.

Purpose: A complete, unique, YMYL-accurate warmtepomp silo — the strongest subsidie differentiator — that clears the build-enforced gates, ready for owner review.
Output: filled `lib/services/warmtepompen.ts` with all 5 nodes at `status: "review"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md
@.planning/phases/04-content-fill-editorial-gate/04-RESEARCH.md
@.planning/phases/04-content-fill-editorial-gate/04-PATTERNS.md
@docs/anti-claim-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fill the warmtepompen PILLAR node with accurate ISDE content + widen draftShell</name>
  <files>lib/services/warmtepompen.ts</files>
  <read_first>
    - lib/services/warmtepompen.ts (full — pillar 28-43, all 5 nodes EMPTY; the local `draftShell` 11-25 takes NO `extra` at all (3-arg only) — must be widened most)
    - lib/services/types.ts:31-50 (`FaqItem`/`StepItem`/`ContentShell`) + :144-167 (the ≥120w / ≥1 step / 3-6 faq gate)
    - lib/services/wtw.ts:31-46 (widest `draftShell` to mirror) + :13-29 (named-const `StepItem[]` pattern)
    - docs/anti-claim-checklist.md (pre-review gate — WP ISDE rules)
    - 04-RESEARCH.md §"Warmtepompen — ISDE: YES" (248-260 — the EXACT durable conditions: eligible (hybride) warmtepomp, bouwjaar vóór 1-1-2019 OR omgevingsvergunning vóór 1-7-2018, volledige installatie door installatiebedrijf, aanvraag binnen 24 maanden, nieuw + meldcodelijst; the 2026 split-restriction; **DO NOT put euro amounts in copy**) and the cite URL https://www.rvo.nl/subsidies-financiering/isde/woningeigenaren/warmtepomp + §"Pattern 1" illustrative copy (181-194)
    - lib/constants.ts SITE.serviceAreas (regio names for localAngle)
  </read_first>
  <action>
    Widen the local `draftShell` (11-25) to a 4th arg `extra?: { intro?: string; steps?: StepItem[]; faqs?: FaqItem[]; localAngle?: string }` defaulting `steps`/`faqs` to `[]` (mirror wtw.ts:31-46; add `FaqItem`/`StepItem` to the `import type` from "./types"). Keep it local.
    Fill the PILLAR node (slug "warmtepompen", intro currently ""): write a ≥120-word unique intro — warmtepomp as duurzame verwarming (lucht/water + hybride), how it works at a high level, energiebesparing vs gas, the brands TPS installs (Daikin, Mitsubishi Ecodan — installed equipment, NOT a dealer claim), Zoetermeer + regio framing, formal `u` (D-14), interim "vakkundige"/"ervaren" (D-02). Add ≥1 pillar-level step (traject: opname/warmteverliesberekening → advies+offerte → installatie → inbedrijfstelling+service). Add 3-6 DECISION FAQs (D-16): the ISDE-subsidie question is the canonical home HERE — answer with the durable conditions in prose + cite the rvo.nl warmtepomp URL literally in the answer string + route amounts to a consult ("De exacte bedragen verschillen per vermogen en type — vraag ons vrijblijvend om een actuele berekening."); plus geluid (buitenunit dB + plaatsing/VvE), hybride-vs-volledig, and garantie/levensduur. Set `localAngle` with Zoetermeer + regio (no city pages). Light D-20 cross-sell where natural (airco↔warmtepomp) in-copy only (note in a comment: `relatedOverride` does not exist — locked infra — so cross-sell is in `intro`/`faqs`). NEVER state a euro bedrag; NEVER claim a dealer/"erkend"/"gecertificeerd" status.
  </action>
  <verify>
    <automated>npx tsx -e "import { WARMTEPOMPEN_PAGES as P } from './lib/services/warmtepompen.ts'; const p=P.find(n=>n.type==='pillar'); const c=p.content; const w=c.intro.trim().split(/\s+/).filter(Boolean).length; if(w<120||c.steps.length<1||c.faqs.length<3||c.faqs.length>6){console.error('pillar gate fail w='+w+' s='+c.steps.length+' f='+c.faqs.length);process.exit(1)} const blob=JSON.stringify(c).toLowerCase(); if(!/rvo\.nl/.test(blob)){console.error('WP ISDE copy must cite rvo.nl');process.exit(1)} if(/€\s*\d|\d+\s*euro/.test(blob)){console.error('euro amount found — route to consult per D-10');process.exit(1)} console.log('WP pillar OK w='+w+' cites rvo.nl, no euro amount')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
  </verify>
  <acceptance_criteria>
    - `draftShell` widened to accept `steps?`/`faqs?`; imports `FaqItem`/`StepItem`.
    - Pillar intro ≥120 words, ≥1 step, 3-6 FAQs.
    - The ISDE FAQ/intro cites `rvo.nl` (the warmtepomp URL) and contains NO euro amount.
    - Brands named as installed equipment only; no "erkend installateur"/"gecertificeerd".
  </acceptance_criteria>
  <done>The WP pillar clears the gate, cites the official RVO source, and routes amounts to a consult.</done>
</task>

<task type="auto">
  <name>Task 2: Fill the 4 warmtepompen sub-service nodes and flip the silo to review</name>
  <files>lib/services/warmtepompen.ts</files>
  <read_first>
    - lib/services/warmtepompen.ts (the 4 EMPTY service nodes: installatie 44-64 with brandIds at 58, onderhoud 65-84, reparatie-storing 85-101, advies 102-118)
    - lib/services/wtw.ts:13-29 (named-const `StepItem[]` pattern)
    - docs/anti-claim-checklist.md (per-node gate)
    - 04-RESEARCH.md Pattern 2 angle map (196-209) + Pattern 3 FAQ allocation (211-224 — sub-service = TASK FAQs; ISDE canonical home is the pillar, so installatie/advies LINK to it, don't re-answer it verbatim)
  </read_first>
  <action>
    For EACH of the 4 service nodes write a unique ≥120-word intro leading with its intent angle (D-15), ≥1 service-specific `step` (per-service `const X_STEPS: StepItem[]` named consts, wtw.ts style), and 3-6 distinct TASK FAQs (no duplication across nodes or with the pillar — D-16 one canonical home; the subsidie DECISION FAQ lives on the pillar, so installatie/advies may briefly reference ISDE-eligibility and link to the pillar rather than restating conditions):
    - installatie: proces + brands (Daikin/Mitsubishi Ecodan as installed equipment, NO dealer claim) + warmteverliesberekening + ISDE-eligibility pointer (link to pillar, don't duplicate the full conditions); TASK FAQs (hoe lang duurt de installatie, lucht/water vs hybride keuze, buitenunit plaatsing/geluid). Brand install content here satisfies CONT-03 in rendered copy.
    - onderhoud: preventie + jaarlijkse checklist (koudemiddel/druk, filters, software) + waarom onderhoud rendement behoudt; TASK FAQs (hoe vaak, wat zit erin, onderhoudscontract — mention as a service/FAQ topic only, NOT a tiered offering per v2 scope).
    - reparatie-storing: snelheid + symptomen (geen warmte, foutcode, lekkage, lawaai) + diagnose + spoed; TASK FAQs (warmtepomp geeft geen warmte — oorzaken, wat kost een reparatie → consult, hoe snel kunt u komen).
    - advies: onafhankelijke keuze + woninggeschiktheid/isolatie + capaciteit + vrijblijvende opname; TASK FAQs (welke warmtepomp past bij mijn woning, is mijn woning geschikt/isolatie, kom ik in aanmerking voor ISDE → short yes + link to the pillar's full conditions). Formal `u`; interim "vakkundig"/"ervaren"; NO euro amounts (route to consult); cite rvo.nl only where ISDE conditions are actually stated.
    After all 5 nodes clear the Zod gate and you have applied `docs/anti-claim-checklist.md`, flip every node `status` "draft" → "review" (D-08/D-09).
  </action>
  <verify>
    <automated>npx tsx -e "import { WARMTEPOMPEN_PAGES as P } from './lib/services/warmtepompen.ts'; let bad=[]; for(const n of P){const c=n.content; const w=c.intro.trim().split(/\s+/).filter(Boolean).length; if(w<120||c.steps.length<1||c.faqs.length<3||c.faqs.length>6) bad.push(n.primaryKeyword+' (w='+w+' s='+c.steps.length+' f='+c.faqs.length+')'); if(n.status!=='review') bad.push(n.primaryKeyword+' status='+n.status);} const allFaqs=P.flatMap(n=>n.content.faqs.map(f=>f.question.toLowerCase().trim())); const dup=allFaqs.filter((q,i)=>allFaqs.indexOf(q)!==i); if(dup.length) bad.push('dup FAQ: '+dup.join(' | ')); if(bad.length){console.error('FAIL: '+bad.join('; '));process.exit(1)} console.log('WP silo OK — 5 nodes review, gate passed, no dup FAQ')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
    <automated>node -e "const s=require('fs').readFileSync('lib/services/warmtepompen.ts','utf8').toLowerCase(); if(/erkend\s+installateur|gecertificeerd/.test(s)){console.error('forbidden claim');process.exit(1)} if(/\b6\s*%\s*btw/.test(s)){console.error('Belgian 6% btw literal');process.exit(1)} if(/€\s*\d|\b\d{3,}\s*euro/.test(s)){console.error('euro amount in copy — route to consult');process.exit(1)} console.log('WP anti-claim + no-amount grep OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 warmtepompen nodes: intro ≥120 words, ≥1 step, 3-6 FAQs; every node `status: "review"`.
    - No FAQ duplicated across the 5 nodes (one canonical home; the ISDE conditions live once on the pillar).
    - `grep -iE "erkend installateur|gecertificeerd|6 *% *btw" lib/services/warmtepompen.ts` returns nothing; no euro amount appears.
    - The ISDE conditions cite rvo.nl and route amounts to a consult.
  </acceptance_criteria>
  <done>Both verify commands print OK (or build GREEN on Vercel CI if tsx deadlocks); the WP silo is complete, accurate, anti-claim-clean, and at `review`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored WP/ISDE content → published YMYL page | An inaccurate subsidie condition or a stated euro bedrag crosses into the live site at the status flip (the highest-stakes YMYL surface this phase) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08 | Tampering (YMYL integrity) | Wrong/outdated ISDE condition or a volatile euro amount stated as fact | mitigate | Durable conditions only (RESEARCH verified-facts table), cite the live rvo.nl URL so readers always reach current detail, amounts routed to a consult (D-10); verify checks for rvo.nl + no euro amount; owner sign-off (04-09) |
| T-04-07 | Spoofing (trust) | Brand names read as a dealer/"erkend" endorsement | mitigate | Brands as installed equipment only; `erkendInstallateur` false; grep gate catches forbidden strings |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Per-node Zod-gate + cite-rvo.nl + no-euro-amount + no-dup-FAQ checks via `npx tsx` (run on Vercel CI if OneDrive deadlocks local tsx).
- Anti-claim + no-amount grep is a pure Node read (OneDrive-safe).
- Status flip validated by the prebuild Zod gate + `assert-no-forbidden-claims.ts` on the next Vercel preview; whole-phase build GREEN ON VERCEL.
</verification>

<success_criteria>
- The warmtepompen silo (5 net-new nodes) has unique ≥120-word intros, service-specific steps, 3-6 distinct FAQs, regio localAngle, brand install content (no dealer claim), and accurate ISDE content citing rvo.nl with amounts routed to a consult.
- All 5 nodes at `status: "review"` and passing the build-enforced uniqueness + anti-claim gates.
</success_criteria>

<output>
Create `.planning/phases/04-content-fill-editorial-gate/04-04-SUMMARY.md` when done.
</output>
