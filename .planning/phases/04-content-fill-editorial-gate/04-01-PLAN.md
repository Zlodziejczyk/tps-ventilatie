---
phase: 04-content-fill-editorial-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/assert-no-forbidden-claims.ts
  - package.json
  - docs/anti-claim-checklist.md
autonomous: true
requirements: [CONT-03, CONT-05, CONT-06]
must_haves:
  truths:
    - "A build run fails (exit 1) if any review/published node's content contains a D-13 forbidden literal (Belgian 6% btw, erkend installateur, gecertificeerd-before-proof)"
    - "The forbidden-claims script runs as part of prebuild, so it blocks on Vercel CI alongside validate-taxonomy"
    - "A written D-13 anti-claim checklist exists that Claude applies to every node before flipping it to review"
  artifacts:
    - path: "scripts/assert-no-forbidden-claims.ts"
      provides: "D-13 anti-claim grep gate over serialized PAGES content"
      contains: "FORBIDDEN"
    - path: "docs/anti-claim-checklist.md"
      provides: "The written D-13 pre-review checklist (nuance the regex cannot catch)"
    - path: "package.json"
      provides: "prebuild chains validate-taxonomy && assert-no-forbidden-claims"
  key_links:
    - from: "package.json prebuild"
      to: "scripts/assert-no-forbidden-claims.ts"
      via: "&& chained tsx invocation"
      pattern: "assert-no-forbidden-claims"
    - from: "scripts/assert-no-forbidden-claims.ts"
      to: "PAGES (lib/services/registry.ts)"
      via: "import + iterate review/published nodes"
      pattern: "from \"@/lib/services/registry\""
---

<objective>
Build the D-13 anti-claim gate BEFORE any pillar copy is flipped to `review`. This is Wave 0 foundation: a build-time `tsx` script that greps the serialized `content` of every `review`/`published` node for forbidden YMYL literals and fails the build on any hit, wired into `prebuild` so it blocks on Vercel CI; plus a written checklist for the nuance a regex cannot catch (e.g. airco-pillar subsidie context). Implements D-02/D-13 enforcement for CONT-03 (no unverified dealer/"erkend installateur" claim), CONT-05 (no Belgian 6% BTW), CONT-06 (no "gecertificeerd" before cert proof).

Purpose: Make the YMYL anti-claim gate build-enforceable and regression-proof, mirroring the project's existing `scripts/assert-*` + prebuild idiom (single-source ethos).
Output: `scripts/assert-no-forbidden-claims.ts`, an updated `package.json` `prebuild`, and `docs/anti-claim-checklist.md`.
</objective>

<phase_source_audit>
> Multi-source coverage audit for ALL of Phase 4 (recorded once, here in the Wave-1 lead plan). Every source item is COVERED by a plan. No silent omissions.

**GOAL (ROADMAP §Phase 4 — 5 success criteria):**
| Goal criterion | Covered by |
|----------------|-----------|
| 1. Every pillar/sub-service has unique copy (intro ≥120w, steps, 3-6 FAQs, local angle); no two pages distinguishable only by H1 | 04-03, 04-04, 04-05, 04-06 (+ build Zod gate exists P1) |
| 2. Per-pillar ISDE accurate+sourced; pricing transparent | 04-04 (WP), 04-05 (WTW), 04-06 (MV), 04-03 (airco=none) ISDE; 04-07 pricing |
| 3. Brand install content + cert block claim only owner-verified marks | 04-03/04-04 (rendered brand install copy), 04-07 (brands.ts blurbs), 04-01/04-02 (cert interim gate), 04-09 (map owner proof) |
| 4. Over-ons "Verhaal van Thomas" + 4 USPs; reviews one source + Google score/count/link on home+key pages | 04-02 |
| 5. Owner reviewed + signed off every page before ship (hard gate) | 04-08 (intake), 04-09 (sign-off + batch publish) |

**REQ (REQUIREMENTS.md CONT-01..CONT-10):**
| Req | Covered by |
|-----|-----------|
| CONT-01 pillar copy | 04-03, 04-04, 04-05, 04-06 |
| CONT-02 sub-service copy | 04-03, 04-04, 04-05, 04-06 |
| CONT-03 brand install content | 04-03, 04-04 (rendered), 04-07 (data), 04-01 (gate) |
| CONT-04 ISDE per pillar | 04-03 (none), 04-04 (WP), 04-05 (WTW), 04-06 (MV) |
| CONT-05 pricing transparency | 04-07 (+ gate 04-01) |
| CONT-06 cert/keurmerk block | 04-01 (gate), 04-02 (interim), 04-09 (map proof) |
| CONT-07 over-ons + USPs | 04-02 |
| CONT-08 reviews consolidation + rating | 04-02 |
| CONT-09 FAQs | 04-03, 04-04, 04-05, 04-06 |
| CONT-10 editorial gate | 04-08, 04-09 |

**RESEARCH (04-RESEARCH.md features/constraints):** ISDE per-pillar facts table → 04-03/04/05/06. Anti-claim grep gate (recommended BOTH) → 04-01. Reviews consolidation + self-serving-rating reframe → 04-02. Brand-asset sourcing (official-only / text-chip fallback) → 04-07. Pricing/BTW facts (NL 21%, never Belgian 6%) → 04-05/06/07 + gate 04-01. Cert/F-gassen facts → 04-02/04-09. All COVERED.

**CONTEXT (D-01..D-20):** D-01→03-06; D-02→01,02,03-06; D-03→03-06; D-04→08; D-05→09; D-06→09; D-07→09; D-08→03-06,09; D-09→09; D-10→04,05,06; D-11→07; D-12→01,02,07; D-13→01; D-14→03-06; D-15→03-06; D-16→03-06; D-17→02; D-18→02; D-19→07; D-20→03-06. All 20 COVERED.

**Two mechanism discrepancies surfaced (decision intent honored via the content layer, NOT by changing locked P1-3 infra):**
- **D-20 (`relatedOverride`)**: the `relatedOverride` field named in D-20/P2-D-11 does **not** exist on `ServicePage` (types.ts:77-82) and `RelatedServices.tsx` is fully deterministic — adding the field would modify LOCKED P1 types + P2 component. Decision OUTCOME (light cross-pillar cross-sell) is delivered as tasteful in-copy cross-links inside `intro`/`faqs` (the sanctioned `ContentShell` layer) — see 04-03/04/05/06. No locked-infra change.
- **D-19 / CONT-03 (brand logos + blurb rendering)**: `BrandGrid.tsx` renders brand NAME text-chips only (line 46) — it reads neither `logo` nor `blurb`, and `public/images/brands/` does not exist. Shipping official SVGs would need a LOCKED P2 component change. Decision OUTCOME (brand install content) is delivered as (a) `brands.ts` `blurb` data fill (forward-compatible, 04-07) + (b) brand-specific install context written into the Installatie node `content` where it renders today (04-03/04-04). `erkendInstallateur` stays false; text-chip fallback remains. Official-SVG sourcing defers to owner intake §9 + Phase 6 visual work.

**Exclusions (not gaps):** all CONTEXT Deferred Ideas; Phase 5/6 items; RESEARCH out-of-scope. No MISSING items → no source-audit escalation.
</phase_source_audit>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md
@.planning/phases/04-content-fill-editorial-gate/04-RESEARCH.md
@.planning/phases/04-content-fill-editorial-gate/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the D-13 anti-claim grep gate and chain it into prebuild</name>
  <files>scripts/assert-no-forbidden-claims.ts, package.json</files>
  <read_first>
    - scripts/validate-taxonomy.ts (the prebuild-wired gate — copy its file-header convention + import + console.error/process.exit shape EXACTLY)
    - scripts/assert-seo.ts (the assert/iterate idiom + `@/` import alias from scripts/)
    - lib/services/registry.ts:152-159 (the `PAGES` export this script iterates) and lib/services/types.ts:25 (`PageStatus` — only gate `review`/`published`)
    - 04-RESEARCH.md §"Code Examples" → "Recommended D-13 anti-claim grep gate" (lines 419-442 — the ready skeleton to reproduce)
    - 04-PATTERNS.md §"scripts/assert-no-forbidden-claims.ts" (the analog map + the package.json wiring warning at lines 195-199)
    - package.json:7 (the current `prebuild` — runs ONLY `tsx scripts/validate-taxonomy.ts`)
  </read_first>
  <action>
    Create `scripts/assert-no-forbidden-claims.ts` mirroring `scripts/validate-taxonomy.ts` line-for-line in structure: same file-header block (state it is a build-time CLI, NOT shipped runtime; console.*/process.exit intentional; the run command `npx tsx scripts/assert-no-forbidden-claims.ts`; what it locks = the D-13 anti-claim list). Import `{ PAGES }` from `@/lib/services/registry`. Define a `FORBIDDEN: { pattern: RegExp; why: string }[]` array with at minimum: `/\b6\s*%\s*btw/i` (Belgian 6% BTW — false in NL, per D-13/CONT-05), `/erkend\s+installateur/i` (dealer claim — gated until owner proof, D-13/CONT-03), `/gecertificeerd/i` (no "gecertificeerd" until cert proof, D-02/D-13/CONT-06). Iterate `PAGES`; `continue` on any node whose `status === "draft"` (only gate review/published — drafts may legitimately hold WIP text); for each gated node, `JSON.stringify(node.content).toLowerCase()` and test every pattern; on a hit `console.error` the node's `primaryKeyword` + the `why`, set a `failed` flag. End with `process.exit(failed ? 1 : 0)` and a `console.log("✅ …")` success line when nothing failed. Add a header note that `/gecertificeerd/i` is to be REMOVED/scoped once F-gassen/STEK is owner-verified (per RESEARCH A2). Then edit `package.json:7` to chain the new script: `"prebuild": "tsx scripts/validate-taxonomy.ts && tsx scripts/assert-no-forbidden-claims.ts"`.
    CRITICAL anti-drift (OneDrive note): keep the forbidden literals ONLY inside the `FORBIDDEN` array's regex sources — do NOT repeat them as plain words in code comments, or the script could false-positive on its own source if ever scanned. Do NOT run `tsx`/`npm`/`next build` locally (OneDrive deadlock) — the gate validates on Vercel CI.
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('scripts/assert-no-forbidden-claims.ts','utf8'); if(!/from ['\"]@\/lib\/services\/registry['\"]/.test(s)||!/FORBIDDEN/.test(s)||!/process\.exit/.test(s)||!/status\s*===?\s*['\"]draft['\"]/.test(s)) {console.error('missing required structure');process.exit(1)} console.log('script shape OK')"</automated>
    <automated>node -e "const p=require('./package.json'); if(!/validate-taxonomy\.ts\s*&&\s*tsx\s+scripts\/assert-no-forbidden-claims\.ts/.test(p.scripts.prebuild)){console.error('prebuild not chained: '+p.scripts.prebuild);process.exit(1)} console.log('prebuild chained OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/assert-no-forbidden-claims.ts` exists, imports `PAGES` from `@/lib/services/registry`, skips `draft` nodes, tests ≥3 forbidden patterns (6% btw, erkend installateur, gecertificeerd) against `JSON.stringify(node.content)`, and `process.exit(1)` on any hit.
    - `package.json` `prebuild` is exactly `tsx scripts/validate-taxonomy.ts && tsx scripts/assert-no-forbidden-claims.ts`.
    - The file carries the standard build-time-CLI header (console/process.exit declared intentional) and a note that `gecertificeerd` is removed once cert is verified.
    - No forbidden literal appears in a code comment (only inside regex sources).
  </acceptance_criteria>
  <done>Both verify commands print OK; the script and the chained prebuild are in place; gate will block on Vercel CI when a review/published node contains a forbidden literal.</done>
</task>

<task type="auto">
  <name>Task 2: Write the D-13 anti-claim review checklist</name>
  <files>docs/anti-claim-checklist.md</files>
  <read_first>
    - .planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md §D-13 (the locked anti-claim list) + §D-02/D-10/D-12
    - .planning/phases/04-content-fill-editorial-gate/04-RESEARCH.md §"Per-Pillar ISDE 2026 Facts" (lines 244-290) and §"Pricing & BTW Facts" (lines 292-301) and §"Certification / Keurmerk Facts" (lines 303-313)
    - docs/owner-intake-spec.md §5 (merken/dealerstatus gate) + §6 (certificeringen HARD GATE)
  </read_first>
  <action>
    Create `docs/anti-claim-checklist.md` — the written per-node pre-`review` gate that captures the nuance the regex cannot. Sections: (1) the hard forbidden literals (mirror the script's three) with a one-line why each; (2) the per-pillar ISDE rule the regex can't judge — airco pillar/subs MUST contain NO subsidie/ISDE claim ("Voor airconditioning (koeling) is geen ISDE-subsidie beschikbaar."); WP yes with conditions; WTW/MV yes-from-2026 ONLY with the isolatiemaatregel precondition, and MV ONLY for CO2-gestuurde systems (≥2 sensoren) — never a blanket "ventilatie is gesubsidieerd"; (3) every ISDE paragraph must cite the exact rvo.nl URL (list the three from RESEARCH) and route euro AMOUNTS to a consult, never state a bedrag (D-10); (4) brand rule — naming a product you install is allowed; a dealer/"erkend installateur" claim and any brand-logo-as-endorsement is NOT until intake §5 proof; (5) cert rule — no named keurmerk (F-gassen/STEK, InstallQ, BRL, VCA, verzekering) and no "gecertificeerd" until intake §6 proof; interim copy uses "vakkundig"/"ervaren"; (6) reviews/rating rule — no star/aggregateRating without a real Google source (intake §8); keep customer quotes verbatim. End with a 6-line copy-paste checkbox block for each node review. nl context where natural; this is an internal doc (not site copy). Do NOT inline the raw forbidden literals more than necessary (a doc is fine to name them, but avoid pasting them into any `lib/services/*` file).
  </action>
  <verify>
    <automated>node -e "const s=require('fs').readFileSync('docs/anti-claim-checklist.md','utf8').toLowerCase(); const need=['isde','rvo.nl','erkend installateur','gecertificeerd','co2','isolatiemaatregel','aggregaterating','verbatim']; const miss=need.filter(k=>!s.includes(k)); if(miss.length){console.error('checklist missing: '+miss.join(', '));process.exit(1)} console.log('checklist coverage OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/anti-claim-checklist.md` exists and covers: the 3 hard literals, the per-pillar ISDE rule (airco=none, WP=yes, WTW/MV=yes-2026+isolatie, MV=CO2-only), the rvo.nl citation + amounts-to-consult rule, the brand dealer-claim gate, the cert gate + interim "vakkundig" wording, and the verbatim-reviews/no-unsourced-rating rule.
    - The verify command finds all required keywords.
  </acceptance_criteria>
  <done>The checklist exists and the keyword-coverage check passes; pillar plans (04-03..06) reference it as the pre-review gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build pipeline → published site | Content authored in `lib/services/*` crosses into the live, indexable site at the `status` flip; a false YMYL claim here is the real risk surface (content-integrity, not classic AppSec) |
| dev machine → npm registry | This plan installs ZERO packages — it adds an in-repo script only (no supply-chain surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (content integrity) | `lib/services/*` node content reaching `review`/`published` with a false YMYL claim (subsidie/cert/BTW) | mitigate | THIS plan: `scripts/assert-no-forbidden-claims.ts` greps serialized content + `docs/anti-claim-checklist.md` + the existing Zod prebuild gate; backed by owner sign-off (04-09) |
| T-04-02 | Information disclosure | Build-time script reading beyond intended scope | accept | Script imports only `@/lib/services/registry`, prints to stdout/stderr, no fs writes, no network (same tight boundary as validate-taxonomy.ts) |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A this phase — Phase 4 installs zero packages (RESEARCH §Package Legitimacy Audit: not applicable); no `[ASSUMED]`/`[SUS]`/`[SLOP]` packages exist |
</threat_model>

<verification>
- `npx tsx scripts/assert-no-forbidden-claims.ts` runs on Vercel CI as part of prebuild and exits 0 while all nodes are `draft` (nothing gated yet), then begins enforcing as pillar plans flip nodes to `review`.
- The two Task-1 node checks and the Task-2 keyword check all print OK locally (they are pure Node string/JSON reads — safe on OneDrive; no `tsx`/`next build`).
- Whole-phase build (`npm run build`) is validated GREEN ON VERCEL CI / preview deploy, never locally (OneDrive deadlock).
</verification>

<success_criteria>
- The D-13 grep gate exists, is wired into `prebuild` (blocks on Vercel CI), and correctly skips `draft` nodes while catching the three forbidden literals in `review`/`published` content.
- The written anti-claim checklist exists and covers the per-pillar nuance the regex cannot.
- Foundation is in place so 04-03..06 can flip pillars to `review` against a genuine, build-enforced YMYL gate.
</success_criteria>

<output>
Create `.planning/phases/04-content-fill-editorial-gate/04-01-SUMMARY.md` when done.
</output>
