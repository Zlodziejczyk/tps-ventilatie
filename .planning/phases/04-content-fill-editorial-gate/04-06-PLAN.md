---
phase: 04-content-fill-editorial-gate
plan: 06
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/services/mechanische-ventilatie.ts
autonomous: true
requirements: [CONT-01, CONT-02, CONT-04, CONT-09]
must_haves:
  truths:
    - "The MV pillar + all 4 sub-service pages have a unique intro >=120 words, >=1 step, and 3-6 distinct FAQs (currently ZERO FAQs anywhere)"
    - "ISDE/subsidie content is accurate and narrow: MV qualifies from 2026 ONLY for CO2-gestuurde systemen (>125 m3/h + >=2 CO2-sensoren) AND only combined with an isolatiemaatregel — never a blanket 'mechanische ventilatie is gesubsidieerd'; cites rvo.nl; amounts to consult"
    - "All 5 MV nodes are flipped draft -> review after clearing the Zod gate + the D-13 checklist"
  artifacts:
    - path: "lib/services/mechanische-ventilatie.ts"
      provides: "Filled/expanded ContentShell for 1 pillar + 4 sub-service nodes; all status:review"
      contains: "CO2"
  key_links:
    - from: "mechanische-ventilatie.ts ISDE copy"
      to: "official RVO ventilatie source"
      via: "cited rvo.nl ventilatie URL + CO2-gestuurd + isolatie conditions"
      pattern: "rvo\\.nl"
---

<objective>
Complete the Mechanische Ventilatie silo (pillar + 4 sub-services: vervangen, onderhoud-reinigen, storing, aanleggen). One sub (onderhoud-reinigen) has a strong ported intro (incl. the folded dakventilator content, P2 D-05); the rest are short or empty, and NO node has FAQs. EXPAND every intro to ≥120 words, add ≥1 step where missing, and add 3-6 distinct FAQs everywhere. The ISDE story (CONT-04, D-10) is the trickiest: MV qualifies from 2026 ONLY for CO2-gestuurde systemen (>125 m³/h + ≥2 CO2-sensoren) AND only combined with an isolatiemaatregel — copy must NOT over-promise a blanket MV subsidy (Pitfall 3). State the narrow conditions, cite the rvo.nl ventilatie URL, route amounts to a consult. Implements CONT-01/02/04/09; honors D-01/D-02/D-08/D-10/D-14/D-15/D-16/D-20.

Purpose: A complete, unique, precisely-accurate MV silo (the easiest place to over-promise subsidy) clearing the build-enforced gates, ready for owner review.
Output: filled/expanded `lib/services/mechanische-ventilatie.ts` with all 5 nodes at `status: "review"`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md
@.planning/phases/04-content-fill-editorial-gate/04-RESEARCH.md
@.planning/phases/04-content-fill-editorial-gate/04-PATTERNS.md
@docs/anti-claim-checklist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the MV pillar with narrow CO2-gestuurd ISDE, add FAQs, widen draftShell</name>
  <files>lib/services/mechanische-ventilatie.ts</files>
  <read_first>
    - lib/services/mechanische-ventilatie.ts (full — pillar 30-45 (EMPTY intro); the local `draftShell` 12-27 accepts `{ intro?; localAngle? }` — NO `steps?`/`faqs?`)
    - lib/services/types.ts:31-50 + :144-167 (the gate)
    - lib/services/wtw.ts:31-46 (widest draftShell to mirror) + :13-29 (named-const StepItem[] pattern)
    - docs/anti-claim-checklist.md (MV rule: CO2-gestuurd ONLY, ≥2 sensoren, + isolatie; never blanket MV subsidy)
    - 04-RESEARCH.md §"Mechanische Ventilatie (CO2-gestuurd) — ISDE: YES from 2026" (275-284 — centrale CO2-gestuurde afzuigventilator min capaciteit >125 m³/h + min 2 CO2-sensoren; same isolatie precondition + 24-maanden window + bouwjaar as WTW; **the nuance: a plain demand-driven box WITHOUT CO2 sensors and WITHOUT isolatie does NOT qualify**) + cite URLs https://www.rvo.nl/subsidies-financiering/isde/woningeigenaren/ventilatie and the "wat wijzigt er 2026" page + Pitfall 3 (376-380)
    - lib/constants.ts SITE.serviceAreas
  </read_first>
  <action>
    Widen the local `draftShell` (12-27) to accept `steps?: StepItem[]` and `faqs?: FaqItem[]` in `extra` (default `[]`; add both to the `import type` from "./types"; mirror wtw.ts:31-46).
    Fill the PILLAR node (slug "mechanische-ventilatie", intro currently ""): ≥120-word unique intro — mechanische ventilatie explained (afzuiging vochtige ruimtes, gezond binnenklimaat, CO2-gestuurd vs traditioneel), the service set (vervangen/onderhoud-reinigen/storing/aanleggen), Zoetermeer + regio; formal `u` (D-14); interim "vakkundig"/"ervaren". Add ≥1 pillar-level step. Add 3-6 DECISION FAQs (D-16) — the MV-subsidie question is the canonical home HERE, answered PRECISELY: MV kan vanaf 2026 ISDE krijgen MAAR alleen voor CO2-gestuurde systemen (>125 m³/h, ≥2 CO2-sensoren) én in combinatie met een isolatiemaatregel — een gewone mechanische ventilatiebox zonder CO2-sturing komt niet in aanmerking; cite the rvo.nl ventilatie URL; route amounts to a consult. Plus geluid, luchtkwaliteit/gezondheid, and CO2-gestuurd-vs-standaard (the energy/comfort upgrade). Set `localAngle` (Zoetermeer + regio, no city pages). Light D-20 cross-sell in-copy only (note `relatedOverride` absent — locked infra). NEVER a blanket "MV is gesubsidieerd"; NEVER a euro bedrag; NO "gecertificeerd"/"erkend installateur".
  </action>
  <verify>
    <automated>npx tsx -e "import { MECHANISCHE_VENTILATIE_PAGES as P } from './lib/services/mechanische-ventilatie.ts'; const p=P.find(n=>n.type==='pillar'); const c=p.content; const w=c.intro.trim().split(/\s+/).filter(Boolean).length; if(w<120||c.steps.length<1||c.faqs.length<3||c.faqs.length>6){console.error('pillar gate fail w='+w+' s='+c.steps.length+' f='+c.faqs.length);process.exit(1)} const blob=JSON.stringify(c).toLowerCase(); if(/subsidie|isde/.test(blob)){ if(!/rvo\.nl/.test(blob)){console.error('MV subsidie mention must cite rvo.nl');process.exit(1)} if(!/co2/.test(blob)||!/isolatie/.test(blob)){console.error('MV subsidie must state CO2-gestuurd + isolatie conditions');process.exit(1)} } console.log('MV pillar OK w='+w+' — narrow CO2+isolatie subsidie, cites rvo.nl')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
  </verify>
  <acceptance_criteria>
    - `draftShell` accepts `steps?`/`faqs?`; imports `FaqItem`/`StepItem`.
    - Pillar intro ≥120 words, ≥1 step, 3-6 FAQs.
    - Any MV subsidie mention cites rvo.nl AND includes BOTH the CO2-gestuurd (≥2 sensoren) and isolatie conditions; no euro amount; no blanket-subsidy claim.
  </acceptance_criteria>
  <done>The MV pillar clears the gate with precisely-scoped 2026 ISDE (CO2-gestuurd + isolatie) citing the RVO ventilatie source.</done>
</task>

<task type="auto">
  <name>Task 2: Expand the 4 MV sub-service nodes, add FAQs, flip the silo to review</name>
  <files>lib/services/mechanische-ventilatie.ts</files>
  <read_first>
    - lib/services/mechanische-ventilatie.ts (the 4 service nodes: vervangen 46-62 (EMPTY intro), onderhoud-reinigen 63-83 (STRONG ported intro incl. folded dakventilator content — keep + ensure ≥120w + add steps/faqs), storing 84-100 (EMPTY), aanleggen 101-117 (EMPTY)) — NONE have FAQs
    - lib/services/wtw.ts:13-29 (named-const StepItem[] pattern)
    - docs/anti-claim-checklist.md
    - 04-RESEARCH.md Pattern 2 angle map (196-209) + Pattern 3 (211-224 — sub = TASK FAQs; ISDE canonical home is the pillar). Porting watch-item (PATTERNS §Specifics): the old "dakventilator" content folds into onderhoud-reinigen/vervangen — no new route.
  </read_first>
  <action>
    For EACH of the 4 service nodes ensure a unique ≥120-word intro (WRITE vervangen/storing/aanleggen from empty; KEEP+verify onderhoud-reinigen's ported intro ≥120w incl. the folded dakventilator content), ≥1 service-specific `step` (declare per-service `const X_STEPS: StepItem[]`), and 3-6 distinct TASK FAQs (no duplication across nodes or with the pillar — D-16; subsidie DECISION FAQ stays on the pillar — aanleggen/vervangen link to it):
    - vervangen: levensduur (oude box ~15 jaar, wisselstroom = tot 80% meer verbruik — the ported dakventilator energy fact fits here too) + zuiniger gelijkstroom + de vervang-sequence; TASK FAQs (wanneer ventilatiebox vervangen, gelijkstroom vs wisselstroom besparing, hoe lang duurt vervangen).
    - onderhoud-reinigen: keep the strong ported intro (kanaalreiniging met roterende borstel, dakventilator-onderhoud iedere 4-5 jaar) — ensure ≥120w; add ≥1 step (inspectie → kanalen reinigen → box/ventielen → controle) and TASK FAQs (hoe vaak kanalen reinigen, waarom reinigen — gezondheid/energie, dakventilator-onderhoud).
    - storing: symptomen (lawaai, trillen, slecht afzuigen, vocht/schimmel-signaal) + snelheid + diagnose; TASK FAQs (ventilatie maakt lawaai — oorzaken, vocht in huis ondanks ventilatie, hoe snel kunt u komen).
    - aanleggen: nieuwbouw/renovatie + kanaalplan + bouwbesluit + when it applies; TASK FAQs (MV aanleggen in bestaande woning, hoe lang duurt aanleg, CO2-gestuurd meteen meenemen → short pointer to the pillar's ISDE conditions). Formal `u`; interim "vakkundig"/"ervaren"; NO euro amounts; cite rvo.nl only where ISDE conditions are stated.
    After all 5 nodes clear the gate and you have applied `docs/anti-claim-checklist.md`, flip every node `status` "draft" → "review" (D-08/D-09).
  </action>
  <verify>
    <automated>npx tsx -e "import { MECHANISCHE_VENTILATIE_PAGES as P } from './lib/services/mechanische-ventilatie.ts'; let bad=[]; for(const n of P){const c=n.content; const w=c.intro.trim().split(/\s+/).filter(Boolean).length; if(w<120||c.steps.length<1||c.faqs.length<3||c.faqs.length>6) bad.push(n.primaryKeyword+' (w='+w+' s='+c.steps.length+' f='+c.faqs.length+')'); if(n.status!=='review') bad.push(n.primaryKeyword+' status='+n.status);} const allFaqs=P.flatMap(n=>n.content.faqs.map(f=>f.question.toLowerCase().trim())); const dup=allFaqs.filter((q,i)=>allFaqs.indexOf(q)!==i); if(dup.length) bad.push('dup FAQ: '+dup.join(' | ')); if(bad.length){console.error('FAIL: '+bad.join('; '));process.exit(1)} console.log('MV silo OK — 5 nodes review, gate passed, no dup FAQ')" 2>&1 || echo "NOTE: tsx may deadlock on OneDrive — validate on Vercel CI"</automated>
    <automated>node -e "const s=require('fs').readFileSync('lib/services/mechanische-ventilatie.ts','utf8').toLowerCase(); if(/erkend\s+installateur|gecertificeerd/.test(s)){console.error('forbidden claim');process.exit(1)} if(/\b6\s*%\s*btw/.test(s)){console.error('Belgian 6% btw literal');process.exit(1)} console.log('MV anti-claim grep OK')"</automated>
  </verify>
  <acceptance_criteria>
    - All 5 MV nodes: intro ≥120 words, ≥1 step, 3-6 FAQs; every node `status: "review"`.
    - No FAQ duplicated across the 5 nodes (the ISDE conditions live once on the pillar).
    - `grep -iE "erkend installateur|gecertificeerd|6 *% *btw" lib/services/mechanische-ventilatie.ts` returns nothing.
    - The folded dakventilator content is retained within onderhoud-reinigen/vervangen (no new route).
  </acceptance_criteria>
  <done>Both verify commands print OK (or build GREEN on Vercel CI if tsx deadlocks); the MV silo is complete, expanded, anti-claim-clean, and at `review`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored MV/ISDE content → published YMYL page | A blanket "mechanische ventilatie is gesubsidieerd" (omitting the CO2-gestuurd + isolatie conditions) crosses into the live site — the single easiest over-promise this phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering (YMYL integrity) | MV subsidy over-promised (no CO2-sensor / no isolatie qualifier) | mitigate | RESEARCH verified narrow conditions; verify check requires both "co2" and "isolatie" wherever subsidie/isde appears; cite rvo.nl; amounts to consult; owner sign-off (04-09) |
| T-04-07 | Spoofing (trust) | Cert/dealer claim creeping into MV copy | mitigate | Interim "vakkundig"; grep gate catches "gecertificeerd"/"erkend installateur" |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Per-node Zod-gate + CO2+isolatie-condition + cite-rvo.nl + no-dup-FAQ checks via `npx tsx` (run on Vercel CI if OneDrive deadlocks local tsx).
- Anti-claim grep is a pure Node read (OneDrive-safe).
- Status flip validated by the prebuild Zod gate + `assert-no-forbidden-claims.ts` on the next Vercel preview; whole-phase build GREEN ON VERCEL.
</verification>

<success_criteria>
- The MV silo (5 nodes) has unique ≥120-word intros, service-specific steps, 3-6 distinct FAQs, regio localAngle, the retained dakventilator content, and precisely-scoped 2026 ISDE (CO2-gestuurd + isolatie, rvo.nl cite, amounts to consult).
- All 5 nodes at `status: "review"` and passing the build-enforced gates.
</success_criteria>

<output>
Create `.planning/phases/04-content-fill-editorial-gate/04-06-SUMMARY.md` when done.
</output>
