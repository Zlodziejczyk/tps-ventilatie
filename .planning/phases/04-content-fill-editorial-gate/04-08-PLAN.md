---
phase: 04-content-fill-editorial-gate
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/owner-intake-spec.md
autonomous: false
requirements: [CONT-10]
must_haves:
  truths:
    - "The Tally owner-intake form 2EojAA is published (password tpsklimaat2026 verified active) and reachable at its public fill URL"
    - "Thomas has received ONE message containing the link, the password, and the photo/logo instruction"
    - "The owner-intake spec records the published status + sent-date so 04-09 can map returns when they arrive"
  artifacts:
    - path: "docs/owner-intake-spec.md"
      provides: "Updated status line (published + message sent) for the intake form"
      contains: "Published"
  key_links:
    - from: "Tally form 2EojAA"
      to: "owner (Thomas)"
      via: "one message: link + password + photo instruction"
      pattern: "2EojAA"
---

<objective>
Publish the owner-intake form and send Thomas one message — the input mechanism the gated claims depend on (D-04, CONT-10). Per D-01 nothing in the drafting plans blocks on this; publishing EARLY (Wave 1) gives the owner maximum response time so 04-09 can map his returns into the gated slots (dealer status, cert proof, Google rating, story, pricing confirm, NAP/hours). This plan is `autonomous: false`: it sends an OUTWARD message to a real client and its second step depends on the owner responding (async, days).

Purpose: Unblock the owner-input dependency as early as possible without blocking drafting.
Output: published Tally form `2EojAA`, one message sent to Thomas, and an updated status line in `docs/owner-intake-spec.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md
@docs/owner-intake-spec.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Verify the Tally password is active and publish form 2EojAA</name>
  <files>docs/owner-intake-spec.md</files>
  <read_first>
    - docs/owner-intake-spec.md (full — the form spec; §5 build checklist step 4 (password + save-for-later + Dutch + Publish); the status line at the top noting DRAFT + the manual-finish note; the edit/fill URLs)
    - .planning/phases/04-content-fill-editorial-gate/04-CONTEXT.md §D-04 (publish + verify password active → publish)
  </read_first>
  <what-built>
    The Tally intake form `2EojAA` already exists (DRAFT, 12 pages / 196 blocks, Dutch). Claude attempts via the Tally MCP: confirm the form loads, ensure the password `tpsklimaat2026` is set under Access, and set status to PUBLISHED (save_form with PUBLISHED). If the Tally MCP cannot toggle the password/publish (known Tally MCP response-bug on these 4 settings, per spec §5 step 4), the human completes it in the Tally UI.
  </what-built>
  <how-to-verify>
    1. Open the public fill link: https://tally.so/r/2EojAA — it must load the form (not a 404 / not "form not published").
    2. Confirm it prompts for the password and that `tpsklimaat2026` grants access (Tally → Settings → Access; may require Tally Pro for password).
    3. Confirm save-for-later + progress bar + Dutch system language are on.
    If any of these cannot be set via the MCP, finish them by hand in the Tally UI (Settings → Access / General), then Publish.
  </how-to-verify>
  <action>
    After publish is confirmed, update the status line at the top of `docs/owner-intake-spec.md` from "DRAFT" to "Published {date}" and note the public fill URL is live + the password is active. (This is the only repo file this plan touches.)
  </action>
  <resume-signal>Type "published" once the fill URL loads, the password works, and the spec status line is updated — or describe the blocker.</resume-signal>
  <acceptance_criteria>
    - https://tally.so/r/2EojAA loads the published form and enforces the `tpsklimaat2026` password.
    - `docs/owner-intake-spec.md` top status line reads "Published" with the date.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Send Thomas one message (link + password + photo/logo instruction)</name>
  <files></files>
  <read_first>
    - docs/owner-intake-spec.md §4 (the fields, so the message can set expectations — ~10-15 min, save-and-resume) + §5 step 6 (send one message: link + password + photo instruction) + §9 (logo/photos — the single biggest content-quality lever)
  </read_first>
  <what-built>
    Claude drafts the message to Thomas (Dutch, warm/informal `je` to match the owner-facing tone — NOT the public `u` voice): the published fill link https://tally.so/r/2EojAA, the password `tpsklimaat2026`, that it takes ~10-15 min and can be saved & resumed, and the explicit instruction to upload the LOGO (SVG/AI/PDF preferred) + real project/work photos + a portrait of Thomas (intake §9) and any certification proofs (§6) and brand dealer-status proof (§5). The human sends it via the owner's preferred channel (WhatsApp/email).
  </what-built>
  <how-to-verify>
    1. The drafted message contains: the fill link, the password, the time estimate + save/resume note, and the photo/logo/cert/dealer-proof instruction.
    2. The message is sent to Thomas via his usual channel.
  </how-to-verify>
  <action>Claude drafts the Dutch owner message (informal `je`, NOT the public `u` voice) containing: the published fill link https://tally.so/r/2EojAA, the password `tpsklimaat2026`, the ~10-15 min + save/resume note, and the explicit logo/photo/cert/dealer-proof upload instruction (intake §9/§6/§5). Claude presents the drafted message; the human sends it via Thomas's preferred channel (WhatsApp/email). No repo file changes in this task.</action>
  <resume-signal>Type "sent" once Thomas has been messaged with link + password + photo/logo/cert instruction.</resume-signal>
  <acceptance_criteria>
    - One message (not a drip) sent to Thomas containing link + password + photo/logo/cert/dealer-proof instruction.
    - The async wait for his response is owned by 04-09 (mapping returns) — drafting (04-03..07) proceeds regardless per D-01.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| owner ↔ Tally form | The owner's business data (NAP, cert scans, dealer proof) crosses into a third-party form — data-privacy surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-12 | Information disclosure | Owner business data / cert scans in the Tally form | mitigate | Tally password (`tpsklimaat2026`) + the unguessable form URL as a second layer (per spec §2); owner data routes through GSD mapping (04-09), not direct edits |
| T-04-13 | Spoofing | Form link intercepted / filled by the wrong person | accept | Password + unguessable URL; low-value internal intake; owner verifies his own submission |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero packages installed (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- Manual: the public fill URL loads, enforces the password, and Thomas has been messaged. These are human-action checkpoints (publishing to a real client + sending an outward message) — there is no CLI substitute.
- No build impact (the only repo change is the spec status line).
</verification>

<success_criteria>
- Form `2EojAA` is published and password-protected; Thomas has one message with link + password + photo/logo/cert instruction; the spec records the published status.
- The owner-input dependency is open as early as possible; mapping returns is handled in 04-09.
</success_criteria>

<output>
Create `.planning/phases/04-content-fill-editorial-gate/04-08-SUMMARY.md` when done.
</output>
