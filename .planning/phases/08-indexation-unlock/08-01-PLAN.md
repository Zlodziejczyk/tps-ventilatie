---
phase: 08-indexation-unlock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/seo/policy.ts
  - lib/seo/invariants.ts
  - scripts/assert-seo.ts
  - scripts/assert-gate-blocks.ts
  - scripts/assert-registry.ts
  - scripts/assert-metadata-seam.ts
  - scripts/check-radius-literals.sh
  - package.json
autonomous: true
requirements: [IDX-01]
must_haves:
  truths:
    - "lib/seo/invariants.ts exports a PURE checkIndexationInvariants() that takes an injectable node collection + sitemap entry list + optional floor and RETURNS a violation list (never throws, never asserts at module top-level), so the same logic can be re-run against perturbed clones (D-03)"
    - "The invariant is RELATIONAL, not a snapshot: for every governed node, sitemap membership must equal isIndexable(node) — true at 5 indexable pages and at 27 alike (D-03)"
    - "The checker is source-agnostic (D-05): it iterates whatever collection policy.ts exposes via the new indexableSurface() export, never PAGES by name, and it flags any sitemap entry with no backing node so a parallel list bolted onto app/sitemap.ts fails"
    - "scripts/assert-gate-blocks.ts PROVES the invariant bites: a pillar reverted to review, a node dropped from the sitemap, a foreign URL appended, and a below-floor count are each rejected, while unperturbed real data yields zero violations (D-03)"
    - "The pre-existing no-op perturbation in assert-gate-blocks.ts is repaired to be CONTENT-addressed (blank the intro) instead of INDEX-addressed (clone[1]) — an index-addressed perturbation is itself a snapshot assertion and had silently decayed to a no-op"
    - "scripts/assert-registry.ts is GREEN again and asserts the routable surface relationally (type composition + a >= floor with an anti-bump message), not via the hardcoded EXPECTED_PAGE_COUNT = 27 snapshot that PAGES.length = 28 already broke"
    - "scripts/assert-metadata-seam.ts asserts no file under app/ declares a literal robots: or index: false outside lib/seo/metadata.ts (app/robots.ts allowlisted), and that every app/**/page.tsx imports buildMetadata — checked against COMMENT-STRIPPED source (D-04)"
    - "All seven guards run in package.json prebuild and are therefore build-blocking on Vercel; check-radius-literals.sh scans app components lib (never scripts/) (D-01/D-07)"
    - "INDEXABLE_FLOOR is NOT introduced in this plan — the floor constant lands with the flip that satisfies it (D-09). This landing is green at 5 indexable pages"
  artifacts:
    - path: "lib/seo/invariants.ts"
      provides: "The pure, injectable, re-runnable indexation invariant checker"
      contains: "checkIndexationInvariants"
    - path: "scripts/assert-metadata-seam.ts"
      provides: "Source-level guard that buildMetadata is the only way a page gets a robots directive"
      contains: "buildMetadata"
    - path: "package.json"
      provides: "The complete build-blocking prebuild chain (7 guards)"
      contains: "assert-metadata-seam"
  key_links:
    - from: "scripts/assert-seo.ts"
      to: "lib/seo/invariants.ts checkIndexationInvariants"
      via: "run on real data, assert zero violations"
      pattern: "checkIndexationInvariants"
    - from: "scripts/assert-gate-blocks.ts"
      to: "lib/seo/invariants.ts checkIndexationInvariants"
      via: "run on perturbed clones, assert each is caught"
      pattern: "checkIndexationInvariants"
    - from: "lib/seo/invariants.ts"
      to: "lib/seo/policy.ts indexableSurface / isIndexable / sitemapEntries"
      via: "source-agnostic default inputs"
      pattern: "indexableSurface"
    - from: "package.json prebuild"
      to: "all 7 guard scripts"
      via: "npm lifecycle — non-zero exit aborts next build"
      pattern: "prebuild"
---

<objective>
Landing 1 of 4 (D-09). Convert the snapshot gate that *enforced* the bug into a relational invariant,
extract it into a pure re-runnable checker, prove it bites on perturbed data, guard the
`buildMetadata` bypass path, and make all seven guards build-blocking. This is IDX-01 — and it must
land before IDX-02 because the current gate hardcodes `entries.length === 5` and would block its own
fix.

**Two repairs are mandatory here, not optional (08-RESEARCH.md §Pitfall 1).** `assert-registry.ts`
and `assert-gate-blocks.ts` are **already RED today** — they are unwired, so nothing has re-run them:
`assert-registry.ts` asserts `PAGES.length === 27` when it is 28 (`/projecten` joined in quick task
`260719-t62`), and `assert-gate-blocks.ts` perturbation (B) mutates `PAGES[1]`, which is now the
content-rich airconditioning pillar, so the perturbation validates and the harness proves nothing.
Wiring them into `prebuild` without repairing them turns the very next Vercel build red.

Purpose: make this class of regression impossible to ship silently again — the gate must assert
relationships and floors, never exact lists, and must be able to demonstrate that it fails when the
world breaks.
Output: `lib/seo/invariants.ts` (new), `scripts/assert-metadata-seam.ts` (new), rewritten
`assert-seo.ts`, extended+repaired `assert-gate-blocks.ts`, repaired `assert-registry.ts`, widened
`check-radius-literals.sh`, and a complete `prebuild` chain. Green at 5 indexable pages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-indexation-unlock/08-CONTEXT.md
@.planning/phases/08-indexation-unlock/08-RESEARCH.md
@.planning/phases/08-indexation-unlock/08-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Branch, and expose the governed collection from policy.ts (D-05)</name>
  <files>lib/seo/policy.ts</files>
  <read_first>
    - lib/seo/policy.ts (the whole file — `isIndexable`, `absoluteUrl`, `sitemapEntries`, and the no-barrel header comment whose style the new export must match)
    - lib/services/registry.ts lines 203-228 (`PAGES`, `urlFor`, `findBySlug`) — the collection being exposed
    - 08-CONTEXT.md §D-05 (source-agnostic requirement) and §D-08 (branch decision)
    - .planning/research/ARCHITECTURE.md §"Single-source indexability" and §"Anti-Pattern 1"
  </read_first>
  <action>
    First create and switch to the phase branch: `git checkout -b gsd/phase-8-indexation-unlock` (D-08 —
    the project config is `branching_strategy: none`, so this is an explicit per-phase choice matching
    how Phase 5 landed; `main` deploys straight to production and must not receive this work directly).

    Then add ONE export to `lib/seo/policy.ts`: `export function indexableSurface(): PageNode[]` returning
    `PAGES`. Rewrite `sitemapEntries()` to read `indexableSurface().filter(isIndexable)` instead of
    `PAGES.filter(isIndexable)` — behavior is byte-identical today, but this becomes the single extension
    point the invariant checker iterates and the place Phase 12's kennisbank articles join (D-05). Document
    it in the file's existing comment idiom: state that this is the collection the indexing policy governs,
    that the invariant checker iterates it rather than naming `PAGES`, and that new indexable entity types
    must join HERE and never via a second list in `app/sitemap.ts`.

    Do NOT change `isIndexable()` in this plan — the predicate unification is 08-02's atomic landing (D-10).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('lib/seo/policy.ts','utf8'); const bad=[]; if(!/export function indexableSurface\(\)/.test(s))bad.push('indexableSurface export missing'); if(!/indexableSurface\(\)\s*\.filter\(isIndexable\)/.test(s))bad.push('sitemapEntries must derive from indexableSurface()'); if(/node\.type === \"static\"/.test(s)===false)bad.push('isIndexable was changed in this plan - predicate unification belongs to 08-02'); if(bad.length){console.error(bad.join('; '));process.exit(1)} console.log('policy.ts extension point OK')"</automated>
    <automated>npx tsx scripts/assert-seo.ts</automated>
    <automated>git rev-parse --abbrev-ref HEAD | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{if(s.trim()!=='gsd/phase-8-indexation-unlock'){console.error('expected branch gsd/phase-8-indexation-unlock, on '+s.trim());process.exit(1)}console.log('branch OK')})"</automated>
  </verify>
  <acceptance_criteria>
    - Working branch is `gsd/phase-8-indexation-unlock`.
    - `lib/seo/policy.ts` exports `indexableSurface()` returning the governed node collection.
    - `sitemapEntries()` derives from `indexableSurface()`, not from `PAGES` directly.
    - `isIndexable()` is unchanged (still carries the `type === "static"` branch — 08-02 removes it).
    - The existing `scripts/assert-seo.ts` still exits 0 (no behavior change: still 5 entries).
  </acceptance_criteria>
  <done>The extension point exists and the sitemap still emits the same 5 URLs.</done>
</task>

<task type="auto">
  <name>Task 2: Create the pure invariant checker (lib/seo/invariants.ts) — D-03/D-05</name>
  <files>lib/seo/invariants.ts</files>
  <read_first>
    - lib/seo/policy.ts (post-Task-1 — `indexableSurface`, `isIndexable`, `absoluteUrl`, `sitemapEntries`; and the no-barrel `lib/seo/*` family header comment this new module must join)
    - lib/services/registry.ts (`urlFor`) and lib/services/types.ts (`PageNode`)
    - .planning/research/ARCHITECTURE.md lines ~115-126 (the relational-invariant sample this implements)
    - 08-RESEARCH.md §"Pattern 1: The pure, injectable invariant checker" (the exact signature, the 5 checks, and the violation codes)
    - 08-CONTEXT.md §D-03 (pure checker), §D-05 (source-agnostic), §D-06 (named constant + anti-bump message)
  </read_first>
  <action>
    Create `lib/seo/invariants.ts` as a pure, server-safe member of the no-barrel `lib/seo/*` family.
    Export `interface IndexationViolation { code: string; url?: string; message: string }` and
    `export function checkIndexationInvariants(opts?: { nodes?: PageNode[]; entries?: { url: string }[]; floor?: number }): IndexationViolation[]`.

    Defaults: `nodes = indexableSurface()`, `entries = sitemapEntries()`. `floor` omitted means the floor
    check is NOT run — that is deliberate (D-09: the floor constant lands with the flip that satisfies it,
    in 08-04). The function MUST return a list and MUST NOT throw or assert; all assertion happens in the
    calling scripts. Nothing may execute at module top-level.

    Checks, accumulating every violation (never early-return):
    1. Per node, the RELATIONAL core: `entries` contains `absoluteUrl(urlFor(node))` must EQUAL
       `isIndexable(node)`. Emit code `sitemap-without-index` when it is in the sitemap but not indexable,
       and `index-without-sitemap` when it is indexable but absent. Each message must name the URL.
    2. Per entry: every sitemap URL must map back to a governed node (code `orphan-entry`) — this is what
       makes a parallel list appended to `app/sitemap.ts` fail (ARCHITECTURE §Anti-Pattern 1).
    3. Per entry: the URL must start with `CANONICAL_ORIGIN` (code `non-absolute-entry`).
    4. Floor, only when `opts.floor` is a number: `nodes.filter(isIndexable).length >= floor`, code
       `below-floor`. The message is load-bearing (D-06) and must say, in substance: a drop below the floor
       means a page was DE-INDEXED — find what went dark; only lower this number when a page is
       deliberately retired. Never raise or lower it to make the build pass.
    5. Duplicate sitemap URLs (code `duplicate-entry`) — cheap, and it catches a merge that double-lists.

    Add a file header in the project's comment idiom explaining WHY this is a separate module: the old
    `assert-seo.ts` asserted at module top-level and therefore could never be re-run against perturbed
    data, so it could not prove it worked. Reference commit `82d897b` as the failure mode this design
    targets. Do NOT export `INDEXABLE_FLOOR` yet.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx -e "import { checkIndexationInvariants } from '@/lib/seo/invariants'; import { indexableSurface, sitemapEntries, isIndexable, absoluteUrl } from '@/lib/seo/policy'; import { urlFor } from '@/lib/services/registry'; const clean = checkIndexationInvariants(); if (clean.length !== 0) { console.error('real data must be clean, got: ' + JSON.stringify(clean)); process.exit(1); } const nodes = indexableSurface(); const dropped = sitemapEntries().slice(1); const v1 = checkIndexationInvariants({ nodes, entries: dropped }); if (!v1.some(v => v.code === 'index-without-sitemap')) { console.error('dropping an entry must yield index-without-sitemap'); process.exit(1); } const orphan = [...sitemapEntries(), { url: absoluteUrl('/niet-bestaand') }]; const v2 = checkIndexationInvariants({ nodes, entries: orphan }); if (!v2.some(v => v.code === 'orphan-entry')) { console.error('a foreign entry must yield orphan-entry'); process.exit(1); } const v3 = checkIndexationInvariants({ floor: 999 }); if (!v3.some(v => v.code === 'below-floor')) { console.error('floor must be enforced when passed'); process.exit(1); } console.log('invariant checker OK — clean on real data, catches drop/orphan/floor');"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('lib/seo/invariants.ts','utf8'); const bad=[]; if(/^\s*assert/m.test(s))bad.push('must not assert - return violations instead'); if(/INDEXABLE_FLOOR/.test(s))bad.push('INDEXABLE_FLOOR must NOT land in this plan (D-09)'); if(!/indexableSurface/.test(s))bad.push('must default to indexableSurface(), not PAGES'); if(/\bPAGES\b/.test(s))bad.push('must not name PAGES (D-05 source-agnostic)'); if(!/82d897b/.test(s))bad.push('header must name the 82d897b failure mode'); if(bad.length){console.error(bad.join('; '));process.exit(1)} console.log('invariants.ts shape OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/seo/invariants.ts` exports `checkIndexationInvariants` and `IndexationViolation`; nothing runs at module top-level.
    - Returns `[]` on real, unperturbed data today (5 indexable, 5 entries).
    - Emits `index-without-sitemap`, `sitemap-without-index`, `orphan-entry`, `non-absolute-entry`, `duplicate-entry`, `below-floor` for the corresponding breakages.
    - Defaults come from `indexableSurface()` / `sitemapEntries()`; the string `PAGES` does not appear.
    - `INDEXABLE_FLOOR` is absent (lands in 08-04).
    - The below-floor message tells the reader a page went dark and forbids bumping the number to pass.
  </acceptance_criteria>
  <done>The invariant exists as re-runnable data, provably clean on reality and provably angry on perturbation.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite scripts/assert-seo.ts onto the invariant (IDX-01)</name>
  <files>scripts/assert-seo.ts</files>
  <read_first>
    - scripts/assert-seo.ts (the whole current file — assertions (1)-(6); (4) canonical-origin and (5) JSON-LD and (6) faq-null-on-empty must SURVIVE the rewrite; (1)(2)(3) are the snapshot assertions being replaced)
    - lib/seo/invariants.ts (Task 2 — the checker being called)
    - 08-CONTEXT.md §D-02 (numeric canary PLUS named structural assertions), §D-06 (anti-bump failure message)
    - .planning/research/PITFALLS.md §"Pitfall 8" (the build gate that enforced the bug, commit 82d897b)
  </read_first>
  <action>
    Rewrite `scripts/assert-seo.ts` so its indexation section is a single call to
    `checkIndexationInvariants()` — assert the returned list is empty, and when it is not, print EVERY
    violation (code + url + message) before exiting non-zero. Delete the three snapshot assertions:
    the `assert.deepEqual(indexableUrls, [...5 static URLs])`, the `entries.length === 5`, and the
    "draft hub must be noindex" / "draft service must be noindex" pair — all three are statements about
    the current state that will be FALSE by design after 08-02/08-03/08-04.

    KEEP, unchanged in substance: the `CANONICAL_ORIGIN` www-host assertions and the `absoluteUrl`
    root/non-root slash assertions; the `businessJsonLd` HVACBusiness / stable `@id` / gated
    `aggregateRating` / no inline `review[]` / `geoRadius === 60000` assertions; and the
    `faqJsonLd(findBySlug("/contact")) === null` null-on-empty assertion.

    KEEP the named structural safety belt that is TRUE TODAY: `isIndexable(findBySlug("/privacy-beleid")) === false`.
    Do NOT add hub/pillar named-indexable assertions here — they are false until 08-03/08-04 and are
    added there (D-02 arrives incrementally, one landing at a time).

    Do NOT pass a `floor` argument (D-09 — no floor this landing). Update the file header comment and the
    final `console.log` to describe what the script now locks (a relational invariant, not a page count);
    the current success line still says "4 indexable statics, sitemap 4 absolute-apex, apex canonical" and
    is stale in three separate ways — it is a small monument to exactly the drift this phase is fixing.
  </action>
  <verify>
    <automated>npx tsx scripts/assert-seo.ts</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('scripts/assert-seo.ts','utf8'); const bad=[]; if(!/checkIndexationInvariants/.test(s))bad.push('must call the invariant checker'); if(/entries\.length,\s*5|length === 5/.test(s))bad.push('hardcoded 5-entry snapshot assertion still present'); if(/deepEqual\(\s*indexableUrls/.test(s))bad.push('hardcoded indexable URL list still present'); if(/draft hub must be noindex/.test(s))bad.push('state-dependent draft-hub assertion still present'); if(!/privacy-beleid/.test(s))bad.push('privacy-beleid safety belt must survive'); if(!/HVACBusiness/.test(s))bad.push('JSON-LD assertions must survive'); if(!/CANONICAL_ORIGIN/.test(s))bad.push('canonical-origin assertions must survive'); if(/INDEXABLE_FLOOR|floor:/.test(s))bad.push('no floor this landing (D-09)'); if(bad.length){console.error(bad.join('; '));process.exit(1)} console.log('assert-seo rewrite OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/assert-seo.ts` asserts `checkIndexationInvariants()` returns an empty list and prints every violation on failure.
    - The 5-URL deep-equal, the `length === 5`, and the "draft hub/service must be noindex" assertions are gone.
    - Canonical-origin, `absoluteUrl` slash, JSON-LD, and faq-null-on-empty assertions survive.
    - `isIndexable(privacy-beleid) === false` is asserted by name.
    - No floor is passed; the success message describes a relational invariant, not a page count.
    - Exits 0 against today's data (5 indexable).
  </acceptance_criteria>
  <done>The gate that enforced the bug now asserts a relationship that is true at 5 pages and at 27 alike.</done>
</task>

<task type="auto">
  <name>Task 4: Prove the gate bites — repair + extend scripts/assert-gate-blocks.ts (D-03)</name>
  <files>scripts/assert-gate-blocks.ts</files>
  <read_first>
    - scripts/assert-gate-blocks.ts (the whole file — the `structuredClone(PAGES)` → mutate → assert-rejection harness pattern, and perturbation (B) which is the broken one)
    - lib/seo/invariants.ts (Task 2) and lib/seo/policy.ts (`indexableSurface`, `sitemapEntries`, `absoluteUrl`, `isIndexable`)
    - lib/services/types.ts lines 144-167 (`publishedContentSchema` — the >=120 word / >=1 step / 3-6 FAQ bar perturbation (B) is meant to trip) and lines 224-237 (`pageSchema`)
    - 08-RESEARCH.md §"Pattern 2: Perturbation proofs" (the P1-P5 table and the content-addressed repair) and §"Guard baseline" (why (B) decayed)
  </read_first>
  <action>
    Two jobs: repair the decayed perturbation, then add invariant perturbations.

    REPAIR (B). It currently does `shortClone[1].status = "published"` and expects `pagesSchema` to reject.
    `PAGES[1]` is now the airconditioning pillar — `review` with a 148-word intro — so it validates and the
    assertion has been proving nothing since Phase 4's content landed. Rewrite it CONTENT-addressed: find a
    node by predicate (`clone.find(n => n.type === "pillar")`), set `status = "published"`, AND replace its
    content with a genuinely thin one (`content: { ...node.content, intro: "Te kort." }`). Add a short
    comment stating the lesson: an index-addressed perturbation is itself a snapshot assertion and decays
    silently — address perturbations by predicate and by the property under test, never by array position.
    Audit (A) and (C) the same way; (C) already uses `.find()`, (A) uses `[0]`/`[1]` for a duplicate-keyword
    test where position is harmless but should still be made predicate-based for consistency.

    EXTEND with the D-03 invariant proofs, each asserting the EXPECTED violation code is present (not merely
    that the list is non-empty — a wrong-reason pass is how the last gate rotted):
    - P1 `sitemap-without-index`: clone the nodes, revert a pillar to `status: "review"`, pass the REAL
      `sitemapEntries()` as `entries`. (Note: after 08-03 this pillar is `published` and in the sitemap, so
      reverting it must be caught. Write the perturbation so it works in both states: if the node is not
      currently indexable, instead make it indexable in the clone and leave `entries` untouched — either
      direction must produce a relational violation. Keep it simple and assert on the union of
      `sitemap-without-index` / `index-without-sitemap`.)
    - P2 `index-without-sitemap`: pass `sitemapEntries().slice(1)` as `entries`.
    - P3 `orphan-entry`: append `{ url: absoluteUrl("/niet-bestaand") }` to the entries.
    - P4 `below-floor`: call with `{ floor: <current indexable count + 1> }` (derived, not hardcoded).
    - P5 clean: `checkIndexationInvariants()` on real data returns exactly `[]`.

    Update the final `console.log` to enumerate what is now proven.
  </action>
  <verify>
    <automated>npx tsx scripts/assert-gate-blocks.ts</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('scripts/assert-gate-blocks.ts','utf8'); const bad=[]; if(/Clone\[1\]|clone\[1\]|shortClone\[1\]/.test(s))bad.push('index-addressed perturbation still present'); for (const code of ['sitemap-without-index','index-without-sitemap','orphan-entry','below-floor']) if(!s.includes(code))bad.push('missing perturbation for '+code); if(!/checkIndexationInvariants/.test(s))bad.push('must exercise the invariant checker'); if(!/intro:/.test(s))bad.push('the thin-content perturbation must blank the intro, not trust an index'); if(bad.length){console.error(bad.join('; '));process.exit(1)} console.log('gate-blocks proofs OK')"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx -e "import { PAGES } from '@/lib/services/registry'; import { pagesSchema } from '@/lib/services/types'; const c = structuredClone(PAGES) as any[]; const p = c.find(n => n.type === 'pillar'); p.status = 'published'; p.content = { ...p.content, intro: 'Te kort.' }; if (pagesSchema.safeParse(c).success) { console.error('REPAIR FAILED: a published pillar with a blanked intro must be rejected'); process.exit(1); } console.log('thin-content bar provably still bites');"</automated>
  </verify>
  <acceptance_criteria>
    - No perturbation addresses a node by array index; every one is predicate/content-addressed.
    - The thin-content perturbation blanks the intro and is provably rejected by `pagesSchema`.
    - P1–P4 each assert the specific expected violation code; P5 asserts real data yields exactly zero violations.
    - `npx tsx scripts/assert-gate-blocks.ts` exits 0 (it exits 1 before this task).
  </acceptance_criteria>
  <done>The gate can demonstrate its own failure modes — the property the previous gate lacked, which is why it rotted.</done>
</task>

<task type="auto">
  <name>Task 5: Repair scripts/assert-registry.ts — relational surface, not a snapshot count</name>
  <files>scripts/assert-registry.ts</files>
  <read_first>
    - scripts/assert-registry.ts lines 30-38 (`EXPECTED_PAGE_COUNT = 27`, the assertion that is RED against the real 28) and the rest of the file (assertions 2-14, all of which are correct and must survive)
    - lib/services/registry.ts (`PAGES`, `findByType`, `pillars`, `childrenOf`) — the composition being asserted
    - 08-RESEARCH.md §"Guard baseline" (why this went red and was never noticed) and §Pitfall 1
    - 08-CONTEXT.md §D-06 (named constant + anti-bump failure message — the same treatment applies here)
  </read_first>
  <action>
    `assert-registry.ts` currently exits 1: `EXPECTED_PAGE_COUNT = 27` but `PAGES.length === 28`. `/projecten`
    joined the surface in quick task `260719-t62`, `assert-seo.ts` was bumped 4→5, and this unwired sibling
    was never touched. Repair it in the D-06 spirit — do NOT simply change 27 to 28, which is the exact move
    commit `82d897b` made.

    Replace the single snapshot count with a relational composition assertion:
    - assert the type composition explicitly by name — exactly 1 hub, exactly 4 pillars, at least 17 services,
      at least 6 statics (services and statics are floors so a legitimately-added page does not require a gate
      edit, mirroring D-02);
    - assert `PAGES.length` equals the SUM of the four `findByType()` lengths (catches a node with an
      unrecognised `type` silently dropping out of every traversal);
    - assert `PAGES.length >= MIN_ROUTABLE_PAGES`, a named constant set to 28 with its derivation stated in a
      comment (1 hub + 4 pillars + 17 sub-services + 6 statics) and an anti-bump failure message in the D-06
      wording: a DROP means a page left the routable surface — find what disappeared; only lower this when a
      page is deliberately retired.

    Fix the stale "5 static" / "= 27" arithmetic in the file header comment and in the closing `console.log`.
    Leave assertions (2)-(14) untouched — they are relational already and they pass.
  </action>
  <verify>
    <automated>npx tsx scripts/assert-registry.ts</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('scripts/assert-registry.ts','utf8'); const bad=[]; if(/EXPECTED_PAGE_COUNT\s*=\s*2[78]/.test(s) && !/MIN_ROUTABLE_PAGES/.test(s))bad.push('still a snapshot count - use a named floor + composition'); if(!/MIN_ROUTABLE_PAGES/.test(s))bad.push('named floor constant missing'); if(!/deliberately retired|bewust/.test(s))bad.push('anti-bump failure message missing (D-06)'); if(!/findByType/.test(s))bad.push('type composition assertion missing'); if(!/trailFor|brandsForPillar/.test(s))bad.push('existing helper assertions must survive'); if(bad.length){console.error(bad.join('; '));process.exit(1)} console.log('assert-registry repair OK')"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx -e "import { PAGES, findByType } from '@/lib/services/registry'; const sum = ['hub','pillar','service','static'].reduce((n, t) => n + findByType(t as any).length, 0); if (sum !== PAGES.length) { console.error('composition sum ' + sum + ' != PAGES.length ' + PAGES.length); process.exit(1); } console.log('composition holds: ' + PAGES.length + ' nodes');"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/assert-registry.ts` exits 0 (it exits 1 before this task).
    - The hardcoded `EXPECTED_PAGE_COUNT = 27` equality is gone; the surface is asserted by type composition plus a named `MIN_ROUTABLE_PAGES` floor with a stated derivation.
    - The failure message forbids bumping the number to make the build pass.
    - Assertions (2)–(14) still present and passing.
  </acceptance_criteria>
  <done>A guard that had been silently red for a month is green — and can no longer be repaired by editing a number.</done>
</task>

<task type="auto">
  <name>Task 6: Guard the buildMetadata bypass path (scripts/assert-metadata-seam.ts) — D-04</name>
  <files>scripts/assert-metadata-seam.ts</files>
  <read_first>
    - lib/seo/metadata.ts (the seam — `buildMetadata` derives `robots: { index: isIndexable(node), follow: true }`; this is the ONLY place a robots directive may be written)
    - app/diensten/page.tsx line 12 and app/diensten/[pillar]/page.tsx lines 93-103 (the two `buildMetadata` call shapes: a module-level `export const metadata` and an async `generateMetadata`)
    - app/robots.ts (the legitimate `robots()` route handler that MUST be allowlisted)
    - scripts/assert-site-shape.ts (house style for a `node:assert`-only build-time CLI with a file-header comment)
    - 08-RESEARCH.md §"Pattern 3: Seam guard" (comment-stripping requirement — all 8 current `robots|noindex` hits under app/ are inside comments) and §"D-04 seam guard — verified tractable"
  </read_first>
  <action>
    Create `scripts/assert-metadata-seam.ts`, a `node:assert/strict` build-time CLI in the same house style
    as `assert-site-shape.ts` (header comment explaining it is not shipped runtime code, intentional console
    usage, non-zero exit aborts the build).

    Walk every `.ts`/`.tsx` file under `app/` recursively using `node:fs`. For each file, STRIP COMMENTS
    FIRST — block comments (`/* … */`, which also covers JSX `{/* … */}`) then line comments, using a
    pattern that does not eat `https://` (match `//` only when not preceded by `:`). This is mandatory: the
    only current `robots`/`noindex` occurrences under `app/` are explanatory comments, so an unstripped
    grep fails on day one.

    Then assert, over the stripped source:
    1. No file under `app/` contains a `robots\s*:` object key, and none contains `index\s*:\s*false`,
       EXCEPT the allowlist `app/robots.ts` (which is the robots.txt route handler and legitimately names
       the concept). Failure message must say: a per-page robots directive may only come from
       `buildMetadata()` in `lib/seo/metadata.ts` — the relational indexation gate cannot see a route that
       writes its own.
    2. Every `app/**/page.tsx` imports `buildMetadata` from `@/lib/seo/metadata`. Failure message must name
       the offending file and point at the seam.

    Print the number of files scanned and the number of page routes verified on success. Keep the
    implementation small; if the comment-stripping regex ever becomes fragile maintenance, D-04 permits
    dropping this guard — note that in the header so a future maintainer knows it is discretionary.
  </action>
  <verify>
    <automated>npx tsx scripts/assert-metadata-seam.ts</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('scripts/assert-metadata-seam.ts','utf8'); const bad=[]; if(!/buildMetadata/.test(s))bad.push('must assert buildMetadata imports'); if(!/app\/robots\.ts|robots\.ts/.test(s))bad.push('app/robots.ts must be allowlisted'); if(!/\/\*|block/.test(s))bad.push('comment stripping missing - required, see 08-RESEARCH Pattern 3'); if(bad.length){console.error(bad.join('; '));process.exit(1)} console.log('seam guard shape OK')"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && cp app/diensten/page.tsx /tmp/seam-backup.tsx && printf '\nexport const bogus = { robots: { index: false } };\n' >> app/diensten/page.tsx && (npx tsx scripts/assert-metadata-seam.ts > /dev/null 2>&1 && echo "SEAM GUARD FAILED TO CATCH A REAL BYPASS" && cp /tmp/seam-backup.tsx app/diensten/page.tsx && exit 1 || (cp /tmp/seam-backup.tsx app/diensten/page.tsx && echo "seam guard provably catches a real bypass"))</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/assert-metadata-seam.ts` exists and exits 0 against the current tree.
    - Comments are stripped before matching (verified: the 8 existing comment-only hits do not trip it).
    - `app/robots.ts` is allowlisted; every `app/**/page.tsx` is asserted to import `buildMetadata`.
    - The negative proof passes: injecting a literal `robots: { index: false }` into a page makes the script exit non-zero, and the file is restored afterwards.
  </acceptance_criteria>
  <done>"The seam is the only way" is enforced instead of merely commented.</done>
</task>

<task type="auto">
  <name>Task 7: Wire all seven guards into prebuild + widen the radius scan (D-01/D-07)</name>
  <files>package.json, scripts/check-radius-literals.sh</files>
  <read_first>
    - package.json (the current `prebuild`: only `validate-taxonomy.ts && assert-no-forbidden-claims.ts` — five guards are executed by NOTHING)
    - scripts/check-radius-literals.sh (the `PATTERN` and the `grep -rn "$PATTERN" app components` line to widen)
    - 08-CONTEXT.md §D-01 (all guards build-blocking, a few seconds of tsx startup accepted) and §D-07 (widen to lib/, watch for false positives)
    - 08-RESEARCH.md §"D-07 radius widening — verified no false positives" (lib/ is clean; scripts/ must NOT be scanned — a comment there contains "60 km")
  </read_first>
  <action>
    Widen `scripts/check-radius-literals.sh`: change the grep target from `app components` to
    `app components lib`. Do NOT add `scripts` — a comment in `assert-seo.ts` contains "60 km" and would
    false-positive. Update the header comment to state the new scope and WHY `lib/` matters now (the
    `/diensten` hub copy authored in 08-04 lives in `lib/services/registry.ts`, and a hardcoded radius there
    would otherwise sail past the guard, contradicting the `SITE.serviceRadiusKm` single source, QA-03).

    Then rewrite `package.json`'s `prebuild` to chain all seven guards with `&&`, cheapest/most structural
    first so a failure reports the most actionable cause:

    `tsx scripts/validate-taxonomy.ts && tsx scripts/assert-registry.ts && tsx scripts/assert-site-shape.ts && tsx scripts/assert-no-forbidden-claims.ts && tsx scripts/assert-gate-blocks.ts && tsx scripts/assert-metadata-seam.ts && tsx scripts/assert-seo.ts && bash scripts/check-radius-literals.sh`

    Also update the "run on demand" line in each guard's header comment that still claims it is not wired
    (`assert-seo.ts`, `assert-site-shape.ts` — whose header explicitly says "it is NOT wired into prebuild" —
    `assert-registry.ts`, `assert-gate-blocks.ts`), so the comments stop describing a world that no longer
    exists. That documentation drift is the same failure mode as the stale assertions.

    CRITICAL (OneDrive): run `npm run prebuild` to exercise the chain, but do NOT run `npm run build` /
    `next build` / `tsc --noEmit` locally — they deadlock on this mount. The Vercel preview is the build gate.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run prebuild</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const p=require('./package.json'); const pre=p.scripts.prebuild||''; const need=['validate-taxonomy','assert-registry','assert-site-shape','assert-no-forbidden-claims','assert-gate-blocks','assert-metadata-seam','assert-seo','check-radius-literals']; const miss=need.filter(k=>!pre.includes(k)); if(miss.length){console.error('prebuild is missing: '+miss.join(', '));process.exit(1)} console.log('prebuild chains all 7 guards')"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('scripts/check-radius-literals.sh','utf8'); const g=s.split('\n').filter(l=>l.includes('grep -rn')).join(''); if(!/app components lib/.test(g)){console.error('radius scan not widened to lib/');process.exit(1)} if(/\bscripts\b/.test(g)){console.error('scripts/ must not be scanned - false positive in assert-seo.ts');process.exit(1)} console.log('radius scan scope OK')" && bash scripts/check-radius-literals.sh</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `prebuild` chains all seven guards with `&&`; a non-zero exit from any one aborts `next build`.
    - `npm run prebuild` exits 0 locally (all seven green — two of them were red before this plan).
    - `check-radius-literals.sh` scans `app components lib` and does NOT scan `scripts/`.
    - No guard header still claims it is "run on demand" / "NOT wired into prebuild".
    - `next build` / `tsc --noEmit` were NOT run locally.
  </acceptance_criteria>
  <done>Nothing stands between a regression and production by accident any more — every guard runs on every build.</done>
</task>

<task type="checkpoint">
  <name>Task 8: Preview gate — the newly-wired guards must run for real</name>
  <files></files>
  <read_first>
    - 08-CONTEXT.md §D-08 (no local `next build` on this mount; the preview build is where the wired gates run for real)
    - .planning/STATE.md §"Blockers/Concerns" (the Vercel preview workflow used in Phases 4-7)
  </read_first>
  <action>
    Commit the plan's work, push the branch, and let Vercel build the preview. This is the first time the
    five newly-wired guards execute inside a real `next build`, so it is the only trustworthy proof that
    the `prebuild` chain does not break the build on Vercel's clean checkout (local `tsx` runs prove the
    assertions, not the lifecycle wiring).

    `git push -u origin gsd/phase-8-indexation-unlock`, then read the Vercel build log for the deployment.
    Confirm the log shows all seven guard success lines before the `next build` output, and that the build
    completes READY. Record the preview URL and the deployment id in the plan summary.

    If the build fails, the failing guard's output IS the diagnosis — fix it in this plan rather than
    carrying it forward; a red gate here would block every later landing.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && git rev-parse --abbrev-ref HEAD | grep -q '^gsd/phase-8-indexation-unlock$' && git log origin/gsd/phase-8-indexation-unlock -1 --oneline</automated>
  </verify>
  <acceptance_criteria>
    - Branch `gsd/phase-8-indexation-unlock` is pushed to origin.
    - The Vercel preview deployment for that branch reaches state READY.
    - The build log shows all seven guard success lines (taxonomy, registry, site-shape, forbidden-claims, gate-blocks, metadata-seam, seo, radius) ahead of the Next build.
    - The preview `sitemap.xml` still lists 5 URLs (this landing changes no indexability).
    - The preview URL and deployment id are recorded in the summary.
  </acceptance_criteria>
  <done>The gate is real: it runs on Vercel, on a clean checkout, on every build — and it is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| taxonomy data → indexing policy | `status` fields authored by hand decide what Google may index; a wrong value silently de-indexes revenue pages |
| indexing policy → served output | `isIndexable()` feeds both `sitemap.xml` and every page's `robots` meta; any bypass breaks the single-source guarantee |
| build gate → production | `prebuild` is the ONLY thing standing between a regression and a live deploy (no CI exists on this repo) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-drift | Tampering | sitemap vs robots directive | mitigate (this plan) | Relational per-node invariant: membership ⇔ `isIndexable()`; asserted build-blocking in `assert-seo.ts` |
| T-8-snapshot | Repudiation | Build gates that encode "what is" | mitigate (this plan) | Snapshot assertions replaced by relationships + named floors; `assert-registry.ts` repaired the same way |
| T-8-bump | Tampering | Human "fix" by editing the expected number | mitigate (this plan) | D-06 anti-bump failure messages on every floor; the derivation is stated inline |
| T-8-decay | Repudiation | A proof harness that silently stops proving | mitigate (this plan) | Index-addressed perturbations replaced with predicate/content-addressed ones; each proof asserts a specific violation code |
| T-8-bypass | Elevation of Privilege | A route writing its own `robots` directive | mitigate (this plan) | `assert-metadata-seam.ts` — no `robots:`/`index:false` outside the seam; every page must import `buildMetadata`; negative proof included |
| T-8-unwired | DoS (of the gate itself) | Guards nothing executes | mitigate (this plan) | All seven chained into `prebuild`; header comments corrected so no guard claims to be on-demand |
| T-8-parallel | Tampering | A second URL list appended to `app/sitemap.ts` | mitigate (this plan) | `orphan-entry` violation — any sitemap URL with no backing node fails the gate |
</threat_model>

<verification>
- Every task verify is a `tsx`/`node -e` run (seconds) — OneDrive-safe. `next build` and full `tsc --noEmit` are NEVER run locally (they deadlock on this mount).
- Two guards are RED at the start of this plan and MUST be green at the end: `assert-registry.ts` (28 ≠ 27) and `assert-gate-blocks.ts` (no-op perturbation). `npm run prebuild` exiting 0 is the composite proof.
- Task 4 and Task 6 each carry a NEGATIVE proof (a real perturbation / a real bypass injected and then reverted) — a gate that has never been observed to fail has not been shown to work.
- Task 8 is the build-lifecycle proof on Vercel; local `tsx` runs cannot prove `prebuild` wiring.
- Indexable count must remain 5 throughout this plan. Any change means the predicate moved, which belongs to 08-02.
</verification>

<success_criteria>
- IDX-01 is met in code: the SEO build gate asserts a relational invariant (sitemap membership ⇔ `isIndexable()` per node) plus per-entry backing, and is provably capable of failing.
- All seven guards are build-blocking and green, including the two that were silently red.
- `buildMetadata` is enforced as the only source of a per-page robots directive.
- The Vercel preview builds green with the full chain, still at 5 indexable pages — the gate is proven before it guards anything (D-09).
</success_criteria>

<output>
Create `.planning/phases/08-indexation-unlock/08-01-SUMMARY.md` when done.
</output>
