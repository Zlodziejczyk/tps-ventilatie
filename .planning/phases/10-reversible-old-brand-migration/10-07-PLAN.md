---
phase: 10-reversible-old-brand-migration
plan: 07
type: execute
wave: 3
depends_on: ["10-01", "10-02", "10-04", "10-05", "10-06"]
files_modified:
  - docs/seo-owner-runbook.md
  - docs/baseline/<capture-date>/dns/vercel-target-records.md
  - docs/baseline/<capture-date>/preflight/
autonomous: false
requirements: [MIG-07, MIG-10]
user_setup:
  - service: vercel
    why: "Both legacy hostnames must be attached to the project as ordinary serving domains; the alias table is dashboard state, not git, and the platform default is the wrong one (D-08)"
    dashboard_config:
      - task: "Add tpsventilatie.nl and www.tpsventilatie.nl to the project, each with the 'Redirect to' dropdown left unset ('No Redirect')"
        location: "Vercel -> project prj_vL6mnZFhKHcxBjmyeCtrhJEKob0Q -> Settings -> Domains"
      - task: "Read the exact A value and CNAME target Vercel shows for these two hostnames and record them verbatim"
        location: "Vercel -> Settings -> Domains (values are project-specific and have changed before)"
must_haves:
  truths:
    - "The redirect map is live in production but inert: every rule is host-gated and no legacy DNS points at Vercel yet, so deploying it is a harmless and fully reversible act (RESEARCH Pattern 4)"
    - "Production still serves direct 200s on the canonical host after the map ships — the live proof that the catch-all is host-gated and did not turn the site into a redirect to itself (RESEARCH Pitfall 2)"
    - "Both legacy hostnames are attached to the Vercel project as ordinary serving domains with NO Vercel-level Redirect to — the platform default would rebuild the exact chain the map exists to kill, and repointing only the apex would strand a three-hop walk (D-08)"
    - "Invalid Configuration on the Vercel Domains page is the EXPECTED state during pre-flight: it is only a DNS check, and the alias table binding hostname to deployment is independent of it — which is precisely why the spoofed-Host probe works before any DNS change (D-09)"
    - "The exact A value and CNAME target were read off this project's Domains page and recorded verbatim, not copied from a document: Vercel issues project-specific values and has changed the apex IP at least once"
    - "The whole map is proven green through a spoofed Host header against the production custom domain BEFORE any DNS record changes — 9 sources on both legacy hostnames plus the catch-all samples and the trailing-slash canary (D-09, D-18)"
    - "The rollback procedure names exactly two triggers — mail failing to send or receive, or either legacy hostname lacking a valid certificate around 30 minutes after the flip — and explicitly excludes search performance and broken redirects, because a broken redirect is fixed by a deploy and a post-migration dip is expected (D-10)"
    - "The rollback procedure is a copy-paste, not a recollection: both the old and the new values are written out, and it says revert the two hostname RECORDS, because the www side changes record type from A to CNAME (RESEARCH Pattern 5)"
    - "The rollback document states day 28 as the declared point of no return and the silent dependency nobody would think to check: the old WordPress install lives exactly as long as the cyberfolks subscription, and that subscription exists only for info@tpsventilatie.nl (D-11)"
    - "The MIG-01 through MIG-04 pre-flight checklist is signed off as an artefact with one evidence pointer per line, so the hard gate is a gate rather than a sentence"
  artifacts:
    - path: "docs/baseline/<capture-date>/dns/vercel-target-records.md"
      provides: "the A value and CNAME target verbatim off the Domains page, with the date read and a screenshot reference"
      contains: "CNAME"
    - path: "docs/baseline/<capture-date>/preflight/preflight-checklist.md"
      provides: "the signed-off pre-flight gate with one evidence pointer per line"
      contains: "MIG-04"
    - path: "docs/seo-owner-runbook.md"
      provides: "the Dutch rollback section — two triggers, both record value sets, day 28, the subscription dependency"
      contains: "Terugdraaien"
  key_links:
    - from: "docs/seo-owner-runbook.md rollback section"
      to: "docs/baseline/<capture-date>/dns/vercel-target-records.md"
      via: "the new values in the rollback table are the ones read off the Domains page, never from a guide"
      pattern: "vercel-target-records"
    - from: "scripts/verify-redirects.ts --via"
      to: "the production custom domain with a spoofed legacy Host header"
      via: "Vercel dispatches on the HTTP Host header, so the whole map runs before any DNS change"
      pattern: "Host"
---

<objective>
Open the pre-flight hard gate and prove it (D-08, D-09; MIG-07's attach half, MIG-10). Land the map in
production where it is live but inert, attach both legacy hostnames with no Vercel-level redirect, read
the real record values off the Domains page, run the whole map green through a spoofed `Host` header
while the old site is still serving, and write the rollback procedure with both value sets.

Purpose: "the pre-flight checklist completes before any DNS change" is only a gate if something actually
runs it. This is the last plan in the phase where every action is reversible with no trace — the next
plan starts a clock, and the one after that opens the one-way door.
Output: production carrying the map, two attached hostnames, a green Mode A probe, the runbook rollback
section, and a signed-off pre-flight checklist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md
@.planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md
@.planning/phases/10-reversible-old-brand-migration/10-PATTERNS.md
@.planning/phases/10-reversible-old-brand-migration/10-VALIDATION.md
@.planning/phases/10-reversible-old-brand-migration/10-05-SUMMARY.md
@docs/seo-owner-runbook.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge to main and land the map in production — live but inert (Execution Sequence step 8)</name>
  <files>docs/baseline/&lt;capture-date&gt;/preflight/</files>
  <read_first>
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pattern 4 — *"The map must already be deployed to production"*: spoofing `Host` against a preview URL still serves production, so a preview cannot be pre-flight-tested this way; the rules are host-gated and until the dd24 records move no real request can carry a legacy `Host`, which is why deploying the map is fully reversible. Also §Pitfall 2 (what an un-gated catch-all does to a live site)
    - .planning/phases/09-measurement-foundation/09-05-PLAN.md Task 1 — the fast-forward merge pattern this project uses, and the note that `main` deploys straight to production and re-runs the guard chain
    - .planning/STATE.md §Session Continuity — the deployment model: `main` deploys to production on `https://www.tpsklimaattechniek.nl`
  </read_first>
  <action>
    Merge the phase branch into `main` fast-forward —
    `git checkout main && git merge --ff-only gsd/phase-10-reversible-old-brand-migration && git push` —
    matching how Phases 5, 6, 8 and 9 landed. If `main` has moved, merge `origin/main` into the phase
    branch first and re-push so the preview re-validates, then fast-forward. Never rebase a pushed branch.

    Wait for the production deployment to reach READY and confirm the build log shows **nine** guard
    success lines — the first time the nine-guard chain runs on the production deploy path.

    Then prove the thing that matters most about this deploy: **production still works.** Fetch the
    canonical home page and two inner pages with redirect following disabled and confirm each returns a
    direct 200. An un-gated catch-all would make every page a 301 to itself; a browser would show
    `ERR_TOO_MANY_REDIRECTS` and Google would drop the site. The preview suggested this was fine;
    production is the statement that counts.

    Confirm the `verify-indexation.yml` run triggered by this deployment is green **and** that its
    redirect step printed its explicit skip line rather than asserting — the cutover is not declared, so
    the post-cutover mode must stand down loudly. A red step here, or a silent one, means the gating in
    plan 10-05 is wrong and must be fixed before going any further.

    Create `docs/baseline/<capture-date>/preflight/` (resolve the dated directory with
    `ls -d docs/baseline/*/legacy-site-mirror | head -1 | xargs dirname`) and start
    `production-landing.md` there: the deployment URL and SHA, the nine guard lines, the three observed
    200s, the workflow run URL and the quoted skip line.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && git rev-parse --abbrev-ref HEAD | grep -qx main && git fetch -q origin main && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" && echo "phase work is on main and pushed"</automated>
    <automated>for p in / /diensten /contact; do c=$(curl -sS -o /dev/null -w '%{http_code}' --max-redirs 0 --max-time 20 "https://www.tpsklimaattechniek.nl$p"); echo "$p -> $c"; test "$c" = "200" || exit 1; done; echo "production serves direct 200s — the catch-all is host-gated in the real deployment"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && gh run list --workflow verify-indexation.yml --limit 5 --json conclusion,event,databaseId --jq '[.[] | select(.event=="deployment_status")][0]' | grep -q '"conclusion":"success"' && echo "the post-deploy workflow is green with the redirect step standing down"</automated>
  </verify>
  <acceptance_criteria>
    - The phase work is merged into `main`, pushed, and the production deployment reached READY with nine guard success lines in its build log.
    - `https://www.tpsklimaattechniek.nl/`, `/diensten` and `/contact` each return a direct 200 with redirect following disabled.
    - The `deployment_status`-triggered `verify-indexation.yml` run concluded `success`, and its redirect step printed an explicit skip line naming the undeclared cutover.
    - `docs/baseline/<date>/preflight/production-landing.md` records the deployment URL and SHA, the guard lines, the three 200s, the run URL and the quoted skip line.
  </acceptance_criteria>
  <done>The map is in production, where the pre-flight probe can reach it, and the live site is demonstrably unharmed.</done>
</task>

<task type="checkpoint">
  <name>Task 2: Attach both legacy hostnames to the Vercel project with no redirect, and record the real record values (D-08)</name>
  <files>docs/baseline/&lt;capture-date&gt;/dns/vercel-target-records.md</files>
  <read_first>
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pattern 5 — the quoted Vercel default this guards against (*"we will attempt to redirect automatically"*, *"We recommend using the www subdomain as your primary domain, with a redirect from the non-www domain to it"*), the dashboard control (**Settings → Domains → Edit → "Redirect to" dropdown**, left unset), why *Invalid Configuration* is expected, the measured sibling-domain pattern, and the naming correction: the `www` side is a **CNAME** in the proven pattern while the legacy `www` is currently an **A** record
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Open Questions #2 — *"step 9 reads the values off the Domains page and the plan records them verbatim before step 17. Do not hardcode from this document"*
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Environment Availability — the Vercel CLI is a local dependency only and `vercel` enable is interactive-only on this machine, so this is a **dashboard action by the user**
    - .planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md §D-08 (the hard constraint) and §D-09 (attach first, DNS later)
  </read_first>
  <action>
    Ask the user to do exactly this in the Vercel dashboard, on project
    `prj_vL6mnZFhKHcxBjmyeCtrhJEKob0Q` → Settings → Domains, and nothing else:

    1. Add `tpsventilatie.nl`. When Vercel offers to add the `www` counterpart with a redirect, **decline
       the redirect** — the "Redirect to" dropdown must remain unset ("No Redirect").
    2. Add `www.tpsventilatie.nl` separately, also with **no** "Redirect to".
    3. Leave the existing `tpsklimaattechniek.nl` apex → `www` redirect exactly as it is. That is the
       canonical domain's own normalisation, it is the same platform mechanism, and it is irrelevant to
       the legacy pair because every legacy destination already points at the `www` host.
    4. Both new entries will show **Invalid Configuration**. That is expected and correct: it is only a
       DNS check, and the DNS still points at WordPress. Do not "fix" it.
    5. Read off, and send back verbatim, the **A value** Vercel shows for the apex and the **CNAME
       target** it shows for `www`. Do not copy them from any document — they are project-specific and
       Vercel has changed the apex IP at least once.
    6. Screenshot the Domains page showing both legacy hostnames with no redirect target.

    Record the answer in `docs/baseline/<capture-date>/dns/vercel-target-records.md`: the two values
    verbatim, the date and time read, a note that they were read off the dashboard rather than copied,
    the screenshot filename, and — critically — the **current** values they will replace
    (`tpsventilatie.nl` A `195.78.67.39` and `www.tpsventilatie.nl` A `195.78.67.39`, both TTL 28800 as
    live-verified). Both sets in one file is what makes the next two plans copy-paste operations. Add a
    row to the dated baseline manifest.

    Do **not** touch DNS in this task. Nothing here changes what any resolver answers.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && D=$(ls -d docs/baseline/*/legacy-site-mirror 2>/dev/null | head -1 | xargs dirname) && node -e "
const fs=require('fs');const f=process.argv[1]+'/dns/vercel-target-records.md';const bad=[];
if(!fs.existsSync(f)){console.error('missing '+f);process.exit(1)}
const t=fs.readFileSync(f,'utf8');
if(!/CNAME/.test(t))bad.push('no CNAME target recorded for the www side');
if(!/\b\d{1,3}(\.\d{1,3}){3}\b/.test(t))bad.push('no A value recorded for the apex');
if(!t.includes('195.78.67.39'))bad.push('the current (pre-cutover) value is not recorded — the revert would be a recollection');
if(!/28800/.test(t))bad.push('the current TTL is not recorded');
if(!/\d{4}-\d{2}-\d{2}/.test(t))bad.push('no date read');
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('both record value sets recorded verbatim with a date');
" "$D"</automated>
    <automated>for h in tpsventilatie.nl www.tpsventilatie.nl; do c=$(curl -sS -o /dev/null -w '%{http_code}' --max-redirs 0 --max-time 20 -H "Host: $h" https://www.tpsklimaattechniek.nl/); echo "$h via spoofed Host -> $c"; test "$c" != "404" || { echo "still DEPLOYMENT_NOT_FOUND — $h is not attached yet"; exit 1; }; done; echo "both legacy hostnames resolve in the Vercel alias table"</automated>
    <automated>for h in tpsventilatie.nl www.tpsventilatie.nl; do a=$(dig +short A "$h" | head -1); echo "$h A -> $a"; test "$a" = "195.78.67.39" || { echo "DNS changed in a task that must not touch DNS"; exit 1; }; done; echo "no DNS was touched — still on the old box"</automated>
  </verify>
  <acceptance_criteria>
    - Both `tpsventilatie.nl` and `www.tpsventilatie.nl` appear on the project's Domains page with the "Redirect to" dropdown unset, evidenced by a screenshot.
    - A spoofed-`Host` request for each legacy hostname against the production custom domain no longer returns 404 `DEPLOYMENT_NOT_FOUND` — proof the alias table binds them despite Invalid Configuration.
    - `docs/baseline/<date>/dns/vercel-target-records.md` records the new A value, the new CNAME target, the current A values and the current TTL 28800, with the date they were read and a note that they came off the dashboard.
    - `dig +short A` for both legacy hostnames still answers `195.78.67.39` — this task changed no DNS.
    - The existing `tpsklimaattechniek.nl` apex redirect was left alone.
  </acceptance_criteria>
  <done>The alias table binds both legacy hostnames, which is what makes a pre-flight proof possible at all — and the exact values the flip will use are written down.</done>
</task>

<task type="checkpoint">
  <name>Task 3: Run the spoofed-Host pre-flight probe green, write the rollback section, and sign off the pre-flight gate (D-09, D-10, D-11, MIG-10)</name>
  <files>docs/seo-owner-runbook.md, docs/baseline/&lt;capture-date&gt;/preflight/</files>
  <read_first>
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pattern 4 (the working pre-flight command and the two preconditions), §Pitfall 3 (the three non-map failures: 403 `x-vercel-mitigated: deny` from aiming at a `*.vercel.app` host, 404 `DEPLOYMENT_NOT_FOUND` from an unattached hostname, and a 308 to the canonical `www` meaning the map is not in production), §Code Examples (the full pre-flight loop, as the cross-check on the script's own report)
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pattern 7 (the runbook rollback section: D-10's two triggers and only those, both record value sets, D-11's day-28 line, the subscription dependency) and §Pattern 8 (where the day-28 declaration will live)
    - docs/seo-owner-runbook.md §7 (lines 149–173: the bulleted operational section — each bullet opens with a bolded noun phrase and closes with the concrete command or consequence) and the closing `### Samenvatting — wat hangt waarvan af`
    - docs/baseline/<capture-date>/dns/vercel-target-records.md (Task 2) and docs/baseline/<capture-date>/owner/ (plan 10-04's three artefacts)
  </read_first>
  <action>
    Run the pre-flight probe in Mode A:
    `npx tsx scripts/verify-redirects.ts --via https://www.tpsklimaattechniek.nl`. It must be **green**:
    9 sources × both legacy hostnames each a direct 301 to the exact expected location, each location a
    direct 200, plus the slash-less canary in one hop, `/robots.txt` and `/sitemap.xml` at 200, the two
    WordPress leftovers path-preserved to an honest 404, and the recorded `http`→`https` platform hop.
    Never aim it at a `*.vercel.app` URL. If it is red, read the diagnosis before touching the map: a 403
    means the wrong endpoint, a 404 means a hostname is not attached, and a 308 to the canonical `www`
    means production does not have the map. Do not "fix" a correct map because of a non-map failure.

    Save the full output to `docs/baseline/<capture-date>/preflight/preflight-probe.txt`. This is the
    single most important artefact in the phase: it is the evidence that the map was proven before the
    door opened, and after the flip it can never be re-taken in this form.

    Append the rollback section to `docs/seo-owner-runbook.md`, in Dutch, in the §7 bulleted register:

    - **Wanneer wel terugdraaien.** Exactly two triggers, and only these: `info@tpsventilatie.nl` fails to
      send **or** receive, or either legacy hostname lacks a valid certificate around 30 minutes after the
      flip.
    - **Wanneer niet.** Search performance never triggers a revert — a post-migration dip is expected, the
      180-day floor and the Phase 9 baseline exist to ride it out, and reverting mid-consolidation makes
      it permanently worse. A broken redirect is fixed by a deploy, not by DNS: pulling the DNS lever for
      an app-layer bug is the heaviest tool for the lightest problem.
    - **Hoe.** A table with both value sets from `vercel-target-records.md`, written out so the revert is
      a copy-paste and not a recollection: apex A back to `195.78.67.39`, and the `www` **CNAME deleted
      and the A record restored** to `195.78.67.39`. Say "de twee hostname-records", not "de twee
      A-records" — the `www` side changes record type. Note that at TTL 300 this reaches resolvers in
      minutes, which is the whole reason the TTLs go down first.
    - **Dag 28.** The declared point of no return: after it, reverting is a second migration, not a
      revert. Point at where the dated declaration will live in `docs/baseline/`.
    - **De stille afhankelijkheid.** The old WordPress install lives exactly as long as the cyberfolks
      subscription, and that subscription exists only for `info@tpsventilatie.nl`. Nobody would think to
      check this; it is the one dependency that can quietly delete the rollback target.
    - **De zoekkant.** A Change of Address can be cancelled within 180 days via **Cancel Move** in the old
      property — the search-side half of a revert, alongside the DNS half.
    - **Het oude install is niet vertrouwd na lange sluimer.** WordPress 7.1.1 is unpatched by design
      (D-02, accepted risk 1), so a revert after months restores a possibly-compromised site. The clean
      copy of the *content* is the mirror in `docs/baseline/`.

    Add the third `Samenvatting` bullet promised in plan 10-04: rollback hangs on TTL 300 until day 28.

    Then write `docs/baseline/<capture-date>/preflight/preflight-checklist.md` — the hard gate as an
    artefact, one line per item with an evidence pointer, and an explicit PASS/FAIL per line:
    MIG-01 (the mirror is committed and was spot-opened offline → `legacy-site-mirror/CAPTURE.md`),
    MIG-03 (webmail 200 and documented → runbook §8 and the 10-04 summary),
    MIG-04 (**both halves** → `owner/mig-04-mechanical.md` and `owner/mig-04-owner-confirmation.md`),
    the map proven green (→ `preflight/preflight-probe.txt`),
    both hostnames attached with no redirect (→ the Domains screenshot),
    and the advisory certificate question (→ `owner/mail-cert-question.md`, **non-blocking**; record the
    answer or "no answer yet" and proceed either way).
    The gate passes only when every blocking line is PASS. If MIG-04's human half is still outstanding,
    **stop here** and say so — it is a named hard gate and a route-only proof is half-true.

    Commit with `docs(10-07): pre-flight gate proven and rollback procedure recorded (D-09/D-10/D-11, MIG-10)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx scripts/verify-redirects.ts --via https://www.tpsklimaattechniek.nl</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && D=$(ls -d docs/baseline/*/legacy-site-mirror 2>/dev/null | head -1 | xargs dirname) && node -e "
const fs=require('fs');const d=process.argv[1];const bad=[];
const probe=d+'/preflight/preflight-probe.txt';
if(!fs.existsSync(probe))bad.push('the pre-flight probe output was not saved');
else{const t=fs.readFileSync(probe,'utf8');if(!/over-ons/.test(t))bad.push('probe output does not cover the mapped sources');if(!/308/.test(t))bad.push('probe output does not record the platform hop');}
const cl=d+'/preflight/preflight-checklist.md';
if(!fs.existsSync(cl))bad.push('no pre-flight checklist');
else{const t=fs.readFileSync(cl,'utf8');
  for(const k of ['MIG-01','MIG-03','MIG-04','PASS'])if(!t.includes(k))bad.push('checklist missing: '+k);
  if(/FAIL/.test(t)&&!/non-blocking|niet blokkerend/i.test(t))bad.push('the checklist contains a FAIL — the gate has not passed');
}
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('pre-flight probe output and signed checklist present, no blocking FAIL');
" "$D"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "
const fs=require('fs');const t=fs.readFileSync('docs/seo-owner-runbook.md','utf8');const bad=[];
for(const k of ['Terugdraaien','195.78.67.39','Cancel Move','cyberfolks','300'])if(!t.includes(k))bad.push('rollback section missing: '+k);
if(!/28/.test(t))bad.push('the day-28 point of no return is not stated');
if(/twee A-records/.test(t))bad.push('says two A records — the www side changes record type to CNAME');
if(/curl[^\n]*(--insecure|\s-k\b)/.test(t))bad.push('an insecure curl appeared in the runbook');
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('rollback section carries both triggers, both value sets, day 28, Cancel Move and the subscription dependency');
"</automated>
    <automated>for h in tpsventilatie.nl www.tpsventilatie.nl; do a=$(dig +short A "$h" | head -1); test "$a" = "195.78.67.39" || { echo "DNS changed before the gate closed"; exit 1; }; done; echo "still pre-DNS: every action in this plan remains reversible with no trace"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-redirects.ts --via https://www.tpsklimaattechniek.nl` exits 0 with all 18 source checks green, the slash-less canary in one hop, the two D-15 samples at 200, the two leftovers at 404 and the platform hop recorded.
    - The full probe output is saved to `docs/baseline/<date>/preflight/preflight-probe.txt` and committed.
    - `docs/seo-owner-runbook.md` has a Dutch rollback section containing both triggers, the exclusions, both record value sets including `195.78.67.39`, the TTL 300 note, day 28, `Cancel Move`, and the cyberfolks-subscription dependency — and it does not say "twee A-records".
    - `docs/baseline/<date>/preflight/preflight-checklist.md` exists with a PASS/FAIL line and an evidence pointer for MIG-01, MIG-03, MIG-04 (both halves), the probe, the attach, and the advisory certificate question marked non-blocking.
    - No blocking line is FAIL; if MIG-04's human half is outstanding, the plan stopped rather than proceeding.
    - `dig +short A` for both legacy hostnames still answers `195.78.67.39` — nothing in this plan touched DNS.
  </acceptance_criteria>
  <done>The hard gate is closed and evidenced. Everything up to this point can still be undone without a trace; nothing after it can.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `main` → production | the merge deploys; the map goes live on the canonical host, where an un-gated rule would be catastrophic |
| Vercel dashboard → the alias table | domain attachment is state outside git that no build gate can assert; the platform default is the wrong one |
| spoofed `Host` header → the edge matcher | the pre-flight deliberately sends a header no real client will send yet; platform refusals must be diagnosed, not mistaken for a broken map |
| the rollback document → a future incident at 03:00 | it will be read by someone under pressure; a recollection is not a procedure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-ungated-catchall | DoS (self-inflicted) | the production deployment | mitigate | three direct-200 checks on the canonical host immediately after the merge; the build-blocking host-gate assertion shipped in plan 10-03 |
| T-10-vercel-chain | Tampering | Vercel "Redirect to" on the legacy pair | mitigate | explicit instruction to leave the dropdown unset, a screenshot as evidence, and the pre-flight probe would show a second hop if it were set |
| T-10-stale-values | Tampering | the record values used at the flip | mitigate | values read off this project's Domains page and recorded verbatim with a date; never copied from a guide; both old and new sets in one file |
| T-10-preflight-misdiagnosis | Repudiation | the pre-flight result | mitigate | the probe diagnoses the three non-map failures by name, and the plan forbids "fixing" a correct map because of one |
| T-10-wp-dormant | Tampering | the rollback target | accept | stated in the rollback section: WordPress 7.1.1 is unpatched by design (D-02 accepted risk 1), so a revert after months restores a possibly-compromised site; the mirror is the clean copy of the content |
| T-10-rollback-target | Tampering | the cyberfolks subscription | mitigate | named explicitly in the rollback section as the silent dependency; it is the one thing that can quietly delete the revert target |
| T-10-mail-tls | DoS | TLS verification | mitigate | no `-k` / `--insecure` anywhere; asserted against the runbook text |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | no packages added by this plan |
</threat_model>

<verification>
- Task 1 merges the phase branch to `main`; from this plan onward the work is on `main`, matching how Phases 5, 6, 8 and 9 landed.
- **The Vercel production build is the build gate** — there is no local `next build`, `tsc --noEmit` or `npm run lint` on this OneDrive mount.
- Both checkpoints are blocking: Task 2 needs the user in the Vercel dashboard (the CLI is interactive-only on this machine), and Task 3 cannot pass while MIG-04's human half is outstanding.
- Every verify in this plan re-asserts that DNS has not moved. This is the boundary: nothing here is irreversible, and the next plan changes that.
</verification>

<success_criteria>
- The map is live in production, host-gated and inert, with the canonical site demonstrably serving direct 200s.
- Both legacy hostnames are attached with no Vercel-level redirect, and the exact record values for the flip are recorded verbatim alongside the current ones.
- The full map is proven green through a spoofed `Host` header before any DNS change, with the output committed.
- The Dutch rollback procedure is a copy-paste operation naming two triggers, day 28 and the subscription dependency.
- The pre-flight hard gate is signed off as an artefact with evidence per line.
</success_criteria>

<output>
Create `.planning/phases/10-reversible-old-brand-migration/10-07-SUMMARY.md` when done.
</output>
