---
phase: 10-reversible-old-brand-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/seo/redirects.ts
  - lib/seo/redirect-invariants.ts
  - lib/seo/cutover.ts
  - scripts/assert-redirects.ts
  - scripts/assert-gate-blocks.ts
  - package.json
  - next.config.ts
autonomous: true
requirements: [MIG-05, MIG-06]
must_haves:
  truths:
    - "lib/seo/redirects.ts holds the 9 legacy entries as typed data with every destination built from CANONICAL_ORIGIN, explicit statusCode 301 rather than permanent, and a per-entry confidence of certain or judgement (D-12, D-14, D-16)"
    - "The dakventilator entry targets the mechanische-ventilatie pillar, not a sub-service, and carries its live-page evidence in the file that outlives .planning/ (D-13, D-16)"
    - "The legacy host/robots.txt and /sitemap.xml are redirected like everything else, through the catch-all's path preservation (D-15)"
    - "The path-preserving catch-all is emitted AFTER the 9 explicit rules and is HOST-GATED — an un-gated /:path* would redirect the live site to itself and take it down, so a build-blocking assertion pins the has:host entry (D-12, RESEARCH Pitfall 2)"
    - "next.config.ts sets skipTrailingSlashRedirect true and re-adds Next's own /:path+/ normalisation rule LAST, because Next 16.2.1 otherwise unshifts that 308 to the FRONT of the array and every slashed legacy URL costs two hops while every build stays green (RESEARCH Pitfall 1, Finding 2)"
    - "The structural checks live in a pure injectable function, not inline asserts, so each one can be watched to fire on a perturbed map — a gate that has never been observed failing has not been shown to work (D-17, PATTERNS load-bearing call)"
    - "The build gate reasons about the array next.config.ts actually returns, so reordering the emitters or leaking a confidence/why key onto an emitted object fails the build rather than production (D-16, D-17)"
    - "assert-redirects.ts never touches the network: a build that depends on production being reachable would assert against the deployment it is producing (D-17)"
  artifacts:
    - path: "lib/seo/redirects.ts"
      provides: "LEGACY_HOST_PATTERN, the typed LEGACY_REDIRECTS map, and the three pure emitters next.config.ts imports"
      exports: ["LEGACY_HOST_PATTERN", "LEGACY_REDIRECTS", "toNextRedirects", "toCatchAllRedirect", "toTrailingSlashRedirect"]
      contains: "judgement"
    - path: "lib/seo/redirect-invariants.ts"
      provides: "checkRedirectInvariants(opts?) — every input injectable, returns every violation, never throws"
      exports: ["checkRedirectInvariants", "RedirectViolation"]
    - path: "lib/seo/cutover.ts"
      provides: "LEGACY_CUTOVER_DATE — the ONE place the cutover date is written; null until plan 10-09 declares it"
      exports: ["LEGACY_CUTOVER_DATE", "isCutoverDeclared"]
    - path: "scripts/assert-redirects.ts"
      provides: "the thin build-time CLI over the checker; prebuild guard #9"
      contains: "checkRedirectInvariants"
    - path: "next.config.ts"
      provides: "skipTrailingSlashRedirect plus redirects() composing the three emitters in order"
      contains: "redirects"
  key_links:
    - from: "next.config.ts"
      to: "lib/seo/redirects.ts"
      via: "imports exactly three emitters and nothing else; the config stays free of the taxonomy graph"
      pattern: "lib/seo/redirects"
    - from: "lib/seo/redirect-invariants.ts"
      to: "lib/seo/policy.ts sitemapEntries()"
      via: "every destination must resolve to a real indexable route, compared by resolved URL not by string"
      pattern: "sitemapEntries"
    - from: "scripts/assert-gate-blocks.ts"
      to: "lib/seo/redirect-invariants.ts"
      via: "perturbed maps fed to the injectable checker; each asserts its specific violation code, plus an unperturbed control"
      pattern: "checkRedirectInvariants"
    - from: "package.json prebuild"
      to: "scripts/assert-redirects.ts"
      via: "guard #9 in the chain, build-blocking on every Vercel build"
      pattern: "assert-redirects"
---

<objective>
Build the data and the build-time gate: the typed 9-entry legacy map plus a host-gated path-preserving
catch-all (D-12…D-16), a pure injectable invariant checker, a thin CLI wired into `prebuild` as guard #9
(D-17), negative proofs that each load-bearing assertion has been *seen* firing, and the `next.config.ts`
wiring that makes one hop reachable at all (RESEARCH Pitfall 1).

Purpose: two of this phase's findings would otherwise be discovered by Google. An un-gated catch-all
takes the live site down; `trailingSlash: false` silently costs a second hop on 100% of legacy traffic
while every build stays green. Both are encoded as build-blocking assertions here, before any DNS moves.
Output: `lib/seo/redirects.ts`, `lib/seo/redirect-invariants.ts`, `scripts/assert-redirects.ts`, the
`prebuild` chain at 9 guards, the `assert-gate-blocks.ts` perturbation set, and a green Vercel preview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md
@.planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md
@.planning/phases/10-reversible-old-brand-migration/10-PATTERNS.md
@.planning/phases/10-reversible-old-brand-migration/10-VALIDATION.md
@lib/seo/policy.ts
@lib/seo/invariants.ts
@scripts/assert-seo.ts
@scripts/assert-gate-blocks.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: The typed map, the cutover constant and the pure injectable checker — lib/seo/redirects.ts, lib/seo/cutover.ts and lib/seo/redirect-invariants.ts (D-12…D-17)</name>
  <files>lib/seo/redirects.ts, lib/seo/cutover.ts, lib/seo/redirect-invariants.ts</files>
  <read_first>
    - lib/seo/policy.ts — lines 1–24 (module header voice: what the module is, the single-source claim, the `NO-BARREL EXCEPTION` note, "pure functions only — no rendering, no I/O, server-safe"), lines 31–36 (`absoluteUrl`, *"The ONLY place a path is joined to CANONICAL_ORIGIN"*), lines 48–59 (`indexableSurface` / `sitemapEntries`, and the warning against a parallel list)
    - lib/seo/invariants.ts — lines 1–24 (why the checker is a pure function and not top-level asserts), lines 31–44 (`INDEXABLE_FLOOR`, a named constant carrying its derivation), lines 46–72 (`IndexationViolation` and the `checkIndexationInvariants(opts?)` signature with the injectable-inputs comment — copy this signature shape verbatim), lines 126–140 (the anti-bump failure message)
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pattern 1 (the full module sketch; the Next.js v16.2.1 `allowedKeys` allow-list; `type: 'host'` typed `{ type: 'host'; key?: undefined; value: string }`; the `permanent`-xor-`statusCode` rule; `matchHas` reading `req.headers.host` and anchoring `new RegExp(\`^${value}$\`)`) and §Pattern 2 (the 10 assertions with the "what wrong means" column)
    - .planning/phases/10-reversible-old-brand-migration/10-PATTERNS.md §`lib/seo/redirects.ts` — **the load-bearing pattern call** (pure injectable checker, not inline asserts) and the ⚠️ root-join divergence note
    - lib/constants.ts — `CANONICAL_ORIGIN` (line 51, with its "the ONLY place the origin string is written" comment), `NAV_LINKS` (line 59), `TARIEVEN_DROPDOWN` (line 78)
  </read_first>
  <action>
    Create `lib/seo/redirects.ts` with the `lib/seo/policy.ts` header voice and the no-barrel exception
    note. It must import **only** `CANONICAL_ORIGIN` from `@/lib/constants` — `next.config.ts` imports
    this module, and pulling `lib/seo/policy.ts` in would drag the whole taxonomy registry into the Next
    config graph. Export:

    - `LEGACY_HOST_PATTERN = "(www\\.)?tpsventilatie\\.nl"` — one anchored regex alternation covering both
      legacy hostnames, with a comment stating the three facts that make it non-negotiable: Next compiles
      `value` into `new RegExp("^" + value + "$")`, so a bare `tpsventilatie.nl` matches the apex only;
      the dots are escaped because an unescaped dot is regex-any; and multiple `has` entries AND together,
      so "host A or host B" is only expressible as alternation inside one `value`.
    - `interface LegacyRedirect { from; to; confidence: "certain" | "judgement"; why?: string }` with a doc
      comment per field — `from` carries the trailing slash the old WordPress site serves and Google
      indexed; `to` is a path on the new site, joined to the origin by the emitter, never hand-typed;
      `why` is REQUIRED when `confidence` is `"judgement"` (D-16).
    - `LEGACY_REDIRECTS: readonly LegacyRedirect[]` with `as const`, holding exactly the nine rows from
      RESEARCH §Pattern 1: `/` → `/`; `/over-ons/` → `/over-ons`; `/contact/` → `/contact`;
      `/privacy-beleid/` → `/privacy-beleid`; `/wtw-unit-vervangen/` → `/diensten/wtw/vervangen`;
      `/wtw-unit-onderhoud-reinigen/` → `/diensten/wtw/onderhoud-reinigen`; `/wtw-unit-inregelen/` →
      `/diensten/wtw/inregelen`; `/mechanische-ventilatie-vervangen/` →
      `/diensten/mechanische-ventilatie/vervangen`; `/mechanische-ventilatie-onderhoud-reinigen/` →
      `/diensten/mechanische-ventilatie/onderhoud-reinigen`. Eight are `confidence: "certain"`.
      The tenth row — `/mechanische-ventilatie-dakventilator/` → `/diensten/mechanische-ventilatie` — is
      `confidence: "judgement"` per **D-13**, and its `why` plus a comment carry the evidence: the live
      page fetched 2026-09-18 has the H2 "Dakventilator Onderhoud" and three packages that are
      maintenance/cleaning (€190, €250) and replacement (€700), with no new-installation content, so the
      research table's `…/aanleggen` proposal is wrong; the page spans maintenance, cleaning **and**
      replacement, so no single sub-service matches and the pillar preserves intent.
    - `toNextRedirects()` — maps each entry to `{ source, destination, statusCode: 301, has: [{ type: "host", value: LEGACY_HOST_PATTERN }] }`. `confidence` and `why` are metadata and **must not** reach
      the returned objects: Next's `checkCustomRoutes()` validates against an allow-list and fails the
      build with `invalid fields: confidence,why`. `statusCode: 301` stands alone (D-14) and `permanent`
      is omitted — Next rejects an object carrying neither and `permanent: true` would emit 308.
    - `toCatchAllRedirect()` — `source: "/:path*"`, path-preserving destination, `statusCode: 301`, and
      **the same `has: host` entry** (D-12 describes the catch-all's path behaviour and its ordering but
      does not restate the host gate; without it every request to `www.tpsklimaattechniek.nl` becomes a
      301 to itself — `ERR_TOO_MANY_REDIRECTS`, and Google drops the site).
    - `toTrailingSlashRedirect()` — `{ source: "/:path+/", destination: "/:path+", permanent: true }`,
      byte-identical to the rule Next `unshift`es for `trailingSlash: false` minus its internal-only
      flags, so canonical-host behaviour is preserved exactly and only the ordering changes.

    Join the origin locally (`${CANONICAL_ORIGIN}${to === "/" ? "" : to}` and
    `${CANONICAL_ORIGIN}/:path*`) and **state the divergence in the file**: `absoluteUrl()` in
    `lib/seo/policy.ts` is "the ONLY place a path is joined to CANONICAL_ORIGIN" and returns the root
    *with* its slash; this module joins locally because it cannot import `policy.ts` without dragging the
    taxonomy into the Next config graph, and the two disagree only on `/`, on purpose — a sitemap needs an
    entry URL, a redirect needs a `Location` value and Vercel normalises the bare origin. Because of that
    the destination check below compares root by **resolved URL**, never by string.

    Create `lib/seo/cutover.ts` — five lines, and the ONE place the cutover date is ever written.
    Export `LEGACY_CUTOVER_DATE: string | null = null` (an ISO `YYYY-MM-DD`, set by plan 10-09 in the
    commit that follows the flip) and `isCutoverDeclared(): boolean`. Two downstream consumers need one
    answer to "has the cutover happened?" — `scripts/verify-redirects.ts` uses it to decide whether its
    post-cutover mode asserts or skips loudly, and `scripts/gsc/thresholds.ts` counts weeks from it for
    the consolidation flag. A second copy of that date in either file would be exactly the parallel-list
    anti-pattern this repo forbids. The header comment must say that setting it is an act, not
    bookkeeping: it arms a live gate.

    Create `lib/seo/redirect-invariants.ts` (the `lib/seo/invariants.ts` sibling shape). Export
    `interface RedirectViolation { code: string; source?: string; message: string }` and
    `checkRedirectInvariants(opts?: { map?: readonly LegacyRedirect[]; emitted?: unknown[]; entries?: { url: string }[]; hostPattern?: string; skipTrailingSlashRedirect?: boolean; internalHrefs?: string[] }): RedirectViolation[]`
    — never early-return, collect everything, never throw, all inputs injectable so the proof harness can
    feed perturbed data. Defaults come from `LEGACY_REDIRECTS`, the three emitters, `sitemapEntries()`
    and `LEGACY_HOST_PATTERN`. The codes and what each means:

    | code | fires when |
    |---|---|
    | `duplicate-source` | two entries share a `from` — the second is dead and invisible |
    | `destination-is-a-source` | a `to` equals some `from` modulo trailing slash — that is the chain MIG-06 exists to stop |
    | `destination-off-origin` | an emitted `destination` does not start with `CANONICAL_ORIGIN` — catches a hand-typed apex |
    | `destination-not-indexable` | a `to` does not resolve (by `new URL(...).href`) to a member of `entries` — a 301 into a page Google will not index funnels the equity into a wall |
    | `source-slash-shape` | a `from` does not start with `/`, or a non-root `from` does not end with `/` |
    | `judgement-without-why` | a `confidence: "judgement"` entry has no non-empty `why` |
    | `emitted-invalid-key` | an emitted object carries a key outside Next's allow-list (`source`, `destination`, `statusCode`, `permanent`, `has`, `missing`, `basePath`, `locale`) |
    | `catchall-not-host-gated` | the element found **by predicate** `source === "/:path*"` has no `has` entry of `type: "host"` whose `value` equals the host pattern |
    | `host-pattern-scope` | under `new RegExp("^" + pattern + "$")` the pattern fails to match `tpsventilatie.nl` or `www.tpsventilatie.nl`, or does match `tpsklimaattechniek.nl` or `www.tpsklimaattechniek.nl` |
    | `normalisation-not-last` | the **last** emitted element is not the `/:path+/` → `/:path+` rule, or `skipTrailingSlashRedirect` is not `true` |
    | `internal-href-slashed` | an internal href other than the root ends in `/` — `skipTrailingSlashRedirect` stops Next normalising client-side navigation, so a slashed internal href would become a real dead end |

    Every message must say what *wrong* means, in the `INDEXABLE_FLOOR` register — a named consequence and
    an anti-bump warning, never a restatement of the value. `normalisation-not-last` and
    `catchall-not-host-gated` in particular must name the production failure they prevent. Address the
    catch-all and the normalisation rule **by predicate, never by array position** (the lesson
    `assert-gate-blocks.ts` lines 14–24 already encodes).
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx -e "
import { LEGACY_REDIRECTS, LEGACY_HOST_PATTERN, toNextRedirects, toCatchAllRedirect, toTrailingSlashRedirect } from '@/lib/seo/redirects';
import { checkRedirectInvariants } from '@/lib/seo/redirect-invariants';
import { CANONICAL_ORIGIN } from '@/lib/constants';
const bad: string[] = [];
if (LEGACY_REDIRECTS.length !== 9) bad.push('expected 9 legacy entries, got ' + LEGACY_REDIRECTS.length);
const dakv = LEGACY_REDIRECTS.find((e) => e.from === '/mechanische-ventilatie-dakventilator/');
if (!dakv) bad.push('the dakventilator entry is missing');
else { if (dakv.to !== '/diensten/mechanische-ventilatie') bad.push('D-13 target wrong: ' + dakv.to); if (dakv.confidence !== 'judgement') bad.push('D-13 entry must be a judgement'); if (!dakv.why) bad.push('D-13 entry carries no why'); }
const re = new RegExp('^' + LEGACY_HOST_PATTERN + '\$');
for (const h of ['tpsventilatie.nl', 'www.tpsventilatie.nl']) if (!re.test(h)) bad.push('pattern does not match ' + h);
for (const h of ['tpsklimaattechniek.nl', 'www.tpsklimaattechniek.nl']) if (re.test(h)) bad.push('pattern DOES match the canonical host ' + h);
const emitted: any[] = [...toNextRedirects(), toCatchAllRedirect(), toTrailingSlashRedirect()];
const allowed = new Set(['source','destination','statusCode','permanent','has','missing','basePath','locale']);
for (const r of emitted) for (const k of Object.keys(r)) if (!allowed.has(k)) bad.push('emitted object carries a forbidden key: ' + k);
for (const r of toNextRedirects()) { if (r.statusCode !== 301) bad.push('statusCode must be 301'); if ('permanent' in r) bad.push('permanent must be omitted'); if (!String(r.destination).startsWith(CANONICAL_ORIGIN)) bad.push('destination off origin: ' + r.destination); }
const ca: any = toCatchAllRedirect();
if (!Array.isArray(ca.has) || !ca.has.some((h: any) => h.type === 'host' && h.value === LEGACY_HOST_PATTERN)) bad.push('CATCH-ALL IS NOT HOST-GATED');
const v = checkRedirectInvariants({ skipTrailingSlashRedirect: true });
if (v.length) bad.push('real map yields violations: ' + v.map((x) => x.code).join(','));
if (bad.length) { console.error(bad.join('\n')); process.exit(1); }
console.log('map OK — 9 entries, D-13 judgement with evidence, host pattern scoped, catch-all gated, emitted keys legal, checker clean on reality');
"</automated>
  </verify>
  <acceptance_criteria>
    - `LEGACY_REDIRECTS` has exactly 9 entries; the dakventilator entry targets `/diensten/mechanische-ventilatie`, is `confidence: "judgement"` and carries a non-empty `why`.
    - `LEGACY_HOST_PATTERN` under `^…$` matches both legacy hostnames and neither canonical hostname.
    - Every object returned by the three emitters carries only keys from Next's allow-list; `confidence` and `why` never appear on them.
    - Every explicit rule has `statusCode: 301`, omits `permanent`, and has a destination starting with `CANONICAL_ORIGIN`.
    - The catch-all carries a `has` entry of `type: "host"` whose `value` equals `LEGACY_HOST_PATTERN`.
    - `checkRedirectInvariants({ skipTrailingSlashRedirect: true })` returns zero violations against the real map.
    - `lib/seo/redirects.ts` imports nothing but `CANONICAL_ORIGIN`, and states in a comment why it joins the origin locally and how that differs from `absoluteUrl()` on `/`.
  </acceptance_criteria>
  <done>The map exists as typed data with its one judgement call explained in the file that outlives every planning document.</done>
</task>

<task type="auto">
  <name>Task 2: The build gate and its negative proofs — assert-redirects.ts as prebuild guard #9, plus perturbations in assert-gate-blocks.ts (D-17)</name>
  <files>scripts/assert-redirects.ts, scripts/assert-gate-blocks.ts, package.json</files>
  <read_first>
    - scripts/assert-seo.ts — lines 1–21 (the four-part header: what it is / what runtime / where it is wired / what it asserts, **and the note that literal grep-target strings are kept out of comments on purpose**), lines 23–29 (import set), lines 31–47 (print every violation, then assert), lines 72–100 (floors not equalities; the vacuous-pass guard), lines 147–152 (the one-line success summary that names every fact proven)
    - scripts/assert-gate-blocks.ts — lines 14–24 (the lesson: address a perturbation **by predicate and by the property under test, never by array position**), lines 38–40 (the `codes()` helper — copy verbatim), lines 136–152 (perturb → assert the *specific* code), lines 195–203 (**(P5) the control**: unperturbed reality must yield exactly zero violations, without which every proof above could be passing because the checker is simply always angry)
    - scripts/verify-indexation.ts — lines 102–104 and 248–251 (`async main()`: the build-time scripts transpile to CJS where top-level await is unavailable)
    - package.json line 7 (the `prebuild` chain of 8 guards; `assert-redirects.ts` joins after `assert-seo.ts` and before the bash guard)
    - .planning/phases/10-reversible-old-brand-migration/10-VALIDATION.md §Wave 0 Requirements (the three assertions that must each be *watched* to fire)
  </read_first>
  <action>
    Write `scripts/assert-redirects.ts` as a **thin CLI over the checker** — the whole point of Task 1's
    injectable signature. Four-part header in the `assert-seo.ts` voice, including the sentence that
    literal grep-target strings are deliberately kept out of comments. It must:

    - import `nextConfig` (the default export) from `@/next.config`, `await nextConfig.redirects()` inside
      an `async main()`, and pass that array as `emitted` together with
      `skipTrailingSlashRedirect: nextConfig.skipTrailingSlashRedirect === true`. Reading the config's own
      values is what makes assertion "normalisation-not-last" real: it checks the array the build will
      actually use, so reordering the emitters or dropping the config key fails here rather than in
      production. Do **not** grep the config file — a value assertion cannot be defeated by a comment.
    - print every violation as `[code] source message` before asserting, then a single
      `assert.equal(violations.length, 0, …)` whose message points at the printed list.
    - carry a vacuous-pass guard in the `assert-seo.ts` style: assert the emitted array length is at least
      11 (9 explicit + catch-all + normalisation) with a message saying that a shorter array means the
      loops below pass vacuously. Assert a **floor**, never an equality on a number that may legitimately
      grow.
    - close with a one-line success summary naming every fact proven: the entry count, that all
      destinations sit on the canonical origin and resolve into the sitemap surface, that the catch-all is
      host-gated, that the host pattern excludes the canonical hostnames, and that the normalisation rule
      is last with the config key set.
    - **touch nothing on the network.** A fetch here would make every Vercel build depend on production
      being reachable and would assert against the deployment it is producing (D-17).

    Append `&& tsx scripts/assert-redirects.ts` to `package.json`'s `prebuild`, positioned after
    `assert-seo.ts` and before `bash scripts/check-radius-literals.sh` — nine guards.

    Extend `scripts/assert-gate-blocks.ts` with an `(R1)`–`(R9)` block alongside the existing `(P1)`–`(P5)`,
    reusing its `codes()` helper and its perturb-then-assert-the-specific-code shape. Each perturbation is
    applied **by predicate**, and each must be *seen* to produce its own code:

    - (R1) strip `has` from the element whose `source === "/:path*"` → `catchall-not-host-gated`
    - (R2) replace the host pattern with the plausible "simplification" `tpsventilatie\.nl` → `host-pattern-scope` (it no longer matches the `www` form)
    - (R3) move the `/:path+/` rule off the last position → `normalisation-not-last`
    - (R4) pass `skipTrailingSlashRedirect: false` with the array otherwise correct → `normalisation-not-last`
    - (R5) duplicate a `from` → `duplicate-source`
    - (R6) point a `to` at another entry's `from` → `destination-is-a-source`
    - (R7) drop `why` from the dakventilator entry → `judgement-without-why`
    - (R8) add a `confidence` key to an emitted object → `emitted-invalid-key`
    - (R9) **the control** — unperturbed reality with `skipTrailingSlashRedirect: true` must yield exactly
      zero violations, or every proof above could be passing because the checker is always angry.

    Commit with `feat(10-03): typed legacy redirect map, pure invariant checker and prebuild guard #9 (MIG-05/MIG-06)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const p=require('./package.json'); const chain=p.scripts.prebuild.split('&&').map(s=>s.trim()); if(!chain.some(c=>c.includes('assert-redirects.ts'))){console.error('assert-redirects is not in the prebuild chain');process.exit(1)} if(chain.length<9){console.error('prebuild has '+chain.length+' guards, expected at least 9');process.exit(1)} const i=chain.findIndex(c=>c.includes('assert-redirects')),j=chain.findIndex(c=>c.includes('assert-seo')); if(i<j){console.error('assert-redirects must run after assert-seo');process.exit(1)} console.log('prebuild chain has '+chain.length+' guards, assert-redirects at position '+(i+1))"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx scripts/assert-redirects.ts && npx tsx scripts/assert-gate-blocks.ts</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "const s=require('fs').readFileSync('scripts/assert-redirects.ts','utf8'); const bad=[]; if(/\bfetch\s*\(|node-fetch|https?:\/\/[a-z]/i.test(s.replace(/^\s*\/\/.*$/gm,'')))bad.push('assert-redirects appears to touch the network — D-17 forbids it'); if(!s.includes('checkRedirectInvariants'))bad.push('it does not call the pure checker'); if(!s.includes('next.config'))bad.push('it does not read the real config'); if(bad.length){console.error(bad.join('\n'));process.exit(1)} console.log('assert-redirects is a thin, offline CLI over the checker that reads the real config')"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx scripts/assert-gate-blocks.ts 2>&1 | grep -Eiq 'R9|control' && echo "the unperturbed control ran"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `prebuild` has at least 9 `&&`-joined guards and `assert-redirects.ts` runs after `assert-seo.ts`.
    - `npx tsx scripts/assert-redirects.ts` exits 0 and prints a summary naming the entry count, the origin/sitemap checks, the host gate, the pattern scope and the normalisation position.
    - `npx tsx scripts/assert-gate-blocks.ts` exits 0 with the `(R1)`–`(R9)` block present, including the unperturbed control.
    - `scripts/assert-redirects.ts` contains no network call, calls `checkRedirectInvariants`, and reads `skipTrailingSlashRedirect` and the emitted array from `next.config` rather than grepping the file.
    - Each of R1, R2, R3 and R4 asserts its own specific violation code — the three findings that would otherwise reach production have each been observed firing.
  </acceptance_criteria>
  <done>Every structural claim the map makes has been watched to fail on a perturbed input, which is the only kind of gate this project trusts.</done>
</task>

<task type="auto">
  <name>Task 3: Wire next.config.ts and prove it on a Vercel preview — the build gate on this repo (RESEARCH Pitfall 1 and Pitfall 2)</name>
  <files>next.config.ts</files>
  <read_first>
    - .planning/phases/10-reversible-old-brand-migration/10-PATTERNS.md §`next.config.ts` — **the current file reproduced in full** (13 lines: a type-only `NextConfig` import, `trailingSlash: false` with its hybrid-hosting comment, `images.formats` with the AVIF comment). **Do not attempt a shell read of this file — it stalls on this mount.** If you need it again: `gh api repos/Zlodziejczyk/tps-ventilatie/contents/next.config.ts --jq '.content' | base64 -d`
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pitfall 1 (the `redirects.unshift` source excerpt, the behaviour table, and the live confirmation that legacy canonicals are slashed), §Pattern 1 (the config block and the exact comment to ship verbatim), §Pitfall 2 (what an un-gated catch-all does and why a preview that will not load is its signature)
    - .planning/PROJECT.md §Constraints (the OneDrive execution constraint: no local `next build`; validate on Vercel preview)
  </read_first>
  <action>
    Rewrite `next.config.ts` as a whole file rather than patching it — the mount refuses reads, and a
    whole-file write needs none. Keep the existing `trailingSlash: false` and `images.formats` entries and
    their comments verbatim, then add:

    - `skipTrailingSlashRedirect: true`, carrying the comment from RESEARCH §Pattern 1 **verbatim**,
      including its last sentence about what removing the line silently does and which script catches it.
      This is the highest-stakes config key in the repo: without it Next 16.2.1 `unshift`es a
      `/:path+/` → `/:path+` 308 to the FRONT of the redirects array, ahead of every rule below, so a
      legacy URL like `/over-ons/` costs two hops and MIG-07 can never be green while every build stays
      green.
    - `async redirects()` returning, in exactly this order: the spread of `toNextRedirects()` (9 explicit,
      most specific first), then `toCatchAllRedirect()` (D-12: after the explicit rules, never before, so
      it can never shadow one), then `toTrailingSlashRedirect()` (canonical-host normalisation, last, so
      the legacy rules win on legacy hosts).
    - an import of exactly those three emitters from `@/lib/seo/redirects` and nothing else.

    Then push the branch and let Vercel be the build gate (there is no local `next build`, `tsc --noEmit`
    or `npm run lint` on this mount — they deadlock at 0% CPU). Two things must be true of the preview:
    the build log shows **nine** guard success lines, and **the preview home page loads**. A preview that
    returns `ERR_TOO_MANY_REDIRECTS` or will not load at all is the signature of an un-gated catch-all —
    which is exactly why the map must reach a preview before it reaches production.

    Also probe the preview for the ordering fact the structural gate cannot see from data alone: a request
    to a slashed path on the **preview host** must not 308 to its slash-less form, because
    `skipTrailingSlashRedirect` is on and the re-added rule is last. Record the observed status codes in
    the summary. Note that the legacy map itself cannot be exercised on a preview — `has: host` matching
    needs a legacy `Host` header, and Vercel dispatches spoofed `Host` to the **production** alias, so the
    real pre-flight probe is plan 10-07's job, not this one.

    Commit with `feat(10-03): wire host-gated legacy redirects and pin the trailing-slash ordering (Pitfall 1/2)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx -e "
import cfg from '@/next.config';
// async main(): the build-time scripts transpile to CJS, where top-level await is unavailable.
async function main() {
const bad: string[] = [];
if (cfg.skipTrailingSlashRedirect !== true) bad.push('skipTrailingSlashRedirect is not true — every legacy URL would cost two hops');
if (cfg.trailingSlash !== false) bad.push('trailingSlash changed');
if (typeof cfg.redirects !== 'function') bad.push('redirects() is missing');
const rs: any[] = await (cfg.redirects as any)();
if (rs.length < 11) bad.push('expected at least 11 emitted rules, got ' + rs.length);
const last = rs[rs.length - 1];
if (last?.source !== '/:path+/' || last?.destination !== '/:path+') bad.push('the normalisation rule is not last: ' + JSON.stringify(last));
const ci = rs.findIndex((r) => r.source === '/:path*');
const li = rs.findIndex((r) => r.source === '/over-ons/');
if (ci === -1) bad.push('no catch-all emitted');
else if (li !== -1 && ci < li) bad.push('the catch-all is emitted BEFORE an explicit rule and would shadow it');
for (const r of rs) if (r.source !== '/:path+/' && !(Array.isArray(r.has) && r.has.some((h: any) => h.type === 'host'))) bad.push('an un-gated rule would apply to the live site: ' + r.source);
if (bad.length) { console.error(bad.join('\n')); process.exit(1); }
console.log('config OK — ' + rs.length + ' rules, catch-all after the explicit nine, normalisation last, every legacy rule host-gated');
}
main();
"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && git push -u origin HEAD && sleep 45 && npx vercel ls --yes 2>/dev/null | head -5 || echo "preview URL must be confirmed from the Vercel dashboard or the GitHub deployment status — record it in the summary"</automated>
  </verify>
  <acceptance_criteria>
    - `next.config.ts` sets `skipTrailingSlashRedirect: true`, keeps `trailingSlash: false` and `images.formats`, and imports exactly three emitters from `@/lib/seo/redirects`.
    - `await nextConfig.redirects()` returns at least 11 rules; the last is `/:path+/` → `/:path+`; the catch-all appears after every explicit legacy rule; and every rule except the normalisation rule carries a `has: host` entry.
    - The pushed branch produced a Vercel preview that reached READY with **nine** guard success lines in the build log.
    - **The preview home page loads** (HTTP 200, not `ERR_TOO_MANY_REDIRECTS`) — the live proof that the catch-all is host-gated.
    - The summary records the preview URL, the nine guard lines, and the observed status code for a slashed path on the preview host.
  </acceptance_criteria>
  <done>The two findings that would otherwise have been discovered by Google are now pinned by a build-blocking gate and confirmed by a deployment that loads.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| request `Host` header → redirect matching | the only request-derived input the rules read; matched against a literal anchored pattern, never interpolated |
| request path → catch-all destination | `:path*` is interpolated into a destination whose origin is a compile-time constant; a path, never a host |
| `next.config.ts` → Vercel edge routing | the emitted array becomes edge config evaluated before any function runs; array order is the whole mechanism |
| build gate → the config it validates | the gate reads the config's own values, so it cannot be satisfied by a comment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-open-redirect | Tampering | the catch-all destination | mitigate | destination built from the `CANONICAL_ORIGIN` constant; `destination-off-origin` is build-blocking; only `:path*` is request-derived and it is a path, never a host |
| T-10-ungated-catchall | DoS (self-inflicted) | `toCatchAllRedirect()` | mitigate | `catchall-not-host-gated` and `host-pattern-scope` are build-blocking, each observed firing on a perturbed map (R1, R2); the preview must load, which is the live proof |
| T-10-two-hop | Tampering | redirect array ordering | mitigate | `normalisation-not-last` asserts against the array `next.config.ts` actually returns plus the config key itself; observed firing on R3 and R4 |
| T-10-dead-target | Tampering | destinations | mitigate | `destination-not-indexable` resolves every target against `sitemapEntries()` by URL, so a 301 into a non-indexable page fails the build |
| T-10-build-network | DoS | `prebuild` | mitigate | `assert-redirects.ts` makes no network call (D-17); asserted by a source check |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | no packages added — `next`, `tsx` and Node builtins only (RESEARCH §Package Legitimacy Audit: no audit required) |
</threat_model>

<verification>
- Runs on branch `gsd/phase-10-reversible-old-brand-migration` (Discretion #3, resolved in RESEARCH §Execution Sequence). Create it from `main` if absent: `git rev-parse --verify gsd/phase-10-reversible-old-brand-migration || git checkout -b gsd/phase-10-reversible-old-brand-migration`.
- **The Vercel preview build of the pushed branch is the TypeScript and build gate.** There is no local `next build`, `tsc --noEmit` or `npm run lint` on this OneDrive mount.
- `npx tsx` runs are fast and reliable here and are the per-task gate.
- `next.config.ts` stalls on shell reads; PATTERNS.md reproduces it in full and `gh api repos/Zlodziejczyk/tps-ventilatie/contents/next.config.ts` is the fallback. A failed read is the mount, not evidence about the file.
</verification>

<success_criteria>
- The typed 9-entry map plus a host-gated path-preserving catch-all is in `lib/seo/redirects.ts`, with every destination derived from `CANONICAL_ORIGIN` and the single judgement call carrying its evidence.
- A pure injectable checker backs an offline `prebuild` guard #9, and each of its three load-bearing assertions has been observed firing on a perturbed map.
- `next.config.ts` pins the trailing-slash ordering and emits the three groups in the only order that yields one hop.
- A Vercel preview reached READY with nine guards green and its home page loads.
</success_criteria>

<output>
Create `.planning/phases/10-reversible-old-brand-migration/10-03-SUMMARY.md` when done.
</output>
