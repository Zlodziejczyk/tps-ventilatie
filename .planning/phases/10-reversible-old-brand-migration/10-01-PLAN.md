---
phase: 10-reversible-old-brand-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/codebase/STACK.md
  - .planning/codebase/ARCHITECTURE.md
  - CLAUDE.md
autonomous: true
requirements: [MIG-01]
must_haves:
  truths:
    - "MIG-01 in .planning/REQUIREMENTS.md states what this phase actually intends to deliver — a credential-free public content mirror — and carries D-02's reasoning in the sentence itself: the DNS change moves two hostname records and touches neither files, database, vhost nor subscription, so a copy does not protect reversibility; not touching the install plus keeping the subscription for mail does (D-02, D-03)"
    - "ROADMAP §Phase 10 success criterion 4 and the MIG-01…04 hard-gate line say the same thing as the amended MIG-01, so the phase verifier cannot pass this phase against a criterion nobody intends to meet (D-03)"
    - ".planning/PROJECT.md §Context no longer claims access we do not hold: we hold dd24 (the domain and its DNS) and nothing else on the old stack — WordPress admin, the cyberfolks hosting panel and the info@ mailbox are held by cyberfolks/Thomas (D-01)"
    - "CLAUDE.md no longer describes this project as a static export: the two GSD source documents that carried that claim are corrected and the tool-generated blocks are regenerated from them rather than hand-edited (RESEARCH Finding 1)"
    - "The three accepted residual risks of D-02 are written down where a future reader meets the requirement, not buried in a planning archive"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "the amended MIG-01 checkbox, in the existing `- [ ] **ID**: <one externally checkable sentence>` shape"
      contains: "legacy-site-mirror"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 10 success criterion 4 and the hard-gate line, aligned with the amended MIG-01"
      contains: "content mirror"
    - path: ".planning/PROJECT.md"
      provides: "a §Context that matches the access reality of D-01"
      contains: "dd24"
    - path: "CLAUDE.md"
      provides: "regenerated project/stack/architecture blocks describing hybrid Vercel hosting"
      contains: "hybrid"
  key_links:
    - from: ".planning/REQUIREMENTS.md MIG-01"
      to: ".planning/ROADMAP.md §Phase 10 criterion 4"
      via: "both restate the same amended deliverable; the verifier reads the ROADMAP criteria"
      pattern: "content mirror"
    - from: ".planning/codebase/STACK.md + ARCHITECTURE.md"
      to: "CLAUDE.md GSD-generated blocks"
      via: "gsd-tools.cjs generate-claude-md --force"
      pattern: "generate-claude-md"
---

<objective>
Make the milestone artefacts tell the truth before the phase acts on them. D-03 rewrites MIG-01 in
`.planning/REQUIREMENTS.md` and realigns `.planning/ROADMAP.md` §Phase 10 criterion 4 and the
`MIG-01…04` hard-gate line; D-01 corrects `.planning/PROJECT.md` §Context; RESEARCH Finding 1 corrects
the stale static-export description that reaches `CLAUDE.md` through two GSD source documents.

Purpose: the phase verifier checks this phase against the ROADMAP success criteria. If criterion 4 still
demands a backup stored off-host and restore-tested while the phase deliberately delivers a content
mirror instead, the phase cannot pass honestly — and a requirement nobody intends to meet as written
rots into a false "complete" checkbox. That is the Phase 8 lesson wearing a different costume.
Output: four amended planning documents and a regenerated `CLAUDE.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md
@.planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md
@.planning/phases/10-reversible-old-brand-migration/10-PATTERNS.md
@.planning/phases/10-reversible-old-brand-migration/10-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Amend MIG-01, ROADMAP criterion 4 and the hard-gate line, and correct PROJECT.md §Context (D-01, D-02, D-03)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/PROJECT.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md — line 32 (the MIG-01 checkbox as written), line 122 (the traceability row, status only), and **line 98** (the `Moving legacy DNS nameservers | Superseded on 2026-09-16 by Phase 9 D-26: …` Out-of-Scope row — the one existing precedent in this repo for superseding a milestone artefact in place; copy its voice: *when*, *by which decision*, *what actually happened*, *where the recipe lives*, all in the row, no footnote)
    - .planning/ROADMAP.md — lines 106–113 (§Phase 10 success criteria 1–6 and the `**Hard gates:**` paragraph)
    - .planning/PROJECT.md — lines 103–112 (§Context, including the `**Access held for v1.1:**` line, the `**Old-site topology (verified 2026-08-19):**` line and the `**New-domain DNS:**` line) and **line 116** (the §Constraints hosting line, which is already correct and is the voice to copy: a parenthetical that states the real mechanism rather than a label)
    - .planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md §D-01, §D-02, §D-03 and §Specifics ("The backup does not protect reversibility")
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pitfall 10 (the `/wp-sitemap.xml` claim CONTEXT repeats is wrong — it returns 200, not 404)
  </read_first>
  <action>
    Rewrite `.planning/REQUIREMENTS.md` line 32 in place, keeping the `- [ ] **MIG-01**: <one sentence,
    present tense, externally checkable>` shape so the verifier can still read it as a criterion, and
    carrying D-02's reasoning *inside* the sentence rather than in a footnote. The new text must state:
    a credential-free public content mirror of the 9 legacy pages plus their assets is committed under
    `docs/baseline/<capture-date>/legacy-site-mirror/`, captured while the old site is still live; amended
    2026-09-21 by Phase 10 D-02/D-03 because the DNS change moves two hostname records and touches neither
    the files, the database, the vhost nor the subscription, so reversibility is protected by never
    touching the install and by the subscription staying alive for `info@tpsventilatie.nl`, not by a copy;
    and the three residual risks are accepted — the install rots unpatched (WordPress 7.1.1), the
    cyberfolks subscription ends, a host-side accident with retention unknown to us — with the note that
    storing a copy at cyberfolks would not be off-host at all.

    **Word it so the old phrasing is not reproduced.** Do not write the literal strings
    `full WordPress backup`, `off-host`, or `restore-tested` anywhere in the new MIG-01 text, in the
    ROADMAP edits, or in any comment — the verification below greps for their absence and a quoted
    original would false-positive (the anti-drift grep-token rule that bit Phase 8 four times). Say
    "a files-plus-database copy" when the rationale needs to refer to the superseded idea.

    Leave `.planning/REQUIREMENTS.md` line 122 (`| MIG-01 | Phase 10 | Pending |`) untouched — that table
    tracks status, not text, and phase close flips it.

    In `.planning/ROADMAP.md`, rewrite success criterion 4 (line 109) to the same deliverable — a
    credential-free public content mirror of the 9 legacy pages committed under `docs/baseline/`, captured
    while the old site is still live; WordPress install left intact and reachable — with a trailing
    pointer `(amended by Phase 10 D-02/D-03 — see REQUIREMENTS MIG-01)`. Then extend the `**Hard gates:**`
    paragraph (line 113) so the `MIG-01…04` pre-flight checklist names what MIG-01 now means, and keep the
    point-of-no-return sentence intact.

    In `.planning/PROJECT.md` §Context, replace the `**Access held for v1.1:**` line (line 109) per D-01:
    we hold **dd24** (the `tpsventilatie.nl` domain and its DNS) plus the repo, Vercel and Search Console;
    WordPress admin, the cyberfolks hosting panel and the `info@tpsventilatie.nl` mailbox are held by
    cyberfolks/Thomas; admin on the TPS Google Business Profile is still ours. Copy line 116's voice.
    While in the same section, correct two other claims the phase has re-measured: the old-site topology
    line still says NS `ns1/ns2.opeiron.com` (the delegation moved to dd24 on 2026-09-16, Phase 9 D-26)
    and still says `/wp-sitemap.xml` 404s (it returns 200 — RESEARCH §Pitfall 10; the pointer becomes
    stale at cutover, which is the real reason D-15 retires it).

    Commit with `docs(10-01): amend MIG-01 to the content mirror and correct the access reality (D-01/D-02/D-03)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "
const fs=require('fs');const bad=[];
const req=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');
const mig=req.split('\n').find(l=>l.includes('**MIG-01**'))||'';
if(!/^- \[[ x]\] \*\*MIG-01\*\*:/.test(mig))bad.push('MIG-01 lost its checkbox shape');
for(const t of ['legacy-site-mirror','D-02','still live'])if(!mig.includes(t))bad.push('MIG-01 missing: '+t);
for(const t of ['full WordPress backup','off-host','restore-tested'])if(req.includes(t))bad.push('REQUIREMENTS still carries the superseded phrase: '+t);
const rm=fs.readFileSync('.planning/ROADMAP.md','utf8');
const p10=rm.slice(rm.indexOf('#### Phase 10'),rm.indexOf('#### Phase 11'));
for(const t of ['content mirror','D-02/D-03','Hard gates'])if(!p10.includes(t))bad.push('ROADMAP Phase 10 missing: '+t);
for(const t of ['off-host','restore-tested'])if(p10.includes(t))bad.push('ROADMAP Phase 10 still carries: '+t);
const pj=fs.readFileSync('.planning/PROJECT.md','utf8');
if(pj.includes('on the old site'+String.fromCharCode(39)+'s hosting'))bad.push('PROJECT.md still claims hosting access');
if(pj.includes('opeiron'))bad.push('PROJECT.md still names the retired nameservers');
if(!/dd24/.test(pj))bad.push('PROJECT.md must name dd24 as what we hold');
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('amendments landed: MIG-01 rewritten, ROADMAP criterion 4 + hard gate aligned, PROJECT.md access reality corrected');
"</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/REQUIREMENTS.md` MIG-01 keeps the `- [ ] **MIG-01**: …` shape and names `legacy-site-mirror`, `D-02` and "still live".
    - The strings `full WordPress backup`, `off-host` and `restore-tested` appear nowhere in `.planning/REQUIREMENTS.md`, and `off-host` / `restore-tested` appear nowhere in the ROADMAP §Phase 10 block.
    - ROADMAP §Phase 10 criterion 4 names a content mirror and cites `D-02/D-03`; the hard-gate line still names the `MIG-01…04` pre-flight checklist and the point of no return.
    - `.planning/PROJECT.md` no longer claims access on the old site's hosting, no longer names `opeiron`, and names dd24 as what we do hold.
    - The three accepted risks (unpatched install, subscription ends, host-side accident) are present in the MIG-01 sentence.
  </acceptance_criteria>
  <done>A future reader — and the phase verifier — meets a requirement this phase actually intends to satisfy, with the reasoning attached.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the two GSD source documents that carry the stale static-export claim, then regenerate CLAUDE.md (RESEARCH Finding 1)</name>
  <files>.planning/codebase/STACK.md, .planning/codebase/ARCHITECTURE.md, CLAUDE.md</files>
  <read_first>
    - .planning/phases/10-reversible-old-brand-migration/10-PATTERNS.md §"No Analog Found" → **The `CLAUDE.md` trap** (the marker names, the stale line numbers, and the rule that content between `<!-- GSD:* -->` markers is refreshed by the workflow, never hand-edited)
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Summary Finding 1 (three independent confirmations that `output: "export"` was dropped in Phase 5: the file on GitHub, PROJECT.md line 116, and a live probe — `POST /api/lead` 400, `GET /api/lead` 405, which a static export cannot produce)
    - .planning/PROJECT.md line 116 (the correct hosting sentence to mirror)
    - `.planning/codebase/STACK.md` lines 25, 43, 53 and `.planning/codebase/ARCHITECTURE.md` lines 94, 210, 214, 217 — **both files stall on every shell read on this OneDrive mount.** Fetch them with `gh api repos/Zlodziejczyk/tps-ventilatie/contents/<path> --jq '.content' | base64 -d`, which is authenticated and reliable (RESEARCH §Pitfall 9)
  </read_first>
  <action>
    PATTERNS.md left this as an open check; it is now settled — both source documents **do** carry the
    stale claim (confirmed 2026-09-21 via `gh api`), so a regeneration alone would reproduce it. Fix the
    sources, then regenerate.

    Because both files stall on a direct read, work through a temp copy: fetch each with
    `gh api repos/Zlodziejczyk/tps-ventilatie/contents/<path> --jq '.content' | base64 -d > "$TMPDIR/<name>.md"`,
    apply the edits to the temp copy, then `cp "$TMPDIR/<name>.md" <path>` — a whole-file overwrite needs
    no read of the existing inode and also rehydrates the placeholder so later reads succeed. A failed
    read is the mount, never evidence the file is missing or already correct.

    `.planning/codebase/STACK.md`: line 25 must describe Next.js 16.2.1 as a full-stack framework in
    **hybrid** mode (default Next.js output — static pages prerender, route handlers run as serverless
    functions), with the Phase 5 note that `output: "export"` was dropped for `app/api/lead/route.ts`;
    line 43 must drop the `unoptimized: true` claim and say Image Optimization is on with
    `images.formats` opted into AVIF plus WebP; line 53 must describe `next.config.ts` as setting
    `trailingSlash: false` and `images.formats` — and, after plan 10-03 lands, `skipTrailingSlashRedirect`
    plus `redirects()`. Also correct the two Platform Requirements bullets that assert a static export to
    `out/` and "no server-side runtime required".

    `.planning/codebase/ARCHITECTURE.md`: line 94 ("All pages are statically exported … no server
    runtime"), line 210 (the `next.config.ts` entry-point responsibility), line 214 (the
    `**Static export only:**` architectural constraint) and line 217 (the `<Suspense>` rationale) must all
    describe hybrid hosting: ~22 pages stay SSG, `app/api/lead/route.ts` runs as a serverless function,
    Image Optimization is available, and `<Suspense>` around `useSearchParams` is a prerendering
    requirement rather than a static-export one.

    Then regenerate the tool-generated blocks rather than hand-editing them:
    `node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" generate-claude-md --force`.
    This also clears the stale §Constraints line inside the project block ("may need to relax to a
    hybrid … open decision"), which is stale relative to its own source — `.planning/PROJECT.md` line 116
    has been correct since Phase 5.

    Do **not** hand-edit anything between the `<!-- GSD:project-start -->`, `<!-- GSD:stack-start -->` or
    `<!-- GSD:architecture-start -->` markers; the hand-maintained guardrails above them stay untouched.
    If the generator itself stalls on the mount, record that in the summary and fall back to the same
    fetch-edit-overwrite route on `CLAUDE.md`, noting the deviation explicitly.

    Commit with `docs(10-01): correct the stale static-export description and regenerate CLAUDE.md (Finding 1)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "
const fs=require('fs');const bad=[];
const c=fs.readFileSync('CLAUDE.md','utf8');
for(const t of ['configured for static export','Static export only','No server-side runtime required','unoptimized: true','statically exported'])
  if(c.includes(t))bad.push('CLAUDE.md still asserts: '+t);
if(!/[Hh]ybrid/.test(c))bad.push('CLAUDE.md never says hybrid');
for(const m of ['GSD:project-start','GSD:stack-start','GSD:architecture-start'])
  if(!c.includes(m))bad.push('CLAUDE.md lost its generated-block marker: '+m);
if(!c.includes('Reference-only dirs'))bad.push('CLAUDE.md lost the hand-maintained guardrails');
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('CLAUDE.md regenerated clean of the static-export claim, markers and guardrails intact');
"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && git status --porcelain .planning/codebase/STACK.md .planning/codebase/ARCHITECTURE.md | grep -qE '^(M| M|A )' || git log -1 --format=%H -- .planning/codebase/STACK.md .planning/codebase/ARCHITECTURE.md | grep -q . && echo "codebase sources were edited or already committed in this phase"</automated>
  </verify>
  <acceptance_criteria>
    - `CLAUDE.md` contains none of: `configured for static export`, `Static export only`, `No server-side runtime required`, `unoptimized: true`, `statically exported`.
    - `CLAUDE.md` describes hosting as hybrid and still carries all three `<!-- GSD:*-start -->` markers plus the hand-maintained `Reference-only dirs` guardrail.
    - `.planning/codebase/STACK.md` and `.planning/codebase/ARCHITECTURE.md` were both modified (the regeneration reads them; a clean CLAUDE.md with unmodified sources is impossible).
    - The summary records whether `generate-claude-md --force` ran cleanly or the mount forced the documented fallback.
  </acceptance_criteria>
  <done>The project's own instruction file stops telling every future agent that `redirects()` is unavailable here.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artefacts → the phase verifier | the ROADMAP criteria are what the verifier grades against; a criterion nobody intends to meet is a false pass waiting to happen |
| GSD-generated blocks → every future agent session | `CLAUDE.md` is loaded as project instructions; a stale claim there misdirects work for months |
| OneDrive mount → file contents | a stalled read is not evidence of file state (RESEARCH §Pitfall 9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-false-pass | Repudiation | ROADMAP criterion 4 | mitigate | criterion and requirement are rewritten together and the verify greps both for the new text and for the absence of the superseded phrasing |
| T-10-stale-instructions | Tampering | `CLAUDE.md` generated blocks | mitigate | sources corrected then regenerated via `gsd-tools.cjs generate-claude-md --force`; markers and hand-maintained guardrails asserted present afterwards |
| T-10-mount-blind-edit | Tampering | `.planning/codebase/*.md` | mitigate | `gh api` fetch → edit temp copy → whole-file `cp` overwrite; never assume an unreadable file is unchanged |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | no packages added by this plan — RESEARCH §Package Legitimacy Audit records "no audit required" for the phase |
</threat_model>

<verification>
- Runs on branch `gsd/phase-10-reversible-old-brand-migration` (Discretion #3, resolved in RESEARCH §Execution Sequence). Create it from `main` if absent: `git rev-parse --verify gsd/phase-10-reversible-old-brand-migration || git checkout -b gsd/phase-10-reversible-old-brand-migration`.
- Documentation-only plan: no build gate applies, and nothing here can break a deploy.
- Both verifies are `node -e` file assertions that run in well under a second on this mount.
- `.planning/codebase/*.md` may refuse a shell read; that is the mount, and the `gh api` fallback is the documented route.
</verification>

<success_criteria>
- MIG-01, ROADMAP criterion 4 and the ROADMAP hard-gate line all describe the same deliverable, with D-02's reasoning attached.
- `.planning/PROJECT.md` §Context matches the access reality of D-01 and drops two other re-measured claims.
- `CLAUDE.md` describes hybrid Vercel hosting, regenerated from corrected sources rather than hand-patched.
</success_criteria>

<output>
Create `.planning/phases/10-reversible-old-brand-migration/10-01-SUMMARY.md` when done.
</output>
