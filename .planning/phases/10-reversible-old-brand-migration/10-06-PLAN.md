---
phase: 10-reversible-old-brand-migration
plan: 06
type: execute
wave: 2
depends_on: ["10-03"]
files_modified:
  - scripts/gsc/thresholds.ts
  - scripts/measure-indexation.ts
  - docs/measurements/README.md
  - .github/workflows/measure-indexation.yml
autonomous: true
requirements: [MIG-09]
must_haves:
  truths:
    - "The existing weekly GSC cron gains a legacy section, so retiring the redirect map becomes evidence-driven rather than date-driven: a dated instruction nobody runs is indistinguishable from no instruction, and this repo has already lived that failure mode (D-25)"
    - "Search Analytics for the legacy Domain property answers the actual 180-day condition — is Search still sending traffic to the old domain? — rather than a calendar approximation"
    - "URL Inspection over the 9 legacy URLs exposes Google's chosen canonical per URL, the definitive consolidation signal, run with siteUrl set to the legacy property because an inspectionUrl must be under the property specified"
    - "legacy-traffic-zero fires only after N = 4 consecutive weekly readings of zero 28-day impressions — roughly two months of genuine silence — because one or two weeks would fire on ordinary seasonal noise for a nine-page local-services site (D-25)"
    - "legacy-canonical-not-moved deliberately does not fire before week 8 after the declared cutover, because Google's lag is weeks and an earlier reading would be read as failure"
    - "Neither flag is an authorisation: retiring the map is a separate decision, never before the 180-day floor (D-25, Deferred Ideas)"
    - "Both flags were observed firing on fabricated readings before the first real run, plus an unperturbed control — the only kind of threshold this project trusts"
    - "A legacy API failure records a gap and continues rather than crashing the run: the 27 inspections above it are the expensive, perishable part, and a transient 5xx must not discard a completed measurement"
    - "The legacy block renders under its own header so the weekly issue body never conflates a legacy consolidation signal with a new-domain indexation regression"
    - "The legacy URL list derives from LEGACY_REDIRECTS; a second hand-typed list would drift and would agree with any bug"
  artifacts:
    - path: "scripts/gsc/thresholds.ts"
      provides: "the two new flag codes, the optional legacy Reading block, and the two derivation-carrying constants"
      contains: "legacy-traffic-zero"
    - path: "scripts/measure-indexation.ts"
      provides: "the legacy Search Analytics call and the 9 legacy URL inspections, non-fatal and sequential"
      contains: "PROPERTY_LEGACY"
    - path: "docs/measurements/README.md"
      provides: "two new rows in the Dutch failure-vocabulary table"
      contains: "legacy-canonical-not-moved"
  key_links:
    - from: "scripts/measure-indexation.ts"
      to: "lib/seo/redirects.ts LEGACY_REDIRECTS"
      via: "the 9 legacy inspection URLs are derived from the map, never re-typed"
      pattern: "LEGACY_REDIRECTS"
    - from: "scripts/gsc/thresholds.ts"
      to: "lib/seo/cutover.ts LEGACY_CUTOVER_DATE"
      via: "weeksSince() against the single declared cutover date; no second copy of that date exists"
      pattern: "LEGACY_CUTOVER_DATE"
    - from: "scripts/measure-indexation.ts"
      to: "scripts/gsc/api.ts PROPERTY_LEGACY / inspectUrl / querySearchAnalytics"
      via: "all three already exist and are unchanged; this is a caller change only"
      pattern: "querySearchAnalytics"
---

<objective>
Extend the Phase 9 weekly cron with a legacy section (D-25): Search Analytics for
`sc-domain:tpsventilatie.nl` plus URL Inspection over the 9 legacy URLs, two new flag codes with their
derivations stated, and both flags observed firing on fabricated readings before reality is asked.

Purpose: the 180-day floor is a minimum, not a retirement signal. The real question — "is Google still
sending traffic to the old domain, and has it moved its chosen canonical?" — is answerable only by
Google, weekly, and only if something asks. D-25 exists because this repo has already watched three
build guards sit red for weeks because nothing executed them. This is also how D-19's declined HTTP
watch gets *late* coverage: a detached Vercel domain or a lapsed certificate surfaces here as a legacy
`pageFetchState` moving to `SERVER_ERROR` / `ACCESS_DENIED` / `REDIRECT_ERROR`.
Output: the two flag codes, the legacy block in the weekly reading, the Dutch vocabulary rows.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md
@.planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md
@.planning/phases/10-reversible-old-brand-migration/10-PATTERNS.md
@.planning/phases/10-reversible-old-brand-migration/10-VALIDATION.md
@.planning/phases/10-reversible-old-brand-migration/10-03-SUMMARY.md
@scripts/gsc/thresholds.ts
@scripts/measure-indexation.ts
@scripts/gsc/api.ts
@docs/measurements/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Two flag codes with their derivations, the optional legacy Reading block, and both flags observed firing (D-25)</name>
  <files>scripts/gsc/thresholds.ts</files>
  <read_first>
    - scripts/gsc/thresholds.ts — lines 17–29 (`UrlReading` and `Reading`; note `sitemap?` and `analytics?` are already optional, which is the precedent for an optional `legacy?` block), 31–43 (the `FlagCode` union and `MeasurementFlag`), 45–81 (`RAMP_ANCHOR` with its derivation comment, `RAMP` as floors with its anti-lowering message, and the pure `weeksSince()` / `rungInForce()` helpers — both reusable as-is against a new anchor), 83–139 (flag emission: append, never early-return; match by URL string, never by position; **the `canonical-mismatch` branch at lines ~130 is the direct template**, inverted — a legacy URL is *healthy* once `googleCanonical` has moved off the old host)
    - lib/seo/cutover.ts (plan 10-03) — `LEGACY_CUTOVER_DATE`, the single place the cutover date is written; `@/` resolves under `tsx` exactly as it does in `scripts/assert-seo.ts`
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pattern 9 — the two flag definitions, the N = 4 reasoning, and the week-8 floor for the consolidation flag
    - scripts/assert-gate-blocks.ts lines 195–203 — the control-proof shape
  </read_first>
  <action>
    Add to the `FlagCode` union: `"legacy-canonical-not-moved"` and `"legacy-traffic-zero"`.

    Extend `Reading` with an **optional** `legacy?: { property: string; impressions28d: number;
    clicks28d: number; urls: { url: string; verdict?: string; coverageState?: string;
    googleCanonical?: string; pageFetchState?: string }[] }`. Optional is load-bearing: every reading
    committed in 2026-09 must stay parseable.

    Add two constants in the `RAMP_ANCHOR` register — a named value that carries its derivation and an
    anti-lowering warning, never a bare number:
    - `LEGACY_CONSOLIDATION_WEEK = 8` — Google's lag is weeks; one reading a week after cutover shows
      nothing and would be read as failure, so the consolidation flag deliberately does not speak before
      week 8. Weeks are counted with `weeksSince(LEGACY_CUTOVER_DATE, todayIso)`; when the cutover has
      not been declared, no legacy week has elapsed and the flag cannot fire at all.
    - `LEGACY_ZERO_WEEKS = 4` — four weekly readings of zero, layered on the 28-day lookback each reading
      already uses, means roughly two months of genuine silence. One or two weeks would fire on ordinary
      seasonal noise for a nine-page local-services site whose baseline is 130 query rows over 16 months.
      The message must state plainly that the flag is **not an authorisation**: retiring the map is a
      separate decision and never happens before the 180-day floor.

    Extend `evaluateReading(prev, curr, todayIso)` — appending, never early-returning:
    - `legacy-canonical-not-moved`: for each `curr.legacy.urls` entry, fire when
      `weeksSince(LEGACY_CUTOVER_DATE, todayIso) >= LEGACY_CONSOLIDATION_WEEK` **and** the entry's
      `googleCanonical` is still on the legacy host. One flag per URL, carrying `url`, so a stuck URL is
      *named* rather than counted. Model it on the `canonical-mismatch` branch, inverted.
    - `legacy-traffic-zero`: fire when `curr.legacy.impressions28d === 0` for `LEGACY_ZERO_WEEKS`
      consecutive readings. `evaluateReading` only sees `prev` and `curr`, so carry the streak in the
      reading itself — add `zeroStreak` inside the legacy block, computed as
      `prev?.legacy?.impressions28d === 0 ? (prev.legacy.zeroStreak ?? 1) + 1 : 1` when current
      impressions are zero, else `0` — and fire on `zeroStreak >= LEGACY_ZERO_WEEKS`. Keeping the streak
      in the committed reading means the history is auditable in git rather than recomputed from a
      directory scan.
    - Neither flag may fire when `curr.legacy` is absent. A reading without a legacy block is a gap, not
      a zero, and must never be silently read as success.

    Prove both flags before reality is asked — the established habit, and the only kind of threshold this
    project trusts. Using `npx tsx -e` against fabricated `Reading` objects, show: (a) a legacy URL whose
    `googleCanonical` is still on the old host at week 9 produces `legacy-canonical-not-moved` naming
    that URL; (b) the same reading at week 3 produces no such flag; (c) a fourth consecutive zero-
    impressions reading produces `legacy-traffic-zero` and a third does not; (d) **the control** — a
    realistic reading with non-zero legacy impressions and moved canonicals produces neither flag, without
    which every proof above could be passing because the evaluator is simply always angry; (e) a reading
    with no `legacy` block produces neither flag.

    Commit with `feat(10-06): legacy consolidation and traffic flags with their derivations (D-25)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx -e "
import { evaluateReading, LEGACY_ZERO_WEEKS, LEGACY_CONSOLIDATION_WEEK, type Reading } from './scripts/gsc/thresholds';
const bad: string[] = [];
const codes = (f: {code:string}[]) => f.map(x=>x.code).join(',');
const base = (legacy: any, taken: string): Reading => ({ taken, property: 'sc-domain:tpsklimaattechniek.nl', floor: 27, urls: [], legacy } as any);
const stuck = { property:'sc-domain:tpsventilatie.nl', impressions28d: 40, clicks28d: 2, zeroStreak: 0, urls:[{ url:'https://tpsventilatie.nl/over-ons/', googleCanonical:'https://tpsventilatie.nl/over-ons/' }] };
const moved = { property:'sc-domain:tpsventilatie.nl', impressions28d: 40, clicks28d: 2, zeroStreak: 0, urls:[{ url:'https://tpsventilatie.nl/over-ons/', googleCanonical:'https://www.tpsklimaattechniek.nl/over-ons' }] };
const late = new Date(Date.now()+0).toISOString().slice(0,10);
const fLate = evaluateReading(undefined, base(stuck, late), late);
const fCtrl = evaluateReading(undefined, base(moved, late), late);
if (codes(fCtrl).includes('legacy-')) bad.push('control fired a legacy flag: ' + codes(fCtrl));
const zeroPrev: any = { ...moved, impressions28d: 0, zeroStreak: LEGACY_ZERO_WEEKS - 1 };
const zeroNow: any = { ...moved, impressions28d: 0, zeroStreak: LEGACY_ZERO_WEEKS };
const fZero = evaluateReading(base(zeroPrev, late), base(zeroNow, late), late);
if (!codes(fZero).includes('legacy-traffic-zero')) bad.push('a fourth consecutive zero reading did not fire legacy-traffic-zero: ' + codes(fZero));
const fNone = evaluateReading(undefined, base(undefined, late), late);
if (codes(fNone).includes('legacy-')) bad.push('a reading with no legacy block fired a legacy flag — a gap is not a zero');
if (typeof LEGACY_ZERO_WEEKS !== 'number' || LEGACY_ZERO_WEEKS !== 4) bad.push('LEGACY_ZERO_WEEKS must be 4');
if (typeof LEGACY_CONSOLIDATION_WEEK !== 'number' || LEGACY_CONSOLIDATION_WEEK !== 8) bad.push('LEGACY_CONSOLIDATION_WEEK must be 8');
if (bad.length) { console.error(bad.join('\n')); process.exit(1); }
console.log('flags proven: control silent, zero-streak fires at N=4, missing legacy block fires nothing; stuck-canonical case observed as ' + (codes(fLate) || 'silent (pre-week-8 or cutover undeclared)'));
"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx scripts/assert-gate-blocks.ts && npx tsx scripts/assert-redirects.ts</automated>
  </verify>
  <acceptance_criteria>
    - `FlagCode` includes `legacy-canonical-not-moved` and `legacy-traffic-zero`; `Reading.legacy` is optional so 2026-09 readings still parse.
    - `LEGACY_CONSOLIDATION_WEEK` is 8 and `LEGACY_ZERO_WEEKS` is 4, each with a comment stating its derivation and, for the zero flag, that it is not an authorisation to retire the map.
    - A stuck legacy canonical at or after week 8 fires `legacy-canonical-not-moved` **naming the URL**; before week 8, or with the cutover undeclared, it does not fire.
    - A fourth consecutive zero-impressions reading fires `legacy-traffic-zero`; a third does not.
    - A reading with no `legacy` block fires neither flag — a gap is not a zero.
    - The unperturbed control produces no legacy flag at all.
  </acceptance_criteria>
  <done>The evidence that will eventually justify retiring the map has a definition, and both halves of it have been seen speaking and staying silent.</done>
</task>

<task type="auto">
  <name>Task 2: The legacy block in measure-indexation.ts — additive, non-fatal, sequential, derived from the map (D-25)</name>
  <files>scripts/measure-indexation.ts</files>
  <read_first>
    - scripts/measure-indexation.ts — lines 30–45 (imports and the `INSPECT_SPACING_MS = 150` constant with its *"sequential keeps per-URL failures attributable"* comment), 116–138 (the sequential inspection loop with per-URL error recorded and the run continuing, plus the all-failed → `exit 2` guard and its "not a Full user on this property" hint), **140–154** (the commit `61de3b7` lesson: *"NEVER fatal: the 27 inspections above are the expensive, perishable part of this run. A transient 5xx here used to crash main(), discarding them AND raising a false indexation alert… Record the gap in the reading instead"* — this rule governs the entire legacy block), 189–204 (the reporting block and table shape)
    - scripts/gsc/api.ts — **no change expected**: `PROPERTY_LEGACY` (line 25), `inspectUrl()` (107–124) and `querySearchAnalytics()` with its full request type including `aggregationType: "byProperty"` (198–235) all already exist. This is a caller change only
    - scripts/export-gsc-performance.ts — line 89 (`const selected = properties.length > 0 ? properties : [PROPERTY_NEW, PROPERTY_LEGACY];`) and the `legacyStartDate()` helper at line 92: the reference for the legacy Search-Analytics call shape. **Do not modify this file**
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pattern 9 — the `Reading.legacy` shape, the property-targeting rule (*"inspectionUrl must be under the property specified in siteUrl"*, so legacy URLs are inspected with the legacy property, and a Domain property covers both the apex and `www` so no second property is needed), and the reporting shape
  </read_first>
  <action>
    Add a legacy section to `measure-indexation.ts`, after the existing sitemap step and before
    `evaluateReading` is called, wrapped so that **any** failure inside it records a gap and lets the run
    continue. The 27 inspections above it are the expensive, perishable part; a transient legacy 5xx must
    never discard them or raise a false indexation alert.

    - Derive the 9 legacy inspection URLs from `LEGACY_REDIRECTS` — `https://tpsventilatie.nl${from}` for
      each entry. Never write a second list: a parallel list drifts from the map and would agree with any
      bug. Inspect each with `inspectUrl(token, PROPERTY_LEGACY, url)`, sequentially, spaced by
      `INSPECT_SPACING_MS`, recording a per-URL error and continuing rather than aborting. Capture
      `verdict`, `coverageState`, `googleCanonical` and `pageFetchState` — the last is what makes D-19's
      declined HTTP watch *late*-covered rather than uncovered: a detached domain or a lapsed certificate
      shows up there as `SERVER_ERROR` / `ACCESS_DENIED` / `REDIRECT_ERROR`.
    - Call `querySearchAnalytics(token, PROPERTY_LEGACY, …)` for the trailing 28 days with
      `aggregationType: "byProperty"`, and record `impressions28d` and `clicks28d`. Compute `zeroStreak`
      from the previous reading's legacy block exactly as Task 1 defined it.
    - Attach the block as `reading.legacy`. When the section fails entirely, leave `reading.legacy`
      **undefined** and print a `note:` line saying the reading was written without the legacy block —
      a gap, never a zero, and Task 1's evaluator already refuses to flag on a missing block.

    Extend the reporting block with a second table under a `— legacy (tpsventilatie.nl) —` header, with
    columns for url, verdict, coverageState, pageFetchState and `googleCanonical`, followed by the
    28-day impressions and clicks and the current zero streak. Keeping it under its own header is what
    stops the weekly issue body conflating a legacy consolidation signal with a new-domain indexation
    regression.

    Prove it on real data without touching the committed readings: run
    `GSC_SERVICE_ACCOUNT_JSON="$(cat ~/.config/tps-klimaattechniek/gsc-service-account.json)" npx tsx
    scripts/measure-indexation.ts --out "$TMPDIR/gsc-legacy-proof"` and confirm the legacy block is
    populated with real numbers (the legacy property had 130 query rows over 16 months at the Phase 9
    baseline, so a zero here would itself be a finding). Do **not** write into `docs/measurements/gsc`
    from this proof run. Note in the summary that the Phase 9 service-account key rotation is still an
    outstanding owner action on this same credential — carried, not this phase's work.

    Commit with `feat(10-06): weekly legacy Search Analytics and URL inspections, additive and non-fatal (D-25)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "
const fs=require('fs');const s=fs.readFileSync('scripts/measure-indexation.ts','utf8');const bad=[];
if(!s.includes('PROPERTY_LEGACY'))bad.push('does not use the legacy property');
if(!s.includes('LEGACY_REDIRECTS'))bad.push('does not derive the legacy URL list from the map');
if(!/aggregationType/.test(s))bad.push('the Search Analytics call does not aggregate by property');
if(!/pageFetchState/.test(s))bad.push('pageFetchState is not captured — that is the late coverage for the declined HTTP watch');
if(!/legacy/i.test(s))bad.push('no legacy block');
const legacyIdx=s.indexOf('legacy');
if(legacyIdx>-1&&!/try\s*\{[\s\S]{0,4000}legacy[\s\S]{0,4000}\}\s*catch/i.test(s))bad.push('the legacy block is not wrapped so a failure records a gap and continues');
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('legacy block derives from the map, targets the legacy property, captures pageFetchState, and is non-fatal');
"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && test -f "$HOME/.config/tps-klimaattechniek/gsc-service-account.json" && GSC_SERVICE_ACCOUNT_JSON="$(cat "$HOME/.config/tps-klimaattechniek/gsc-service-account.json")" npx tsx scripts/measure-indexation.ts --out "${TMPDIR:-/tmp}/gsc-legacy-proof" 2>&1 | tee /dev/stderr | grep -q 'legacy (tpsventilatie.nl)' && ls "${TMPDIR:-/tmp}/gsc-legacy-proof" | grep -q '\.json' && node -e "const fs=require('fs');const d=process.env.TMPDIR||'/tmp';const dir=d.replace(/\/\$/,'')+'/gsc-legacy-proof';const f=fs.readdirSync(dir).filter(x=>x.endsWith('.json')).sort().pop();const r=JSON.parse(fs.readFileSync(dir+'/'+f,'utf8'));if(!r.legacy){console.error('the proof run wrote no legacy block');process.exit(1)}if(!Array.isArray(r.legacy.urls)||r.legacy.urls.length!==9){console.error('legacy block has '+(r.legacy.urls||[]).length+' URLs, expected 9');process.exit(1)}console.log('real run: legacy impressions28d='+r.legacy.impressions28d+', '+r.legacy.urls.length+' URLs inspected')"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && git status --porcelain docs/measurements/gsc | grep -q . && { echo "the proof run must not write into the committed readings"; exit 1; } || echo "committed readings untouched"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/measure-indexation.ts` derives its 9 legacy URLs from `LEGACY_REDIRECTS`, inspects them with `PROPERTY_LEGACY` sequentially with the existing spacing, and captures `googleCanonical` plus `pageFetchState`.
    - The legacy Search Analytics call uses `aggregationType: "byProperty"` over a 28-day window.
    - The whole legacy section is wrapped so a failure records a gap and the run continues; a total failure leaves `reading.legacy` undefined and prints a `note:` line.
    - A real run against the legacy property writes a reading containing a `legacy` block with 9 URLs and a real impressions figure, printed under the `— legacy (tpsventilatie.nl) —` header.
    - The proof run wrote to a temp directory; `docs/measurements/gsc` is untouched.
    - `scripts/gsc/api.ts`, `scripts/export-gsc-performance.ts` and `.github/workflows/measure-indexation.yml`'s executable content were not modified.
  </acceptance_criteria>
  <done>The machine that will eventually answer "has Google consolidated?" is wired to a credential and a schedule that already exist.</done>
</task>

<task type="auto">
  <name>Task 3: Dutch failure-vocabulary rows and the workflow's flag-vocabulary comment (D-25, D-19 reconciliation)</name>
  <files>docs/measurements/README.md, .github/workflows/measure-indexation.yml</files>
  <read_first>
    - docs/measurements/README.md — lines 26–42 (the `| code | betekenis | wat te doen |` flag table and the closing house rule *"Verlaag nooit een drempel om groen te krijgen — zoek uit welke pagina vastzit."*) and the surrounding Dutch register
    - .github/workflows/measure-indexation.yml — lines 10–14 (the header comment that lists the flag vocabulary; **only this comment changes**, the executable content stays as-is)
    - .planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md §D-19 and §D-25's reconciliation paragraph — "late coverage, not equivalent coverage", and the explicit instruction not to widen `verify-redirects.ts` into a cron to 'fix' it
  </read_first>
  <action>
    Add two rows to the Dutch flag table in `docs/measurements/README.md`, in the established register —
    code, what it means, what to do — and keep the closing house rule as the last line of the section:

    - `legacy-canonical-not-moved`: Google still names the old address as the canonical for a legacy URL,
      eight or more weeks after the switch. What to do: check that that specific URL still redirects in
      one step (`verify-redirects.ts`), that the Change of Address is still active for the property it
      belongs to, and that the destination is indexable. Do **not** change the week threshold to silence
      it — the URL is named for a reason.
    - `legacy-traffic-zero`: no impressions on the old domain for four consecutive weekly readings.
      What to do: **nothing automatic.** It opens a conversation about retiring the redirect map; it is
      not permission, and the map never comes down before the 180-day floor.

    Add a short paragraph under the table explaining the legacy section in plain Dutch: the weekly run now
    also asks Google what it did with the nine old addresses, and reports it under its own heading so the
    two domains are never mixed up in one alert.

    Append the two codes to the flag-vocabulary list in the `.github/workflows/measure-indexation.yml`
    header comment. **Change nothing executable in that file** — the legacy section rides the existing
    `npx tsx scripts/measure-indexation.ts` invocation and the existing issue-body pipe unchanged.

    Record the D-19 reconciliation in the summary in the exact terms CONTEXT gives it: the weekly legacy
    section *partially* covers the drift a scheduled HTTP watch would have caught, surfacing it with
    Google's lag rather than a direct check. Treat it as late coverage, not equivalent coverage, and do
    not widen `verify-redirects.ts` into a cron to compensate.

    Commit with `docs(10-06): Dutch vocabulary for the two legacy flags and the workflow comment (D-25)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "
const fs=require('fs');const bad=[];
const r=fs.readFileSync('docs/measurements/README.md','utf8');
for(const k of ['legacy-canonical-not-moved','legacy-traffic-zero','180'])if(!r.includes(k))bad.push('measurements README missing: '+k);
if(!/Verlaag nooit een drempel/.test(r))bad.push('the house rule line was lost');
const w=fs.readFileSync('.github/workflows/measure-indexation.yml','utf8');
for(const k of ['legacy-canonical-not-moved','legacy-traffic-zero'])if(!w.includes(k))bad.push('workflow comment missing: '+k);
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('Dutch vocabulary rows and workflow flag comment in place');
"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && git diff --unified=0 origin/main -- .github/workflows/measure-indexation.yml | grep -E '^\+' | grep -v '^\+\+\+' | grep -vE '^\+\s*#' | grep -q . && { echo "executable content of the weekly workflow was changed — only the comment should be"; exit 1; } || echo "weekly workflow: comment-only change"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/measurements/README.md` gained rows for both new codes in the existing Dutch table, names the 180-day floor, and still closes with the house rule.
    - The `measure-indexation.yml` header comment lists both new codes.
    - `git diff` against `origin/main` for `measure-indexation.yml` shows added lines that are **only** comments — no executable change.
    - The summary states the D-19 reconciliation as late coverage rather than equivalent coverage, and records that no scheduled HTTP watch was added.
  </acceptance_criteria>
  <done>The two new signals have plain-Dutch meanings the owner can act on, and neither is mistakable for permission to take the map down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Google Search Console API → our reading files | responses are untrusted input; a transient 5xx must record a gap, never discard a completed measurement or raise a false alarm |
| GSC service account → the legacy property | the same credential Phase 9 provisioned, against a property it already holds; no new credential and no scope change |
| weekly reading → the retirement decision | a flag must never read as authorisation; the 180-day floor and the "separate decision" rule are the real guards |
| the map → the inspected URL list | a second hand-typed list would drift and agree with any bug |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-gsc-ownership | Tampering | the legacy properties | mitigate | this plan only *reads*; the DNS TXT that keeps all three legacy properties verified is treated as immutable during the dd24 sessions (plans 10-08 and 10-09) |
| T-10-false-zero | Repudiation | a missing legacy block | mitigate | the evaluator refuses to flag when `reading.legacy` is absent — a gap is not a zero — and the run prints a `note:` line naming the gap |
| T-10-discarded-measurement | DoS | the weekly run | mitigate | the entire legacy section is non-fatal; the 27 perishable inspections above it can never be discarded by a legacy API failure (the commit `61de3b7` lesson) |
| T-10-premature-retire | Repudiation | `legacy-traffic-zero` | mitigate | N = 4 with its derivation stated, plus an explicit "this is not an authorisation" line in the message and in the Dutch vocabulary row |
| T-10-secret-in-logs | Information disclosure | the proof run | mitigate | the key is read from `~/.config/…` into an env var for one command; nothing is echoed and no key material is written to the repo. The Phase 9 rotation of this key remains an outstanding owner action |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | no packages added; the existing `scripts/gsc/*` client, `tsx` and Node builtins only |
</threat_model>

<verification>
- Runs on branch `gsd/phase-10-reversible-old-brand-migration`. Create it from `main` if absent: `git rev-parse --verify gsd/phase-10-reversible-old-brand-migration || git checkout -b gsd/phase-10-reversible-old-brand-migration`.
- The Vercel preview of the pushed branch is the TypeScript gate for the changed scripts; there is no local `next build` or `tsc --noEmit` on this mount.
- The flag proofs are `npx tsx -e` runs against fabricated readings and take under a second; the real-data proof is one live API run writing to a temp directory.
- The weekly workflow itself is unchanged except for a comment, so it needs no new CI proof — it already ran green on real events in Phase 9.
</verification>

<success_criteria>
- Two flag codes exist with stated derivations, and both have been observed firing and staying silent on fabricated readings plus an unperturbed control.
- The weekly reading carries a legacy block with 9 inspected URLs and a 28-day impressions figure, derived from the map and additive to a run that cannot be crashed by it.
- The Dutch vocabulary explains both signals and denies that either is permission to retire the map.
- `scripts/gsc/api.ts`, `scripts/export-gsc-performance.ts` and the weekly workflow's executable content are untouched.
</success_criteria>

<output>
Create `.planning/phases/10-reversible-old-brand-migration/10-06-SUMMARY.md` when done.
</output>
