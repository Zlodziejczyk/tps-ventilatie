---
phase: 10-reversible-old-brand-migration
plan: 10
type: execute
wave: 6
depends_on: ["10-09"]
files_modified:
  - docs/baseline/<capture-date>/gsc/
  - docs/baseline/<capture-date>/README.md
  - docs/baseline/<capture-date>/migration-final.md
  - docs/seo-owner-runbook.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [MIG-09, MIG-10]
user_setup:
  - service: google search console
    why: "The Change of Address has no API. Google is the observer and its pre-move checks are the verdict (D-24)"
    dashboard_config:
      - task: "File a Change of Address from sc-domain:tpsventilatie.nl to sc-domain:tpsklimaattechniek.nl"
        location: "Search Console -> old property -> Settings -> Change of address"
      - task: "File a Change of Address from https://tpsventilatie.nl/ to the new property"
        location: "Search Console -> old property -> Settings -> Change of address"
      - task: "File a Change of Address from https://www.tpsventilatie.nl/ to the new property"
        location: "Search Console -> old property -> Settings -> Change of address"
must_haves:
  truths:
    - "A Change of Address is filed from every verified legacy variant — the Domain property AND both legacy URL-prefix properties — because Google states the tool does not move any subdomains below the specified domain, including www, and www.tpsventilatie.nl is exactly where the old chain lived and where a large share of inbound links point (D-24, MIG-09)"
    - "All three filings happen the same day, once the one-hop probe is green from both legacy hostnames: Google runs pre-move checks that sample the redirects at submission, so filing before the probe is green wastes the attempt (D-24)"
    - "The google-site-verification TXT record is what made all three filings possible; it stayed untouched through both dd24 sessions (D-06)"
    - "Every post-cutover artefact is committed under the dated baseline with a manifest row: the zone snapshot and its diff, the probe output, the certificate readings, the mail .eml, the T+30 decision and the Change of Address confirmations"
    - "Day 28 and the mail-certificate check fall AFTER this phase's execution window. They are recorded as dated obligations with a named home in docs/baseline/, not modelled as completable tasks, and the phase's verification does not depend on them (D-11)"
    - "migration-final.md exists as a dated, pre-addressed obligation stating exactly what will be written into it on day 28 — the declared-final date, the TTL restoration to 28800 with dig output, the pre- and post-cutover DNS diff, that day's probe output, the first legacy GSC reading, and a one-line restatement of the subscription dependency (D-11)"
    - "The mail-certificate check on or around 2026-10-30 is a calendar obligation, not a probe: the certificate for mail.tpsventilatie.nl expires 2026-10-29 and two of its three SANs are the names this phase repointed. No probe run during this phase can see it, and it is mitigated by prevention and detection, never by rollback (RESEARCH Pitfall 4)"
    - "The GBP website-URL edit is deliberately NOT done here: it stays in Phase 11, in the low-risk early group, and must never be batched with the name change, because editing name plus categories plus URL together reads to Google as a listing takeover and 34 reviews with a 4,9 rating ride on it (D-27)"
    - "The WordPress install is left intact and reachable through the hosts-override route, verified after the cutover — MIG-10's second half"
  artifacts:
    - path: "docs/baseline/<capture-date>/gsc/change-of-address.md"
      provides: "one dated row per property: the source property, the target, the pre-move check result and the confirmation screenshot"
      contains: "sc-domain:tpsventilatie.nl"
    - path: "docs/baseline/<capture-date>/migration-final.md"
      provides: "the day-28 declaration, pre-addressed with its due date and the checks it will carry"
      contains: "28800"
    - path: "docs/baseline/<capture-date>/README.md"
      provides: "the completed Phase 10 evidence manifest, one row per artefact"
      contains: "opnieuw maken"
  key_links:
    - from: "docs/baseline/<capture-date>/migration-final.md"
      to: "docs/seo-owner-runbook.md rollback section"
      via: "the runbook links to the declaration as the dated end of the revert window"
      pattern: "migration-final"
    - from: "the three Change of Address filings"
      to: "the one-hop redirects on both legacy hostnames"
      via: "Google's pre-move checks sample them at submission, so the redirect tier must be green first"
      pattern: "Change of address"
---

<objective>
Close the search-side of the migration on the same day as the flip (D-24; MIG-09) and leave the phase's
dated obligations in a place someone will actually find them (D-11, MIG-10). File the Change of Address
from all three verified legacy properties, consolidate every post-cutover artefact into the dated
baseline manifest, and pre-address the day-28 declaration and the 2026-10-30 mail-certificate check as
recorded obligations rather than as tasks this phase can complete.

Purpose: Google validates the redirects at submission, so the CoA has to follow the green probe
immediately — and it has to be filed three times, because the tool does not move subdomains and
`www.tpsventilatie.nl` is exactly where the old chain lived. Meanwhile two commitments land weeks from
now; a documented instruction nobody runs is indistinguishable from no instruction, so each gets a dated
home and a named owner.
Output: three filings with evidence, a complete manifest, `migration-final.md`, and the Phase 11 handoff.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md
@.planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md
@.planning/phases/10-reversible-old-brand-migration/10-VALIDATION.md
@.planning/phases/10-reversible-old-brand-migration/10-09-SUMMARY.md
@docs/baseline/2026-09-16/gsc/legacy-url-prefix-status.md
@docs/seo-owner-runbook.md
</context>

<tasks>

<task type="checkpoint">
  <name>Task 1: File the Change of Address from all three verified legacy properties, same day (D-24, MIG-09)</name>
  <files>docs/baseline/&lt;capture-date&gt;/gsc/change-of-address.md</files>
  <read_first>
    - docs/baseline/2026-09-16/gsc/legacy-url-prefix-status.md — **the exact property identifiers**: the Domain property `sc-domain:tpsventilatie.nl` plus the two URL-prefix properties `https://tpsventilatie.nl/` and `https://www.tpsventilatie.nl/`, all three recorded as *verified (inherited)* from the DNS-TXT-verified Domain property, with Google's own notice *"To stay verified, don't remove the DNS record"*
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pattern 9 — what Google actually requires, quoted: the tool works only on domain-level properties; you must own both sides; *"The tool does not move any subdomains below the specified domain (including www)"* and *"make sure to use this tool for all subdomain variants of the old domain, including www and non-www… even if you're not actively using these variants currently"*; the pre-move checks; the 180-day window; and **Cancel Move** as the search-side half of a revert
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pitfall 6 (filing from one property leaves the `www` hostname with no site-move signal) and §Pitfall 7 (filing before the probe is green wastes the attempt and can leave a confusing partial state)
    - docs/baseline/<capture-date>/cutover/post-cutover-probe.txt (plan 10-09) — the green probe that is the precondition
  </read_first>
  <action>
    Re-run `npx tsx scripts/verify-redirects.ts` and confirm it is green **before** asking the user to
    file anything. Google's pre-move checks sample the redirects at submission; filing against a red map
    wastes the attempt. This is a hard dependency edge, not two adjacent steps.

    Then ask the user to file the Change of Address three times in the Search Console UI — there is no
    API, and Google is the observer whose pre-move checks are the verdict. For each of the three source
    properties below, target the new Domain property `sc-domain:tpsklimaattechniek.nl`:

    1. `sc-domain:tpsventilatie.nl` — the Domain property
    2. `https://tpsventilatie.nl/` — the apex URL-prefix property
    3. `https://www.tpsventilatie.nl/` — the `www` URL-prefix property

    Three filings is a requirement, not belt-and-braces: Google states the tool does not move subdomains
    below the specified domain, including `www`, and `www.tpsventilatie.nl` is precisely where the old
    `www`-to-apex 301 lived and where a large share of inbound links point. Per property, screenshot
    **both** the pre-move check result and the confirmation.

    Record the outcome in `docs/baseline/<capture-date>/gsc/change-of-address.md`: one row per property
    with the exact source identifier, the target, the UTC timestamp, the pre-move check result and the
    screenshot filename — plus two standing notes: that Google's site-move actions run for 180 days,
    after which it recognises no relationship between the old and new sites, so the redirects must
    outlive that and longer if Search still sends traffic; and that a move can be cancelled within 180
    days via **Cancel Move** in the old property, which is the search-side half of a revert alongside the
    DNS half already in the runbook.

    If any filing fails a pre-move check, record the exact message and fix the underlying cause — never
    work around it by filing from fewer properties.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npx tsx scripts/verify-redirects.ts</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && D=$(ls -d docs/baseline/*/legacy-site-mirror 2>/dev/null | head -1 | xargs dirname) && node -e "
const fs=require('fs');const f=process.argv[1]+'/gsc/change-of-address.md';const bad=[];
if(!fs.existsSync(f)){console.error('no Change of Address record');process.exit(1)}
const t=fs.readFileSync(f,'utf8');
for(const k of ['sc-domain:tpsventilatie.nl','https://tpsventilatie.nl/','https://www.tpsventilatie.nl/','sc-domain:tpsklimaattechniek.nl','Cancel Move','180'])
  if(!t.includes(k))bad.push('record missing: '+k);
if(!/\d{4}-\d{2}-\d{2}/.test(t))bad.push('no dated filing');
const shots=fs.readdirSync(process.argv[1]+'/gsc').filter(x=>/\.(png|jpg|jpeg)\$/i.test(x));
if(shots.length<3)bad.push('expected at least 3 confirmation screenshots, found '+shots.length);
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('three filings recorded with dates, targets, pre-move results and screenshots');
" "$D"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && for ns in $(dig +short NS tpsventilatie.nl); do ns=${ns%.}; dig @"$ns" +short TXT tpsventilatie.nl | grep -q 'google-site-verification=DvCnCNBbXd73JTab3-DsDmq_KgkQmlCZ7onK6OqDkoI' || { echo "the GSC token is gone at $ns — the filings would be invalidated"; exit 1; }; done; echo "all three legacy properties remain verified through the untouched TXT record"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsx scripts/verify-redirects.ts` was green immediately before the filings.
    - A Change of Address was filed from all three source properties — `sc-domain:tpsventilatie.nl`, `https://tpsventilatie.nl/` and `https://www.tpsventilatie.nl/` — each targeting `sc-domain:tpsklimaattechniek.nl`, on the same day as the cutover.
    - `docs/baseline/<date>/gsc/change-of-address.md` has one dated row per property with the pre-move check result and a screenshot filename, plus the 180-day and Cancel Move notes.
    - At least three confirmation screenshots are committed under `docs/baseline/<date>/gsc/`.
    - The `google-site-verification` TXT is still present at all three dd24 nameservers.
    - Any failed pre-move check was recorded verbatim and its cause fixed; no filing was skipped.
  </acceptance_criteria>
  <done>Google has been told about the move from every variant it will look at, on the day the redirects went live.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate the post-cutover evidence and pre-address the day-28 declaration (D-11, MIG-10)</name>
  <files>docs/baseline/&lt;capture-date&gt;/README.md, docs/baseline/&lt;capture-date&gt;/migration-final.md</files>
  <read_first>
    - docs/baseline/2026-09-16/README.md — §1 "Doel en datumregel" (the directory is named for the first capture and is never renamed; it lives in `docs/` so someone can reach it during an incident at three in the morning) and §2's five-column manifest, in particular the `dns/…-pre-switch-…txt` row as the precedent for a **not-reproducible** artefact
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pattern 8 — exactly what `migration-final.md` must contain on day 28: the declared-final date, the TTL restoration to 28800 with the `dig` output proving it, a pointer to the pre- and post-cutover DNS snapshots and their diff, that day's `verify-redirects.ts` output, the first legacy GSC reading showing consolidation, and a one-line restatement of the subscription dependency; and why it sits in `docs/baseline/` rather than in the runbook
    - .planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md §D-11 and §D-04 (the TTLs go back to 28800 at the day-28 declaration, not before)
    - docs/baseline/<capture-date>/cutover/t30-decision.md (plan 10-09) — the computed day-28 date
  </read_first>
  <action>
    Complete `docs/baseline/<capture-date>/README.md` so the manifest has a row for every artefact this
    phase produced, each with what it proves, its UTC `taken` time, its source and its `opnieuw maken`
    recipe: the legacy mirror and `CAPTURE.md`, the owner confirmations, the Vercel target records, the
    pre-flight probe output and checklist, both dd24 session sheets, the three-nameserver readings, the
    post-cutover zone snapshot and diff, the certificate readings, the post-cutover probe output, the
    mail `.eml`, the T+30 decision and the Change of Address record.

    Two rows need the not-reproducible voice, copied from the `dns/…-pre-switch-…txt` precedent: the
    **legacy mirror** (reproducible only while the old site still served — and it no longer does) and the
    **pre-flight probe output** (it proved the map through a spoofed `Host` while the legacy DNS still
    pointed at WordPress; that condition cannot be recreated). Extend the privacy sentence to cover the
    mail headers: nothing is redacted except any third-party address, and the file contains no
    credentials.

    Then create `docs/baseline/<capture-date>/migration-final.md` as a **pre-addressed obligation**, not
    a finished document. Front-matter-style header with `due:` set to the day-28 date computed in plan
    10-09 and `status: pending`. Body: a short statement of what day 28 means — before it, a revert is a
    live option; after it, a revert is a second migration — and then the checklist of what must be
    written into this file on that date:
    - the declared-final date and who declared it;
    - the TTL restoration from 300 back to 28800 on both hostname records, with the `dig` output at all
      three nameservers proving it (D-04: the TTLs stay low for the whole window, precisely because week
      2 to 3 is when someone is most likely to notice a problem);
    - a pointer to the pre- and post-cutover DNS snapshots and their diff;
    - that day's `npx tsx scripts/verify-redirects.ts` output;
    - the first legacy GSC reading showing consolidation, from the weekly cron's legacy section;
    - one line restating the silent dependency: the old WordPress install lives exactly as long as the
      cyberfolks subscription, and that subscription exists only for `info@tpsventilatie.nl`.

    Link it from the runbook's rollback section as the dated end of the revert window.

    **This file is not completed by this phase, and the phase's verification must not depend on it.**
    Day 28 falls after the execution window; the deliverable here is the recorded, dated, addressed
    obligation, with `status: pending`.

    Commit with `docs(10-10): consolidate the post-cutover evidence and pre-address the day-28 declaration (D-11, MIG-10)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && D=$(ls -d docs/baseline/*/legacy-site-mirror 2>/dev/null | head -1 | xargs dirname) && node -e "
const fs=require('fs'),p=require('path');const d=process.argv[1];const bad=[];
const rd=fs.readFileSync(d+'/README.md','utf8');
for(const k of ['legacy-site-mirror','CAPTURE.md','vercel-target-records','preflight-probe','post-cutover-probe','change-of-address','opnieuw maken'])
  if(!rd.includes(k))bad.push('manifest missing a row for: '+k);
if(!/niet.{0,30}opnieuw/i.test(rd))bad.push('no not-reproducible row voice for the mirror and the pre-flight probe');
if(!/\.eml/.test(rd))bad.push('the mail evidence has no manifest row');
const mf=d+'/migration-final.md';
if(!fs.existsSync(mf))bad.push('migration-final.md was not created');
else{const t=fs.readFileSync(mf,'utf8');
  for(const k of ['28800','status: pending','due:','cyberfolks','verify-redirects'])if(!t.includes(k))bad.push('migration-final.md missing: '+k);
  if(!/\d{4}-\d{2}-\d{2}/.test(t))bad.push('migration-final.md carries no due date');
}
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('manifest complete and the day-28 declaration is pre-addressed and pending');
" "$D"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q 'migration-final' docs/seo-owner-runbook.md && echo "the runbook links to the dated end of the revert window"</automated>
  </verify>
  <acceptance_criteria>
    - `docs/baseline/<date>/README.md` has a manifest row for every artefact this phase produced, with the five columns filled.
    - The legacy mirror and the pre-flight probe output carry the not-reproducible voice; the privacy sentence covers the mail headers.
    - `docs/baseline/<date>/migration-final.md` exists with `status: pending`, a `due:` date equal to the computed day-28 date, and the full checklist including the TTL restoration to 28800 and the subscription dependency.
    - `docs/seo-owner-runbook.md` links to `migration-final.md` from the rollback section.
    - Nothing in this plan's verification depends on day 28 having arrived.
  </acceptance_criteria>
  <done>Everything this phase learned is in one dated directory, and the one thing it cannot finish is written down with a due date instead of being remembered.</done>
</task>

<task type="auto">
  <name>Task 3: Record the dated obligations that outlive the phase, and hand off to Phase 11 (D-11, D-27, RESEARCH Pitfall 4)</name>
  <files>docs/seo-owner-runbook.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Pitfall 4 — the measured certificate (`CN=mail.tpsventilatie.nl`, SANs `mail.` plus the apex and `www`, `notAfter` **2026-10-29**), the three-step mitigation in descending order of value (ask cyberfolks, check on the date, fall back to `s161.cyber-folks.pl` which is valid to 2026-12-24), and **why it belongs in the rollback document**: it is a mail failure with a known date that may fall after the revert window closes, so the mitigation has to be prevention and detection, never rollback
    - .planning/phases/10-reversible-old-brand-migration/10-RESEARCH.md §Code Examples → the `openssl s_client -connect mail.tpsventilatie.nl:993` check
    - .planning/phases/10-reversible-old-brand-migration/10-CONTEXT.md §D-27 — the GBP website-URL edit stays in Phase 11, in the low-risk early group, and must never be batched with the name change
    - .planning/ROADMAP.md §"Phase 11: Local Presence — GBP & Citations" — where the handoff note belongs, and its existing sequencing paragraph
    - docs/baseline/<capture-date>/owner/mail-cert-question.md (plan 10-04) — cyberfolks' answer, or the recorded absence of one
  </read_first>
  <action>
    Add a short **"Gedateerde controles"** block to the runbook's rollback section listing the two
    commitments that land after this phase's execution window, each with its date, its command and what a
    failure means. Both are obligations with a named home, not tasks this phase can tick:

    1. **On or around 2026-10-30 — the mail certificate.** Run
       `openssl s_client -servername mail.tpsventilatie.nl -connect mail.tpsventilatie.nl:993` piped into
       `openssl x509 -noout -dates -ext subjectAltName`. PASS if `notAfter` has moved past 2026-10-29.
       FAIL means the renewal broke because two of the three SANs on that certificate are the names this
       phase repointed, and cyberfolks' ACME client may have answered its HTTP-01 challenges at Vercel.
       **This is never a rollback trigger** — the date may fall after the revert window closes. The
       escape hatch is to point the mail client's server hostname at `s161.cyber-folks.pl`, covered by
       the `*.cyber-folks.pl` wildcard valid to 2026-12-24, which is the same host-level hostname the
       webmail section already teaches. Record cyberfolks' answer from plan 10-04 here: if they confirmed
       a split certificate or DNS-01 validation, say the risk is closed; if there was no answer, say so
       plainly rather than implying reassurance.
    2. **Day 28 — the declaration.** Point at `docs/baseline/<capture-date>/migration-final.md`, restate
       that the TTLs go back to 28800 on that day and not before, and that after it a revert is a second
       migration.

    Then write the Phase 11 handoff into `.planning/ROADMAP.md` §"Phase 11", as one bullet under its
    existing sequencing paragraph: the Google Business Profile website URL still points at the legacy
    domain, deliberately (D-27); after this cutover it lands correctly in one hop, so it is a slightly
    weaker signal rather than a failure; it belongs in Phase 11's **low-risk early group** alongside
    hours, Services and photos, and it must **not** be batched with the name change — editing name plus
    categories plus URL together reads to Google's automated systems as a listing takeover, and 34
    reviews plus a 4,9 rating ride on that.

    Finally, state in the summary — and nowhere pretend otherwise — which requirements are complete at
    phase close and which carry dated obligations: MIG-01 through MIG-10 are met by this phase; the day-28
    declaration and the mail-certificate check are recorded obligations that outlive it; and the Phase 9
    service-account key rotation remains an outstanding owner action on the same credential the weekly
    legacy section uses.

    Commit with `docs(10-10): dated obligations after the window and the Phase 11 GBP handoff (D-11/D-27, Pitfall 4)`.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && node -e "
const fs=require('fs');const bad=[];
const rb=fs.readFileSync('docs/seo-owner-runbook.md','utf8');
for(const k of ['2026-10-29','s161.cyber-folks.pl','993','migration-final','28800'])if(!rb.includes(k))bad.push('runbook dated-checks block missing: '+k);
if(!/2026-10-3[01]|2026-10-30/.test(rb))bad.push('the mail-certificate check has no date to run on');
if(/curl[^\n]*(--insecure|\s-k\b)/.test(rb))bad.push('an insecure curl appeared in the runbook');
const rm=fs.readFileSync('.planning/ROADMAP.md','utf8');
const p11=rm.slice(rm.indexOf('#### Phase 11'),rm.indexOf('#### Phase 12'));
for(const k of ['D-27','website'])if(!p11.includes(k))bad.push('Phase 11 handoff missing: '+k);
if(!/niet|not.*batch|never.*batch/i.test(p11))bad.push('the handoff does not forbid batching the URL edit with the name change');
if(bad.length){console.error(bad.join('\n'));process.exit(1)}
console.log('dated obligations recorded in the runbook and the Phase 11 handoff is in the roadmap');
"</automated>
    <automated>echo | openssl s_client -servername mail.tpsventilatie.nl -connect mail.tpsventilatie.nl:993 2>/dev/null | openssl x509 -noout -dates -ext subjectAltName | tee /dev/stderr | grep -q 'notAfter' && echo "baseline certificate reading taken today — the dated check on 2026-10-30 compares against this"</automated>
    <automated>cd "$(git rev-parse --show-toplevel)" && curl -sS --max-time 20 --resolve tpsventilatie.nl:443:195.78.67.39 https://tpsventilatie.nl/wp-login.php | grep -qi 'wp-submit\|loginform\|wp-login' && echo "MIG-10: the WordPress install is still intact and reachable after the cutover"</automated>
  </verify>
  <acceptance_criteria>
    - The runbook has a dated-checks block naming `2026-10-29`, the `openssl s_client … :993` command, the `s161.cyber-folks.pl` escape hatch, and the statement that this is never a rollback trigger.
    - It records cyberfolks' answer from plan 10-04 honestly, including "no answer" if that is the case.
    - The day-28 entry points at `migration-final.md` and states the TTL restoration to 28800.
    - `.planning/ROADMAP.md` §Phase 11 carries the D-27 handoff, placing the website-URL edit in the low-risk early group and forbidding it being batched with the name change.
    - A baseline `openssl` reading of the mail certificate was taken today, for the 2026-10-30 check to compare against.
    - `curl --resolve … /wp-login.php` still serves the real WordPress login form — MIG-10's second half, verified after the cutover.
    - The summary states which requirements are complete and which are dated obligations, without implying the obligations are done.
  </acceptance_criteria>
  <done>The two things that happen after everyone stops paying attention have a date, a command and a named home.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search Console UI → the site-move signal | there is no API; Google is the observer and its pre-move checks are the verdict |
| the DNS TXT record → three legacy properties | removing it would silently unverify all three and invalidate every filing |
| a dated obligation → the future | anything not written down with a date, a command and a home is a memory, and this repo has already watched three guards rot for weeks |
| Phase 10 → Phase 11 | the GBP listing carries 34 reviews and a 4,9 rating; a batched edit reads to Google as a takeover |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-gsc-ownership | Tampering | the `google-site-verification` TXT | mitigate | asserted present at all three nameservers as part of this plan's verification; its removal would invalidate the filings |
| T-10-coa-partial | Repudiation | the site-move signal | mitigate | three filings, one per verified variant, because the tool does not move subdomains; a failed pre-move check is fixed rather than worked around by filing from fewer properties |
| T-10-coa-premature | Repudiation | pre-move checks | mitigate | the probe is re-run green immediately before the filings — a hard dependency edge, not two adjacent steps |
| T-10-mail-tls | DoS | the shared-SAN mail certificate expiring 2026-10-29 | mitigate | prevention recorded from plan 10-04, detection scheduled as a dated runbook check with a baseline reading taken today, fallback documented; **never** a rollback trigger, because the date may fall outside the revert window |
| T-10-wp-dormant | Tampering | the rollback target | accept | reachability re-verified after the cutover; the accepted risk that a long-dormant unpatched install may be compromised is already stated in the rollback section, with the mirror as the clean copy of the content |
| T-10-gbp-takeover | Tampering | the Google Business Profile | transfer | deferred to Phase 11 by decision, with an explicit handoff placing the URL edit in the low-risk early group and forbidding a batched edit |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | no packages added; `openssl`, `curl`, `dig` and existing scripts only |
</threat_model>

<verification>
- Work is on `main`; this plan changes documentation and planning artefacts only.
- The Change of Address has **no API**: its verification is the artefact — three dated rows plus three screenshots — which is the honest form for a claim only Google can settle.
- Every automated check here runs in seconds and none of them depends on a future date. The day-28 declaration and the 2026-10-30 certificate check are recorded obligations; the phase must be verifiable without them.
- `npx tsx scripts/verify-redirects.ts` is the live gate re-run before filing, and again as this plan's first check.
</verification>

<success_criteria>
- A Change of Address is filed and evidenced from all three verified legacy properties, on cutover day, after a green probe — MIG-09 met.
- The dated baseline manifest covers every artefact the phase produced, with the two not-reproducible ones marked as such.
- `migration-final.md` exists as a pending, dated, pre-addressed obligation for day 28, linked from the runbook.
- The 2026-10-30 mail-certificate check is in the runbook with its command, its meaning and its escape hatch, and a baseline reading was taken today.
- The WordPress install is still intact and reachable through the documented route — MIG-10 met.
- Phase 11 has the D-27 handoff in writing, in the roadmap.
</success_criteria>

<output>
Create `.planning/phases/10-reversible-old-brand-migration/10-10-SUMMARY.md` when done.
</output>
